private alias AEC = Amazonite::EC2
private alias Core = Amazonite::Core

module Amazonite::EC2
  class Client < Core::Client
    Log               = ::Log.for("amazonite.ec2.client")
    EXCEPTION_FACTORY = ExceptionFactory.new

    def initialize(config = Core::Config.new)
      super("AmazonEC2", "ec2", "1.0", EXCEPTION_FACTORY, config)
    end

    # Accepts an Elastic IP address transfer. For more information, see [Accept a transferred Elastic
    # IP
    # address](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-eips.html#using-instance-addressing-eips-transfer-accept)
    # in the *Amazon VPC User Guide*.
    def accept_address_transfer(input : AEC::AcceptAddressTransferRequest) : Core::ParsedResponse(AEC::AcceptAddressTransferResult)
      Log.info { "performing 'AcceptAddressTransfer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptAddressTransfer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptAddressTransfer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptAddressTransferResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptAddressTransferResult).new(response, result)
    end

    # Accepts a request to assign billing of the available capacity of a shared Capacity Reservation
    # to your account. For more information, see [ Billing assignment for shared Amazon EC2 Capacity
    # Reservations](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/assign-billing.html).
    def accept_capacity_reservation_billing_ownership(input : AEC::AcceptCapacityReservationBillingOwnershipRequest) : Core::ParsedResponse(AEC::AcceptCapacityReservationBillingOwnershipResult)
      Log.info { "performing 'AcceptCapacityReservationBillingOwnership' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptCapacityReservationBillingOwnership")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptCapacityReservationBillingOwnership", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptCapacityReservationBillingOwnershipResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptCapacityReservationBillingOwnershipResult).new(response, result)
    end

    # Accepts the Convertible Reserved Instance exchange quote described in the
    # GetReservedInstancesExchangeQuote call.
    def accept_reserved_instances_exchange_quote(input : AEC::AcceptReservedInstancesExchangeQuoteRequest) : Core::ParsedResponse(AEC::AcceptReservedInstancesExchangeQuoteResult)
      Log.info { "performing 'AcceptReservedInstancesExchangeQuote' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptReservedInstancesExchangeQuote")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptReservedInstancesExchangeQuote", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptReservedInstancesExchangeQuoteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptReservedInstancesExchangeQuoteResult).new(response, result)
    end

    # Accepts a Transit Gateway attachment request for a Client VPN endpoint. The Transit Gateway
    # owner must accept the attachment request before the Client VPN endpoint can route traffic
    # through the Transit Gateway.
    def accept_transit_gateway_client_vpn_attachment(input : AEC::AcceptTransitGatewayClientVpnAttachmentRequest) : Core::ParsedResponse(AEC::AcceptTransitGatewayClientVpnAttachmentResult)
      Log.info { "performing 'AcceptTransitGatewayClientVpnAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptTransitGatewayClientVpnAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptTransitGatewayClientVpnAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptTransitGatewayClientVpnAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptTransitGatewayClientVpnAttachmentResult).new(response, result)
    end

    # Accepts a request to associate subnets with a transit gateway multicast domain.
    def accept_transit_gateway_multicast_domain_associations(input : AEC::AcceptTransitGatewayMulticastDomainAssociationsRequest) : Core::ParsedResponse(AEC::AcceptTransitGatewayMulticastDomainAssociationsResult)
      Log.info { "performing 'AcceptTransitGatewayMulticastDomainAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptTransitGatewayMulticastDomainAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptTransitGatewayMulticastDomainAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptTransitGatewayMulticastDomainAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptTransitGatewayMulticastDomainAssociationsResult).new(response, result)
    end

    # Accepts a transit gateway peering attachment request. The peering attachment must be in the
    # `pendingAcceptance` state.
    def accept_transit_gateway_peering_attachment(input : AEC::AcceptTransitGatewayPeeringAttachmentRequest) : Core::ParsedResponse(AEC::AcceptTransitGatewayPeeringAttachmentResult)
      Log.info { "performing 'AcceptTransitGatewayPeeringAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptTransitGatewayPeeringAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptTransitGatewayPeeringAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptTransitGatewayPeeringAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptTransitGatewayPeeringAttachmentResult).new(response, result)
    end

    # Accepts a request to attach a VPC to a transit gateway.
    #
    # The VPC attachment must be in the `pendingAcceptance` state. Use
    # DescribeTransitGatewayVpcAttachments to view your pending VPC attachment requests. Use
    # RejectTransitGatewayVpcAttachment to reject a VPC attachment request.
    def accept_transit_gateway_vpc_attachment(input : AEC::AcceptTransitGatewayVpcAttachmentRequest) : Core::ParsedResponse(AEC::AcceptTransitGatewayVpcAttachmentResult)
      Log.info { "performing 'AcceptTransitGatewayVpcAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptTransitGatewayVpcAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptTransitGatewayVpcAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptTransitGatewayVpcAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptTransitGatewayVpcAttachmentResult).new(response, result)
    end

    # Accepts connection requests to your VPC endpoint service.
    def accept_vpc_endpoint_connections(input : AEC::AcceptVpcEndpointConnectionsRequest) : Core::ParsedResponse(AEC::AcceptVpcEndpointConnectionsResult)
      Log.info { "performing 'AcceptVpcEndpointConnections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptVpcEndpointConnections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptVpcEndpointConnections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptVpcEndpointConnectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptVpcEndpointConnectionsResult).new(response, result)
    end

    # Accept a VPC peering connection request. To accept a request, the VPC peering connection must be
    # in the `pending-acceptance` state, and you must be the owner of the peer VPC. Use
    # DescribeVpcPeeringConnections to view your outstanding VPC peering connection requests.
    #
    # For an inter-Region VPC peering connection request, you must accept the VPC peering connection
    # in the Region of the accepter VPC.
    def accept_vpc_peering_connection(input : AEC::AcceptVpcPeeringConnectionRequest) : Core::ParsedResponse(AEC::AcceptVpcPeeringConnectionResult)
      Log.info { "performing 'AcceptVpcPeeringConnection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AcceptVpcPeeringConnection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AcceptVpcPeeringConnection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AcceptVpcPeeringConnectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AcceptVpcPeeringConnectionResult).new(response, result)
    end

    # Advertises an IPv4 or IPv6 address range that is provisioned for use with your Amazon Web
    # Services resources through bring your own IP addresses (BYOIP).
    #
    # You can perform this operation at most once every 10 seconds, even if you specify different
    # address ranges each time.
    #
    # We recommend that you stop advertising the BYOIP CIDR from other locations when you advertise it
    # from Amazon Web Services. To minimize down time, you can configure your Amazon Web Services
    # resources to use an address from a BYOIP CIDR before it is advertised, and then simultaneously
    # stop advertising it from the current location and start advertising it through Amazon Web
    # Services.
    #
    # It can take a few minutes before traffic to the specified addresses starts routing to Amazon Web
    # Services because of BGP propagation delays.
    def advertise_byoip_cidr(input : AEC::AdvertiseByoipCidrRequest) : Core::ParsedResponse(AEC::AdvertiseByoipCidrResult)
      Log.info { "performing 'AdvertiseByoipCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AdvertiseByoipCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AdvertiseByoipCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AdvertiseByoipCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AdvertiseByoipCidrResult).new(response, result)
    end

    # Allocates an Elastic IP address to your Amazon Web Services account. After you allocate the
    # Elastic IP address you can associate it with an instance or network interface. After you release
    # an Elastic IP address, it is released to the IP address pool and can be allocated to a different
    # Amazon Web Services account.
    #
    # You can allocate an Elastic IP address from one of the following address pools:
    #
    # - Amazon's pool of IPv4 addresses
    #
    # - Public IPv4 address range that you own and bring to your Amazon Web Services account using
    # [Bring Your Own IP Addresses
    # (BYOIP)](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-byoip.html)
    #
    # - An IPv4 IPAM pool with an Amazon-provided or BYOIP public IPv4 address range
    #
    # - IPv4 addresses from your on-premises network made available for use with an Outpost using a
    # [customer-owned IP address
    # pool](https://docs.aws.amazon.com/outposts/latest/userguide/routing.html#ip-addressing) (CoIP
    # pool)
    #
    # For more information, see [Elastic IP
    # Addresses](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html) in
    # the *Amazon EC2 User Guide*.
    #
    # If you release an Elastic IP address, you might be able to recover it. You cannot recover an
    # Elastic IP address that you released after it is allocated to another Amazon Web Services
    # account. To attempt to recover an Elastic IP address that you released, specify it in this
    # operation.
    #
    # You can allocate a carrier IP address which is a public IP address from a telecommunication
    # carrier, to a network interface which resides in a subnet in a Wavelength Zone (for example an
    # EC2 instance).
    def allocate_address(input : AEC::AllocateAddressRequest) : Core::ParsedResponse(AEC::AllocateAddressResult)
      Log.info { "performing 'AllocateAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AllocateAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AllocateAddress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AllocateAddressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AllocateAddressResult).new(response, result)
    end

    # Allocates a Dedicated Host to your account. At a minimum, specify the supported instance type or
    # instance family, the Availability Zone in which to allocate the host, and the number of hosts to
    # allocate.
    def allocate_hosts(input : AEC::AllocateHostsRequest) : Core::ParsedResponse(AEC::AllocateHostsResult)
      Log.info { "performing 'AllocateHosts' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AllocateHosts")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AllocateHosts", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AllocateHostsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AllocateHostsResult).new(response, result)
    end

    # Allocate a CIDR from an IPAM pool. The Region you use should be the IPAM pool locale. The locale
    # is the Amazon Web Services Region where this IPAM pool is available for allocations.
    #
    # In IPAM, an allocation is a CIDR assignment from an IPAM pool to another IPAM pool or to a
    # resource. For more information, see [Allocate
    # CIDRs](https://docs.aws.amazon.com/vpc/latest/ipam/allocate-cidrs-ipam.html) in the *Amazon VPC
    # IPAM User Guide*.
    #
    # This action creates an allocation with strong consistency. The returned CIDR will not overlap
    # with any other allocations from the same pool.
    def allocate_ipam_pool_cidr(input : AEC::AllocateIpamPoolCidrRequest) : Core::ParsedResponse(AEC::AllocateIpamPoolCidrResult)
      Log.info { "performing 'AllocateIpamPoolCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AllocateIpamPoolCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AllocateIpamPoolCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AllocateIpamPoolCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AllocateIpamPoolCidrResult).new(response, result)
    end

    # Applies a security group to the association between the target network and the Client VPN
    # endpoint. This action replaces the existing security groups with the specified security groups.
    def apply_security_groups_to_client_vpn_target_network(input : AEC::ApplySecurityGroupsToClientVpnTargetNetworkRequest) : Core::ParsedResponse(AEC::ApplySecurityGroupsToClientVpnTargetNetworkResult)
      Log.info { "performing 'ApplySecurityGroupsToClientVpnTargetNetwork' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ApplySecurityGroupsToClientVpnTargetNetwork")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ApplySecurityGroupsToClientVpnTargetNetwork", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ApplySecurityGroupsToClientVpnTargetNetworkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ApplySecurityGroupsToClientVpnTargetNetworkResult).new(response, result)
    end

    # Assigns the specified IPv6 addresses to the specified network interface. You can specify
    # specific IPv6 addresses, or you can specify the number of IPv6 addresses to be automatically
    # assigned from the subnet's IPv6 CIDR block range. You can assign as many IPv6 addresses to a
    # network interface as you can assign private IPv4 addresses, and the limit varies by instance
    # type.
    #
    # You must specify either the IPv6 addresses or the IPv6 address count in the request.
    #
    # You can optionally use Prefix Delegation on the network interface. You must specify either the
    # IPV6 Prefix Delegation prefixes, or the IPv6 Prefix Delegation count. For information, see [
    # Assigning prefixes to network
    # interfaces](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-prefix-eni.html) in the
    # *Amazon EC2 User Guide*.
    def assign_ipv_6_addresses(input : AEC::AssignIpv6AddressesRequest) : Core::ParsedResponse(AEC::AssignIpv6AddressesResult)
      Log.info { "performing 'AssignIpv6Addresses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssignIpv6Addresses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssignIpv6Addresses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssignIpv6AddressesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssignIpv6AddressesResult).new(response, result)
    end

    # Assigns the specified secondary private IP addresses to the specified network interface.
    #
    # You can specify specific secondary IP addresses, or you can specify the number of secondary IP
    # addresses to be automatically assigned from the subnet's CIDR block range. The number of
    # secondary IP addresses that you can assign to an instance varies by instance type. For more
    # information about Elastic IP addresses, see [Elastic IP
    # Addresses](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html) in
    # the *Amazon EC2 User Guide*.
    #
    # When you move a secondary private IP address to another network interface, any Elastic IP
    # address that is associated with the IP address is also moved.
    #
    # Remapping an IP address is an asynchronous operation. When you move an IP address from one
    # network interface to another, check `network/interfaces/macs/mac/local-ipv4s` in the instance
    # metadata to confirm that the remapping is complete.
    #
    # You must specify either the IP addresses or the IP address count in the request.
    #
    # You can optionally use Prefix Delegation on the network interface. You must specify either the
    # IPv4 Prefix Delegation prefixes, or the IPv4 Prefix Delegation count. For information, see [
    # Assigning prefixes to network
    # interfaces](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-prefix-eni.html) in the
    # *Amazon EC2 User Guide*.
    def assign_private_ip_addresses(input : AEC::AssignPrivateIpAddressesRequest) : Core::ParsedResponse(AEC::AssignPrivateIpAddressesResult)
      Log.info { "performing 'AssignPrivateIpAddresses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssignPrivateIpAddresses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssignPrivateIpAddresses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssignPrivateIpAddressesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssignPrivateIpAddressesResult).new(response, result)
    end

    # Assigns private IPv4 addresses to a private NAT gateway. For more information, see [Work with
    # NAT gateways](https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateway-working-with.html) in
    # the *Amazon VPC User Guide*.
    def assign_private_nat_gateway_address(input : AEC::AssignPrivateNatGatewayAddressRequest) : Core::ParsedResponse(AEC::AssignPrivateNatGatewayAddressResult)
      Log.info { "performing 'AssignPrivateNatGatewayAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssignPrivateNatGatewayAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssignPrivateNatGatewayAddress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssignPrivateNatGatewayAddressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssignPrivateNatGatewayAddressResult).new(response, result)
    end

    # Associates an Elastic IP address, or carrier IP address (for instances that are in subnets in
    # Wavelength Zones) with an instance or a network interface. Before you can use an Elastic IP
    # address, you must allocate it to your account.
    #
    # If the Elastic IP address is already associated with a different instance, it is disassociated
    # from that instance and associated with the specified instance. If you associate an Elastic IP
    # address with an instance that has an existing Elastic IP address, the existing address is
    # disassociated from the instance, but remains allocated to your account.
    #
    # [Subnets in Wavelength Zones] You can associate an IP address from the telecommunication carrier
    # to the instance or network interface.
    #
    # You cannot associate an Elastic IP address with an interface in a different network border
    # group.
    #
    # This is an idempotent operation. If you perform the operation more than once, Amazon EC2 doesn't
    # return an error, and you may be charged for each time the Elastic IP address is remapped to the
    # same instance. For more information, see the *Elastic IP Addresses* section of [Amazon EC2
    # Pricing](http://aws.amazon.com/ec2/pricing/).
    def associate_address(input : AEC::AssociateAddressRequest) : Core::ParsedResponse(AEC::AssociateAddressResult)
      Log.info { "performing 'AssociateAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateAddress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateAddressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateAddressResult).new(response, result)
    end

    # Associates an application status check with instances or
    # [tags](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html). Once you create an
    # association, health monitoring automatically begins for the specified instances or for instances
    # that match the specified tags. The following rules apply:
    #
    # - You must specify either `TargetTagAssociations` or `InstanceIds`, but not both. Specifying
    # both results in an `InvalidParameterCombination` error.
    #
    # - You must own the application status check. The check must already exist in your account.
    #
    # - You must not leave tag keys blank.
    #
    # - You can create a maximum of 50 tag associations for each application status check.
    #
    # - You can use `DisassociateApplicationStatusCheck` to remove associations.
    #
    # - You can associate [tags](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html)
    # so that the application status check automatically monitors all current and future instances
    # that have the specified tags.
    def associate_application_status_check(input : AEC::AssociateApplicationStatusCheckRequest) : Core::ParsedResponse(AEC::AssociateApplicationStatusCheckResult)
      Log.info { "performing 'AssociateApplicationStatusCheck' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateApplicationStatusCheck")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateApplicationStatusCheck", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateApplicationStatusCheckResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateApplicationStatusCheckResult).new(response, result)
    end

    # Initiates a request to assign billing of the unused capacity of a shared Capacity Reservation to
    # a consumer account that is consolidated under the same Amazon Web Services organizations payer
    # account. For more information, see [Billing assignment for shared Amazon EC2 Capacity
    # Reservations](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/assign-billing.html).
    def associate_capacity_reservation_billing_owner(input : AEC::AssociateCapacityReservationBillingOwnerRequest) : Core::ParsedResponse(AEC::AssociateCapacityReservationBillingOwnerResult)
      Log.info { "performing 'AssociateCapacityReservationBillingOwner' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateCapacityReservationBillingOwner")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateCapacityReservationBillingOwner", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateCapacityReservationBillingOwnerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateCapacityReservationBillingOwnerResult).new(response, result)
    end

    # Associates a target network with a Client VPN endpoint. A target network is a subnet in a VPC.
    # You can associate multiple subnets from the same VPC with a Client VPN endpoint. You can
    # associate only one subnet in each Availability Zone. We recommend that you associate at least
    # two subnets to provide Availability Zone redundancy.
    #
    # If you specified a VPC when you created the Client VPN endpoint or if you have previous subnet
    # associations, the specified subnet must be in the same VPC. To specify a subnet that's in a
    # different VPC, you must first modify the Client VPN endpoint (ModifyClientVpnEndpoint) and
    # change the VPC that's associated with it.
    def associate_client_vpn_target_network(input : AEC::AssociateClientVpnTargetNetworkRequest) : Core::ParsedResponse(AEC::AssociateClientVpnTargetNetworkResult)
      Log.info { "performing 'AssociateClientVpnTargetNetwork' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateClientVpnTargetNetwork")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateClientVpnTargetNetwork", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateClientVpnTargetNetworkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateClientVpnTargetNetworkResult).new(response, result)
    end

    # Associates a set of DHCP options (that you've previously created) with the specified VPC, or
    # associates no DHCP options with the VPC.
    #
    # After you associate the options with the VPC, any existing instances and all new instances that
    # you launch in that VPC use the options. You don't need to restart or relaunch the instances.
    # They automatically pick up the changes within a few hours, depending on how frequently the
    # instance renews its DHCP lease. You can explicitly renew the lease using the operating system on
    # the instance.
    #
    # For more information, see [DHCP option
    # sets](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_DHCP_Options.html) in the *Amazon VPC
    # User Guide*.
    def associate_dhcp_options(input : AEC::AssociateDhcpOptionsRequest) : Core::Response
      Log.info { "performing 'AssociateDhcpOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateDhcpOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateDhcpOptions", body)
      Core::Response.new(response)
    end

    # Associates an Identity and Access Management (IAM) role with an Certificate Manager (ACM)
    # certificate. This enables the certificate to be used by the ACM for Nitro Enclaves application
    # inside an enclave. For more information, see [Certificate Manager for Nitro
    # Enclaves](https://docs.aws.amazon.com/enclaves/latest/user/nitro-enclave-refapp.html) in the
    # *Amazon Web Services Nitro Enclaves User Guide*.
    #
    # When the IAM role is associated with the ACM certificate, the certificate, certificate chain,
    # and encrypted private key are placed in an Amazon S3 location that only the associated IAM role
    # can access. The private key of the certificate is encrypted with an Amazon Web Services managed
    # key that has an attached attestation-based key policy.
    #
    # To enable the IAM role to access the Amazon S3 object, you must grant it permission to call
    # `s3:GetObject` on the Amazon S3 bucket returned by the command. To enable the IAM role to access
    # the KMS key, you must grant it permission to call `kms:Decrypt` on the KMS key returned by the
    # command. For more information, see [ Grant the role permission to access the certificate and
    # encryption
    # key](https://docs.aws.amazon.com/enclaves/latest/user/nitro-enclave-refapp.html#add-policy) in
    # the *Amazon Web Services Nitro Enclaves User Guide*.
    def associate_enclave_certificate_iam_role(input : AEC::AssociateEnclaveCertificateIamRoleRequest) : Core::ParsedResponse(AEC::AssociateEnclaveCertificateIamRoleResult)
      Log.info { "performing 'AssociateEnclaveCertificateIamRole' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateEnclaveCertificateIamRole")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateEnclaveCertificateIamRole", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateEnclaveCertificateIamRoleResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateEnclaveCertificateIamRoleResult).new(response, result)
    end

    # Associates an IAM instance profile with a running or stopped instance. You cannot associate more
    # than one IAM instance profile with an instance.
    def associate_iam_instance_profile(input : AEC::AssociateIamInstanceProfileRequest) : Core::ParsedResponse(AEC::AssociateIamInstanceProfileResult)
      Log.info { "performing 'AssociateIamInstanceProfile' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateIamInstanceProfile")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateIamInstanceProfile", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateIamInstanceProfileResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateIamInstanceProfileResult).new(response, result)
    end

    # Associates one or more targets with an event window. Only one type of target (instance IDs,
    # Dedicated Host IDs, or tags) can be specified with an event window.
    #
    # For more information, see [Define event windows for scheduled
    # events](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/event-windows.html) in the *Amazon
    # EC2 User Guide*.
    def associate_instance_event_window(input : AEC::AssociateInstanceEventWindowRequest) : Core::ParsedResponse(AEC::AssociateInstanceEventWindowResult)
      Log.info { "performing 'AssociateInstanceEventWindow' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateInstanceEventWindow")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateInstanceEventWindow", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateInstanceEventWindowResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateInstanceEventWindowResult).new(response, result)
    end

    # Associates your Autonomous System Number (ASN) with a BYOIP CIDR that you own in the same Amazon
    # Web Services Region. For more information, see [Tutorial: Bring your ASN to
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/tutorials-byoasn.html) in the *Amazon VPC IPAM
    # guide*.
    #
    # After the association succeeds, the ASN is eligible for advertisement. You can view the
    # association with
    # [DescribeByoipCidrs](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeByoipCidrs.html).
    # You can advertise the CIDR with
    # [AdvertiseByoipCidr](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_AdvertiseByoipCidr.html).
    def associate_ipam_byoasn(input : AEC::AssociateIpamByoasnRequest) : Core::ParsedResponse(AEC::AssociateIpamByoasnResult)
      Log.info { "performing 'AssociateIpamByoasn' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateIpamByoasn")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateIpamByoasn", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateIpamByoasnResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateIpamByoasnResult).new(response, result)
    end

    # Associates an IPAM resource discovery with an Amazon VPC IPAM. A resource discovery is an IPAM
    # component that enables IPAM to manage and monitor resources that belong to the owning account.
    def associate_ipam_resource_discovery(input : AEC::AssociateIpamResourceDiscoveryRequest) : Core::ParsedResponse(AEC::AssociateIpamResourceDiscoveryResult)
      Log.info { "performing 'AssociateIpamResourceDiscovery' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateIpamResourceDiscovery")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateIpamResourceDiscovery", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateIpamResourceDiscoveryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateIpamResourceDiscoveryResult).new(response, result)
    end

    # Associates Elastic IP addresses (EIPs) and private IPv4 addresses with a public NAT gateway. For
    # more information, see [Work with NAT
    # gateways](https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateway-working-with.html) in the
    # *Amazon VPC User Guide*.
    #
    # By default, you can associate up to 2 Elastic IP addresses per public NAT gateway. You can
    # increase the limit by requesting a quota adjustment. For more information, see [Elastic IP
    # address
    # quotas](https://docs.aws.amazon.com/vpc/latest/userguide/amazon-vpc-limits.html#vpc-limits-eips)
    # in the *Amazon VPC User Guide*.
    #
    # When you associate an EIP or secondary EIPs with a public NAT gateway, the network border group
    # of the EIPs must match the network border group of the Availability Zone (AZ) that the public
    # NAT gateway is in. If it's not the same, the EIP will fail to associate. You can see the network
    # border group for the subnet's AZ by viewing the details of the subnet. Similarly, you can view
    # the network border group of an EIP by viewing the details of the EIP address. For more
    # information about network border groups and EIPs, see [Allocate an Elastic IP
    # address](https://docs.aws.amazon.com/vpc/latest/userguide/WorkWithEIPs.html) in the *Amazon VPC
    # User Guide*.
    def associate_nat_gateway_address(input : AEC::AssociateNatGatewayAddressRequest) : Core::ParsedResponse(AEC::AssociateNatGatewayAddressResult)
      Log.info { "performing 'AssociateNatGatewayAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateNatGatewayAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateNatGatewayAddress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateNatGatewayAddressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateNatGatewayAddressResult).new(response, result)
    end

    # Associates a route server with a VPC to enable dynamic route updates.
    #
    # A route server association is the connection established between a route server and a VPC.
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def associate_route_server(input : AEC::AssociateRouteServerRequest) : Core::ParsedResponse(AEC::AssociateRouteServerResult)
      Log.info { "performing 'AssociateRouteServer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateRouteServer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateRouteServer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateRouteServerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateRouteServerResult).new(response, result)
    end

    # Associates a subnet in your VPC or an internet gateway or virtual private gateway attached to
    # your VPC with a route table in your VPC. This association causes traffic from the subnet or
    # gateway to be routed according to the routes in the route table. The action returns an
    # association ID, which you need in order to disassociate the route table later. A route table can
    # be associated with multiple subnets.
    #
    # For more information, see [Route
    # tables](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html) in the *Amazon
    # VPC User Guide*.
    def associate_route_table(input : AEC::AssociateRouteTableRequest) : Core::ParsedResponse(AEC::AssociateRouteTableResult)
      Log.info { "performing 'AssociateRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateRouteTableResult).new(response, result)
    end

    # Associates a security group with another VPC in the same Region. This enables you to use the
    # same security group with network interfaces and instances in the specified VPC.
    #
    # - The VPC you want to associate the security group with must be in the same Region.
    #
    # - You can associate the security group with another VPC if your account owns the VPC or if the
    # VPC was shared with you.
    #
    # - You must own the security group.
    #
    # - You cannot use this feature with default security groups.
    #
    # - You cannot use this feature with the default VPC.
    def associate_security_group_vpc(input : AEC::AssociateSecurityGroupVpcRequest) : Core::ParsedResponse(AEC::AssociateSecurityGroupVpcResult)
      Log.info { "performing 'AssociateSecurityGroupVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateSecurityGroupVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateSecurityGroupVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateSecurityGroupVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateSecurityGroupVpcResult).new(response, result)
    end

    # Associates a CIDR block with your subnet. You can only associate a single IPv6 CIDR block with
    # your subnet.
    def associate_subnet_cidr_block(input : AEC::AssociateSubnetCidrBlockRequest) : Core::ParsedResponse(AEC::AssociateSubnetCidrBlockResult)
      Log.info { "performing 'AssociateSubnetCidrBlock' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateSubnetCidrBlock")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateSubnetCidrBlock", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateSubnetCidrBlockResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateSubnetCidrBlockResult).new(response, result)
    end

    # Associates the specified subnets and transit gateway attachments with the specified transit
    # gateway multicast domain.
    #
    # The transit gateway attachment must be in the available state before you can add a resource. Use
    # [DescribeTransitGatewayAttachments](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeTransitGatewayAttachments.html)
    # to see the state of the attachment.
    def associate_transit_gateway_multicast_domain(input : AEC::AssociateTransitGatewayMulticastDomainRequest) : Core::ParsedResponse(AEC::AssociateTransitGatewayMulticastDomainResult)
      Log.info { "performing 'AssociateTransitGatewayMulticastDomain' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateTransitGatewayMulticastDomain")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateTransitGatewayMulticastDomain", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateTransitGatewayMulticastDomainResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateTransitGatewayMulticastDomainResult).new(response, result)
    end

    # Associates the specified transit gateway attachment with a transit gateway policy table.
    def associate_transit_gateway_policy_table(input : AEC::AssociateTransitGatewayPolicyTableRequest) : Core::ParsedResponse(AEC::AssociateTransitGatewayPolicyTableResult)
      Log.info { "performing 'AssociateTransitGatewayPolicyTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateTransitGatewayPolicyTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateTransitGatewayPolicyTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateTransitGatewayPolicyTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateTransitGatewayPolicyTableResult).new(response, result)
    end

    # Associates the specified attachment with the specified transit gateway route table. You can
    # associate only one route table with an attachment.
    def associate_transit_gateway_route_table(input : AEC::AssociateTransitGatewayRouteTableRequest) : Core::ParsedResponse(AEC::AssociateTransitGatewayRouteTableResult)
      Log.info { "performing 'AssociateTransitGatewayRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateTransitGatewayRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateTransitGatewayRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateTransitGatewayRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateTransitGatewayRouteTableResult).new(response, result)
    end

    # Associates a branch network interface with a trunk network interface.
    #
    # Before you create the association, use
    # [CreateNetworkInterface](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateNetworkInterface.html)
    # command and set the interface type to `trunk`. You must also create a network interface for each
    # branch network interface that you want to associate with the trunk network interface.
    def associate_trunk_interface(input : AEC::AssociateTrunkInterfaceRequest) : Core::ParsedResponse(AEC::AssociateTrunkInterfaceResult)
      Log.info { "performing 'AssociateTrunkInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateTrunkInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateTrunkInterface", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateTrunkInterfaceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateTrunkInterfaceResult).new(response, result)
    end

    # Associates a CIDR block with your VPC. You can associate a secondary IPv4 CIDR block, an
    # Amazon-provided IPv6 CIDR block, or an IPv6 CIDR block from an IPv6 address pool that you
    # provisioned through bring your own IP addresses
    # ([BYOIP](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-byoip.html)).
    #
    # You must specify one of the following in the request: an IPv4 CIDR block, an IPv6 pool, or an
    # Amazon-provided IPv6 CIDR block.
    #
    # For more information about associating CIDR blocks with your VPC and applicable restrictions,
    # see [IP addressing for your VPCs and
    # subnets](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-ip-addressing.html) in the *Amazon
    # VPC User Guide*.
    def associate_vpc_cidr_block(input : AEC::AssociateVpcCidrBlockRequest) : Core::ParsedResponse(AEC::AssociateVpcCidrBlockResult)
      Log.info { "performing 'AssociateVpcCidrBlock' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AssociateVpcCidrBlock")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AssociateVpcCidrBlock", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AssociateVpcCidrBlockResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AssociateVpcCidrBlockResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Links an EC2-Classic instance to a ClassicLink-enabled VPC through one or more of the VPC
    # security groups. You cannot link an EC2-Classic instance to more than one VPC at a time. You can
    # only link an instance that's in the `running` state. An instance is automatically unlinked from
    # a VPC when it's stopped - you can link it to the VPC again when you restart it.
    #
    # After you've linked an instance, you cannot change the VPC security groups that are associated
    # with it. To change the security groups, you must first unlink the instance, and then link it
    # again.
    #
    # Linking your instance to a VPC is sometimes referred to as *attaching* your instance.
    def attach_classic_link_vpc(input : AEC::AttachClassicLinkVpcRequest) : Core::ParsedResponse(AEC::AttachClassicLinkVpcResult)
      Log.info { "performing 'AttachClassicLinkVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AttachClassicLinkVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AttachClassicLinkVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AttachClassicLinkVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AttachClassicLinkVpcResult).new(response, result)
    end

    # Attaches a watermark to a non-public AMI. The watermark is a structured identifier that
    # automatically propagates to all derivative images created through
    # [CreateImage](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateImage.html), and
    # [CopyImage](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CopyImage.html).
    #
    # Only the AMI owner can attach watermarks. Watermarks cannot be added to public AMIs.
    def attach_image_watermark(input : AEC::AttachImageWatermarkRequest) : Core::ParsedResponse(AEC::AttachImageWatermarkResult)
      Log.info { "performing 'AttachImageWatermark' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AttachImageWatermark")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AttachImageWatermark", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AttachImageWatermarkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AttachImageWatermarkResult).new(response, result)
    end

    # Attaches an internet gateway or a virtual private gateway to a VPC, enabling connectivity
    # between the internet and the VPC. For more information, see [Internet
    # gateways](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Internet_Gateway.html) in the
    # *Amazon VPC User Guide*.
    def attach_internet_gateway(input : AEC::AttachInternetGatewayRequest) : Core::Response
      Log.info { "performing 'AttachInternetGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AttachInternetGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AttachInternetGateway", body)
      Core::Response.new(response)
    end

    # Attaches a network interface to an instance.
    def attach_network_interface(input : AEC::AttachNetworkInterfaceRequest) : Core::ParsedResponse(AEC::AttachNetworkInterfaceResult)
      Log.info { "performing 'AttachNetworkInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AttachNetworkInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AttachNetworkInterface", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AttachNetworkInterfaceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AttachNetworkInterfaceResult).new(response, result)
    end

    # Attaches the specified Amazon Web Services Verified Access trust provider to the specified
    # Amazon Web Services Verified Access instance.
    def attach_verified_access_trust_provider(input : AEC::AttachVerifiedAccessTrustProviderRequest) : Core::ParsedResponse(AEC::AttachVerifiedAccessTrustProviderResult)
      Log.info { "performing 'AttachVerifiedAccessTrustProvider' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AttachVerifiedAccessTrustProvider")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AttachVerifiedAccessTrustProvider", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AttachVerifiedAccessTrustProviderResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AttachVerifiedAccessTrustProviderResult).new(response, result)
    end

    # Attaches an Amazon EBS volume to a `running` or `stopped` instance, and exposes it to the
    # instance with the specified device name.
    #
    # The maximum number of Amazon EBS volumes that you can attach to an instance depends on the
    # instance type. If you exceed the volume attachment limit for an instance type, the attachment
    # request fails with the `AttachmentLimitExceeded` error. For more information, see [Instance
    # volume limits](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/volume_limits.html).
    #
    # After you attach an EBS volume, you must make it available for use. For more information, see
    # [Make an EBS volume available for
    # use](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-using-volumes.html).
    #
    # If a volume has an Amazon Web Services Marketplace product code:
    #
    # - The volume can be attached only to a stopped instance.
    #
    # - Amazon Web Services Marketplace product codes are copied from the volume to the instance.
    #
    # - You must be subscribed to the product.
    #
    # - The instance type and operating system of the instance must support the product. For example,
    # you can't detach a volume from a Windows instance and attach it to a Linux instance.
    #
    # For more information, see [Attach an Amazon EBS volume to an
    # instance](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-attaching-volume.html) in the
    # *Amazon EBS User Guide*.
    def attach_volume(input : AEC::AttachVolumeRequest) : Core::ParsedResponse(AEC::VolumeAttachment)
      Log.info { "performing 'AttachVolume' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AttachVolume")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AttachVolume", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::VolumeAttachment.from_xml(result_node)
      Core::ParsedResponse(AEC::VolumeAttachment).new(response, result)
    end

    # Attaches an available virtual private gateway to a VPC. You can attach one virtual private
    # gateway to one VPC at a time.
    #
    # For more information, see [Amazon Web Services Site-to-Site
    # VPN](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPC_VPN.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def attach_vpn_gateway(input : AEC::AttachVpnGatewayRequest) : Core::ParsedResponse(AEC::AttachVpnGatewayResult)
      Log.info { "performing 'AttachVpnGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AttachVpnGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AttachVpnGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AttachVpnGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AttachVpnGatewayResult).new(response, result)
    end

    # Adds an ingress authorization rule to a Client VPN endpoint. Ingress authorization rules act as
    # firewall rules that grant access to networks. You must configure ingress authorization rules to
    # enable clients to access resources in Amazon Web Services or on-premises networks.
    def authorize_client_vpn_ingress(input : AEC::AuthorizeClientVpnIngressRequest) : Core::ParsedResponse(AEC::AuthorizeClientVpnIngressResult)
      Log.info { "performing 'AuthorizeClientVpnIngress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AuthorizeClientVpnIngress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AuthorizeClientVpnIngress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AuthorizeClientVpnIngressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AuthorizeClientVpnIngressResult).new(response, result)
    end

    # Adds the specified outbound (egress) rules to a security group.
    #
    # An outbound rule permits instances to send traffic to the specified IPv4 or IPv6 address ranges,
    # the IP address ranges specified by a prefix list, or the instances that are associated with a
    # source security group. For more information, see [Security group
    # rules](https://docs.aws.amazon.com/vpc/latest/userguide/security-group-rules.html).
    #
    # You must specify exactly one of the following destinations: an IPv4 or IPv6 address range, a
    # prefix list, or a security group. You must specify a protocol for each rule (for example, TCP).
    # If the protocol is TCP or UDP, you must also specify a port or port range. If the protocol is
    # ICMP or ICMPv6, you must also specify the ICMP type and code.
    #
    # Rule changes are propagated to instances associated with the security group as quickly as
    # possible. However, a small delay might occur.
    #
    # For examples of rules that you can add to security groups for specific access scenarios, see
    # [Security group rules for different use
    # cases](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/security-group-rules-reference.html)
    # in the *Amazon EC2 User Guide*.
    #
    # For information about security group quotas, see [Amazon VPC
    # quotas](https://docs.aws.amazon.com/vpc/latest/userguide/amazon-vpc-limits.html) in the *Amazon
    # VPC User Guide*.
    def authorize_security_group_egress(input : AEC::AuthorizeSecurityGroupEgressRequest) : Core::ParsedResponse(AEC::AuthorizeSecurityGroupEgressResult)
      Log.info { "performing 'AuthorizeSecurityGroupEgress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AuthorizeSecurityGroupEgress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AuthorizeSecurityGroupEgress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AuthorizeSecurityGroupEgressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AuthorizeSecurityGroupEgressResult).new(response, result)
    end

    # Adds the specified inbound (ingress) rules to a security group.
    #
    # An inbound rule permits instances to receive traffic from the specified IPv4 or IPv6 address
    # range, the IP address ranges that are specified by a prefix list, or the instances that are
    # associated with a destination security group. For more information, see [Security group
    # rules](https://docs.aws.amazon.com/vpc/latest/userguide/security-group-rules.html).
    #
    # You must specify exactly one of the following sources: an IPv4 or IPv6 address range, a prefix
    # list, or a security group. You must specify a protocol for each rule (for example, TCP). If the
    # protocol is TCP or UDP, you must also specify a port or port range. If the protocol is ICMP or
    # ICMPv6, you must also specify the ICMP/ICMPv6 type and code.
    #
    # Rule changes are propagated to instances associated with the security group as quickly as
    # possible. However, a small delay might occur.
    #
    # For examples of rules that you can add to security groups for specific access scenarios, see
    # [Security group rules for different use
    # cases](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/security-group-rules-reference.html)
    # in the *Amazon EC2 User Guide*.
    #
    # For more information about security group quotas, see [Amazon VPC
    # quotas](https://docs.aws.amazon.com/vpc/latest/userguide/amazon-vpc-limits.html) in the *Amazon
    # VPC User Guide*.
    def authorize_security_group_ingress(input : AEC::AuthorizeSecurityGroupIngressRequest) : Core::ParsedResponse(AEC::AuthorizeSecurityGroupIngressResult)
      Log.info { "performing 'AuthorizeSecurityGroupIngress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "AuthorizeSecurityGroupIngress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("AuthorizeSecurityGroupIngress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::AuthorizeSecurityGroupIngressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::AuthorizeSecurityGroupIngressResult).new(response, result)
    end

    # Modifies multiple routing policy registrations in a single operation. You can create, update, or
    # delete Route Origin Authorizations (ROAs) in batch.
    def batch_modify_ipam_routing_policy_registrations(input : AEC::BatchModifyIpamRoutingPolicyRegistrationsRequest) : Core::ParsedResponse(AEC::BatchModifyIpamRoutingPolicyRegistrationsResult)
      Log.info { "performing 'BatchModifyIpamRoutingPolicyRegistrations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "BatchModifyIpamRoutingPolicyRegistrations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("BatchModifyIpamRoutingPolicyRegistrations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::BatchModifyIpamRoutingPolicyRegistrationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::BatchModifyIpamRoutingPolicyRegistrationsResult).new(response, result)
    end

    # Bundles an Amazon instance store-backed Windows instance.
    #
    # During bundling, only the root device volume (C:\) is bundled. Data on other instance store
    # volumes is not preserved.
    #
    # BundleInstance is no longer supported. To create an AMI, use
    # [CreateImage](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateImage.html)
    # instead. For more information about creating an Amazon EBS-backed AMI, see [ Create an Amazon
    # EBS-backed AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/creating-an-ami-ebs.html) in
    # the *Amazon EC2 User Guide*.
    def bundle_instance(input : AEC::BundleInstanceRequest) : Core::ParsedResponse(AEC::BundleInstanceResult)
      Log.info { "performing 'BundleInstance' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "BundleInstance")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("BundleInstance", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::BundleInstanceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::BundleInstanceResult).new(response, result)
    end

    # Cancels a bundling operation for an instance store-backed Windows instance.
    #
    # CancelBundleTask is no longer supported because
    # [BundleInstance](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_BundleInstance.html),
    # the operation it cancels, is no longer supported.
    def cancel_bundle_task(input : AEC::CancelBundleTaskRequest) : Core::ParsedResponse(AEC::CancelBundleTaskResult)
      Log.info { "performing 'CancelBundleTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelBundleTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelBundleTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelBundleTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelBundleTaskResult).new(response, result)
    end

    # Cancels the specified Capacity Reservation, releases the reserved capacity, and changes the
    # Capacity Reservation's state to `cancelled`.
    #
    # You can cancel a Capacity Reservation that is in the following states:
    #
    # - `assessing`
    #
    # - `scheduled` — requires a cancellation quote. Use `CreateCapacityReservationCancellationQuote`
    # to generate a quote, then pass the quote ID with `ApplyCancellationCharges` set to
    # `commitment-wind-down`. The cancellation charge depends on how close the reservation is to its
    # start date.
    #
    # - `active` and there is no commitment duration or the commitment duration has elapsed.
    #
    # - `active` during the commitment duration — requires a cancellation quote. Use
    # `CreateCapacityReservationCancellationQuote` to generate a quote, then pass the quote ID with
    # `ApplyCancellationCharges` set to `commitment-wind-down`. The Capacity Reservation transitions
    # to `cancelling` while charges are applied.
    #
    # - `delayed` — the commitment duration is waived, so no cancellation charge applies.
    #
    # You can't modify or cancel a Capacity Block. For more information, see [Capacity Blocks for
    # ML](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-capacity-blocks.html).
    #
    # Instances running in the reserved capacity continue running until you stop them. Stopped
    # instances that target the Capacity Reservation can no longer launch. Modify these instances to
    # either target a different Capacity Reservation, launch On-Demand Instance capacity, or run in
    # any open Capacity Reservation that has matching attributes and sufficient capacity.
    def cancel_capacity_reservation(input : AEC::CancelCapacityReservationRequest) : Core::ParsedResponse(AEC::CancelCapacityReservationResult)
      Log.info { "performing 'CancelCapacityReservation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelCapacityReservation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelCapacityReservation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelCapacityReservationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelCapacityReservationResult).new(response, result)
    end

    # Cancels one or more Capacity Reservation Fleets. When you cancel a Capacity Reservation Fleet,
    # the following happens:
    #
    # - The Capacity Reservation Fleet's status changes to `cancelled`.
    #
    # - The individual Capacity Reservations in the Fleet are cancelled. Instances running in the
    # Capacity Reservations at the time of cancelling the Fleet continue to run in shared capacity.
    #
    # - The Fleet stops creating new Capacity Reservations.
    def cancel_capacity_reservation_fleets(input : AEC::CancelCapacityReservationFleetsRequest) : Core::ParsedResponse(AEC::CancelCapacityReservationFleetsResult)
      Log.info { "performing 'CancelCapacityReservationFleets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelCapacityReservationFleets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelCapacityReservationFleets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelCapacityReservationFleetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelCapacityReservationFleetsResult).new(response, result)
    end

    # Cancels an active conversion task. The task can be the import of an instance or volume. The
    # action removes all artifacts of the conversion, including a partially uploaded volume or
    # instance. If the conversion is complete or is in the process of transferring the final disk
    # image, the command fails and returns an exception.
    def cancel_conversion_task(input : AEC::CancelConversionRequest) : Core::Response
      Log.info { "performing 'CancelConversionTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelConversionTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelConversionTask", body)
      Core::Response.new(response)
    end

    # Cancels the generation of an account status report.
    #
    # You can only cancel a report while it has the `running` status. Reports with other statuses
    # (`complete`, `cancelled`, or `error`) can't be canceled.
    #
    # For more information, see [Generating the account status report for declarative
    # policies](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_declarative_status-report.html)
    # in the *Amazon Web Services Organizations User Guide*.
    def cancel_declarative_policies_report(input : AEC::CancelDeclarativePoliciesReportRequest) : Core::ParsedResponse(AEC::CancelDeclarativePoliciesReportResult)
      Log.info { "performing 'CancelDeclarativePoliciesReport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelDeclarativePoliciesReport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelDeclarativePoliciesReport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelDeclarativePoliciesReportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelDeclarativePoliciesReportResult).new(response, result)
    end

    # Cancels an active export task. The request removes all artifacts of the export, including any
    # partially-created Amazon S3 objects. If the export task is complete or is in the process of
    # transferring the final disk image, the command fails and returns an error.
    def cancel_export_task(input : AEC::CancelExportTaskRequest) : Core::Response
      Log.info { "performing 'CancelExportTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelExportTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelExportTask", body)
      Core::Response.new(response)
    end

    # Removes your Amazon Web Services account from the launch permissions for the specified AMI. For
    # more information, see [Cancel having an AMI shared with your Amazon Web Services
    # account](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/cancel-sharing-an-AMI.html) in the
    # *Amazon EC2 User Guide*.
    def cancel_image_launch_permission(input : AEC::CancelImageLaunchPermissionRequest) : Core::ParsedResponse(AEC::CancelImageLaunchPermissionResult)
      Log.info { "performing 'CancelImageLaunchPermission' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelImageLaunchPermission")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelImageLaunchPermission", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelImageLaunchPermissionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelImageLaunchPermissionResult).new(response, result)
    end

    # Cancels an in-process import virtual machine or import snapshot task.
    def cancel_import_task(input : AEC::CancelImportTaskRequest) : Core::ParsedResponse(AEC::CancelImportTaskResult)
      Log.info { "performing 'CancelImportTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelImportTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelImportTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelImportTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelImportTaskResult).new(response, result)
    end

    # Cancels the specified Reserved Instance listing in the Reserved Instance Marketplace.
    #
    # For more information, see [Sell in the Reserved Instance
    # Marketplace](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ri-market-general.html) in the
    # *Amazon EC2 User Guide*.
    def cancel_reserved_instances_listing(input : AEC::CancelReservedInstancesListingRequest) : Core::ParsedResponse(AEC::CancelReservedInstancesListingResult)
      Log.info { "performing 'CancelReservedInstancesListing' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelReservedInstancesListing")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelReservedInstancesListing", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelReservedInstancesListingResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelReservedInstancesListingResult).new(response, result)
    end

    # Cancels the specified Spot Fleet requests.
    #
    # After you cancel a Spot Fleet request, the Spot Fleet launches no new instances.
    #
    # You must also specify whether a canceled Spot Fleet request should terminate its instances. If
    # you choose to terminate the instances, the Spot Fleet request enters the `cancelled_terminating`
    # state. Otherwise, the Spot Fleet request enters the `cancelled_running` state and the instances
    # continue to run until they are interrupted or you terminate them manually.
    #
    # **Terminating an instance is permanent and irreversible.**
    #
    # After you terminate an instance, you can no longer connect to it, and it can't be recovered. All
    # attached Amazon EBS volumes that are configured to be deleted on termination are also
    # permanently deleted and can't be recovered. All data stored on instance store volumes is
    # permanently lost. For more information, see [ How instance termination
    # works](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/how-ec2-instance-termination-works.html).
    #
    # Before you terminate an instance, ensure that you have backed up all data that you need to
    # retain after the termination to persistent storage.
    #
    # **Restrictions**
    #
    # - You can delete up to 100 fleets in a single request. If you exceed the specified number, no
    # fleets are deleted.
    def cancel_spot_fleet_requests(input : AEC::CancelSpotFleetRequestsRequest) : Core::ParsedResponse(AEC::CancelSpotFleetRequestsResponse)
      Log.info { "performing 'CancelSpotFleetRequests' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelSpotFleetRequests")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelSpotFleetRequests", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelSpotFleetRequestsResponse.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelSpotFleetRequestsResponse).new(response, result)
    end

    # Cancels one or more Spot Instance requests.
    #
    # Canceling a Spot Instance request does not terminate running Spot Instances associated with the
    # request.
    def cancel_spot_instance_requests(input : AEC::CancelSpotInstanceRequestsRequest) : Core::ParsedResponse(AEC::CancelSpotInstanceRequestsResult)
      Log.info { "performing 'CancelSpotInstanceRequests' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CancelSpotInstanceRequests")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CancelSpotInstanceRequests", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CancelSpotInstanceRequestsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CancelSpotInstanceRequestsResult).new(response, result)
    end

    # Determines whether a product code is associated with an instance. This action can only be used
    # by the owner of the product code. It is useful when a product code owner must verify whether
    # another user's instance is eligible for support.
    def confirm_product_instance(input : AEC::ConfirmProductInstanceRequest) : Core::ParsedResponse(AEC::ConfirmProductInstanceResult)
      Log.info { "performing 'ConfirmProductInstance' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ConfirmProductInstance")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ConfirmProductInstance", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ConfirmProductInstanceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ConfirmProductInstanceResult).new(response, result)
    end

    # Copies the specified Amazon FPGA Image (AFI) to the current Region.
    def copy_fpga_image(input : AEC::CopyFpgaImageRequest) : Core::ParsedResponse(AEC::CopyFpgaImageResult)
      Log.info { "performing 'CopyFpgaImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CopyFpgaImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CopyFpgaImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CopyFpgaImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CopyFpgaImageResult).new(response, result)
    end

    # Initiates an AMI copy operation. You must specify the source AMI ID and both the source and
    # destination locations. The copy operation must be initiated in the destination Region.
    #
    # **CopyImage supports the following source to destination copies:**
    #
    # - Region to Region
    #
    # - Region to Outpost
    #
    # - Parent Region to Local Zone
    #
    # - Local Zone to parent Region
    #
    # - Between Local Zones with the same parent Region (only supported for certain Local Zones)
    #
    # **CopyImage does not support the following source to destination copies:**
    #
    # - Local Zone to non-parent Regions
    #
    # - Between Local Zones with different parent Regions
    #
    # - Local Zone to Outpost
    #
    # - Outpost to Local Zone
    #
    # - Outpost to Region
    #
    # - Between Outposts
    #
    # - Within same Outpost
    #
    # - Cross-partition copies (use
    # [CreateStoreImageTask](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateStoreImageTask.html)
    # instead)
    #
    # **Destination specification**
    #
    # - Region to Region: The destination Region is the Region in which you initiate the copy
    # operation.
    #
    # - Region to Outpost: Specify the destination using the `DestinationOutpostArn` parameter (the
    # ARN of the Outpost)
    #
    # - Region to Local Zone, and Local Zone to Local Zone copies: Specify the destination using the
    # `DestinationAvailabilityZone` parameter (the name of the destination Local Zone) or
    # `DestinationAvailabilityZoneId` parameter (the ID of the destination Local Zone).
    #
    # **Snapshot encryption**
    #
    # - Region to Outpost: Backing snapshots copied to an Outpost are encrypted by default using the
    # default encryption key for the Region or the key that you specify. Outposts do not support
    # unencrypted snapshots.
    #
    # - Region to Local Zone, and Local Zone to Local Zone: Not all Local Zones require encrypted
    # snapshots. In Local Zones that require encrypted snapshots, backing snapshots are automatically
    # encrypted during copy. In Local Zones where encryption is not required, snapshots retain their
    # original encryption state (encrypted or unencrypted) by default.
    #
    # For more information, including the required permissions for copying an AMI, see [Copy an Amazon
    # EC2 AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/CopyingAMIs.html) in the *Amazon
    # EC2 User Guide*.
    def copy_image(input : AEC::CopyImageRequest) : Core::ParsedResponse(AEC::CopyImageResult)
      Log.info { "performing 'CopyImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CopyImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CopyImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CopyImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CopyImageResult).new(response, result)
    end

    # Creates an exact copy of an Amazon EBS snapshot.
    #
    # The location of the source snapshot determines whether you can copy it or not, and the allowed
    # destinations for the snapshot copy.
    #
    # - If the source snapshot is in a Region, you can copy it within that Region, to another Region,
    # to an Outpost associated with that Region, or to a Local Zone in that Region.
    #
    # - If the source snapshot is in a Local Zone, you can copy it within that Local Zone, to another
    # Local Zone in the same zone group, or to the parent Region of the Local Zone.
    #
    # - If the source snapshot is on an Outpost, you can't copy it.
    #
    # When copying snapshots to a Region, the encryption outcome for the snapshot copy depends on the
    # Amazon EBS encryption by default setting for the destination Region, the encryption status of
    # the source snapshot, and the encryption parameters you specify in the request. For more
    # information, see [ Encryption and snapshot
    # copying](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-copy-snapshot.html#creating-encrypted-snapshots).
    #
    # Snapshots copied to an Outpost must be encrypted. Unencrypted snapshots are not supported on
    # Outposts. For more information, [ Amazon EBS local snapshots on
    # Outposts](https://docs.aws.amazon.com/ebs/latest/userguide/snapshots-outposts.html#considerations).
    #
    # Snapshots copies have an arbitrary source volume ID. Do not use this volume ID for any purpose.
    #
    # For more information, see [Copy an Amazon EBS
    # snapshot](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-copy-snapshot.html) in the
    # *Amazon EBS User Guide*.
    def copy_snapshot(input : AEC::CopySnapshotRequest) : Core::ParsedResponse(AEC::CopySnapshotResult)
      Log.info { "performing 'CopySnapshot' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CopySnapshot")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CopySnapshot", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CopySnapshotResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CopySnapshotResult).new(response, result)
    end

    # Creates a crash-consistent, point-in-time copy of an existing Amazon EBS volume within the same
    # Availability Zone. The volume copy can be attached to an Amazon EC2 instance once it reaches the
    # `available` state. For more information, see [Copy an Amazon EBS
    # volume](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-copying-volume.html).
    def copy_volumes(input : AEC::CopyVolumesRequest) : Core::ParsedResponse(AEC::CopyVolumesResult)
      Log.info { "performing 'CopyVolumes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CopyVolumes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CopyVolumes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CopyVolumesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CopyVolumesResult).new(response, result)
    end

    # Creates an application status check for monitoring the health of applications running on your
    # instances. You can configure the protocol, port, path, and thresholds for the health check. The
    # following rules apply:
    #
    # - You can create a maximum of 50 application status checks for each account.
    #
    # - You must associate the check with instances or tags using `AssociateApplicationStatusCheck`
    # before health checks start.
    #
    # - You must set the `Timeout` value to less than the `Interval` value.
    #
    # - You must start the `Path` with a forward slash (`/`). Default: `/`.
    #
    # - You can specify `Aggregation` as `included` or `excluded`. If you do not specify a value, it
    # defaults to `included`, which means the check contributes to the instance-level application
    # status.
    #
    # - You can use the following default values: `Interval` is 60 seconds, `Timeout` is 6 seconds,
    # `FailureThreshold` is 2, `SuccessThreshold` is 2, `StatusCodeMatcher` is `200`,
    # `InitializationGracePeriodSeconds` is 300 seconds.
    #
    # - You can tag the application status check during creation. For more information, see [Tag your
    # Amazon EC2 resources](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html).
    def create_application_status_check(input : AEC::CreateApplicationStatusCheckRequest) : Core::ParsedResponse(AEC::CreateApplicationStatusCheckResult)
      Log.info { "performing 'CreateApplicationStatusCheck' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateApplicationStatusCheck")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateApplicationStatusCheck", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateApplicationStatusCheckResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateApplicationStatusCheckResult).new(response, result)
    end

    # Creates a new data export configuration for EC2 Capacity Manager. This allows you to
    # automatically export capacity usage data to an S3 bucket on a scheduled basis. The exported data
    # includes metrics for On-Demand, Spot, and Capacity Reservations usage across your organization.
    def create_capacity_manager_data_export(input : AEC::CreateCapacityManagerDataExportRequest) : Core::ParsedResponse(AEC::CreateCapacityManagerDataExportResult)
      Log.info { "performing 'CreateCapacityManagerDataExport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCapacityManagerDataExport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCapacityManagerDataExport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCapacityManagerDataExportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCapacityManagerDataExportResult).new(response, result)
    end

    # Creates a new Capacity Reservation with the specified attributes. Capacity Reservations enable
    # you to reserve capacity for your Amazon EC2 instances in a specific Availability Zone for any
    # duration.
    #
    # You can create a Capacity Reservation at any time, and you can choose when it starts. You can
    # create a Capacity Reservation for immediate use or you can request a Capacity Reservation for a
    # future date.
    #
    # For more information, see [ Reserve compute capacity with On-Demand Capacity
    # Reservations](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-capacity-reservations.html)
    # in the *Amazon EC2 User Guide*.
    #
    # Your request to create a Capacity Reservation could fail if:
    #
    # - Amazon EC2 does not have sufficient capacity. In this case, try again at a later time, try in
    # a different Availability Zone, or request a smaller Capacity Reservation. If your workload is
    # flexible across instance types and sizes, try with different instance attributes.
    #
    # - The requested quantity exceeds your On-Demand Instance quota. In this case, increase your
    # On-Demand Instance quota for the requested instance type and try again. For more information,
    # see [ Amazon EC2 Service
    # Quotas](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-resource-limits.html) in the
    # *Amazon EC2 User Guide*.
    def create_capacity_reservation(input : AEC::CreateCapacityReservationRequest) : Core::ParsedResponse(AEC::CreateCapacityReservationResult)
      Log.info { "performing 'CreateCapacityReservation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCapacityReservation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCapacityReservation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCapacityReservationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCapacityReservationResult).new(response, result)
    end

    # Create a new Capacity Reservation by splitting the capacity of the source Capacity Reservation.
    # The new Capacity Reservation will have the same attributes as the source Capacity Reservation
    # except for tags. The source Capacity Reservation must be `active` and owned by your Amazon Web
    # Services account.
    def create_capacity_reservation_by_splitting(input : AEC::CreateCapacityReservationBySplittingRequest) : Core::ParsedResponse(AEC::CreateCapacityReservationBySplittingResult)
      Log.info { "performing 'CreateCapacityReservationBySplitting' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCapacityReservationBySplitting")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCapacityReservationBySplitting", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCapacityReservationBySplittingResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCapacityReservationBySplittingResult).new(response, result)
    end

    # Generates a cancellation quote for a future-dated Capacity Reservation that is within its
    # commitment duration. The quote includes the cancellation terms and a quote ID that you can pass
    # to the `CancelCapacityReservation` action. Cancellation quotes are valid for 24 hours.
    def create_capacity_reservation_cancellation_quote(input : AEC::CreateCapacityReservationCancellationQuoteRequest) : Core::ParsedResponse(AEC::CreateCapacityReservationCancellationQuoteResult)
      Log.info { "performing 'CreateCapacityReservationCancellationQuote' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCapacityReservationCancellationQuote")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCapacityReservationCancellationQuote", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCapacityReservationCancellationQuoteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCapacityReservationCancellationQuoteResult).new(response, result)
    end

    # Creates a Capacity Reservation Fleet. For more information, see [Create a Capacity Reservation
    # Fleet](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/work-with-cr-fleets.html#create-crfleet)
    # in the *Amazon EC2 User Guide*.
    def create_capacity_reservation_fleet(input : AEC::CreateCapacityReservationFleetRequest) : Core::ParsedResponse(AEC::CreateCapacityReservationFleetResult)
      Log.info { "performing 'CreateCapacityReservationFleet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCapacityReservationFleet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCapacityReservationFleet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCapacityReservationFleetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCapacityReservationFleetResult).new(response, result)
    end

    # Creates a carrier gateway. For more information about carrier gateways, see [Carrier
    # gateways](https://docs.aws.amazon.com/wavelength/latest/developerguide/how-wavelengths-work.html#wavelength-carrier-gateway)
    # in the *Amazon Web Services Wavelength Developer Guide*.
    def create_carrier_gateway(input : AEC::CreateCarrierGatewayRequest) : Core::ParsedResponse(AEC::CreateCarrierGatewayResult)
      Log.info { "performing 'CreateCarrierGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCarrierGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCarrierGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCarrierGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCarrierGatewayResult).new(response, result)
    end

    # Creates a Client VPN endpoint. A Client VPN endpoint is the resource you create and configure to
    # enable and manage client VPN sessions. It is the destination endpoint at which all client VPN
    # sessions are terminated.
    def create_client_vpn_endpoint(input : AEC::CreateClientVpnEndpointRequest) : Core::ParsedResponse(AEC::CreateClientVpnEndpointResult)
      Log.info { "performing 'CreateClientVpnEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateClientVpnEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateClientVpnEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateClientVpnEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateClientVpnEndpointResult).new(response, result)
    end

    # Adds a route to a network to a Client VPN endpoint. Each Client VPN endpoint has a route table
    # that describes the available destination network routes. Each route in the route table specifies
    # the path for traﬃc to speciﬁc resources or networks.
    def create_client_vpn_route(input : AEC::CreateClientVpnRouteRequest) : Core::ParsedResponse(AEC::CreateClientVpnRouteResult)
      Log.info { "performing 'CreateClientVpnRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateClientVpnRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateClientVpnRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateClientVpnRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateClientVpnRouteResult).new(response, result)
    end

    # Creates a range of customer-owned IP addresses.
    def create_coip_cidr(input : AEC::CreateCoipCidrRequest) : Core::ParsedResponse(AEC::CreateCoipCidrResult)
      Log.info { "performing 'CreateCoipCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCoipCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCoipCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCoipCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCoipCidrResult).new(response, result)
    end

    # Creates a pool of customer-owned IP (CoIP) addresses.
    def create_coip_pool(input : AEC::CreateCoipPoolRequest) : Core::ParsedResponse(AEC::CreateCoipPoolResult)
      Log.info { "performing 'CreateCoipPool' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCoipPool")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCoipPool", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCoipPoolResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCoipPoolResult).new(response, result)
    end

    # Provides information to Amazon Web Services about your customer gateway device. The customer
    # gateway device is the appliance at your end of the VPN connection. You must provide the IP
    # address of the customer gateway device’s external interface. The IP address must be static and
    # can be behind a device performing network address translation (NAT).
    #
    # For devices that use Border Gateway Protocol (BGP), you can also provide the device's BGP
    # Autonomous System Number (ASN). You can use an existing ASN assigned to your network. If you
    # don't have an ASN already, you can use a private ASN. For more information, see [Customer
    # gateway options for your Site-to-Site VPN
    # connection](https://docs.aws.amazon.com/vpn/latest/s2svpn/cgw-options.html) in the *Amazon Web
    # Services Site-to-Site VPN User Guide*.
    #
    # To create more than one customer gateway with the same VPN type, IP address, and BGP ASN,
    # specify a unique device name for each customer gateway. An identical request returns information
    # about the existing customer gateway; it doesn't create a new customer gateway.
    def create_customer_gateway(input : AEC::CreateCustomerGatewayRequest) : Core::ParsedResponse(AEC::CreateCustomerGatewayResult)
      Log.info { "performing 'CreateCustomerGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateCustomerGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateCustomerGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateCustomerGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateCustomerGatewayResult).new(response, result)
    end

    # Creates a default subnet with a size `/20` IPv4 CIDR block in the specified Availability Zone in
    # your default VPC. You can have only one default subnet per Availability Zone. For more
    # information, see [Create a default
    # subnet](https://docs.aws.amazon.com/vpc/latest/userguide/work-with-default-vpc.html#create-default-subnet)
    # in the *Amazon VPC User Guide*.
    def create_default_subnet(input : AEC::CreateDefaultSubnetRequest) : Core::ParsedResponse(AEC::CreateDefaultSubnetResult)
      Log.info { "performing 'CreateDefaultSubnet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateDefaultSubnet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateDefaultSubnet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateDefaultSubnetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateDefaultSubnetResult).new(response, result)
    end

    # Creates a default VPC with a size `/16` IPv4 CIDR block and a default subnet in each
    # Availability Zone. For more information about the components of a default VPC, see [Default
    # VPCs](https://docs.aws.amazon.com/vpc/latest/userguide/default-vpc.html) in the *Amazon VPC User
    # Guide*. You cannot specify the components of the default VPC yourself.
    #
    # If you deleted your previous default VPC, you can create a default VPC. You cannot have more
    # than one default VPC per Region.
    def create_default_vpc(input : AEC::CreateDefaultVpcRequest) : Core::ParsedResponse(AEC::CreateDefaultVpcResult)
      Log.info { "performing 'CreateDefaultVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateDefaultVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateDefaultVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateDefaultVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateDefaultVpcResult).new(response, result)
    end

    # Delegates ownership of the Amazon EBS root volume for an Apple silicon Mac instance to an
    # administrative user.
    def create_delegate_mac_volume_ownership_task(input : AEC::CreateDelegateMacVolumeOwnershipTaskRequest) : Core::ParsedResponse(AEC::CreateDelegateMacVolumeOwnershipTaskResult)
      Log.info { "performing 'CreateDelegateMacVolumeOwnershipTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateDelegateMacVolumeOwnershipTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateDelegateMacVolumeOwnershipTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateDelegateMacVolumeOwnershipTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateDelegateMacVolumeOwnershipTaskResult).new(response, result)
    end

    # Creates a custom set of DHCP options. After you create a DHCP option set, you associate it with
    # a VPC. After you associate a DHCP option set with a VPC, all existing and newly launched
    # instances in the VPC use this set of DHCP options.
    #
    # The following are the individual DHCP options you can specify. For more information, see [DHCP
    # option sets](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_DHCP_Options.html) in the
    # *Amazon VPC User Guide*.
    #
    # - `domain-name` - If you're using AmazonProvidedDNS in `us-east-1`, specify `ec2.internal`. If
    # you're using AmazonProvidedDNS in any other Region, specify `region.compute.internal`.
    # Otherwise, specify a custom domain name. This value is used to complete unqualified DNS
    # hostnames.
    #
    # Some Linux operating systems accept multiple domain names separated by spaces. However, Windows
    # and other Linux operating systems treat the value as a single domain, which results in
    # unexpected behavior. If your DHCP option set is associated with a VPC that has instances running
    # operating systems that treat the value as a single domain, specify only one domain name.
    #
    # - `domain-name-servers` - The IP addresses of up to four DNS servers, or AmazonProvidedDNS. To
    # specify multiple domain name servers in a single parameter, separate the IP addresses using
    # commas. To have your instances receive custom DNS hostnames as specified in `domain-name`, you
    # must specify a custom DNS server.
    #
    # - `ntp-servers` - The IP addresses of up to eight Network Time Protocol (NTP) servers (four IPv4
    # addresses and four IPv6 addresses).
    #
    # - `netbios-name-servers` - The IP addresses of up to four NetBIOS name servers.
    #
    # - `netbios-node-type` - The NetBIOS node type (1, 2, 4, or 8). We recommend that you specify 2.
    # Broadcast and multicast are not supported. For more information about NetBIOS node types, see
    # [RFC 2132](https://www.ietf.org/rfc/rfc2132.txt).
    #
    # - `ipv6-address-preferred-lease-time` - A value (in seconds, minutes, hours, or years) for how
    # frequently a running instance with an IPv6 assigned to it goes through DHCPv6 lease renewal.
    # Acceptable values are between 140 and 2147483647 seconds (approximately 68 years). If no value
    # is entered, the default lease time is 140 seconds. If you use long-term addressing for EC2
    # instances, you can increase the lease time and avoid frequent lease renewal requests. Lease
    # renewal typically occurs when half of the lease time has elapsed.
    def create_dhcp_options(input : AEC::CreateDhcpOptionsRequest) : Core::ParsedResponse(AEC::CreateDhcpOptionsResult)
      Log.info { "performing 'CreateDhcpOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateDhcpOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateDhcpOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateDhcpOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateDhcpOptionsResult).new(response, result)
    end

    # [IPv6 only] Creates an egress-only internet gateway for your VPC. An egress-only internet
    # gateway is used to enable outbound communication over IPv6 from instances in your VPC to the
    # internet, and prevents hosts outside of your VPC from initiating an IPv6 connection with your
    # instance.
    def create_egress_only_internet_gateway(input : AEC::CreateEgressOnlyInternetGatewayRequest) : Core::ParsedResponse(AEC::CreateEgressOnlyInternetGatewayResult)
      Log.info { "performing 'CreateEgressOnlyInternetGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateEgressOnlyInternetGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateEgressOnlyInternetGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateEgressOnlyInternetGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateEgressOnlyInternetGatewayResult).new(response, result)
    end

    # Creates an EC2 Fleet that contains the configuration information for On-Demand Instances and
    # Spot Instances. Instances are launched immediately if there is available capacity.
    #
    # A single EC2 Fleet can include multiple launch specifications that vary by instance type, AMI,
    # Availability Zone, or subnet.
    #
    # For more information, see [EC2
    # Fleet](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-fleet.html) in the *Amazon EC2
    # User Guide*.
    def create_fleet(input : AEC::CreateFleetRequest) : Core::ParsedResponse(AEC::CreateFleetResult)
      Log.info { "performing 'CreateFleet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateFleet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateFleet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateFleetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateFleetResult).new(response, result)
    end

    # Creates one or more flow logs to capture information about IP traffic for a specific network
    # interface, subnet, or VPC.
    #
    # Flow log data for a monitored network interface is recorded as flow log records, which are log
    # events consisting of fields that describe the traffic flow. For more information, see [Flow log
    # records](https://docs.aws.amazon.com/vpc/latest/userguide/flow-log-records.html) in the *Amazon
    # VPC User Guide*.
    #
    # When publishing to CloudWatch Logs, flow log records are published to a log group, and each
    # network interface has a unique log stream in the log group. When publishing to Amazon S3, flow
    # log records for all of the monitored network interfaces are published to a single log file
    # object that is stored in the specified bucket.
    #
    # For more information, see [VPC Flow
    # Logs](https://docs.aws.amazon.com/vpc/latest/userguide/flow-logs.html) in the *Amazon VPC User
    # Guide*.
    def create_flow_logs(input : AEC::CreateFlowLogsRequest) : Core::ParsedResponse(AEC::CreateFlowLogsResult)
      Log.info { "performing 'CreateFlowLogs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateFlowLogs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateFlowLogs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateFlowLogsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateFlowLogsResult).new(response, result)
    end

    # Creates an Amazon FPGA Image (AFI) from the specified design checkpoint (DCP).
    #
    # The create operation is asynchronous. To verify that the AFI was successfully created and is
    # ready for use, check the output logs.
    #
    # An AFI contains the FPGA bitstream that is ready to download to an FPGA. You can securely deploy
    # an AFI on multiple FPGA-accelerated instances. For more information, see the [Amazon Web
    # Services FPGA Hardware Development Kit](https://github.com/aws/aws-fpga/).
    def create_fpga_image(input : AEC::CreateFpgaImageRequest) : Core::ParsedResponse(AEC::CreateFpgaImageResult)
      Log.info { "performing 'CreateFpgaImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateFpgaImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateFpgaImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateFpgaImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateFpgaImageResult).new(response, result)
    end

    # Creates an Amazon EBS-backed AMI from an Amazon EBS-backed instance that is either running or
    # stopped.
    #
    # If you customized your instance with instance store volumes or Amazon EBS volumes in addition to
    # the root device volume, the new AMI contains block device mapping information for those volumes.
    # When you launch an instance from this new AMI, the instance automatically launches with those
    # additional volumes.
    #
    # The location of the source instance determines where you can create the snapshots of the AMI:
    #
    # - If the source instance is in a Region, you must create the snapshots in the same Region as the
    # instance.
    #
    # - If the source instance is in a Local Zone, you can create the snapshots in the same Local Zone
    # or in its parent Region.
    #
    # - If the source instance is on an Outpost that supports local snapshots, you can create the
    # snapshots on the same Outpost or in the parent Region of that Outpost. In this case, you must
    # use the `SnapshotLocation` parameter to specify where to create the snapshots.
    #
    # For more information, see [Create an Amazon EBS-backed
    # AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/creating-an-ami-ebs.html) in the
    # *Amazon Elastic Compute Cloud User Guide*.
    def create_image(input : AEC::CreateImageRequest) : Core::ParsedResponse(AEC::CreateImageResult)
      Log.info { "performing 'CreateImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateImageResult).new(response, result)
    end

    # Creates a report that shows how your image is used across other Amazon Web Services accounts.
    # The report provides visibility into which accounts are using the specified image, and how many
    # resources (EC2 instances or launch templates) are referencing it.
    #
    # For more information, see [View your AMI
    # usage](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/your-ec2-ami-usage.html) in the
    # *Amazon EC2 User Guide*.
    def create_image_usage_report(input : AEC::CreateImageUsageReportRequest) : Core::ParsedResponse(AEC::CreateImageUsageReportResult)
      Log.info { "performing 'CreateImageUsageReport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateImageUsageReport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateImageUsageReport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateImageUsageReportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateImageUsageReportResult).new(response, result)
    end

    # Creates an EC2 Instance Connect Endpoint.
    #
    # An EC2 Instance Connect Endpoint allows you to connect to an instance, without requiring the
    # instance to have a public IPv4 or public IPv6 address. For more information, see [Connect to
    # your instances using EC2 Instance Connect
    # Endpoint](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Connect-using-EC2-Instance-Connect-Endpoint.html)
    # in the *Amazon EC2 User Guide*.
    def create_instance_connect_endpoint(input : AEC::CreateInstanceConnectEndpointRequest) : Core::ParsedResponse(AEC::CreateInstanceConnectEndpointResult)
      Log.info { "performing 'CreateInstanceConnectEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateInstanceConnectEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateInstanceConnectEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateInstanceConnectEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateInstanceConnectEndpointResult).new(response, result)
    end

    # Creates an event window in which scheduled events for the associated Amazon EC2 instances can
    # run.
    #
    # You can define either a set of time ranges or a cron expression when creating the event window,
    # but not both. All event window times are in UTC.
    #
    # You can create up to 200 event windows per Amazon Web Services Region.
    #
    # When you create the event window, targets (instance IDs, Dedicated Host IDs, or tags) are not
    # yet associated with it. To ensure that the event window can be used, you must associate one or
    # more targets with it by using the AssociateInstanceEventWindow API.
    #
    # Event windows are applicable only for scheduled events that stop, reboot, or terminate
    # instances.
    #
    # Event windows are *not* applicable for:
    #
    # - Expedited scheduled events and network maintenance events.
    #
    # - Unscheduled maintenance such as AutoRecovery and unplanned reboots.
    #
    # For more information, see [Define event windows for scheduled
    # events](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/event-windows.html) in the *Amazon
    # EC2 User Guide*.
    def create_instance_event_window(input : AEC::CreateInstanceEventWindowRequest) : Core::ParsedResponse(AEC::CreateInstanceEventWindowResult)
      Log.info { "performing 'CreateInstanceEventWindow' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateInstanceEventWindow")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateInstanceEventWindow", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateInstanceEventWindowResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateInstanceEventWindowResult).new(response, result)
    end

    # Exports a running or stopped instance to an Amazon S3 bucket.
    #
    # For information about the prerequisites for your Amazon S3 bucket, supported operating systems,
    # image formats, and known limitations for the types of instances you can export, see [Exporting
    # an instance as a VM Using VM
    # Import/Export](https://docs.aws.amazon.com/vm-import/latest/userguide/vmexport.html) in the *VM
    # Import/Export User Guide*.
    def create_instance_export_task(input : AEC::CreateInstanceExportTaskRequest) : Core::ParsedResponse(AEC::CreateInstanceExportTaskResult)
      Log.info { "performing 'CreateInstanceExportTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateInstanceExportTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateInstanceExportTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateInstanceExportTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateInstanceExportTaskResult).new(response, result)
    end

    # Creates an internet gateway for use with a VPC. After creating the internet gateway, you attach
    # it to a VPC using AttachInternetGateway.
    #
    # For more information, see [Internet
    # gateways](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Internet_Gateway.html) in the
    # *Amazon VPC User Guide*.
    def create_internet_gateway(input : AEC::CreateInternetGatewayRequest) : Core::ParsedResponse(AEC::CreateInternetGatewayResult)
      Log.info { "performing 'CreateInternetGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateInternetGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateInternetGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateInternetGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateInternetGatewayResult).new(response, result)
    end

    # Creates an interruptible Capacity Reservation by specifying the number of unused instances you
    # want to allocate from your source reservation. This helps you make unused capacity available for
    # other workloads within your account while maintaining control to reclaim it.
    def create_interruptible_capacity_reservation_allocation(input : AEC::CreateInterruptibleCapacityReservationAllocationRequest) : Core::ParsedResponse(AEC::CreateInterruptibleCapacityReservationAllocationResult)
      Log.info { "performing 'CreateInterruptibleCapacityReservationAllocation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateInterruptibleCapacityReservationAllocation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateInterruptibleCapacityReservationAllocation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateInterruptibleCapacityReservationAllocationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateInterruptibleCapacityReservationAllocationResult).new(response, result)
    end

    # Create an IPAM. Amazon VPC IP Address Manager (IPAM) is a VPC feature that you can use to
    # automate your IP address management workflows including assigning, tracking, troubleshooting,
    # and auditing IP addresses across Amazon Web Services Regions and accounts throughout your Amazon
    # Web Services Organization.
    #
    # For more information, see [Create an
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/create-ipam.html) in the *Amazon VPC IPAM User
    # Guide*.
    def create_ipam(input : AEC::CreateIpamRequest) : Core::ParsedResponse(AEC::CreateIpamResult)
      Log.info { "performing 'CreateIpam' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpam")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpam", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamResult).new(response, result)
    end

    # Create a verification token.
    #
    # A verification token is an Amazon Web Services-generated random value that you can use to prove
    # ownership of an external resource. For example, you can use a verification token to validate
    # that you control a public IP address range when you bring an IP address range to Amazon Web
    # Services (BYOIP).
    def create_ipam_external_resource_verification_token(input : AEC::CreateIpamExternalResourceVerificationTokenRequest) : Core::ParsedResponse(AEC::CreateIpamExternalResourceVerificationTokenResult)
      Log.info { "performing 'CreateIpamExternalResourceVerificationToken' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamExternalResourceVerificationToken")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamExternalResourceVerificationToken", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamExternalResourceVerificationTokenResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamExternalResourceVerificationTokenResult).new(response, result)
    end

    # Creates an association between an IPAM and a Regional Internet Registry (RIR) for Resource
    # Public Key Infrastructure (RPKI) management. You can use this association to create Route Origin
    # Authorizations (ROAs) for IP address prefixes registered with the internet registry. Your IPAM
    # must be in the Advanced tier to use this feature.
    def create_ipam_internet_registry_association(input : AEC::CreateIpamInternetRegistryAssociationRequest) : Core::ParsedResponse(AEC::CreateIpamInternetRegistryAssociationResult)
      Log.info { "performing 'CreateIpamInternetRegistryAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamInternetRegistryAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamInternetRegistryAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamInternetRegistryAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamInternetRegistryAssociationResult).new(response, result)
    end

    # Creates an IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    #
    # For more information, see [Define public IPv4 allocation strategy with IPAM
    # policies](https://docs.aws.amazon.com/vpc/latest/ipam/define-public-ipv4-allocation-strategy-with-ipam-policies.html)
    # in the *Amazon VPC IPAM User Guide*.
    def create_ipam_policy(input : AEC::CreateIpamPolicyRequest) : Core::ParsedResponse(AEC::CreateIpamPolicyResult)
      Log.info { "performing 'CreateIpamPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamPolicyResult).new(response, result)
    end

    # Create an IP address pool for Amazon VPC IP Address Manager (IPAM). In IPAM, a pool is a
    # collection of contiguous IP addresses CIDRs. Pools enable you to organize your IP addresses
    # according to your routing and security needs. For example, if you have separate routing and
    # security needs for development and production applications, you can create a pool for each.
    #
    # For more information, see [Create a top-level
    # pool](https://docs.aws.amazon.com/vpc/latest/ipam/create-top-ipam.html) in the *Amazon VPC IPAM
    # User Guide*.
    def create_ipam_pool(input : AEC::CreateIpamPoolRequest) : Core::ParsedResponse(AEC::CreateIpamPoolResult)
      Log.info { "performing 'CreateIpamPool' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamPool")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamPool", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamPoolResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamPoolResult).new(response, result)
    end

    # Creates an IPAM prefix list resolver.
    #
    # An IPAM prefix list resolver is a component that manages the synchronization between IPAM's CIDR
    # selection rules and customer-managed prefix lists. It automates connectivity configurations by
    # selecting CIDRs from IPAM's database based on your business logic and synchronizing them with
    # prefix lists used in resources such as VPC route tables and security groups.
    #
    # For more information about IPAM prefix list resolver, see [Automate prefix list updates with
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/automate-prefix-list-updates.html) in the
    # *Amazon VPC IPAM User Guide*.
    def create_ipam_prefix_list_resolver(input : AEC::CreateIpamPrefixListResolverRequest) : Core::ParsedResponse(AEC::CreateIpamPrefixListResolverResult)
      Log.info { "performing 'CreateIpamPrefixListResolver' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamPrefixListResolver")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamPrefixListResolver", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamPrefixListResolverResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamPrefixListResolverResult).new(response, result)
    end

    # Creates an IPAM prefix list resolver target.
    #
    # An IPAM prefix list resolver target is an association between a specific customer-managed prefix
    # list and an IPAM prefix list resolver. The target enables the resolver to synchronize CIDRs
    # selected by its rules into the specified prefix list, which can then be referenced in Amazon Web
    # Services resources.
    #
    # For more information about IPAM prefix list resolver, see [Automate prefix list updates with
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/automate-prefix-list-updates.html) in the
    # *Amazon VPC IPAM User Guide*.
    def create_ipam_prefix_list_resolver_target(input : AEC::CreateIpamPrefixListResolverTargetRequest) : Core::ParsedResponse(AEC::CreateIpamPrefixListResolverTargetResult)
      Log.info { "performing 'CreateIpamPrefixListResolverTarget' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamPrefixListResolverTarget")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamPrefixListResolverTarget", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamPrefixListResolverTargetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamPrefixListResolverTargetResult).new(response, result)
    end

    # Creates an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM
    # to manage and monitor resources that belong to the owning account.
    def create_ipam_resource_discovery(input : AEC::CreateIpamResourceDiscoveryRequest) : Core::ParsedResponse(AEC::CreateIpamResourceDiscoveryResult)
      Log.info { "performing 'CreateIpamResourceDiscovery' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamResourceDiscovery")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamResourceDiscovery", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamResourceDiscoveryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamResourceDiscoveryResult).new(response, result)
    end

    # Creates a routing policy registration and publishes Route Origin Authorizations (ROAs) to the
    # RPKI for the specified CIDR prefix and ASNs.
    def create_ipam_routing_policy_registration(input : AEC::CreateIpamRoutingPolicyRegistrationRequest) : Core::ParsedResponse(AEC::CreateIpamRoutingPolicyRegistrationResult)
      Log.info { "performing 'CreateIpamRoutingPolicyRegistration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamRoutingPolicyRegistration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamRoutingPolicyRegistration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamRoutingPolicyRegistrationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamRoutingPolicyRegistrationResult).new(response, result)
    end

    # Create an IPAM scope. In IPAM, a scope is the highest-level container within IPAM. An IPAM
    # contains two default scopes. Each scope represents the IP space for a single network. The
    # private scope is intended for all private IP address space. The public scope is intended for all
    # public IP address space. Scopes enable you to reuse IP addresses across multiple unconnected
    # networks without causing IP address overlap or conflict.
    #
    # For more information, see [Add a
    # scope](https://docs.aws.amazon.com/vpc/latest/ipam/add-scope-ipam.html) in the *Amazon VPC IPAM
    # User Guide*.
    def create_ipam_scope(input : AEC::CreateIpamScopeRequest) : Core::ParsedResponse(AEC::CreateIpamScopeResult)
      Log.info { "performing 'CreateIpamScope' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateIpamScope")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateIpamScope", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateIpamScopeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateIpamScopeResult).new(response, result)
    end

    # Creates an ED25519 or 2048-bit RSA key pair with the specified name and in the specified format.
    # Amazon EC2 stores the public key and displays the private key for you to save to a file. The
    # private key is returned as an unencrypted PEM encoded PKCS#1 private key or an unencrypted PPK
    # formatted private key for use with PuTTY. If a key with the specified name already exists,
    # Amazon EC2 returns an error.
    #
    # The key pair returned to you is available only in the Amazon Web Services Region in which you
    # create it. If you prefer, you can create your own key pair using a third-party tool and upload
    # it to any Region using ImportKeyPair.
    #
    # You can have up to 5,000 key pairs per Amazon Web Services Region.
    #
    # For more information, see [Amazon EC2 key
    # pairs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-key-pairs.html) in the *Amazon
    # EC2 User Guide*.
    def create_key_pair(input : AEC::CreateKeyPairRequest) : Core::ParsedResponse(AEC::KeyPair)
      Log.info { "performing 'CreateKeyPair' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateKeyPair")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateKeyPair", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::KeyPair.from_xml(result_node)
      Core::ParsedResponse(AEC::KeyPair).new(response, result)
    end

    # Creates a launch template.
    #
    # A launch template contains the parameters to launch an instance. When you launch an instance
    # using RunInstances, you can specify a launch template instead of providing the launch parameters
    # in the request. For more information, see [Store instance launch parameters in Amazon EC2 launch
    # templates](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-launch-templates.html) in the
    # *Amazon EC2 User Guide*.
    #
    # To clone an existing launch template as the basis for a new launch template, use the Amazon EC2
    # console. The API, SDKs, and CLI do not support cloning a template. For more information, see
    # [Create a launch template from an existing launch
    # template](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/create-launch-template.html#create-launch-template-from-existing-launch-template)
    # in the *Amazon EC2 User Guide*.
    def create_launch_template(input : AEC::CreateLaunchTemplateRequest) : Core::ParsedResponse(AEC::CreateLaunchTemplateResult)
      Log.info { "performing 'CreateLaunchTemplate' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLaunchTemplate")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLaunchTemplate", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLaunchTemplateResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLaunchTemplateResult).new(response, result)
    end

    # Creates a new version of a launch template. You must specify an existing launch template, either
    # by name or ID. You can determine whether the new version inherits parameters from a source
    # version, and add or overwrite parameters as needed.
    #
    # Launch template versions are numbered in the order in which they are created. You can't specify,
    # change, or replace the numbering of launch template versions.
    #
    # Launch templates are immutable; after you create a launch template, you can't modify it.
    # Instead, you can create a new version of the launch template that includes the changes that you
    # require.
    #
    # For more information, see [Modify a launch template (manage launch template
    # versions)](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/manage-launch-template-versions.html)
    # in the *Amazon EC2 User Guide*.
    def create_launch_template_version(input : AEC::CreateLaunchTemplateVersionRequest) : Core::ParsedResponse(AEC::CreateLaunchTemplateVersionResult)
      Log.info { "performing 'CreateLaunchTemplateVersion' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLaunchTemplateVersion")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLaunchTemplateVersion", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLaunchTemplateVersionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLaunchTemplateVersionResult).new(response, result)
    end

    # Creates a static route for the specified local gateway route table. You must specify one of the
    # following targets:
    #
    # - `LocalGatewayVirtualInterfaceGroupId`
    #
    # - `NetworkInterfaceId`
    def create_local_gateway_route(input : AEC::CreateLocalGatewayRouteRequest) : Core::ParsedResponse(AEC::CreateLocalGatewayRouteResult)
      Log.info { "performing 'CreateLocalGatewayRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLocalGatewayRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLocalGatewayRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLocalGatewayRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLocalGatewayRouteResult).new(response, result)
    end

    # Creates a local gateway route table.
    def create_local_gateway_route_table(input : AEC::CreateLocalGatewayRouteTableRequest) : Core::ParsedResponse(AEC::CreateLocalGatewayRouteTableResult)
      Log.info { "performing 'CreateLocalGatewayRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLocalGatewayRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLocalGatewayRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLocalGatewayRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLocalGatewayRouteTableResult).new(response, result)
    end

    # Creates a local gateway route table virtual interface group association.
    def create_local_gateway_route_table_virtual_interface_group_association(input : AEC::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest) : Core::ParsedResponse(AEC::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult)
      Log.info { "performing 'CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult).new(response, result)
    end

    # Associates the specified VPC with the specified local gateway route table.
    def create_local_gateway_route_table_vpc_association(input : AEC::CreateLocalGatewayRouteTableVpcAssociationRequest) : Core::ParsedResponse(AEC::CreateLocalGatewayRouteTableVpcAssociationResult)
      Log.info { "performing 'CreateLocalGatewayRouteTableVpcAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLocalGatewayRouteTableVpcAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLocalGatewayRouteTableVpcAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLocalGatewayRouteTableVpcAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLocalGatewayRouteTableVpcAssociationResult).new(response, result)
    end

    # Create a virtual interface for a local gateway.
    def create_local_gateway_virtual_interface(input : AEC::CreateLocalGatewayVirtualInterfaceRequest) : Core::ParsedResponse(AEC::CreateLocalGatewayVirtualInterfaceResult)
      Log.info { "performing 'CreateLocalGatewayVirtualInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLocalGatewayVirtualInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLocalGatewayVirtualInterface", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLocalGatewayVirtualInterfaceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLocalGatewayVirtualInterfaceResult).new(response, result)
    end

    # Create a local gateway virtual interface group.
    def create_local_gateway_virtual_interface_group(input : AEC::CreateLocalGatewayVirtualInterfaceGroupRequest) : Core::ParsedResponse(AEC::CreateLocalGatewayVirtualInterfaceGroupResult)
      Log.info { "performing 'CreateLocalGatewayVirtualInterfaceGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateLocalGatewayVirtualInterfaceGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateLocalGatewayVirtualInterfaceGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateLocalGatewayVirtualInterfaceGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateLocalGatewayVirtualInterfaceGroupResult).new(response, result)
    end

    # Creates a System Integrity Protection (SIP) modification task to configure the SIP settings for
    # an x86 Mac instance or Apple silicon Mac instance. For more information, see [ Configure SIP for
    # Amazon EC2
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/mac-sip-settings.html#mac-sip-configure)
    # in the *Amazon EC2 User Guide*.
    #
    # When you configure the SIP settings for your instance, you can either enable or disable all SIP
    # settings, or you can specify a custom SIP configuration that selectively enables or disables
    # specific SIP settings.
    #
    # If you implement a custom configuration, [ connect to the instance and verify the
    # settings](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/mac-sip-settings.html#mac-sip-check-settings)
    # to ensure that your requirements are properly implemented and functioning as intended.
    #
    # SIP configurations might change with macOS updates. We recommend that you review custom SIP
    # settings after any macOS version upgrade to ensure continued compatibility and proper
    # functionality of your security configurations.
    #
    # To enable or disable all SIP settings, use the **MacSystemIntegrityProtectionStatus** parameter
    # only. For example, to enable all SIP settings, specify the following:
    #
    # - `MacSystemIntegrityProtectionStatus=enabled`
    #
    # To specify a custom configuration that selectively enables or disables specific SIP settings,
    # use the **MacSystemIntegrityProtectionStatus** parameter to enable or disable all SIP settings,
    # and then use the **MacSystemIntegrityProtectionConfiguration** parameter to specify exceptions.
    # In this case, the exceptions you specify for **MacSystemIntegrityProtectionConfiguration**
    # override the value you specify for **MacSystemIntegrityProtectionStatus**. For example, to
    # enable all SIP settings, except `NvramProtections`, specify the following:
    #
    # - `MacSystemIntegrityProtectionStatus=enabled`
    #
    # - `MacSystemIntegrityProtectionConfigurationRequest "NvramProtections=disabled"`
    def create_mac_system_integrity_protection_modification_task(input : AEC::CreateMacSystemIntegrityProtectionModificationTaskRequest) : Core::ParsedResponse(AEC::CreateMacSystemIntegrityProtectionModificationTaskResult)
      Log.info { "performing 'CreateMacSystemIntegrityProtectionModificationTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateMacSystemIntegrityProtectionModificationTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateMacSystemIntegrityProtectionModificationTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateMacSystemIntegrityProtectionModificationTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateMacSystemIntegrityProtectionModificationTaskResult).new(response, result)
    end

    # Creates a managed prefix list. You can specify entries for the prefix list. Each entry consists
    # of a CIDR block and an optional description.
    def create_managed_prefix_list(input : AEC::CreateManagedPrefixListRequest) : Core::ParsedResponse(AEC::CreateManagedPrefixListResult)
      Log.info { "performing 'CreateManagedPrefixList' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateManagedPrefixList")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateManagedPrefixList", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateManagedPrefixListResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateManagedPrefixListResult).new(response, result)
    end

    # Creates a NAT gateway in the specified subnet. This action creates a network interface in the
    # specified subnet with a private IP address from the IP address range of the subnet. You can
    # create either a public NAT gateway or a private NAT gateway.
    #
    # With a public NAT gateway, internet-bound traffic from a private subnet can be routed to the NAT
    # gateway, so that instances in a private subnet can connect to the internet.
    #
    # With a private NAT gateway, private communication is routed across VPCs and on-premises networks
    # through a transit gateway or virtual private gateway. Common use cases include running large
    # workloads behind a small pool of allowlisted IPv4 addresses, preserving private IPv4 addresses,
    # and communicating between overlapping networks.
    #
    # For more information, see [NAT
    # gateways](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-nat-gateway.html) in the *Amazon
    # VPC User Guide*.
    #
    # When you create a public NAT gateway and assign it an EIP or secondary EIPs, the network border
    # group of the EIPs must match the network border group of the Availability Zone (AZ) that the
    # public NAT gateway is in. If it's not the same, the NAT gateway will fail to launch. You can see
    # the network border group for the subnet's AZ by viewing the details of the subnet. Similarly,
    # you can view the network border group of an EIP by viewing the details of the EIP address. For
    # more information about network border groups and EIPs, see [Allocate an Elastic IP
    # address](https://docs.aws.amazon.com/vpc/latest/userguide/WorkWithEIPs.html) in the *Amazon VPC
    # User Guide*.
    def create_nat_gateway(input : AEC::CreateNatGatewayRequest) : Core::ParsedResponse(AEC::CreateNatGatewayResult)
      Log.info { "performing 'CreateNatGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateNatGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateNatGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateNatGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateNatGatewayResult).new(response, result)
    end

    # Creates a network ACL in a VPC. Network ACLs provide an optional layer of security (in addition
    # to security groups) for the instances in your VPC.
    #
    # For more information, see [Network
    # ACLs](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-network-acls.html) in the *Amazon VPC
    # User Guide*.
    def create_network_acl(input : AEC::CreateNetworkAclRequest) : Core::ParsedResponse(AEC::CreateNetworkAclResult)
      Log.info { "performing 'CreateNetworkAcl' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateNetworkAcl")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateNetworkAcl", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateNetworkAclResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateNetworkAclResult).new(response, result)
    end

    # Creates an entry (a rule) in a network ACL with the specified rule number. Each network ACL has
    # a set of numbered ingress rules and a separate set of numbered egress rules. When determining
    # whether a packet should be allowed in or out of a subnet associated with the ACL, we process the
    # entries in the ACL according to the rule numbers, in ascending order. Each network ACL has a set
    # of ingress rules and a separate set of egress rules.
    #
    # We recommend that you leave room between the rule numbers (for example, 100, 110, 120, ...), and
    # not number them one right after the other (for example, 101, 102, 103, ...). This makes it
    # easier to add a rule between existing ones without having to renumber the rules.
    #
    # After you add an entry, you can't modify it; you must either replace it, or create an entry and
    # delete the old one.
    #
    # For more information about network ACLs, see [Network
    # ACLs](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-network-acls.html) in the *Amazon VPC
    # User Guide*.
    def create_network_acl_entry(input : AEC::CreateNetworkAclEntryRequest) : Core::Response
      Log.info { "performing 'CreateNetworkAclEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateNetworkAclEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateNetworkAclEntry", body)
      Core::Response.new(response)
    end

    # Creates a Network Access Scope.
    #
    # Amazon Web Services Network Access Analyzer enables cloud networking and cloud operations teams
    # to verify that their networks on Amazon Web Services conform to their network security and
    # governance objectives. For more information, see the [Amazon Web Services Network Access
    # Analyzer Guide](https://docs.aws.amazon.com/vpc/latest/network-access-analyzer/).
    def create_network_insights_access_scope(input : AEC::CreateNetworkInsightsAccessScopeRequest) : Core::ParsedResponse(AEC::CreateNetworkInsightsAccessScopeResult)
      Log.info { "performing 'CreateNetworkInsightsAccessScope' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateNetworkInsightsAccessScope")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateNetworkInsightsAccessScope", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateNetworkInsightsAccessScopeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateNetworkInsightsAccessScopeResult).new(response, result)
    end

    # Creates a path to analyze for reachability.
    #
    # Reachability Analyzer enables you to analyze and debug network reachability between two
    # resources in your virtual private cloud (VPC). For more information, see the [Reachability
    # Analyzer Guide](https://docs.aws.amazon.com/vpc/latest/reachability/).
    def create_network_insights_path(input : AEC::CreateNetworkInsightsPathRequest) : Core::ParsedResponse(AEC::CreateNetworkInsightsPathResult)
      Log.info { "performing 'CreateNetworkInsightsPath' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateNetworkInsightsPath")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateNetworkInsightsPath", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateNetworkInsightsPathResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateNetworkInsightsPathResult).new(response, result)
    end

    # Creates a network interface in the specified subnet.
    #
    # The number of IP addresses you can assign to a network interface varies by instance type.
    #
    # For more information about network interfaces, see [Elastic network
    # interfaces](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-eni.html) in the *Amazon
    # EC2 User Guide*.
    def create_network_interface(input : AEC::CreateNetworkInterfaceRequest) : Core::ParsedResponse(AEC::CreateNetworkInterfaceResult)
      Log.info { "performing 'CreateNetworkInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateNetworkInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateNetworkInterface", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateNetworkInterfaceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateNetworkInterfaceResult).new(response, result)
    end

    # Grants an Amazon Web Services-authorized account permission to attach the specified network
    # interface to an instance in their account.
    #
    # You can grant permission to a single Amazon Web Services account only, and only one account at a
    # time.
    def create_network_interface_permission(input : AEC::CreateNetworkInterfacePermissionRequest) : Core::ParsedResponse(AEC::CreateNetworkInterfacePermissionResult)
      Log.info { "performing 'CreateNetworkInterfacePermission' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateNetworkInterfacePermission")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateNetworkInterfacePermission", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateNetworkInterfacePermissionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateNetworkInterfacePermissionResult).new(response, result)
    end

    # Creates a placement group in which to launch instances. The strategy of the placement group
    # determines how the instances are organized within the group.
    #
    # A `cluster` placement group is a logical grouping of instances within a single Availability Zone
    # that benefit from low network latency, high network throughput. A `spread` placement group
    # places instances on distinct hardware. A `partition` placement group places groups of instances
    # in different partitions, where instances in one partition do not share the same hardware with
    # instances in another partition. A `precision-time` placement group places instances on supported
    # hardware with direct access to high-precision time sources in Amazon Web Services
    # infrastructure.
    #
    # For more information, see [Placement
    # groups](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/placement-groups.html) in the
    # *Amazon EC2 User Guide*.
    def create_placement_group(input : AEC::CreatePlacementGroupRequest) : Core::ParsedResponse(AEC::CreatePlacementGroupResult)
      Log.info { "performing 'CreatePlacementGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreatePlacementGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreatePlacementGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreatePlacementGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreatePlacementGroupResult).new(response, result)
    end

    # Creates a public IPv4 address pool. A public IPv4 pool is an EC2 IP address pool required for
    # the public IPv4 CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6
    # addresses you bring to Amazon Web Services, however, use IPAM pools only. To monitor the status
    # of pool creation, use
    # [DescribePublicIpv4Pools](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribePublicIpv4Pools.html).
    def create_public_ipv_4_pool(input : AEC::CreatePublicIpv4PoolRequest) : Core::ParsedResponse(AEC::CreatePublicIpv4PoolResult)
      Log.info { "performing 'CreatePublicIpv4Pool' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreatePublicIpv4Pool")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreatePublicIpv4Pool", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreatePublicIpv4PoolResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreatePublicIpv4PoolResult).new(response, result)
    end

    # Replaces the EBS-backed root volume for a `running` instance with a new volume that is restored
    # to the original root volume's launch state, that is restored to a specific snapshot taken from
    # the original root volume, that is restored from an AMI that has the same key characteristics as
    # that of the instance, or that is replaced by a specified volume.
    #
    # For more information, see [Replace a root
    # volume](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/replace-root.html) in the *Amazon
    # EC2 User Guide*.
    def create_replace_root_volume_task(input : AEC::CreateReplaceRootVolumeTaskRequest) : Core::ParsedResponse(AEC::CreateReplaceRootVolumeTaskResult)
      Log.info { "performing 'CreateReplaceRootVolumeTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateReplaceRootVolumeTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateReplaceRootVolumeTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateReplaceRootVolumeTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateReplaceRootVolumeTaskResult).new(response, result)
    end

    # Creates a listing for Amazon EC2 Standard Reserved Instances to be sold in the Reserved Instance
    # Marketplace. You can submit one Standard Reserved Instance listing at a time. To get a list of
    # your Standard Reserved Instances, you can use the DescribeReservedInstances operation.
    #
    # Only Standard Reserved Instances can be sold in the Reserved Instance Marketplace. Convertible
    # Reserved Instances cannot be sold.
    #
    # The Reserved Instance Marketplace matches sellers who want to resell Standard Reserved Instance
    # capacity that they no longer need with buyers who want to purchase additional capacity. Reserved
    # Instances bought and sold through the Reserved Instance Marketplace work like any other Reserved
    # Instances.
    #
    # To sell your Standard Reserved Instances, you must first register as a seller in the Reserved
    # Instance Marketplace. After completing the registration process, you can create a Reserved
    # Instance Marketplace listing of some or all of your Standard Reserved Instances, and specify the
    # upfront price to receive for them. Your Standard Reserved Instance listings then become
    # available for purchase. To view the details of your Standard Reserved Instance listing, you can
    # use the DescribeReservedInstancesListings operation.
    #
    # For more information, see [Sell in the Reserved Instance
    # Marketplace](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ri-market-general.html) in the
    # *Amazon EC2 User Guide*.
    def create_reserved_instances_listing(input : AEC::CreateReservedInstancesListingRequest) : Core::ParsedResponse(AEC::CreateReservedInstancesListingResult)
      Log.info { "performing 'CreateReservedInstancesListing' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateReservedInstancesListing")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateReservedInstancesListing", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateReservedInstancesListingResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateReservedInstancesListingResult).new(response, result)
    end

    # Starts a task that restores an AMI from an Amazon S3 object that was previously created by using
    # [CreateStoreImageTask](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateStoreImageTask.html).
    #
    # To use this API, you must have the required permissions. For more information, see [Permissions
    # for storing and restoring AMIs using
    # S3](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/work-with-ami-store-restore.html#ami-s3-permissions)
    # in the *Amazon EC2 User Guide*.
    #
    # For more information, see [Store and restore an AMI using
    # S3](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-store-restore.html) in the *Amazon
    # EC2 User Guide*.
    def create_restore_image_task(input : AEC::CreateRestoreImageTaskRequest) : Core::ParsedResponse(AEC::CreateRestoreImageTaskResult)
      Log.info { "performing 'CreateRestoreImageTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateRestoreImageTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateRestoreImageTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateRestoreImageTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateRestoreImageTaskResult).new(response, result)
    end

    # Creates a route in a route table within a VPC.
    #
    # You must specify either a destination CIDR block or a prefix list ID. You must also specify
    # exactly one of the resources from the parameter list.
    #
    # When determining how to route traffic, we use the route with the most specific match. For
    # example, traffic is destined for the IPv4 address `192.0.2.3`, and the route table includes the
    # following two IPv4 routes:
    #
    # - `192.0.2.0/24` (goes to some target A)
    #
    # - `192.0.2.0/28` (goes to some target B)
    #
    # Both routes apply to the traffic destined for `192.0.2.3`. However, the second route in the list
    # covers a smaller number of IP addresses and is therefore more specific, so we use that route to
    # determine where to target the traffic.
    #
    # For more information about route tables, see [Route
    # tables](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html) in the *Amazon
    # VPC User Guide*.
    def create_route(input : AEC::CreateRouteRequest) : Core::ParsedResponse(AEC::CreateRouteResult)
      Log.info { "performing 'CreateRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateRouteResult).new(response, result)
    end

    # Creates a new route server to manage dynamic routing in a VPC.
    #
    # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed
    # within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates
    # VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing
    # fault tolerance for those workloads. This enables you to automatically reroute traffic within a
    # VPC, which increases the manageability of VPC routing and interoperability with third-party
    # workloads.
    #
    # Route server supports the follow route table types:
    #
    # - VPC route tables not associated with subnets
    #
    # - Subnet route tables
    #
    # - Internet gateway route tables
    #
    # Route server does not support route tables associated with virtual private gateways. To
    # propagate routes into a transit gateway route table, use [Transit Gateway
    # Connect](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html).
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def create_route_server(input : AEC::CreateRouteServerRequest) : Core::ParsedResponse(AEC::CreateRouteServerResult)
      Log.info { "performing 'CreateRouteServer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateRouteServer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateRouteServer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateRouteServerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateRouteServerResult).new(response, result)
    end

    # Creates a new endpoint for a route server in a specified subnet.
    #
    # A route server endpoint is an Amazon Web Services-managed component inside a subnet that
    # facilitates [BGP (Border Gateway
    # Protocol)](https://en.wikipedia.org/wiki/Border_Gateway_Protocol) connections between your route
    # server and your BGP peers.
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def create_route_server_endpoint(input : AEC::CreateRouteServerEndpointRequest) : Core::ParsedResponse(AEC::CreateRouteServerEndpointResult)
      Log.info { "performing 'CreateRouteServerEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateRouteServerEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateRouteServerEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateRouteServerEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateRouteServerEndpointResult).new(response, result)
    end

    # Creates a new BGP peer for a specified route server endpoint.
    #
    # A route server peer is a session between a route server endpoint and the device deployed in
    # Amazon Web Services (such as a firewall appliance or other network security function running on
    # an EC2 instance). The device must meet these requirements:
    #
    # - Have an elastic network interface in the VPC
    #
    # - Support BGP (Border Gateway Protocol)
    #
    # - Can initiate BGP sessions
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def create_route_server_peer(input : AEC::CreateRouteServerPeerRequest) : Core::ParsedResponse(AEC::CreateRouteServerPeerResult)
      Log.info { "performing 'CreateRouteServerPeer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateRouteServerPeer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateRouteServerPeer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateRouteServerPeerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateRouteServerPeerResult).new(response, result)
    end

    # Creates a route table for the specified VPC. After you create a route table, you can add routes
    # and associate the table with a subnet.
    #
    # For more information, see [Route
    # tables](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html) in the *Amazon
    # VPC User Guide*.
    def create_route_table(input : AEC::CreateRouteTableRequest) : Core::ParsedResponse(AEC::CreateRouteTableResult)
      Log.info { "performing 'CreateRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateRouteTableResult).new(response, result)
    end

    # Creates a secondary network.
    #
    # The allowed size for a secondary network CIDR block is between /28 netmask (16 IP addresses) and
    # /12 netmask (1,048,576 IP addresses).
    def create_secondary_network(input : AEC::CreateSecondaryNetworkRequest) : Core::ParsedResponse(AEC::CreateSecondaryNetworkResult)
      Log.info { "performing 'CreateSecondaryNetwork' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSecondaryNetwork")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSecondaryNetwork", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateSecondaryNetworkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateSecondaryNetworkResult).new(response, result)
    end

    # Creates a secondary subnet in a secondary network.
    #
    # A secondary subnet CIDR block must not overlap with the CIDR block of an existing secondary
    # subnet in the secondary network. After you create a secondary subnet, you can't change its CIDR
    # block.
    #
    # The allowed size for a secondary subnet CIDR block is between /28 netmask (16 IP addresses) and
    # /12 netmask (1,048,576 IP addresses). Amazon reserves the first four IP addresses and the last
    # IP address in each secondary subnet for internal use.
    def create_secondary_subnet(input : AEC::CreateSecondarySubnetRequest) : Core::ParsedResponse(AEC::CreateSecondarySubnetResult)
      Log.info { "performing 'CreateSecondarySubnet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSecondarySubnet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSecondarySubnet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateSecondarySubnetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateSecondarySubnetResult).new(response, result)
    end

    # Creates a security group.
    #
    # A security group acts as a virtual firewall for your instance to control inbound and outbound
    # traffic. For more information, see [Amazon EC2 security
    # groups](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-network-security.html) in the
    # *Amazon EC2 User Guide* and [Security groups for your
    # VPC](https://docs.aws.amazon.com/AmazonVPC/latest/UserGuide/VPC_SecurityGroups.html) in the
    # *Amazon VPC User Guide*.
    #
    # When you create a security group, you specify a friendly name of your choice. You can't have two
    # security groups for the same VPC with the same name.
    #
    # You have a default security group for use in your VPC. If you don't specify a security group
    # when you launch an instance, the instance is launched into the appropriate default security
    # group. A default security group includes a default rule that grants instances unrestricted
    # network access to each other.
    #
    # You can add or remove rules from your security groups using AuthorizeSecurityGroupIngress,
    # AuthorizeSecurityGroupEgress, RevokeSecurityGroupIngress, and RevokeSecurityGroupEgress.
    #
    # For more information about VPC security group limits, see [Amazon VPC
    # Limits](https://docs.aws.amazon.com/vpc/latest/userguide/amazon-vpc-limits.html).
    def create_security_group(input : AEC::CreateSecurityGroupRequest) : Core::ParsedResponse(AEC::CreateSecurityGroupResult)
      Log.info { "performing 'CreateSecurityGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSecurityGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSecurityGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateSecurityGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateSecurityGroupResult).new(response, result)
    end

    # Creates a snapshot of an EBS volume and stores it in Amazon S3. You can use snapshots for
    # backups, to make copies of EBS volumes, and to save data before shutting down an instance.
    #
    # The location of the source EBS volume determines where you can create the snapshot.
    #
    # - If the source volume is in a Region, you must create the snapshot in the same Region as the
    # volume.
    #
    # - If the source volume is in a Local Zone, you can create the snapshot in the same Local Zone or
    # in its parent Amazon Web Services Region.
    #
    # - If the source volume is on an Outpost, you can create the snapshot on the same Outpost or in
    # its parent Amazon Web Services Region.
    #
    # When a snapshot is created, any Amazon Web Services Marketplace product codes that are
    # associated with the source volume are propagated to the snapshot.
    #
    # You can take a snapshot of an attached volume that is in use. However, snapshots only capture
    # data that has been written to your Amazon EBS volume at the time the snapshot command is issued;
    # this might exclude any data that has been cached by any applications or the operating system. If
    # you can pause any file systems on the volume long enough to take a snapshot, your snapshot
    # should be complete. However, if you cannot pause all file writes to the volume, you should
    # unmount the volume from within the instance, issue the snapshot command, and then remount the
    # volume to ensure a consistent and complete snapshot. You may remount and use your volume while
    # the snapshot status is `pending`.
    #
    # When you create a snapshot for an EBS volume that serves as a root device, we recommend that you
    # stop the instance before taking the snapshot.
    #
    # Snapshots that are taken from encrypted volumes are automatically encrypted. Volumes that are
    # created from encrypted snapshots are also automatically encrypted. Your encrypted volumes and
    # any associated snapshots always remain protected. For more information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    def create_snapshot(input : AEC::CreateSnapshotRequest) : Core::ParsedResponse(AEC::Snapshot)
      Log.info { "performing 'CreateSnapshot' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSnapshot")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSnapshot", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::Snapshot.from_xml(result_node)
      Core::ParsedResponse(AEC::Snapshot).new(response, result)
    end

    # Creates crash-consistent snapshots of multiple EBS volumes attached to an Amazon EC2 instance.
    # Volumes are chosen by specifying an instance. Each volume attached to the specified instance
    # will produce one snapshot that is crash-consistent across the instance. You can include all of
    # the volumes currently attached to the instance, or you can exclude the root volume or specific
    # data (non-root) volumes from the multi-volume snapshot set.
    #
    # The location of the source instance determines where you can create the snapshots.
    #
    # - If the source instance is in a Region, you must create the snapshots in the same Region as the
    # instance.
    #
    # - If the source instance is in a Local Zone, you can create the snapshots in the same Local Zone
    # or in its parent Amazon Web Services Region.
    #
    # - If the source instance is on an Outpost, you can create the snapshots on the same Outpost or
    # in its parent Amazon Web Services Region.
    def create_snapshots(input : AEC::CreateSnapshotsRequest) : Core::ParsedResponse(AEC::CreateSnapshotsResult)
      Log.info { "performing 'CreateSnapshots' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSnapshots")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSnapshots", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateSnapshotsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateSnapshotsResult).new(response, result)
    end

    # Creates a data feed for Spot Instances, enabling you to view Spot Instance usage logs. You can
    # create one data feed per Amazon Web Services account. For more information, see [Spot Instance
    # data feed](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-data-feeds.html) in the
    # *Amazon EC2 User Guide*.
    def create_spot_datafeed_subscription(input : AEC::CreateSpotDatafeedSubscriptionRequest) : Core::ParsedResponse(AEC::CreateSpotDatafeedSubscriptionResult)
      Log.info { "performing 'CreateSpotDatafeedSubscription' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSpotDatafeedSubscription")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSpotDatafeedSubscription", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateSpotDatafeedSubscriptionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateSpotDatafeedSubscriptionResult).new(response, result)
    end

    # Stores an AMI as a single object in an Amazon S3 bucket.
    #
    # To use this API, you must have the required permissions. For more information, see [Permissions
    # for storing and restoring AMIs using
    # S3](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/work-with-ami-store-restore.html#ami-s3-permissions)
    # in the *Amazon EC2 User Guide*.
    #
    # For more information, see [Store and restore an AMI using
    # S3](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-store-restore.html) in the *Amazon
    # EC2 User Guide*.
    def create_store_image_task(input : AEC::CreateStoreImageTaskRequest) : Core::ParsedResponse(AEC::CreateStoreImageTaskResult)
      Log.info { "performing 'CreateStoreImageTask' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateStoreImageTask")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateStoreImageTask", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateStoreImageTaskResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateStoreImageTaskResult).new(response, result)
    end

    # Creates a subnet in the specified VPC. For an IPv4 only subnet, specify an IPv4 CIDR block. If
    # the VPC has an IPv6 CIDR block, you can create an IPv6 only subnet or a dual stack subnet
    # instead. For an IPv6 only subnet, specify an IPv6 CIDR block. For a dual stack subnet, specify
    # both an IPv4 CIDR block and an IPv6 CIDR block.
    #
    # A subnet CIDR block must not overlap the CIDR block of an existing subnet in the VPC. After you
    # create a subnet, you can't change its CIDR block.
    #
    # The allowed size for an IPv4 subnet is between a /28 netmask (16 IP addresses) and a /16 netmask
    # (65,536 IP addresses). Amazon Web Services reserves both the first four and the last IPv4
    # address in each subnet's CIDR block. They're not available for your use.
    #
    # If you've associated an IPv6 CIDR block with your VPC, you can associate an IPv6 CIDR block with
    # a subnet when you create it.
    #
    # If you add more than one subnet to a VPC, they're set up in a star topology with a logical
    # router in the middle.
    #
    # When you stop an instance in a subnet, it retains its private IPv4 address. It's therefore
    # possible to have a subnet with no running instances (they're all stopped), but no remaining IP
    # addresses available.
    #
    # For more information, see
    # [Subnets](https://docs.aws.amazon.com/vpc/latest/userguide/configure-subnets.html) in the
    # *Amazon VPC User Guide*.
    def create_subnet(input : AEC::CreateSubnetRequest) : Core::ParsedResponse(AEC::CreateSubnetResult)
      Log.info { "performing 'CreateSubnet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSubnet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSubnet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateSubnetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateSubnetResult).new(response, result)
    end

    # Creates a subnet CIDR reservation. For more information, see [Subnet CIDR
    # reservations](https://docs.aws.amazon.com/vpc/latest/userguide/subnet-cidr-reservation.html) in
    # the *Amazon VPC User Guide* and [Manage prefixes for your network
    # interfaces](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/work-with-prefixes.html) in the
    # *Amazon EC2 User Guide*.
    def create_subnet_cidr_reservation(input : AEC::CreateSubnetCidrReservationRequest) : Core::ParsedResponse(AEC::CreateSubnetCidrReservationResult)
      Log.info { "performing 'CreateSubnetCidrReservation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateSubnetCidrReservation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateSubnetCidrReservation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateSubnetCidrReservationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateSubnetCidrReservationResult).new(response, result)
    end

    # Adds or overwrites only the specified tags for the specified Amazon EC2 resource or resources.
    # When you specify an existing tag key, the value is overwritten with the new value. Each resource
    # can have a maximum of 50 tags. Each tag consists of a key and optional value. Tag keys must be
    # unique per resource.
    #
    # For more information about tags, see [Tag your Amazon EC2
    # resources](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html) in the *Amazon
    # Elastic Compute Cloud User Guide*. For more information about creating IAM policies that control
    # users' access to resources based on tags, see [Supported resource-level permissions for Amazon
    # EC2 API
    # actions](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-supported-iam-actions-resources.html)
    # in the *Amazon Elastic Compute Cloud User Guide*.
    def create_tags(input : AEC::CreateTagsRequest) : Core::Response
      Log.info { "performing 'CreateTags' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTags")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTags", body)
      Core::Response.new(response)
    end

    # Creates a Traffic Mirror filter.
    #
    # A Traffic Mirror filter is a set of rules that defines the traffic to mirror.
    #
    # By default, no traffic is mirrored. To mirror traffic, use
    # [CreateTrafficMirrorFilterRule](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateTrafficMirrorFilterRule.htm)
    # to add Traffic Mirror rules to the filter. The rules you add define what traffic gets mirrored.
    # You can also use
    # [ModifyTrafficMirrorFilterNetworkServices](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_ModifyTrafficMirrorFilterNetworkServices.html)
    # to mirror supported network services.
    def create_traffic_mirror_filter(input : AEC::CreateTrafficMirrorFilterRequest) : Core::ParsedResponse(AEC::CreateTrafficMirrorFilterResult)
      Log.info { "performing 'CreateTrafficMirrorFilter' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTrafficMirrorFilter")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTrafficMirrorFilter", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTrafficMirrorFilterResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTrafficMirrorFilterResult).new(response, result)
    end

    # Creates a Traffic Mirror filter rule.
    #
    # A Traffic Mirror rule defines the Traffic Mirror source traffic to mirror.
    #
    # You need the Traffic Mirror filter ID when you create the rule.
    def create_traffic_mirror_filter_rule(input : AEC::CreateTrafficMirrorFilterRuleRequest) : Core::ParsedResponse(AEC::CreateTrafficMirrorFilterRuleResult)
      Log.info { "performing 'CreateTrafficMirrorFilterRule' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTrafficMirrorFilterRule")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTrafficMirrorFilterRule", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTrafficMirrorFilterRuleResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTrafficMirrorFilterRuleResult).new(response, result)
    end

    # Creates a Traffic Mirror session.
    #
    # A Traffic Mirror session actively copies packets from a Traffic Mirror source to a Traffic
    # Mirror target. Create a filter, and then assign it to the session to define a subset of the
    # traffic to mirror, for example all TCP traffic.
    #
    # The Traffic Mirror source and the Traffic Mirror target (monitoring appliances) can be in the
    # same VPC, or in a different VPC connected via VPC peering or a transit gateway.
    #
    # By default, no traffic is mirrored. Use
    # [CreateTrafficMirrorFilter](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateTrafficMirrorFilter.html)
    # to create filter rules that specify the traffic to mirror.
    def create_traffic_mirror_session(input : AEC::CreateTrafficMirrorSessionRequest) : Core::ParsedResponse(AEC::CreateTrafficMirrorSessionResult)
      Log.info { "performing 'CreateTrafficMirrorSession' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTrafficMirrorSession")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTrafficMirrorSession", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTrafficMirrorSessionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTrafficMirrorSessionResult).new(response, result)
    end

    # Creates a target for your Traffic Mirror session.
    #
    # A Traffic Mirror target is the destination for mirrored traffic. The Traffic Mirror source and
    # the Traffic Mirror target (monitoring appliances) can be in the same VPC, or in different VPCs
    # connected via VPC peering or a transit gateway.
    #
    # A Traffic Mirror target can be a network interface, a Network Load Balancer, or a Gateway Load
    # Balancer endpoint.
    #
    # To use the target in a Traffic Mirror session, use
    # [CreateTrafficMirrorSession](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_CreateTrafficMirrorSession.htm).
    def create_traffic_mirror_target(input : AEC::CreateTrafficMirrorTargetRequest) : Core::ParsedResponse(AEC::CreateTrafficMirrorTargetResult)
      Log.info { "performing 'CreateTrafficMirrorTarget' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTrafficMirrorTarget")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTrafficMirrorTarget", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTrafficMirrorTargetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTrafficMirrorTargetResult).new(response, result)
    end

    # Creates a transit gateway.
    #
    # You can use a transit gateway to interconnect your virtual private clouds (VPC) and on-premises
    # networks. After the transit gateway enters the `available` state, you can attach your VPCs and
    # VPN connections to the transit gateway.
    #
    # To attach your VPCs, use CreateTransitGatewayVpcAttachment.
    #
    # To attach a VPN connection, use CreateCustomerGateway to create a customer gateway and specify
    # the ID of the customer gateway and the ID of the transit gateway in a call to
    # CreateVpnConnection.
    #
    # When you create a transit gateway, we create a default transit gateway route table and use it as
    # the default association route table and the default propagation route table. You can use
    # CreateTransitGatewayRouteTable to create additional transit gateway route tables. If you disable
    # automatic route propagation, we do not create a default transit gateway route table. You can use
    # EnableTransitGatewayRouteTablePropagation to propagate routes from a resource attachment to a
    # transit gateway route table. If you disable automatic associations, you can use
    # AssociateTransitGatewayRouteTable to associate a resource attachment with a transit gateway
    # route table.
    def create_transit_gateway(input : AEC::CreateTransitGatewayRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayResult)
      Log.info { "performing 'CreateTransitGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayResult).new(response, result)
    end

    # Creates a Connect attachment from a specified transit gateway attachment. A Connect attachment
    # is a GRE-based tunnel attachment that you can use to establish a connection between a transit
    # gateway and an appliance.
    #
    # A Connect attachment uses an existing VPC or Amazon Web Services Direct Connect attachment as
    # the underlying transport mechanism.
    def create_transit_gateway_connect(input : AEC::CreateTransitGatewayConnectRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayConnectResult)
      Log.info { "performing 'CreateTransitGatewayConnect' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayConnect")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayConnect", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayConnectResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayConnectResult).new(response, result)
    end

    # Creates a Connect peer for a specified transit gateway Connect attachment between a transit
    # gateway and an appliance.
    #
    # The peer address and transit gateway address must be the same IP address family (IPv4 or IPv6).
    #
    # For more information, see [Connect
    # peers](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html#tgw-connect-peer) in the
    # *Amazon Web Services Transit Gateways Guide*.
    def create_transit_gateway_connect_peer(input : AEC::CreateTransitGatewayConnectPeerRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayConnectPeerResult)
      Log.info { "performing 'CreateTransitGatewayConnectPeer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayConnectPeer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayConnectPeer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayConnectPeerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayConnectPeerResult).new(response, result)
    end

    # Creates a metering policy for a transit gateway to track and measure network traffic.
    def create_transit_gateway_metering_policy(input : AEC::CreateTransitGatewayMeteringPolicyRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayMeteringPolicyResult)
      Log.info { "performing 'CreateTransitGatewayMeteringPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayMeteringPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayMeteringPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayMeteringPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayMeteringPolicyResult).new(response, result)
    end

    # Creates an entry in a transit gateway metering policy to define traffic measurement rules.
    def create_transit_gateway_metering_policy_entry(input : AEC::CreateTransitGatewayMeteringPolicyEntryRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayMeteringPolicyEntryResult)
      Log.info { "performing 'CreateTransitGatewayMeteringPolicyEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayMeteringPolicyEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayMeteringPolicyEntry", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayMeteringPolicyEntryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayMeteringPolicyEntryResult).new(response, result)
    end

    # Creates a multicast domain using the specified transit gateway.
    #
    # The transit gateway must be in the available state before you create a domain. Use
    # [DescribeTransitGateways](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeTransitGateways.html)
    # to see the state of transit gateway.
    def create_transit_gateway_multicast_domain(input : AEC::CreateTransitGatewayMulticastDomainRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayMulticastDomainResult)
      Log.info { "performing 'CreateTransitGatewayMulticastDomain' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayMulticastDomain")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayMulticastDomain", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayMulticastDomainResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayMulticastDomainResult).new(response, result)
    end

    # Requests a transit gateway peering attachment between the specified transit gateway (requester)
    # and a peer transit gateway (accepter). The peer transit gateway can be in your account or a
    # different Amazon Web Services account.
    #
    # After you create the peering attachment, the owner of the accepter transit gateway must accept
    # the attachment request.
    def create_transit_gateway_peering_attachment(input : AEC::CreateTransitGatewayPeeringAttachmentRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayPeeringAttachmentResult)
      Log.info { "performing 'CreateTransitGatewayPeeringAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayPeeringAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayPeeringAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayPeeringAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayPeeringAttachmentResult).new(response, result)
    end

    # Creates a transit gateway policy table.
    def create_transit_gateway_policy_table(input : AEC::CreateTransitGatewayPolicyTableRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayPolicyTableResult)
      Log.info { "performing 'CreateTransitGatewayPolicyTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayPolicyTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayPolicyTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayPolicyTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayPolicyTableResult).new(response, result)
    end

    # Creates an entry in a transit gateway policy table to route matching traffic to a specified
    # route table.
    def create_transit_gateway_policy_table_entry(input : AEC::CreateTransitGatewayPolicyTableEntryRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayPolicyTableEntryResult)
      Log.info { "performing 'CreateTransitGatewayPolicyTableEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayPolicyTableEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayPolicyTableEntry", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayPolicyTableEntryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayPolicyTableEntryResult).new(response, result)
    end

    # Creates a reference (route) to a prefix list in a specified transit gateway route table.
    def create_transit_gateway_prefix_list_reference(input : AEC::CreateTransitGatewayPrefixListReferenceRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayPrefixListReferenceResult)
      Log.info { "performing 'CreateTransitGatewayPrefixListReference' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayPrefixListReference")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayPrefixListReference", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayPrefixListReferenceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayPrefixListReferenceResult).new(response, result)
    end

    # Creates a static route for the specified transit gateway route table.
    def create_transit_gateway_route(input : AEC::CreateTransitGatewayRouteRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayRouteResult)
      Log.info { "performing 'CreateTransitGatewayRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayRouteResult).new(response, result)
    end

    # Creates a route table for the specified transit gateway.
    def create_transit_gateway_route_table(input : AEC::CreateTransitGatewayRouteTableRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayRouteTableResult)
      Log.info { "performing 'CreateTransitGatewayRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayRouteTableResult).new(response, result)
    end

    # Advertises a new transit gateway route table.
    def create_transit_gateway_route_table_announcement(input : AEC::CreateTransitGatewayRouteTableAnnouncementRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayRouteTableAnnouncementResult)
      Log.info { "performing 'CreateTransitGatewayRouteTableAnnouncement' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayRouteTableAnnouncement")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayRouteTableAnnouncement", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayRouteTableAnnouncementResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayRouteTableAnnouncementResult).new(response, result)
    end

    # Attaches the specified VPC to the specified transit gateway.
    #
    # If you attach a VPC with a CIDR range that overlaps the CIDR range of a VPC that is already
    # attached, the new VPC CIDR range is not propagated to the default propagation route table.
    #
    # To send VPC traffic to an attached transit gateway, add a route to the VPC route table using
    # CreateRoute.
    def create_transit_gateway_vpc_attachment(input : AEC::CreateTransitGatewayVpcAttachmentRequest) : Core::ParsedResponse(AEC::CreateTransitGatewayVpcAttachmentResult)
      Log.info { "performing 'CreateTransitGatewayVpcAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateTransitGatewayVpcAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateTransitGatewayVpcAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateTransitGatewayVpcAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateTransitGatewayVpcAttachmentResult).new(response, result)
    end

    # An Amazon Web Services Verified Access endpoint is where you define your application along with
    # an optional endpoint-level access policy.
    def create_verified_access_endpoint(input : AEC::CreateVerifiedAccessEndpointRequest) : Core::ParsedResponse(AEC::CreateVerifiedAccessEndpointResult)
      Log.info { "performing 'CreateVerifiedAccessEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVerifiedAccessEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVerifiedAccessEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVerifiedAccessEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVerifiedAccessEndpointResult).new(response, result)
    end

    # An Amazon Web Services Verified Access group is a collection of Amazon Web Services Verified
    # Access endpoints who's associated applications have similar security requirements. Each instance
    # within a Verified Access group shares an Verified Access policy. For example, you can group all
    # Verified Access instances associated with "sales" applications together and use one common
    # Verified Access policy.
    def create_verified_access_group(input : AEC::CreateVerifiedAccessGroupRequest) : Core::ParsedResponse(AEC::CreateVerifiedAccessGroupResult)
      Log.info { "performing 'CreateVerifiedAccessGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVerifiedAccessGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVerifiedAccessGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVerifiedAccessGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVerifiedAccessGroupResult).new(response, result)
    end

    # An Amazon Web Services Verified Access instance is a regional entity that evaluates application
    # requests and grants access only when your security requirements are met.
    def create_verified_access_instance(input : AEC::CreateVerifiedAccessInstanceRequest) : Core::ParsedResponse(AEC::CreateVerifiedAccessInstanceResult)
      Log.info { "performing 'CreateVerifiedAccessInstance' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVerifiedAccessInstance")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVerifiedAccessInstance", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVerifiedAccessInstanceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVerifiedAccessInstanceResult).new(response, result)
    end

    # A trust provider is a third-party entity that creates, maintains, and manages identity
    # information for users and devices. When an application request is made, the identity information
    # sent by the trust provider is evaluated by Verified Access before allowing or denying the
    # application request.
    def create_verified_access_trust_provider(input : AEC::CreateVerifiedAccessTrustProviderRequest) : Core::ParsedResponse(AEC::CreateVerifiedAccessTrustProviderResult)
      Log.info { "performing 'CreateVerifiedAccessTrustProvider' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVerifiedAccessTrustProvider")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVerifiedAccessTrustProvider", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVerifiedAccessTrustProviderResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVerifiedAccessTrustProviderResult).new(response, result)
    end

    # Creates an EBS volume that can be attached to an instance in the same Availability Zone.
    #
    # You can create a new empty volume or restore a volume from an EBS snapshot. Any Amazon Web
    # Services Marketplace product codes from the snapshot are propagated to the volume.
    #
    # You can create encrypted volumes. Encrypted volumes must be attached to instances that support
    # Amazon EBS encryption. Volumes that are created from encrypted snapshots are also automatically
    # encrypted. For more information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    #
    # You can tag your volumes during creation. For more information, see [Tag your Amazon EC2
    # resources](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html) in the *Amazon
    # EC2 User Guide*.
    #
    # For more information, see [Create an Amazon EBS
    # volume](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-creating-volume.html) in the
    # *Amazon EBS User Guide*.
    def create_volume(input : AEC::CreateVolumeRequest) : Core::ParsedResponse(AEC::Volume)
      Log.info { "performing 'CreateVolume' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVolume")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVolume", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::Volume.from_xml(result_node)
      Core::ParsedResponse(AEC::Volume).new(response, result)
    end

    # Creates a VPC with the specified CIDR blocks.
    #
    # A VPC must have an associated IPv4 CIDR block. You can choose an IPv4 CIDR block or an
    # IPAM-allocated IPv4 CIDR block. You can optionally associate an IPv6 CIDR block with a VPC. You
    # can choose an IPv6 CIDR block, an Amazon-provided IPv6 CIDR block, an IPAM-allocated IPv6 CIDR
    # block, or an IPv6 CIDR block that you brought to Amazon Web Services. For more information, see
    # [IP addressing for your VPCs and
    # subnets](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-ip-addressing.html) in the *Amazon
    # VPC User Guide*.
    #
    # By default, each instance that you launch in the VPC has the default DHCP options, which include
    # only a default DNS server that we provide (AmazonProvidedDNS). For more information, see [DHCP
    # option sets](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_DHCP_Options.html) in the
    # *Amazon VPC User Guide*.
    #
    # You can specify DNS options and tenancy for a VPC when you create it. You can't change the
    # tenancy of a VPC after you create it. For more information, see [VPC configuration
    # options](https://docs.aws.amazon.com/vpc/latest/userguide/create-vpc-options.html) in the
    # *Amazon VPC User Guide*.
    def create_vpc(input : AEC::CreateVpcRequest) : Core::ParsedResponse(AEC::CreateVpcResult)
      Log.info { "performing 'CreateVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpcResult).new(response, result)
    end

    # Create a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be
    # applied to a single VPC or subnet that exempts it from the account’s BPA mode and will allow
    # bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even
    # when BPA is not enabled on the account to ensure that there is no traffic disruption to the
    # exclusions when VPC BPA is turned on. To learn more about VPC BPA, see [Block public access to
    # VPCs and subnets](https://docs.aws.amazon.com/vpc/latest/userguide/security-vpc-bpa.html) in the
    # *Amazon VPC User Guide*.
    def create_vpc_block_public_access_exclusion(input : AEC::CreateVpcBlockPublicAccessExclusionRequest) : Core::ParsedResponse(AEC::CreateVpcBlockPublicAccessExclusionResult)
      Log.info { "performing 'CreateVpcBlockPublicAccessExclusion' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpcBlockPublicAccessExclusion")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpcBlockPublicAccessExclusion", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpcBlockPublicAccessExclusionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpcBlockPublicAccessExclusionResult).new(response, result)
    end

    # Creates a VPC Encryption Control configuration for a specified VPC. VPC Encryption Control
    # enables you to enforce encryption for all data in transit within and between VPCs to meet
    # compliance requirements for standards like HIPAA, FedRAMP, and PCI DSS.
    #
    # For more information, see [Enforce VPC encryption in
    # transit](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-encryption-controls.html) in the
    # *Amazon VPC User Guide*.
    def create_vpc_encryption_control(input : AEC::CreateVpcEncryptionControlRequest) : Core::ParsedResponse(AEC::CreateVpcEncryptionControlResult)
      Log.info { "performing 'CreateVpcEncryptionControl' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpcEncryptionControl")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpcEncryptionControl", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpcEncryptionControlResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpcEncryptionControlResult).new(response, result)
    end

    # Creates a VPC endpoint. A VPC endpoint provides a private connection between the specified VPC
    # and the specified endpoint service. You can use an endpoint service provided by Amazon Web
    # Services, an Amazon Web Services Marketplace Partner, or another Amazon Web Services account.
    # For more information, see the [Amazon Web Services PrivateLink User
    # Guide](https://docs.aws.amazon.com/vpc/latest/privatelink/).
    def create_vpc_endpoint(input : AEC::CreateVpcEndpointRequest) : Core::ParsedResponse(AEC::CreateVpcEndpointResult)
      Log.info { "performing 'CreateVpcEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpcEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpcEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpcEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpcEndpointResult).new(response, result)
    end

    # Creates a connection notification for a specified VPC endpoint or VPC endpoint service. A
    # connection notification notifies you of specific endpoint events. You must create an SNS topic
    # to receive notifications. For more information, see [Creating an Amazon SNS
    # topic](https://docs.aws.amazon.com/sns/latest/dg/CreateTopic.html) in the *Amazon SNS Developer
    # Guide*.
    #
    # You can create a connection notification for interface endpoints only.
    def create_vpc_endpoint_connection_notification(input : AEC::CreateVpcEndpointConnectionNotificationRequest) : Core::ParsedResponse(AEC::CreateVpcEndpointConnectionNotificationResult)
      Log.info { "performing 'CreateVpcEndpointConnectionNotification' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpcEndpointConnectionNotification")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpcEndpointConnectionNotification", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpcEndpointConnectionNotificationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpcEndpointConnectionNotificationResult).new(response, result)
    end

    # Creates a VPC endpoint service to which service consumers (Amazon Web Services accounts, users,
    # and IAM roles) can connect.
    #
    # Before you create an endpoint service, you must create one of the following for your service:
    #
    # - A [Network Load Balancer](https://docs.aws.amazon.com/elasticloadbalancing/latest/network/).
    # Service consumers connect to your service using an interface endpoint.
    #
    # - A [Gateway Load Balancer](https://docs.aws.amazon.com/elasticloadbalancing/latest/gateway/).
    # Service consumers connect to your service using a Gateway Load Balancer endpoint.
    #
    # If you set the private DNS name, you must prove that you own the private DNS domain name.
    #
    # For more information, see the [Amazon Web Services PrivateLink
    # Guide](https://docs.aws.amazon.com/vpc/latest/privatelink/).
    def create_vpc_endpoint_service_configuration(input : AEC::CreateVpcEndpointServiceConfigurationRequest) : Core::ParsedResponse(AEC::CreateVpcEndpointServiceConfigurationResult)
      Log.info { "performing 'CreateVpcEndpointServiceConfiguration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpcEndpointServiceConfiguration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpcEndpointServiceConfiguration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpcEndpointServiceConfigurationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpcEndpointServiceConfigurationResult).new(response, result)
    end

    # Requests a VPC peering connection between two VPCs: a requester VPC that you own and an accepter
    # VPC with which to create the connection. The accepter VPC can belong to another Amazon Web
    # Services account and can be in a different Region to the requester VPC. The requester VPC and
    # accepter VPC cannot have overlapping CIDR blocks.
    #
    # Limitations and rules apply to a VPC peering connection. For more information, see the [VPC
    # peering
    # limitations](https://docs.aws.amazon.com/vpc/latest/peering/vpc-peering-basics.html#vpc-peering-limitations)
    # in the *VPC Peering Guide*.
    #
    # The owner of the accepter VPC must accept the peering request to activate the peering
    # connection. The VPC peering connection request expires after 7 days, after which it cannot be
    # accepted or rejected.
    #
    # If you create a VPC peering connection request between VPCs with overlapping CIDR blocks, the
    # VPC peering connection has a status of `failed`.
    def create_vpc_peering_connection(input : AEC::CreateVpcPeeringConnectionRequest) : Core::ParsedResponse(AEC::CreateVpcPeeringConnectionResult)
      Log.info { "performing 'CreateVpcPeeringConnection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpcPeeringConnection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpcPeeringConnection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpcPeeringConnectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpcPeeringConnectionResult).new(response, result)
    end

    # Creates a VPN concentrator that aggregates multiple VPN connections to a transit gateway.
    def create_vpn_concentrator(input : AEC::CreateVpnConcentratorRequest) : Core::ParsedResponse(AEC::CreateVpnConcentratorResult)
      Log.info { "performing 'CreateVpnConcentrator' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpnConcentrator")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpnConcentrator", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpnConcentratorResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpnConcentratorResult).new(response, result)
    end

    # Creates a VPN connection between an existing virtual private gateway or transit gateway and a
    # customer gateway. The supported connection type is `ipsec.1`.
    #
    # The response includes information that you need to give to your network administrator to
    # configure your customer gateway.
    #
    # We strongly recommend that you use HTTPS when calling this operation because the response
    # contains sensitive cryptographic information for configuring your customer gateway device.
    #
    # If you decide to shut down your VPN connection for any reason and later create a new VPN
    # connection, you must reconfigure your customer gateway with the new information returned from
    # this call.
    #
    # This is an idempotent operation. If you perform the operation more than once, Amazon EC2 doesn't
    # return an error.
    #
    # For more information, see [Amazon Web Services Site-to-Site
    # VPN](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPC_VPN.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def create_vpn_connection(input : AEC::CreateVpnConnectionRequest) : Core::ParsedResponse(AEC::CreateVpnConnectionResult)
      Log.info { "performing 'CreateVpnConnection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpnConnection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpnConnection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpnConnectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpnConnectionResult).new(response, result)
    end

    # Creates a static route associated with a VPN connection between an existing virtual private
    # gateway and a VPN customer gateway. The static route allows traffic to be routed from the
    # virtual private gateway to the VPN customer gateway.
    #
    # For more information, see [Amazon Web Services Site-to-Site
    # VPN](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPC_VPN.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def create_vpn_connection_route(input : AEC::CreateVpnConnectionRouteRequest) : Core::Response
      Log.info { "performing 'CreateVpnConnectionRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpnConnectionRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpnConnectionRoute", body)
      Core::Response.new(response)
    end

    # Creates a virtual private gateway. A virtual private gateway is the endpoint on the VPC side of
    # your VPN connection. You can create a virtual private gateway before creating the VPC itself.
    #
    # For more information, see [Amazon Web Services Site-to-Site
    # VPN](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPC_VPN.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def create_vpn_gateway(input : AEC::CreateVpnGatewayRequest) : Core::ParsedResponse(AEC::CreateVpnGatewayResult)
      Log.info { "performing 'CreateVpnGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "CreateVpnGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("CreateVpnGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::CreateVpnGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::CreateVpnGatewayResult).new(response, result)
    end

    # Deletes an application status check. The following rules apply:
    #
    # - Deleting a check automatically removes all of its associations.
    #
    # - Use `DescribeApplicationStatusChecks` to view existing checks before deleting.
    def delete_application_status_check(input : AEC::DeleteApplicationStatusCheckRequest) : Core::ParsedResponse(AEC::DeleteApplicationStatusCheckResult)
      Log.info { "performing 'DeleteApplicationStatusCheck' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteApplicationStatusCheck")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteApplicationStatusCheck", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteApplicationStatusCheckResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteApplicationStatusCheckResult).new(response, result)
    end

    # Deletes an existing Capacity Manager data export configuration. This stops future scheduled
    # exports but does not delete previously exported files from S3.
    def delete_capacity_manager_data_export(input : AEC::DeleteCapacityManagerDataExportRequest) : Core::ParsedResponse(AEC::DeleteCapacityManagerDataExportResult)
      Log.info { "performing 'DeleteCapacityManagerDataExport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteCapacityManagerDataExport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteCapacityManagerDataExport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteCapacityManagerDataExportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteCapacityManagerDataExportResult).new(response, result)
    end

    # Deletes a carrier gateway.
    #
    # If you do not delete the route that contains the carrier gateway as the Target, the route is a
    # blackhole route. For information about how to delete a route, see
    # [DeleteRoute](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DeleteRoute.html).
    def delete_carrier_gateway(input : AEC::DeleteCarrierGatewayRequest) : Core::ParsedResponse(AEC::DeleteCarrierGatewayResult)
      Log.info { "performing 'DeleteCarrierGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteCarrierGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteCarrierGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteCarrierGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteCarrierGatewayResult).new(response, result)
    end

    # Deletes the specified Client VPN endpoint. You must disassociate all target networks before you
    # can delete a Client VPN endpoint.
    def delete_client_vpn_endpoint(input : AEC::DeleteClientVpnEndpointRequest) : Core::ParsedResponse(AEC::DeleteClientVpnEndpointResult)
      Log.info { "performing 'DeleteClientVpnEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteClientVpnEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteClientVpnEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteClientVpnEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteClientVpnEndpointResult).new(response, result)
    end

    # Deletes a route from a Client VPN endpoint. You can only delete routes that you manually added
    # using the **CreateClientVpnRoute** action. You cannot delete routes that were automatically
    # added when associating a subnet. To remove routes that have been automatically added,
    # disassociate the target subnet from the Client VPN endpoint.
    def delete_client_vpn_route(input : AEC::DeleteClientVpnRouteRequest) : Core::ParsedResponse(AEC::DeleteClientVpnRouteResult)
      Log.info { "performing 'DeleteClientVpnRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteClientVpnRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteClientVpnRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteClientVpnRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteClientVpnRouteResult).new(response, result)
    end

    # Deletes a range of customer-owned IP addresses.
    def delete_coip_cidr(input : AEC::DeleteCoipCidrRequest) : Core::ParsedResponse(AEC::DeleteCoipCidrResult)
      Log.info { "performing 'DeleteCoipCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteCoipCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteCoipCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteCoipCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteCoipCidrResult).new(response, result)
    end

    # Deletes a pool of customer-owned IP (CoIP) addresses.
    def delete_coip_pool(input : AEC::DeleteCoipPoolRequest) : Core::ParsedResponse(AEC::DeleteCoipPoolResult)
      Log.info { "performing 'DeleteCoipPool' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteCoipPool")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteCoipPool", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteCoipPoolResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteCoipPoolResult).new(response, result)
    end

    # Deletes the specified customer gateway. You must delete the VPN connection before you can delete
    # the customer gateway.
    def delete_customer_gateway(input : AEC::DeleteCustomerGatewayRequest) : Core::Response
      Log.info { "performing 'DeleteCustomerGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteCustomerGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteCustomerGateway", body)
      Core::Response.new(response)
    end

    # Deletes the specified set of DHCP options. You must disassociate the set of DHCP options before
    # you can delete it. You can disassociate the set of DHCP options by associating either a new set
    # of options or the default set of options with the VPC.
    def delete_dhcp_options(input : AEC::DeleteDhcpOptionsRequest) : Core::Response
      Log.info { "performing 'DeleteDhcpOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteDhcpOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteDhcpOptions", body)
      Core::Response.new(response)
    end

    # Deletes an egress-only internet gateway.
    def delete_egress_only_internet_gateway(input : AEC::DeleteEgressOnlyInternetGatewayRequest) : Core::ParsedResponse(AEC::DeleteEgressOnlyInternetGatewayResult)
      Log.info { "performing 'DeleteEgressOnlyInternetGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteEgressOnlyInternetGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteEgressOnlyInternetGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteEgressOnlyInternetGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteEgressOnlyInternetGatewayResult).new(response, result)
    end

    # Deletes the specified EC2 Fleet request.
    #
    # After you delete an EC2 Fleet request, it launches no new instances.
    #
    # You must also specify whether a deleted EC2 Fleet request should terminate its instances. If you
    # choose to terminate the instances, the EC2 Fleet request enters the `deleted_terminating` state.
    # Otherwise, it enters the `deleted_running` state, and the instances continue to run until they
    # are interrupted or you terminate them manually.
    #
    # A deleted `instant` fleet with running instances is not supported. When you delete an `instant`
    # fleet, Amazon EC2 automatically terminates all its instances. For fleets with more than 1000
    # instances, the deletion request might fail. If your fleet has more than 1000 instances, first
    # terminate most of the instances manually, leaving 1000 or fewer. Then delete the fleet, and the
    # remaining instances will be terminated automatically.
    #
    # **Terminating an instance is permanent and irreversible.**
    #
    # After you terminate an instance, you can no longer connect to it, and it can't be recovered. All
    # attached Amazon EBS volumes that are configured to be deleted on termination are also
    # permanently deleted and can't be recovered. All data stored on instance store volumes is
    # permanently lost. For more information, see [ How instance termination
    # works](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/how-ec2-instance-termination-works.html).
    #
    # Before you terminate an instance, ensure that you have backed up all data that you need to
    # retain after the termination to persistent storage.
    #
    # **Restrictions**
    #
    # - You can delete up to 25 fleets of type `instant` in a single request.
    #
    # - You can delete up to 100 fleets of type `maintain` or `request` in a single request.
    #
    # - You can delete up to 125 fleets in a single request, provided you do not exceed the quota for
    # each fleet type, as specified above.
    #
    # - If you exceed the specified number of fleets to delete, no fleets are deleted.
    #
    # For more information, see [Delete an EC2 Fleet request and the instances in the
    # fleet](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/delete-fleet.html) in the *Amazon EC2
    # User Guide*.
    def delete_fleets(input : AEC::DeleteFleetsRequest) : Core::ParsedResponse(AEC::DeleteFleetsResult)
      Log.info { "performing 'DeleteFleets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteFleets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteFleets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteFleetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteFleetsResult).new(response, result)
    end

    # Deletes one or more flow logs.
    def delete_flow_logs(input : AEC::DeleteFlowLogsRequest) : Core::ParsedResponse(AEC::DeleteFlowLogsResult)
      Log.info { "performing 'DeleteFlowLogs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteFlowLogs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteFlowLogs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteFlowLogsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteFlowLogsResult).new(response, result)
    end

    # Deletes the specified Amazon FPGA Image (AFI).
    def delete_fpga_image(input : AEC::DeleteFpgaImageRequest) : Core::ParsedResponse(AEC::DeleteFpgaImageResult)
      Log.info { "performing 'DeleteFpgaImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteFpgaImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteFpgaImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteFpgaImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteFpgaImageResult).new(response, result)
    end

    # Deletes the specified image usage report.
    #
    # For more information, see [View your AMI
    # usage](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/your-ec2-ami-usage.html) in the
    # *Amazon EC2 User Guide*.
    def delete_image_usage_report(input : AEC::DeleteImageUsageReportRequest) : Core::ParsedResponse(AEC::DeleteImageUsageReportResult)
      Log.info { "performing 'DeleteImageUsageReport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteImageUsageReport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteImageUsageReport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteImageUsageReportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteImageUsageReportResult).new(response, result)
    end

    # Deletes the specified EC2 Instance Connect Endpoint.
    def delete_instance_connect_endpoint(input : AEC::DeleteInstanceConnectEndpointRequest) : Core::ParsedResponse(AEC::DeleteInstanceConnectEndpointResult)
      Log.info { "performing 'DeleteInstanceConnectEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteInstanceConnectEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteInstanceConnectEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteInstanceConnectEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteInstanceConnectEndpointResult).new(response, result)
    end

    # Deletes the specified event window.
    #
    # For more information, see [Define event windows for scheduled
    # events](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/event-windows.html) in the *Amazon
    # EC2 User Guide*.
    def delete_instance_event_window(input : AEC::DeleteInstanceEventWindowRequest) : Core::ParsedResponse(AEC::DeleteInstanceEventWindowResult)
      Log.info { "performing 'DeleteInstanceEventWindow' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteInstanceEventWindow")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteInstanceEventWindow", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteInstanceEventWindowResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteInstanceEventWindowResult).new(response, result)
    end

    # Deletes the specified internet gateway. You must detach the internet gateway from the VPC before
    # you can delete it.
    def delete_internet_gateway(input : AEC::DeleteInternetGatewayRequest) : Core::Response
      Log.info { "performing 'DeleteInternetGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteInternetGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteInternetGateway", body)
      Core::Response.new(response)
    end

    # Delete an IPAM. Deleting an IPAM removes all monitored data associated with the IPAM including
    # the historical data for CIDRs.
    #
    # For more information, see [Delete an
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/delete-ipam.html) in the *Amazon VPC IPAM User
    # Guide*.
    def delete_ipam(input : AEC::DeleteIpamRequest) : Core::ParsedResponse(AEC::DeleteIpamResult)
      Log.info { "performing 'DeleteIpam' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpam")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpam", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamResult).new(response, result)
    end

    # Delete a verification token.
    #
    # A verification token is an Amazon Web Services-generated random value that you can use to prove
    # ownership of an external resource. For example, you can use a verification token to validate
    # that you control a public IP address range when you bring an IP address range to Amazon Web
    # Services (BYOIP).
    def delete_ipam_external_resource_verification_token(input : AEC::DeleteIpamExternalResourceVerificationTokenRequest) : Core::ParsedResponse(AEC::DeleteIpamExternalResourceVerificationTokenResult)
      Log.info { "performing 'DeleteIpamExternalResourceVerificationToken' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamExternalResourceVerificationToken")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamExternalResourceVerificationToken", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamExternalResourceVerificationTokenResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamExternalResourceVerificationTokenResult).new(response, result)
    end

    # Deletes an IPAM internet registry association. Before deleting, you must remove all routing
    # policy registrations associated with the internet registry.
    def delete_ipam_internet_registry_association(input : AEC::DeleteIpamInternetRegistryAssociationRequest) : Core::ParsedResponse(AEC::DeleteIpamInternetRegistryAssociationResult)
      Log.info { "performing 'DeleteIpamInternetRegistryAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamInternetRegistryAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamInternetRegistryAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamInternetRegistryAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamInternetRegistryAssociationResult).new(response, result)
    end

    # Deletes an IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    def delete_ipam_policy(input : AEC::DeleteIpamPolicyRequest) : Core::ParsedResponse(AEC::DeleteIpamPolicyResult)
      Log.info { "performing 'DeleteIpamPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamPolicyResult).new(response, result)
    end

    # Delete an IPAM pool.
    #
    # You cannot delete an IPAM pool if there are allocations in it or CIDRs provisioned to it. To
    # release allocations, see
    # [ReleaseIpamPoolAllocation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_ReleaseIpamPoolAllocation.html).
    # To deprovision pool CIDRs, see
    # [DeprovisionIpamPoolCidr](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DeprovisionIpamPoolCidr.html).
    #
    # For more information, see [Delete a
    # pool](https://docs.aws.amazon.com/vpc/latest/ipam/delete-pool-ipam.html) in the *Amazon VPC IPAM
    # User Guide*.
    def delete_ipam_pool(input : AEC::DeleteIpamPoolRequest) : Core::ParsedResponse(AEC::DeleteIpamPoolResult)
      Log.info { "performing 'DeleteIpamPool' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamPool")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamPool", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamPoolResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamPoolResult).new(response, result)
    end

    # Deletes an IPAM prefix list resolver. Before deleting a resolver, you must first delete all
    # resolver targets associated with it.
    def delete_ipam_prefix_list_resolver(input : AEC::DeleteIpamPrefixListResolverRequest) : Core::ParsedResponse(AEC::DeleteIpamPrefixListResolverResult)
      Log.info { "performing 'DeleteIpamPrefixListResolver' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamPrefixListResolver")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamPrefixListResolver", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamPrefixListResolverResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamPrefixListResolverResult).new(response, result)
    end

    # Deletes an IPAM prefix list resolver target. This removes the association between the resolver
    # and the managed prefix list, stopping automatic CIDR synchronization.
    #
    # For more information about IPAM prefix list resolver, see [Automate prefix list updates with
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/automate-prefix-list-updates.html) in the
    # *Amazon VPC IPAM User Guide*.
    def delete_ipam_prefix_list_resolver_target(input : AEC::DeleteIpamPrefixListResolverTargetRequest) : Core::ParsedResponse(AEC::DeleteIpamPrefixListResolverTargetResult)
      Log.info { "performing 'DeleteIpamPrefixListResolverTarget' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamPrefixListResolverTarget")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamPrefixListResolverTarget", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamPrefixListResolverTargetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamPrefixListResolverTargetResult).new(response, result)
    end

    # Deletes an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM
    # to manage and monitor resources that belong to the owning account.
    def delete_ipam_resource_discovery(input : AEC::DeleteIpamResourceDiscoveryRequest) : Core::ParsedResponse(AEC::DeleteIpamResourceDiscoveryResult)
      Log.info { "performing 'DeleteIpamResourceDiscovery' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamResourceDiscovery")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamResourceDiscovery", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamResourceDiscoveryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamResourceDiscoveryResult).new(response, result)
    end

    # Deletes a routing policy registration for a specified CIDR prefix.
    def delete_ipam_routing_policy_registration(input : AEC::DeleteIpamRoutingPolicyRegistrationRequest) : Core::ParsedResponse(AEC::DeleteIpamRoutingPolicyRegistrationResult)
      Log.info { "performing 'DeleteIpamRoutingPolicyRegistration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamRoutingPolicyRegistration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamRoutingPolicyRegistration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamRoutingPolicyRegistrationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamRoutingPolicyRegistrationResult).new(response, result)
    end

    # Delete the scope for an IPAM. You cannot delete the default scopes.
    #
    # For more information, see [Delete a
    # scope](https://docs.aws.amazon.com/vpc/latest/ipam/delete-scope-ipam.html) in the *Amazon VPC
    # IPAM User Guide*.
    def delete_ipam_scope(input : AEC::DeleteIpamScopeRequest) : Core::ParsedResponse(AEC::DeleteIpamScopeResult)
      Log.info { "performing 'DeleteIpamScope' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteIpamScope")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteIpamScope", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteIpamScopeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteIpamScopeResult).new(response, result)
    end

    # Deletes the specified key pair, by removing the public key from Amazon EC2.
    def delete_key_pair(input : AEC::DeleteKeyPairRequest) : Core::ParsedResponse(AEC::DeleteKeyPairResult)
      Log.info { "performing 'DeleteKeyPair' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteKeyPair")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteKeyPair", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteKeyPairResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteKeyPairResult).new(response, result)
    end

    # Deletes a launch template. Deleting a launch template deletes all of its versions.
    def delete_launch_template(input : AEC::DeleteLaunchTemplateRequest) : Core::ParsedResponse(AEC::DeleteLaunchTemplateResult)
      Log.info { "performing 'DeleteLaunchTemplate' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLaunchTemplate")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLaunchTemplate", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLaunchTemplateResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLaunchTemplateResult).new(response, result)
    end

    # Deletes one or more versions of a launch template.
    #
    # You can't delete the default version of a launch template; you must first assign a different
    # version as the default. If the default version is the only version for the launch template, you
    # must delete the entire launch template using DeleteLaunchTemplate.
    #
    # You can delete up to 200 launch template versions in a single request. To delete more than 200
    # versions in a single request, use DeleteLaunchTemplate, which deletes the launch template and
    # all of its versions.
    #
    # For more information, see [Delete a launch template
    # version](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/delete-launch-template.html#delete-launch-template-version)
    # in the *Amazon EC2 User Guide*.
    def delete_launch_template_versions(input : AEC::DeleteLaunchTemplateVersionsRequest) : Core::ParsedResponse(AEC::DeleteLaunchTemplateVersionsResult)
      Log.info { "performing 'DeleteLaunchTemplateVersions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLaunchTemplateVersions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLaunchTemplateVersions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLaunchTemplateVersionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLaunchTemplateVersionsResult).new(response, result)
    end

    # Deletes the specified route from the specified local gateway route table.
    def delete_local_gateway_route(input : AEC::DeleteLocalGatewayRouteRequest) : Core::ParsedResponse(AEC::DeleteLocalGatewayRouteResult)
      Log.info { "performing 'DeleteLocalGatewayRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLocalGatewayRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLocalGatewayRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLocalGatewayRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLocalGatewayRouteResult).new(response, result)
    end

    # Deletes a local gateway route table.
    def delete_local_gateway_route_table(input : AEC::DeleteLocalGatewayRouteTableRequest) : Core::ParsedResponse(AEC::DeleteLocalGatewayRouteTableResult)
      Log.info { "performing 'DeleteLocalGatewayRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLocalGatewayRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLocalGatewayRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLocalGatewayRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLocalGatewayRouteTableResult).new(response, result)
    end

    # Deletes a local gateway route table virtual interface group association.
    def delete_local_gateway_route_table_virtual_interface_group_association(input : AEC::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest) : Core::ParsedResponse(AEC::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult)
      Log.info { "performing 'DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult).new(response, result)
    end

    # Deletes the specified association between a VPC and local gateway route table.
    def delete_local_gateway_route_table_vpc_association(input : AEC::DeleteLocalGatewayRouteTableVpcAssociationRequest) : Core::ParsedResponse(AEC::DeleteLocalGatewayRouteTableVpcAssociationResult)
      Log.info { "performing 'DeleteLocalGatewayRouteTableVpcAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLocalGatewayRouteTableVpcAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLocalGatewayRouteTableVpcAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLocalGatewayRouteTableVpcAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLocalGatewayRouteTableVpcAssociationResult).new(response, result)
    end

    # Deletes the specified local gateway virtual interface.
    def delete_local_gateway_virtual_interface(input : AEC::DeleteLocalGatewayVirtualInterfaceRequest) : Core::ParsedResponse(AEC::DeleteLocalGatewayVirtualInterfaceResult)
      Log.info { "performing 'DeleteLocalGatewayVirtualInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLocalGatewayVirtualInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLocalGatewayVirtualInterface", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLocalGatewayVirtualInterfaceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLocalGatewayVirtualInterfaceResult).new(response, result)
    end

    # Delete the specified local gateway interface group.
    def delete_local_gateway_virtual_interface_group(input : AEC::DeleteLocalGatewayVirtualInterfaceGroupRequest) : Core::ParsedResponse(AEC::DeleteLocalGatewayVirtualInterfaceGroupResult)
      Log.info { "performing 'DeleteLocalGatewayVirtualInterfaceGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteLocalGatewayVirtualInterfaceGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteLocalGatewayVirtualInterfaceGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteLocalGatewayVirtualInterfaceGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteLocalGatewayVirtualInterfaceGroupResult).new(response, result)
    end

    # Deletes the specified managed prefix list. You must first remove all references to the prefix
    # list in your resources.
    def delete_managed_prefix_list(input : AEC::DeleteManagedPrefixListRequest) : Core::ParsedResponse(AEC::DeleteManagedPrefixListResult)
      Log.info { "performing 'DeleteManagedPrefixList' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteManagedPrefixList")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteManagedPrefixList", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteManagedPrefixListResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteManagedPrefixListResult).new(response, result)
    end

    # Deletes the specified NAT gateway. Deleting a public NAT gateway disassociates its Elastic IP
    # address, but does not release the address from your account. Deleting a NAT gateway does not
    # delete any NAT gateway routes in your route tables.
    def delete_nat_gateway(input : AEC::DeleteNatGatewayRequest) : Core::ParsedResponse(AEC::DeleteNatGatewayResult)
      Log.info { "performing 'DeleteNatGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNatGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNatGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteNatGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteNatGatewayResult).new(response, result)
    end

    # Deletes the specified network ACL. You can't delete the ACL if it's associated with any subnets.
    # You can't delete the default network ACL.
    def delete_network_acl(input : AEC::DeleteNetworkAclRequest) : Core::Response
      Log.info { "performing 'DeleteNetworkAcl' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkAcl")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkAcl", body)
      Core::Response.new(response)
    end

    # Deletes the specified ingress or egress entry (rule) from the specified network ACL.
    def delete_network_acl_entry(input : AEC::DeleteNetworkAclEntryRequest) : Core::Response
      Log.info { "performing 'DeleteNetworkAclEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkAclEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkAclEntry", body)
      Core::Response.new(response)
    end

    # Deletes the specified Network Access Scope.
    def delete_network_insights_access_scope(input : AEC::DeleteNetworkInsightsAccessScopeRequest) : Core::ParsedResponse(AEC::DeleteNetworkInsightsAccessScopeResult)
      Log.info { "performing 'DeleteNetworkInsightsAccessScope' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkInsightsAccessScope")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkInsightsAccessScope", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteNetworkInsightsAccessScopeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteNetworkInsightsAccessScopeResult).new(response, result)
    end

    # Deletes the specified Network Access Scope analysis.
    def delete_network_insights_access_scope_analysis(input : AEC::DeleteNetworkInsightsAccessScopeAnalysisRequest) : Core::ParsedResponse(AEC::DeleteNetworkInsightsAccessScopeAnalysisResult)
      Log.info { "performing 'DeleteNetworkInsightsAccessScopeAnalysis' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkInsightsAccessScopeAnalysis")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkInsightsAccessScopeAnalysis", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteNetworkInsightsAccessScopeAnalysisResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteNetworkInsightsAccessScopeAnalysisResult).new(response, result)
    end

    # Deletes the specified network insights analysis.
    def delete_network_insights_analysis(input : AEC::DeleteNetworkInsightsAnalysisRequest) : Core::ParsedResponse(AEC::DeleteNetworkInsightsAnalysisResult)
      Log.info { "performing 'DeleteNetworkInsightsAnalysis' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkInsightsAnalysis")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkInsightsAnalysis", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteNetworkInsightsAnalysisResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteNetworkInsightsAnalysisResult).new(response, result)
    end

    # Deletes the specified path.
    def delete_network_insights_path(input : AEC::DeleteNetworkInsightsPathRequest) : Core::ParsedResponse(AEC::DeleteNetworkInsightsPathResult)
      Log.info { "performing 'DeleteNetworkInsightsPath' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkInsightsPath")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkInsightsPath", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteNetworkInsightsPathResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteNetworkInsightsPathResult).new(response, result)
    end

    # Deletes the specified network interface. You must detach the network interface before you can
    # delete it.
    def delete_network_interface(input : AEC::DeleteNetworkInterfaceRequest) : Core::Response
      Log.info { "performing 'DeleteNetworkInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkInterface", body)
      Core::Response.new(response)
    end

    # Deletes a permission for a network interface. By default, you cannot delete the permission if
    # the account for which you're removing the permission has attached the network interface to an
    # instance. However, you can force delete the permission, regardless of any attachment.
    def delete_network_interface_permission(input : AEC::DeleteNetworkInterfacePermissionRequest) : Core::ParsedResponse(AEC::DeleteNetworkInterfacePermissionResult)
      Log.info { "performing 'DeleteNetworkInterfacePermission' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteNetworkInterfacePermission")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteNetworkInterfacePermission", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteNetworkInterfacePermissionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteNetworkInterfacePermissionResult).new(response, result)
    end

    # Deletes the specified placement group. You must terminate all instances in the placement group
    # before you can delete the placement group. You cannot delete a placement group that is a parent
    # of a cluster placement group. Delete the cluster placement groups first. For more information,
    # see [Placement
    # groups](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/placement-groups.html) in the
    # *Amazon EC2 User Guide*.
    def delete_placement_group(input : AEC::DeletePlacementGroupRequest) : Core::Response
      Log.info { "performing 'DeletePlacementGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeletePlacementGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeletePlacementGroup", body)
      Core::Response.new(response)
    end

    # Delete a public IPv4 pool. A public IPv4 pool is an EC2 IP address pool required for the public
    # IPv4 CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6 addresses you
    # bring to Amazon Web Services, however, use IPAM pools only.
    def delete_public_ipv_4_pool(input : AEC::DeletePublicIpv4PoolRequest) : Core::ParsedResponse(AEC::DeletePublicIpv4PoolResult)
      Log.info { "performing 'DeletePublicIpv4Pool' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeletePublicIpv4Pool")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeletePublicIpv4Pool", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeletePublicIpv4PoolResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeletePublicIpv4PoolResult).new(response, result)
    end

    # Deletes the queued purchases for the specified Reserved Instances.
    def delete_queued_reserved_instances(input : AEC::DeleteQueuedReservedInstancesRequest) : Core::ParsedResponse(AEC::DeleteQueuedReservedInstancesResult)
      Log.info { "performing 'DeleteQueuedReservedInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteQueuedReservedInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteQueuedReservedInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteQueuedReservedInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteQueuedReservedInstancesResult).new(response, result)
    end

    # Deletes the specified route from the specified route table.
    def delete_route(input : AEC::DeleteRouteRequest) : Core::Response
      Log.info { "performing 'DeleteRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteRoute", body)
      Core::Response.new(response)
    end

    # Deletes the specified route server.
    #
    # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed
    # within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates
    # VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing
    # fault tolerance for those workloads. This enables you to automatically reroute traffic within a
    # VPC, which increases the manageability of VPC routing and interoperability with third-party
    # workloads.
    #
    # Route server supports the follow route table types:
    #
    # - VPC route tables not associated with subnets
    #
    # - Subnet route tables
    #
    # - Internet gateway route tables
    #
    # Route server does not support route tables associated with virtual private gateways. To
    # propagate routes into a transit gateway route table, use [Transit Gateway
    # Connect](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html).
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def delete_route_server(input : AEC::DeleteRouteServerRequest) : Core::ParsedResponse(AEC::DeleteRouteServerResult)
      Log.info { "performing 'DeleteRouteServer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteRouteServer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteRouteServer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteRouteServerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteRouteServerResult).new(response, result)
    end

    # Deletes the specified route server endpoint.
    #
    # A route server endpoint is an Amazon Web Services-managed component inside a subnet that
    # facilitates [BGP (Border Gateway
    # Protocol)](https://en.wikipedia.org/wiki/Border_Gateway_Protocol) connections between your route
    # server and your BGP peers.
    def delete_route_server_endpoint(input : AEC::DeleteRouteServerEndpointRequest) : Core::ParsedResponse(AEC::DeleteRouteServerEndpointResult)
      Log.info { "performing 'DeleteRouteServerEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteRouteServerEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteRouteServerEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteRouteServerEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteRouteServerEndpointResult).new(response, result)
    end

    # Deletes the specified BGP peer from a route server.
    #
    # A route server peer is a session between a route server endpoint and the device deployed in
    # Amazon Web Services (such as a firewall appliance or other network security function running on
    # an EC2 instance). The device must meet these requirements:
    #
    # - Have an elastic network interface in the VPC
    #
    # - Support BGP (Border Gateway Protocol)
    #
    # - Can initiate BGP sessions
    def delete_route_server_peer(input : AEC::DeleteRouteServerPeerRequest) : Core::ParsedResponse(AEC::DeleteRouteServerPeerResult)
      Log.info { "performing 'DeleteRouteServerPeer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteRouteServerPeer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteRouteServerPeer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteRouteServerPeerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteRouteServerPeerResult).new(response, result)
    end

    # Deletes the specified route table. You must disassociate the route table from any subnets before
    # you can delete it. You can't delete the main route table.
    def delete_route_table(input : AEC::DeleteRouteTableRequest) : Core::Response
      Log.info { "performing 'DeleteRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteRouteTable", body)
      Core::Response.new(response)
    end

    # Deletes a secondary network. You must delete all secondary subnets in the secondary network
    # before you can delete the secondary network.
    def delete_secondary_network(input : AEC::DeleteSecondaryNetworkRequest) : Core::ParsedResponse(AEC::DeleteSecondaryNetworkResult)
      Log.info { "performing 'DeleteSecondaryNetwork' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteSecondaryNetwork")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteSecondaryNetwork", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteSecondaryNetworkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteSecondaryNetworkResult).new(response, result)
    end

    # Deletes a secondary subnet. A secondary subnet must not contain any secondary interfaces prior
    # to deletion.
    def delete_secondary_subnet(input : AEC::DeleteSecondarySubnetRequest) : Core::ParsedResponse(AEC::DeleteSecondarySubnetResult)
      Log.info { "performing 'DeleteSecondarySubnet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteSecondarySubnet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteSecondarySubnet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteSecondarySubnetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteSecondarySubnetResult).new(response, result)
    end

    # Deletes a security group.
    #
    # If you attempt to delete a security group that is associated with an instance or network
    # interface, is referenced by another security group in the same VPC, or has a VPC association,
    # the operation fails with `DependencyViolation`.
    def delete_security_group(input : AEC::DeleteSecurityGroupRequest) : Core::ParsedResponse(AEC::DeleteSecurityGroupResult)
      Log.info { "performing 'DeleteSecurityGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteSecurityGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteSecurityGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteSecurityGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteSecurityGroupResult).new(response, result)
    end

    # Deletes the specified snapshot.
    #
    # When you make periodic snapshots of a volume, the snapshots are incremental, and only the blocks
    # on the device that have changed since your last snapshot are saved in the new snapshot. When you
    # delete a snapshot, only the data not needed for any other snapshot is removed. So regardless of
    # which prior snapshots have been deleted, all active snapshots will have access to all the
    # information needed to restore the volume.
    #
    # You cannot delete a snapshot of the root device of an EBS volume used by a registered AMI. You
    # must first deregister the AMI before you can delete the snapshot.
    #
    # For more information, see [Delete an Amazon EBS
    # snapshot](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-deleting-snapshot.html) in the
    # *Amazon EBS User Guide*.
    def delete_snapshot(input : AEC::DeleteSnapshotRequest) : Core::Response
      Log.info { "performing 'DeleteSnapshot' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteSnapshot")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteSnapshot", body)
      Core::Response.new(response)
    end

    # Deletes the data feed for Spot Instances.
    def delete_spot_datafeed_subscription(input : AEC::DeleteSpotDatafeedSubscriptionRequest) : Core::Response
      Log.info { "performing 'DeleteSpotDatafeedSubscription' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteSpotDatafeedSubscription")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteSpotDatafeedSubscription", body)
      Core::Response.new(response)
    end

    # Deletes the specified subnet. You must terminate all running instances in the subnet before you
    # can delete the subnet.
    def delete_subnet(input : AEC::DeleteSubnetRequest) : Core::Response
      Log.info { "performing 'DeleteSubnet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteSubnet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteSubnet", body)
      Core::Response.new(response)
    end

    # Deletes a subnet CIDR reservation.
    def delete_subnet_cidr_reservation(input : AEC::DeleteSubnetCidrReservationRequest) : Core::ParsedResponse(AEC::DeleteSubnetCidrReservationResult)
      Log.info { "performing 'DeleteSubnetCidrReservation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteSubnetCidrReservation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteSubnetCidrReservation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteSubnetCidrReservationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteSubnetCidrReservationResult).new(response, result)
    end

    # Deletes the specified set of tags from the specified set of resources.
    #
    # To list the current tags, use DescribeTags. For more information about tags, see [Tag your
    # Amazon EC2 resources](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html) in
    # the *Amazon Elastic Compute Cloud User Guide*.
    def delete_tags(input : AEC::DeleteTagsRequest) : Core::Response
      Log.info { "performing 'DeleteTags' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTags")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTags", body)
      Core::Response.new(response)
    end

    # Deletes the specified Traffic Mirror filter.
    #
    # You cannot delete a Traffic Mirror filter that is in use by a Traffic Mirror session.
    def delete_traffic_mirror_filter(input : AEC::DeleteTrafficMirrorFilterRequest) : Core::ParsedResponse(AEC::DeleteTrafficMirrorFilterResult)
      Log.info { "performing 'DeleteTrafficMirrorFilter' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTrafficMirrorFilter")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTrafficMirrorFilter", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTrafficMirrorFilterResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTrafficMirrorFilterResult).new(response, result)
    end

    # Deletes the specified Traffic Mirror rule.
    def delete_traffic_mirror_filter_rule(input : AEC::DeleteTrafficMirrorFilterRuleRequest) : Core::ParsedResponse(AEC::DeleteTrafficMirrorFilterRuleResult)
      Log.info { "performing 'DeleteTrafficMirrorFilterRule' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTrafficMirrorFilterRule")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTrafficMirrorFilterRule", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTrafficMirrorFilterRuleResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTrafficMirrorFilterRuleResult).new(response, result)
    end

    # Deletes the specified Traffic Mirror session.
    def delete_traffic_mirror_session(input : AEC::DeleteTrafficMirrorSessionRequest) : Core::ParsedResponse(AEC::DeleteTrafficMirrorSessionResult)
      Log.info { "performing 'DeleteTrafficMirrorSession' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTrafficMirrorSession")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTrafficMirrorSession", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTrafficMirrorSessionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTrafficMirrorSessionResult).new(response, result)
    end

    # Deletes the specified Traffic Mirror target.
    #
    # You cannot delete a Traffic Mirror target that is in use by a Traffic Mirror session.
    def delete_traffic_mirror_target(input : AEC::DeleteTrafficMirrorTargetRequest) : Core::ParsedResponse(AEC::DeleteTrafficMirrorTargetResult)
      Log.info { "performing 'DeleteTrafficMirrorTarget' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTrafficMirrorTarget")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTrafficMirrorTarget", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTrafficMirrorTargetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTrafficMirrorTargetResult).new(response, result)
    end

    # Deletes the specified transit gateway.
    def delete_transit_gateway(input : AEC::DeleteTransitGatewayRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayResult)
      Log.info { "performing 'DeleteTransitGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayResult).new(response, result)
    end

    # Deletes a Transit Gateway attachment for a Client VPN endpoint. The Transit Gateway owner can
    # delete the attachment to remove the association between the Client VPN endpoint and the Transit
    # Gateway.
    def delete_transit_gateway_client_vpn_attachment(input : AEC::DeleteTransitGatewayClientVpnAttachmentRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayClientVpnAttachmentResult)
      Log.info { "performing 'DeleteTransitGatewayClientVpnAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayClientVpnAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayClientVpnAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayClientVpnAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayClientVpnAttachmentResult).new(response, result)
    end

    # Deletes the specified Connect attachment. You must first delete any Connect peers for the
    # attachment.
    def delete_transit_gateway_connect(input : AEC::DeleteTransitGatewayConnectRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayConnectResult)
      Log.info { "performing 'DeleteTransitGatewayConnect' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayConnect")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayConnect", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayConnectResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayConnectResult).new(response, result)
    end

    # Deletes the specified Connect peer.
    def delete_transit_gateway_connect_peer(input : AEC::DeleteTransitGatewayConnectPeerRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayConnectPeerResult)
      Log.info { "performing 'DeleteTransitGatewayConnectPeer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayConnectPeer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayConnectPeer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayConnectPeerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayConnectPeerResult).new(response, result)
    end

    # Deletes a transit gateway metering policy.
    def delete_transit_gateway_metering_policy(input : AEC::DeleteTransitGatewayMeteringPolicyRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayMeteringPolicyResult)
      Log.info { "performing 'DeleteTransitGatewayMeteringPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayMeteringPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayMeteringPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayMeteringPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayMeteringPolicyResult).new(response, result)
    end

    # Deletes an entry from a transit gateway metering policy.
    def delete_transit_gateway_metering_policy_entry(input : AEC::DeleteTransitGatewayMeteringPolicyEntryRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayMeteringPolicyEntryResult)
      Log.info { "performing 'DeleteTransitGatewayMeteringPolicyEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayMeteringPolicyEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayMeteringPolicyEntry", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayMeteringPolicyEntryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayMeteringPolicyEntryResult).new(response, result)
    end

    # Deletes the specified transit gateway multicast domain.
    def delete_transit_gateway_multicast_domain(input : AEC::DeleteTransitGatewayMulticastDomainRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayMulticastDomainResult)
      Log.info { "performing 'DeleteTransitGatewayMulticastDomain' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayMulticastDomain")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayMulticastDomain", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayMulticastDomainResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayMulticastDomainResult).new(response, result)
    end

    # Deletes a transit gateway peering attachment.
    def delete_transit_gateway_peering_attachment(input : AEC::DeleteTransitGatewayPeeringAttachmentRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayPeeringAttachmentResult)
      Log.info { "performing 'DeleteTransitGatewayPeeringAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayPeeringAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayPeeringAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayPeeringAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayPeeringAttachmentResult).new(response, result)
    end

    # Deletes the specified transit gateway policy table.
    def delete_transit_gateway_policy_table(input : AEC::DeleteTransitGatewayPolicyTableRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayPolicyTableResult)
      Log.info { "performing 'DeleteTransitGatewayPolicyTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayPolicyTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayPolicyTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayPolicyTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayPolicyTableResult).new(response, result)
    end

    # Deletes the specified transit gateway policy table entry.
    def delete_transit_gateway_policy_table_entry(input : AEC::DeleteTransitGatewayPolicyTableEntryRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayPolicyTableEntryResult)
      Log.info { "performing 'DeleteTransitGatewayPolicyTableEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayPolicyTableEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayPolicyTableEntry", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayPolicyTableEntryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayPolicyTableEntryResult).new(response, result)
    end

    # Deletes a reference (route) to a prefix list in a specified transit gateway route table.
    def delete_transit_gateway_prefix_list_reference(input : AEC::DeleteTransitGatewayPrefixListReferenceRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayPrefixListReferenceResult)
      Log.info { "performing 'DeleteTransitGatewayPrefixListReference' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayPrefixListReference")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayPrefixListReference", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayPrefixListReferenceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayPrefixListReferenceResult).new(response, result)
    end

    # Deletes the specified route from the specified transit gateway route table.
    def delete_transit_gateway_route(input : AEC::DeleteTransitGatewayRouteRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayRouteResult)
      Log.info { "performing 'DeleteTransitGatewayRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayRouteResult).new(response, result)
    end

    # Deletes the specified transit gateway route table. If there are any route tables associated with
    # the transit gateway route table, you must first run DisassociateRouteTable before you can delete
    # the transit gateway route table. This removes any route tables associated with the transit
    # gateway route table.
    def delete_transit_gateway_route_table(input : AEC::DeleteTransitGatewayRouteTableRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayRouteTableResult)
      Log.info { "performing 'DeleteTransitGatewayRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayRouteTableResult).new(response, result)
    end

    # Advertises to the transit gateway that a transit gateway route table is deleted.
    def delete_transit_gateway_route_table_announcement(input : AEC::DeleteTransitGatewayRouteTableAnnouncementRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayRouteTableAnnouncementResult)
      Log.info { "performing 'DeleteTransitGatewayRouteTableAnnouncement' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayRouteTableAnnouncement")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayRouteTableAnnouncement", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayRouteTableAnnouncementResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayRouteTableAnnouncementResult).new(response, result)
    end

    # Deletes the specified VPC attachment.
    def delete_transit_gateway_vpc_attachment(input : AEC::DeleteTransitGatewayVpcAttachmentRequest) : Core::ParsedResponse(AEC::DeleteTransitGatewayVpcAttachmentResult)
      Log.info { "performing 'DeleteTransitGatewayVpcAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteTransitGatewayVpcAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteTransitGatewayVpcAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteTransitGatewayVpcAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteTransitGatewayVpcAttachmentResult).new(response, result)
    end

    # Delete an Amazon Web Services Verified Access endpoint.
    def delete_verified_access_endpoint(input : AEC::DeleteVerifiedAccessEndpointRequest) : Core::ParsedResponse(AEC::DeleteVerifiedAccessEndpointResult)
      Log.info { "performing 'DeleteVerifiedAccessEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVerifiedAccessEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVerifiedAccessEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVerifiedAccessEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVerifiedAccessEndpointResult).new(response, result)
    end

    # Delete an Amazon Web Services Verified Access group.
    def delete_verified_access_group(input : AEC::DeleteVerifiedAccessGroupRequest) : Core::ParsedResponse(AEC::DeleteVerifiedAccessGroupResult)
      Log.info { "performing 'DeleteVerifiedAccessGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVerifiedAccessGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVerifiedAccessGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVerifiedAccessGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVerifiedAccessGroupResult).new(response, result)
    end

    # Delete an Amazon Web Services Verified Access instance.
    def delete_verified_access_instance(input : AEC::DeleteVerifiedAccessInstanceRequest) : Core::ParsedResponse(AEC::DeleteVerifiedAccessInstanceResult)
      Log.info { "performing 'DeleteVerifiedAccessInstance' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVerifiedAccessInstance")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVerifiedAccessInstance", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVerifiedAccessInstanceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVerifiedAccessInstanceResult).new(response, result)
    end

    # Delete an Amazon Web Services Verified Access trust provider.
    def delete_verified_access_trust_provider(input : AEC::DeleteVerifiedAccessTrustProviderRequest) : Core::ParsedResponse(AEC::DeleteVerifiedAccessTrustProviderResult)
      Log.info { "performing 'DeleteVerifiedAccessTrustProvider' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVerifiedAccessTrustProvider")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVerifiedAccessTrustProvider", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVerifiedAccessTrustProviderResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVerifiedAccessTrustProviderResult).new(response, result)
    end

    # Deletes the specified EBS volume. The volume must be in the `available` state (not attached to
    # an instance).
    #
    # The volume can remain in the `deleting` state for several minutes.
    #
    # For more information, see [Delete an Amazon EBS
    # volume](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-deleting-volume.html) in the
    # *Amazon EBS User Guide*.
    def delete_volume(input : AEC::DeleteVolumeRequest) : Core::Response
      Log.info { "performing 'DeleteVolume' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVolume")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVolume", body)
      Core::Response.new(response)
    end

    # Deletes the specified VPC. You must detach or delete all gateways and resources that are
    # associated with the VPC before you can delete it. For example, you must terminate all instances
    # running in the VPC, delete all security groups associated with the VPC (except the default one),
    # delete all route tables associated with the VPC (except the default one), and so on. When you
    # delete the VPC, it deletes the default security group, network ACL, and route table for the VPC.
    #
    # If you created a flow log for the VPC that you are deleting, note that flow logs for deleted
    # VPCs are eventually automatically removed.
    def delete_vpc(input : AEC::DeleteVpcRequest) : Core::Response
      Log.info { "performing 'DeleteVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpc", body)
      Core::Response.new(response)
    end

    # Delete a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be
    # applied to a single VPC or subnet that exempts it from the account’s BPA mode and will allow
    # bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even
    # when BPA is not enabled on the account to ensure that there is no traffic disruption to the
    # exclusions when VPC BPA is turned on. To learn more about VPC BPA, see [Block public access to
    # VPCs and subnets](https://docs.aws.amazon.com/vpc/latest/userguide/security-vpc-bpa.html) in the
    # *Amazon VPC User Guide*.
    def delete_vpc_block_public_access_exclusion(input : AEC::DeleteVpcBlockPublicAccessExclusionRequest) : Core::ParsedResponse(AEC::DeleteVpcBlockPublicAccessExclusionResult)
      Log.info { "performing 'DeleteVpcBlockPublicAccessExclusion' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpcBlockPublicAccessExclusion")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpcBlockPublicAccessExclusion", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVpcBlockPublicAccessExclusionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVpcBlockPublicAccessExclusionResult).new(response, result)
    end

    # Deletes a VPC Encryption Control configuration. This removes the encryption policy enforcement
    # from the specified VPC.
    #
    # For more information, see [Enforce VPC encryption in
    # transit](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-encryption-controls.html) in the
    # *Amazon VPC User Guide*.
    def delete_vpc_encryption_control(input : AEC::DeleteVpcEncryptionControlRequest) : Core::ParsedResponse(AEC::DeleteVpcEncryptionControlResult)
      Log.info { "performing 'DeleteVpcEncryptionControl' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpcEncryptionControl")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpcEncryptionControl", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVpcEncryptionControlResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVpcEncryptionControlResult).new(response, result)
    end

    # Deletes the specified VPC endpoint connection notifications.
    def delete_vpc_endpoint_connection_notifications(input : AEC::DeleteVpcEndpointConnectionNotificationsRequest) : Core::ParsedResponse(AEC::DeleteVpcEndpointConnectionNotificationsResult)
      Log.info { "performing 'DeleteVpcEndpointConnectionNotifications' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpcEndpointConnectionNotifications")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpcEndpointConnectionNotifications", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVpcEndpointConnectionNotificationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVpcEndpointConnectionNotificationsResult).new(response, result)
    end

    # Deletes the specified VPC endpoints.
    #
    # When you delete a gateway endpoint, we delete the endpoint routes in the route tables for the
    # endpoint.
    #
    # When you delete a Gateway Load Balancer endpoint, we delete its endpoint network interfaces. You
    # can only delete Gateway Load Balancer endpoints when the routes that are associated with the
    # endpoint are deleted.
    #
    # When you delete an interface endpoint, we delete its endpoint network interfaces.
    def delete_vpc_endpoints(input : AEC::DeleteVpcEndpointsRequest) : Core::ParsedResponse(AEC::DeleteVpcEndpointsResult)
      Log.info { "performing 'DeleteVpcEndpoints' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpcEndpoints")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpcEndpoints", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVpcEndpointsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVpcEndpointsResult).new(response, result)
    end

    # Deletes the specified VPC endpoint service configurations. Before you can delete an endpoint
    # service configuration, you must reject any `Available` or `PendingAcceptance` interface endpoint
    # connections that are attached to the service.
    def delete_vpc_endpoint_service_configurations(input : AEC::DeleteVpcEndpointServiceConfigurationsRequest) : Core::ParsedResponse(AEC::DeleteVpcEndpointServiceConfigurationsResult)
      Log.info { "performing 'DeleteVpcEndpointServiceConfigurations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpcEndpointServiceConfigurations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpcEndpointServiceConfigurations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVpcEndpointServiceConfigurationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVpcEndpointServiceConfigurationsResult).new(response, result)
    end

    # Deletes a VPC peering connection. Either the owner of the requester VPC or the owner of the
    # accepter VPC can delete the VPC peering connection if it's in the `active` state. The owner of
    # the requester VPC can delete a VPC peering connection in the `pending-acceptance` state. You
    # cannot delete a VPC peering connection that's in the `failed` or `rejected` state.
    def delete_vpc_peering_connection(input : AEC::DeleteVpcPeeringConnectionRequest) : Core::ParsedResponse(AEC::DeleteVpcPeeringConnectionResult)
      Log.info { "performing 'DeleteVpcPeeringConnection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpcPeeringConnection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpcPeeringConnection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVpcPeeringConnectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVpcPeeringConnectionResult).new(response, result)
    end

    # Deletes the specified VPN concentrator.
    def delete_vpn_concentrator(input : AEC::DeleteVpnConcentratorRequest) : Core::ParsedResponse(AEC::DeleteVpnConcentratorResult)
      Log.info { "performing 'DeleteVpnConcentrator' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpnConcentrator")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpnConcentrator", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeleteVpnConcentratorResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeleteVpnConcentratorResult).new(response, result)
    end

    # Deletes the specified VPN connection.
    #
    # If you're deleting the VPC and its associated components, we recommend that you detach the
    # virtual private gateway from the VPC and delete the VPC before deleting the VPN connection. If
    # you believe that the tunnel credentials for your VPN connection have been compromised, you can
    # delete the VPN connection and create a new one that has new keys, without needing to delete the
    # VPC or virtual private gateway. If you create a new VPN connection, you must reconfigure the
    # customer gateway device using the new configuration information returned with the new VPN
    # connection ID.
    #
    # For certificate-based authentication, delete all Certificate Manager (ACM) private certificates
    # used for the Amazon Web Services-side tunnel endpoints for the VPN connection before deleting
    # the VPN connection.
    def delete_vpn_connection(input : AEC::DeleteVpnConnectionRequest) : Core::Response
      Log.info { "performing 'DeleteVpnConnection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpnConnection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpnConnection", body)
      Core::Response.new(response)
    end

    # Deletes the specified static route associated with a VPN connection between an existing virtual
    # private gateway and a VPN customer gateway. The static route allows traffic to be routed from
    # the virtual private gateway to the VPN customer gateway.
    def delete_vpn_connection_route(input : AEC::DeleteVpnConnectionRouteRequest) : Core::Response
      Log.info { "performing 'DeleteVpnConnectionRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpnConnectionRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpnConnectionRoute", body)
      Core::Response.new(response)
    end

    # Deletes the specified virtual private gateway. You must first detach the virtual private gateway
    # from the VPC. Note that you don't need to delete the virtual private gateway if you plan to
    # delete and recreate the VPN connection between your VPC and your network.
    def delete_vpn_gateway(input : AEC::DeleteVpnGatewayRequest) : Core::Response
      Log.info { "performing 'DeleteVpnGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeleteVpnGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeleteVpnGateway", body)
      Core::Response.new(response)
    end

    # Releases the specified address range that you provisioned for use with your Amazon Web Services
    # resources through bring your own IP addresses (BYOIP) and deletes the corresponding address
    # pool.
    #
    # Before you can release an address range, you must stop advertising it and you must not have any
    # IP addresses allocated from its address range.
    def deprovision_byoip_cidr(input : AEC::DeprovisionByoipCidrRequest) : Core::ParsedResponse(AEC::DeprovisionByoipCidrResult)
      Log.info { "performing 'DeprovisionByoipCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeprovisionByoipCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeprovisionByoipCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeprovisionByoipCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeprovisionByoipCidrResult).new(response, result)
    end

    # Deprovisions your Autonomous System Number (ASN) from your Amazon Web Services account. This
    # action can only be called after any BYOIP CIDR associations are removed from your Amazon Web
    # Services account with
    # [DisassociateIpamByoasn](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DisassociateIpamByoasn.html).
    # For more information, see [Tutorial: Bring your ASN to
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/tutorials-byoasn.html) in the *Amazon VPC IPAM
    # guide*.
    def deprovision_ipam_byoasn(input : AEC::DeprovisionIpamByoasnRequest) : Core::ParsedResponse(AEC::DeprovisionIpamByoasnResult)
      Log.info { "performing 'DeprovisionIpamByoasn' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeprovisionIpamByoasn")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeprovisionIpamByoasn", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeprovisionIpamByoasnResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeprovisionIpamByoasnResult).new(response, result)
    end

    # Deprovision a CIDR provisioned from an IPAM pool. If you deprovision a CIDR from a pool that has
    # a source pool, the CIDR is recycled back into the source pool. For more information, see
    # [Deprovision pool CIDRs](https://docs.aws.amazon.com/vpc/latest/ipam/depro-pool-cidr-ipam.html)
    # in the *Amazon VPC IPAM User Guide*.
    def deprovision_ipam_pool_cidr(input : AEC::DeprovisionIpamPoolCidrRequest) : Core::ParsedResponse(AEC::DeprovisionIpamPoolCidrResult)
      Log.info { "performing 'DeprovisionIpamPoolCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeprovisionIpamPoolCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeprovisionIpamPoolCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeprovisionIpamPoolCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeprovisionIpamPoolCidrResult).new(response, result)
    end

    # Deprovision a CIDR from a public IPv4 pool.
    def deprovision_public_ipv_4_pool_cidr(input : AEC::DeprovisionPublicIpv4PoolCidrRequest) : Core::ParsedResponse(AEC::DeprovisionPublicIpv4PoolCidrResult)
      Log.info { "performing 'DeprovisionPublicIpv4PoolCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeprovisionPublicIpv4PoolCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeprovisionPublicIpv4PoolCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeprovisionPublicIpv4PoolCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeprovisionPublicIpv4PoolCidrResult).new(response, result)
    end

    # Deregisters the specified AMI. A deregistered AMI can't be used to launch new instances.
    #
    # If a deregistered EBS-backed AMI matches a Recycle Bin retention rule, it moves to the Recycle
    # Bin for the specified retention period. It can be restored before its retention period expires,
    # after which it is permanently deleted. If the deregistered AMI doesn't match a retention rule,
    # it is permanently deleted immediately. For more information, see [Recover deleted Amazon EBS
    # snapshots and EBS-backed AMIs with Recycle
    # Bin](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/recycle-bin.html) in the *Amazon EBS
    # User Guide*.
    #
    # When deregistering an EBS-backed AMI, you can optionally delete its associated snapshots at the
    # same time. However, if a snapshot is associated with multiple AMIs, it won't be deleted even if
    # specified for deletion, although the AMI will still be deregistered.
    #
    # Deregistering an AMI does not delete the following:
    #
    # - Instances already launched from the AMI. You'll continue to incur usage costs for the
    # instances until you terminate them.
    #
    # - For EBS-backed AMIs: Snapshots that are associated with multiple AMIs. You'll continue to
    # incur snapshot storage costs.
    #
    # - For instance store-backed AMIs: The files uploaded to Amazon S3 during AMI creation. You'll
    # continue to incur S3 storage costs.
    #
    # For more information, see [Deregister an Amazon EC2
    # AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/deregister-ami.html) in the *Amazon EC2
    # User Guide*.
    def deregister_image(input : AEC::DeregisterImageRequest) : Core::ParsedResponse(AEC::DeregisterImageResult)
      Log.info { "performing 'DeregisterImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeregisterImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeregisterImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeregisterImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeregisterImageResult).new(response, result)
    end

    # Deregisters tag keys to prevent tags that have the specified tag keys from being included in
    # scheduled event notifications for resources in the Region.
    def deregister_instance_event_notification_attributes(input : AEC::DeregisterInstanceEventNotificationAttributesRequest) : Core::ParsedResponse(AEC::DeregisterInstanceEventNotificationAttributesResult)
      Log.info { "performing 'DeregisterInstanceEventNotificationAttributes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeregisterInstanceEventNotificationAttributes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeregisterInstanceEventNotificationAttributes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeregisterInstanceEventNotificationAttributesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeregisterInstanceEventNotificationAttributesResult).new(response, result)
    end

    # Deregisters the specified members (network interfaces) from the transit gateway multicast group.
    def deregister_transit_gateway_multicast_group_members(input : AEC::DeregisterTransitGatewayMulticastGroupMembersRequest) : Core::ParsedResponse(AEC::DeregisterTransitGatewayMulticastGroupMembersResult)
      Log.info { "performing 'DeregisterTransitGatewayMulticastGroupMembers' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeregisterTransitGatewayMulticastGroupMembers")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeregisterTransitGatewayMulticastGroupMembers", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeregisterTransitGatewayMulticastGroupMembersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeregisterTransitGatewayMulticastGroupMembersResult).new(response, result)
    end

    # Deregisters the specified sources (network interfaces) from the transit gateway multicast group.
    def deregister_transit_gateway_multicast_group_sources(input : AEC::DeregisterTransitGatewayMulticastGroupSourcesRequest) : Core::ParsedResponse(AEC::DeregisterTransitGatewayMulticastGroupSourcesResult)
      Log.info { "performing 'DeregisterTransitGatewayMulticastGroupSources' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DeregisterTransitGatewayMulticastGroupSources")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DeregisterTransitGatewayMulticastGroupSources", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DeregisterTransitGatewayMulticastGroupSourcesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DeregisterTransitGatewayMulticastGroupSourcesResult).new(response, result)
    end

    # Describes attributes of your Amazon Web Services account. The following are the supported
    # account attributes:
    #
    # - `default-vpc`: The ID of the default VPC for your account, or `none`.
    #
    # - `max-instances`: This attribute is no longer supported. The returned value does not reflect
    # your actual vCPU limit for running On-Demand Instances. For more information, see [On-Demand
    # Instance
    # Limits](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-on-demand-instances.html#ec2-on-demand-instances-limits)
    # in the *Amazon Elastic Compute Cloud User Guide*.
    #
    # - `max-elastic-ips`: The maximum number of Elastic IP addresses that you can allocate.
    #
    # - `supported-platforms`: This attribute is deprecated.
    #
    # - `vpc-max-elastic-ips`: The maximum number of Elastic IP addresses that you can allocate.
    #
    # - `vpc-max-security-groups-per-interface`: The maximum number of security groups that you can
    # assign to a network interface.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_account_attributes(input : AEC::DescribeAccountAttributesRequest) : Core::ParsedResponse(AEC::DescribeAccountAttributesResult)
      Log.info { "performing 'DescribeAccountAttributes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAccountAttributes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAccountAttributes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAccountAttributesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAccountAttributesResult).new(response, result)
    end

    # Describes the account-level VPC Encryption Control configuration for your account. VPC
    # Encryption Control enables you to enforce encryption for all data in transit within and between
    # VPCs to meet compliance requirements.
    #
    # For more information, see [Enforce VPC encryption in
    # transit](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-encryption-controls.html) in the
    # *Amazon VPC User Guide*.
    def describe_account_vpc_encryption_control(input : AEC::DescribeAccountVpcEncryptionControlRequest) : Core::ParsedResponse(AEC::DescribeAccountVpcEncryptionControlResult)
      Log.info { "performing 'DescribeAccountVpcEncryptionControl' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAccountVpcEncryptionControl")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAccountVpcEncryptionControl", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAccountVpcEncryptionControlResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAccountVpcEncryptionControlResult).new(response, result)
    end

    # Describes the specified Elastic IP addresses or all of your Elastic IP addresses.
    def describe_addresses(input : AEC::DescribeAddressesRequest) : Core::ParsedResponse(AEC::DescribeAddressesResult)
      Log.info { "performing 'DescribeAddresses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAddresses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAddresses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAddressesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAddressesResult).new(response, result)
    end

    # Describes the attributes of the specified Elastic IP addresses. For requirements, see [Using
    # reverse DNS for email
    # applications](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html#Using_Elastic_Addressing_Reverse_DNS).
    def describe_addresses_attribute(input : AEC::DescribeAddressesAttributeRequest) : Core::ParsedResponse(AEC::DescribeAddressesAttributeResult)
      Log.info { "performing 'DescribeAddressesAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAddressesAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAddressesAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAddressesAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAddressesAttributeResult).new(response, result)
    end

    # Describes an Elastic IP address transfer. For more information, see [Transfer Elastic IP
    # addresses](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-eips.html#transfer-EIPs-intro)
    # in the *Amazon VPC User Guide*.
    #
    # When you transfer an Elastic IP address, there is a two-step handshake between the source and
    # transfer Amazon Web Services accounts. When the source account starts the transfer, the transfer
    # account has seven days to accept the Elastic IP address transfer. During those seven days, the
    # source account can view the pending transfer by using this action. After seven days, the
    # transfer expires and ownership of the Elastic IP address returns to the source account. Accepted
    # transfers are visible to the source account for 14 days after the transfers have been accepted.
    def describe_address_transfers(input : AEC::DescribeAddressTransfersRequest) : Core::ParsedResponse(AEC::DescribeAddressTransfersResult)
      Log.info { "performing 'DescribeAddressTransfers' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAddressTransfers")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAddressTransfers", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAddressTransfersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAddressTransfersResult).new(response, result)
    end

    # Describes the longer ID format settings for all resource types in a specific Region. This
    # request is useful for performing a quick audit to determine whether a specific Region is fully
    # opted in for longer IDs (17-character IDs).
    #
    # This request only returns information about resource types that support longer IDs.
    #
    # The following resource types support longer IDs: `bundle` | `conversion-task` |
    # `customer-gateway` | `dhcp-options` | `elastic-ip-allocation` | `elastic-ip-association` |
    # `export-task` | `flow-log` | `image` | `import-task` | `instance` | `internet-gateway` |
    # `network-acl` | `network-acl-association` | `network-interface` | `network-interface-attachment`
    # | `prefix-list` | `reservation` | `route-table` | `route-table-association` | `security-group` |
    # `snapshot` | `subnet` | `subnet-cidr-block-association` | `volume` | `vpc` |
    # `vpc-cidr-block-association` | `vpc-endpoint` | `vpc-peering-connection` | `vpn-connection` |
    # `vpn-gateway`.
    def describe_aggregate_id_format(input : AEC::DescribeAggregateIdFormatRequest) : Core::ParsedResponse(AEC::DescribeAggregateIdFormatResult)
      Log.info { "performing 'DescribeAggregateIdFormat' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAggregateIdFormat")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAggregateIdFormat", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAggregateIdFormatResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAggregateIdFormatResult).new(response, result)
    end

    # Describes the aggregated application health status for the specified instances. The following
    # rules apply:
    #
    # - The instance-level status is derived from all application status checks with the aggregation
    # setting set to `included`.
    #
    # - Use `DescribeApplicationStatusChecks` to view the configuration of individual checks.
    #
    # - Use `EnableApplicationStatusCheckSuppression` to temporarily suppress health check results
    # from affecting the instance-level status.
    def describe_application_status(input : AEC::DescribeApplicationStatusRequest) : Core::ParsedResponse(AEC::DescribeApplicationStatusResult)
      Log.info { "performing 'DescribeApplicationStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeApplicationStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeApplicationStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeApplicationStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeApplicationStatusResult).new(response, result)
    end

    # Describes the associations for one or more application status checks. For more information, see
    # [Application status
    # checks](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/monitoring-application-status-checks.html).
    # To avoid timeouts and retrieve complete results, use the pagination parameters.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    def describe_application_status_check_associations(input : AEC::DescribeApplicationStatusCheckAssociationsRequest) : Core::ParsedResponse(AEC::DescribeApplicationStatusCheckAssociationsResult)
      Log.info { "performing 'DescribeApplicationStatusCheckAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeApplicationStatusCheckAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeApplicationStatusCheckAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeApplicationStatusCheckAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeApplicationStatusCheckAssociationsResult).new(response, result)
    end

    # Describes application status checks, including configuration details such as protocol, port,
    # path, thresholds, and associations. Results are paginated. Use the `NextToken` parameter to
    # retrieve additional results. The following rules apply:
    #
    # - If you do not specify any application status check IDs, all checks in your account are
    # returned.
    #
    # - Use `DescribeApplicationStatus` to see the actual health status of instances.
    def describe_application_status_checks(input : AEC::DescribeApplicationStatusChecksRequest) : Core::ParsedResponse(AEC::DescribeApplicationStatusChecksResult)
      Log.info { "performing 'DescribeApplicationStatusChecks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeApplicationStatusChecks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeApplicationStatusChecks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeApplicationStatusChecksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeApplicationStatusChecksResult).new(response, result)
    end

    # Describes the Availability Zones, Local Zones, and Wavelength Zones that are available to you.
    #
    # For more information about Availability Zones, Local Zones, and Wavelength Zones, see [Regions
    # and
    # zones](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-regions-availability-zones.html)
    # in the *Amazon EC2 User Guide*.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_availability_zones(input : AEC::DescribeAvailabilityZonesRequest) : Core::ParsedResponse(AEC::DescribeAvailabilityZonesResult)
      Log.info { "performing 'DescribeAvailabilityZones' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAvailabilityZones")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAvailabilityZones", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAvailabilityZonesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAvailabilityZonesResult).new(response, result)
    end

    # Describes the current Infrastructure Performance metric subscriptions.
    def describe_aws_network_performance_metric_subscriptions(input : AEC::DescribeAwsNetworkPerformanceMetricSubscriptionsRequest) : Core::ParsedResponse(AEC::DescribeAwsNetworkPerformanceMetricSubscriptionsResult)
      Log.info { "performing 'DescribeAwsNetworkPerformanceMetricSubscriptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeAwsNetworkPerformanceMetricSubscriptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeAwsNetworkPerformanceMetricSubscriptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeAwsNetworkPerformanceMetricSubscriptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeAwsNetworkPerformanceMetricSubscriptionsResult).new(response, result)
    end

    # Describes the specified bundle tasks or all of your bundle tasks.
    #
    # Completed bundle tasks are listed for only a limited time. If your bundle task is no longer in
    # the list, you can still register an AMI from it. Just use `RegisterImage` with the Amazon S3
    # bucket name and image manifest name you provided to the bundle task.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_bundle_tasks(input : AEC::DescribeBundleTasksRequest) : Core::ParsedResponse(AEC::DescribeBundleTasksResult)
      Log.info { "performing 'DescribeBundleTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeBundleTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeBundleTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeBundleTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeBundleTasksResult).new(response, result)
    end

    # Describes the IP address ranges that were provisioned for use with Amazon Web Services resources
    # through through bring your own IP addresses (BYOIP).
    def describe_byoip_cidrs(input : AEC::DescribeByoipCidrsRequest) : Core::ParsedResponse(AEC::DescribeByoipCidrsResult)
      Log.info { "performing 'DescribeByoipCidrs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeByoipCidrs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeByoipCidrs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeByoipCidrsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeByoipCidrsResult).new(response, result)
    end

    # Describes the events for the specified Capacity Block extension during the specified time.
    def describe_capacity_block_extension_history(input : AEC::DescribeCapacityBlockExtensionHistoryRequest) : Core::ParsedResponse(AEC::DescribeCapacityBlockExtensionHistoryResult)
      Log.info { "performing 'DescribeCapacityBlockExtensionHistory' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityBlockExtensionHistory")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityBlockExtensionHistory", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityBlockExtensionHistoryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityBlockExtensionHistoryResult).new(response, result)
    end

    # Describes Capacity Block extension offerings available for purchase in the Amazon Web Services
    # Region that you're currently using.
    def describe_capacity_block_extension_offerings(input : AEC::DescribeCapacityBlockExtensionOfferingsRequest) : Core::ParsedResponse(AEC::DescribeCapacityBlockExtensionOfferingsResult)
      Log.info { "performing 'DescribeCapacityBlockExtensionOfferings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityBlockExtensionOfferings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityBlockExtensionOfferings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityBlockExtensionOfferingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityBlockExtensionOfferingsResult).new(response, result)
    end

    # Describes Capacity Block offerings available for purchase in the Amazon Web Services Region that
    # you're currently using. With Capacity Blocks, you can purchase a specific GPU instance type or
    # EC2 UltraServer for a period of time.
    #
    # To search for an available Capacity Block offering, you specify a reservation duration and
    # instance count.
    def describe_capacity_block_offerings(input : AEC::DescribeCapacityBlockOfferingsRequest) : Core::ParsedResponse(AEC::DescribeCapacityBlockOfferingsResult)
      Log.info { "performing 'DescribeCapacityBlockOfferings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityBlockOfferings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityBlockOfferings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityBlockOfferingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityBlockOfferingsResult).new(response, result)
    end

    # Describes details about Capacity Blocks in the Amazon Web Services Region that you're currently
    # using.
    def describe_capacity_blocks(input : AEC::DescribeCapacityBlocksRequest) : Core::ParsedResponse(AEC::DescribeCapacityBlocksResult)
      Log.info { "performing 'DescribeCapacityBlocks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityBlocks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityBlocks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityBlocksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityBlocksResult).new(response, result)
    end

    # Describes the availability of capacity for the specified Capacity blocks, or all of your
    # Capacity Blocks.
    def describe_capacity_block_status(input : AEC::DescribeCapacityBlockStatusRequest) : Core::ParsedResponse(AEC::DescribeCapacityBlockStatusResult)
      Log.info { "performing 'DescribeCapacityBlockStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityBlockStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityBlockStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityBlockStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityBlockStatusResult).new(response, result)
    end

    # Describes one or more Capacity Manager data export configurations. Returns information about
    # export settings, delivery status, and recent export activity.
    def describe_capacity_manager_data_exports(input : AEC::DescribeCapacityManagerDataExportsRequest) : Core::ParsedResponse(AEC::DescribeCapacityManagerDataExportsResult)
      Log.info { "performing 'DescribeCapacityManagerDataExports' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityManagerDataExports")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityManagerDataExports", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityManagerDataExportsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityManagerDataExportsResult).new(response, result)
    end

    # Describes a request to assign the billing of the unused capacity of a Capacity Reservation. For
    # more information, see [ Billing assignment for shared Amazon EC2 Capacity
    # Reservations](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/transfer-billing.html).
    def describe_capacity_reservation_billing_requests(input : AEC::DescribeCapacityReservationBillingRequestsRequest) : Core::ParsedResponse(AEC::DescribeCapacityReservationBillingRequestsResult)
      Log.info { "performing 'DescribeCapacityReservationBillingRequests' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityReservationBillingRequests")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityReservationBillingRequests", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityReservationBillingRequestsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityReservationBillingRequestsResult).new(response, result)
    end

    # Describes one or more Capacity Reservation cancellation quotes. The results describe only the
    # quotes that you have previously generated by using the
    # `CreateCapacityReservationCancellationQuote` action.
    def describe_capacity_reservation_cancellation_quotes(input : AEC::DescribeCapacityReservationCancellationQuotesRequest) : Core::ParsedResponse(AEC::DescribeCapacityReservationCancellationQuotesResult)
      Log.info { "performing 'DescribeCapacityReservationCancellationQuotes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityReservationCancellationQuotes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityReservationCancellationQuotes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityReservationCancellationQuotesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityReservationCancellationQuotesResult).new(response, result)
    end

    # Describes one or more Capacity Reservation Fleets.
    def describe_capacity_reservation_fleets(input : AEC::DescribeCapacityReservationFleetsRequest) : Core::ParsedResponse(AEC::DescribeCapacityReservationFleetsResult)
      Log.info { "performing 'DescribeCapacityReservationFleets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityReservationFleets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityReservationFleets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityReservationFleetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityReservationFleetsResult).new(response, result)
    end

    # Describes one or more of your Capacity Reservations. The results describe only the Capacity
    # Reservations in the Amazon Web Services Region that you're currently using.
    def describe_capacity_reservations(input : AEC::DescribeCapacityReservationsRequest) : Core::ParsedResponse(AEC::DescribeCapacityReservationsResult)
      Log.info { "performing 'DescribeCapacityReservations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityReservations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityReservations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityReservationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityReservationsResult).new(response, result)
    end

    # Describes a tree-based hierarchy that represents the physical host placement of your pending or
    # active Capacity Reservations within an Availability Zone or Local Zone. You can use this
    # information to determine the relative proximity of your capacity within the Amazon Web Services
    # network before it is launched and use this information to allocate capacity together to support
    # your tightly coupled workloads.
    #
    # Capacity Reservation topology is supported for specific instance types only. For more
    # information, see [Prerequisites for Amazon EC2 instance
    # topology](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-topology-prerequisites.html)
    # in the *Amazon EC2 User Guide*.
    #
    # The Amazon EC2 API follows an eventual consistency model due to the distributed nature of the
    # system supporting it. As a result, when you call the DescribeCapacityReservationTopology API
    # command immediately after launching instances, the response might return a `null` value for
    # `capacityBlockId` because the data might not have fully propagated across all subsystems. For
    # more information, see [Eventual consistency in the Amazon EC2
    # API](https://docs.aws.amazon.com/ec2/latest/devguide/eventual-consistency.html) in the *Amazon
    # EC2 Developer Guide*.
    #
    # For more information, see [Amazon EC2
    # topology](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-topology.html) in the
    # *Amazon EC2 User Guide*.
    def describe_capacity_reservation_topology(input : AEC::DescribeCapacityReservationTopologyRequest) : Core::ParsedResponse(AEC::DescribeCapacityReservationTopologyResult)
      Log.info { "performing 'DescribeCapacityReservationTopology' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCapacityReservationTopology")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCapacityReservationTopology", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCapacityReservationTopologyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCapacityReservationTopologyResult).new(response, result)
    end

    # Describes one or more of your carrier gateways.
    def describe_carrier_gateways(input : AEC::DescribeCarrierGatewaysRequest) : Core::ParsedResponse(AEC::DescribeCarrierGatewaysResult)
      Log.info { "performing 'DescribeCarrierGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCarrierGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCarrierGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCarrierGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCarrierGatewaysResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Describes your linked EC2-Classic instances. This request only returns information about
    # EC2-Classic instances linked to a VPC through ClassicLink. You cannot use this request to return
    # information about other instances.
    def describe_classic_link_instances(input : AEC::DescribeClassicLinkInstancesRequest) : Core::ParsedResponse(AEC::DescribeClassicLinkInstancesResult)
      Log.info { "performing 'DescribeClassicLinkInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeClassicLinkInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeClassicLinkInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeClassicLinkInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeClassicLinkInstancesResult).new(response, result)
    end

    # Describes the authorization rules for a specified Client VPN endpoint.
    def describe_client_vpn_authorization_rules(input : AEC::DescribeClientVpnAuthorizationRulesRequest) : Core::ParsedResponse(AEC::DescribeClientVpnAuthorizationRulesResult)
      Log.info { "performing 'DescribeClientVpnAuthorizationRules' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeClientVpnAuthorizationRules")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeClientVpnAuthorizationRules", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeClientVpnAuthorizationRulesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeClientVpnAuthorizationRulesResult).new(response, result)
    end

    # Describes active client connections and connections that have been terminated within the last 60
    # minutes for the specified Client VPN endpoint.
    def describe_client_vpn_connections(input : AEC::DescribeClientVpnConnectionsRequest) : Core::ParsedResponse(AEC::DescribeClientVpnConnectionsResult)
      Log.info { "performing 'DescribeClientVpnConnections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeClientVpnConnections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeClientVpnConnections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeClientVpnConnectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeClientVpnConnectionsResult).new(response, result)
    end

    # Describes one or more Client VPN endpoints in the account.
    def describe_client_vpn_endpoints(input : AEC::DescribeClientVpnEndpointsRequest) : Core::ParsedResponse(AEC::DescribeClientVpnEndpointsResult)
      Log.info { "performing 'DescribeClientVpnEndpoints' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeClientVpnEndpoints")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeClientVpnEndpoints", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeClientVpnEndpointsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeClientVpnEndpointsResult).new(response, result)
    end

    # Describes the routes for the specified Client VPN endpoint.
    def describe_client_vpn_routes(input : AEC::DescribeClientVpnRoutesRequest) : Core::ParsedResponse(AEC::DescribeClientVpnRoutesResult)
      Log.info { "performing 'DescribeClientVpnRoutes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeClientVpnRoutes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeClientVpnRoutes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeClientVpnRoutesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeClientVpnRoutesResult).new(response, result)
    end

    # Describes the target networks associated with the specified Client VPN endpoint.
    def describe_client_vpn_target_networks(input : AEC::DescribeClientVpnTargetNetworksRequest) : Core::ParsedResponse(AEC::DescribeClientVpnTargetNetworksResult)
      Log.info { "performing 'DescribeClientVpnTargetNetworks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeClientVpnTargetNetworks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeClientVpnTargetNetworks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeClientVpnTargetNetworksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeClientVpnTargetNetworksResult).new(response, result)
    end

    # Describes the specified customer-owned address pools or all of your customer-owned address
    # pools.
    def describe_coip_pools(input : AEC::DescribeCoipPoolsRequest) : Core::ParsedResponse(AEC::DescribeCoipPoolsResult)
      Log.info { "performing 'DescribeCoipPools' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCoipPools")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCoipPools", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCoipPoolsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCoipPoolsResult).new(response, result)
    end

    # Describes the specified conversion tasks or all your conversion tasks. For more information, see
    # the [VM Import/Export User Guide](https://docs.aws.amazon.com/vm-import/latest/userguide/).
    #
    # For information about the import manifest referenced by this API action, see [VM Import
    # Manifest](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/manifest.html).
    def describe_conversion_tasks(input : AEC::DescribeConversionTasksRequest) : Core::ParsedResponse(AEC::DescribeConversionTasksResult)
      Log.info { "performing 'DescribeConversionTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeConversionTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeConversionTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeConversionTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeConversionTasksResult).new(response, result)
    end

    # Describes one or more of your VPN customer gateways.
    #
    # For more information, see [Amazon Web Services Site-to-Site
    # VPN](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPC_VPN.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def describe_customer_gateways(input : AEC::DescribeCustomerGatewaysRequest) : Core::ParsedResponse(AEC::DescribeCustomerGatewaysResult)
      Log.info { "performing 'DescribeCustomerGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeCustomerGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeCustomerGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeCustomerGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeCustomerGatewaysResult).new(response, result)
    end

    # Describes the metadata of an account status report, including the status of the report.
    #
    # To view the full report, download it from the Amazon S3 bucket where it was saved. Reports are
    # accessible only when they have the `complete` status. Reports with other statuses (`running`,
    # `cancelled`, or `error`) are not available in the S3 bucket. For more information about
    # downloading objects from an S3 bucket, see [Downloading
    # objects](https://docs.aws.amazon.com/AmazonS3/latest/userguide/download-objects.html) in the
    # *Amazon Simple Storage Service User Guide*.
    #
    # For more information, see [Generating the account status report for declarative
    # policies](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_declarative_status-report.html)
    # in the *Amazon Web Services Organizations User Guide*.
    def describe_declarative_policies_reports(input : AEC::DescribeDeclarativePoliciesReportsRequest) : Core::ParsedResponse(AEC::DescribeDeclarativePoliciesReportsResult)
      Log.info { "performing 'DescribeDeclarativePoliciesReports' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeDeclarativePoliciesReports")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeDeclarativePoliciesReports", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeDeclarativePoliciesReportsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeDeclarativePoliciesReportsResult).new(response, result)
    end

    # Describes your DHCP option sets. The default is to describe all your DHCP option sets.
    # Alternatively, you can specify specific DHCP option set IDs or filter the results to include
    # only the DHCP option sets that match specific criteria.
    #
    # For more information, see [DHCP option
    # sets](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_DHCP_Options.html) in the *Amazon VPC
    # User Guide*.
    def describe_dhcp_options(input : AEC::DescribeDhcpOptionsRequest) : Core::ParsedResponse(AEC::DescribeDhcpOptionsResult)
      Log.info { "performing 'DescribeDhcpOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeDhcpOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeDhcpOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeDhcpOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeDhcpOptionsResult).new(response, result)
    end

    # Describes your egress-only internet gateways. The default is to describe all your egress-only
    # internet gateways. Alternatively, you can specify specific egress-only internet gateway IDs or
    # filter the results to include only the egress-only internet gateways that match specific
    # criteria.
    def describe_egress_only_internet_gateways(input : AEC::DescribeEgressOnlyInternetGatewaysRequest) : Core::ParsedResponse(AEC::DescribeEgressOnlyInternetGatewaysResult)
      Log.info { "performing 'DescribeEgressOnlyInternetGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeEgressOnlyInternetGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeEgressOnlyInternetGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeEgressOnlyInternetGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeEgressOnlyInternetGatewaysResult).new(response, result)
    end

    # Amazon Elastic Graphics reached end of life on January 8, 2024.
    #
    # Describes the Elastic Graphics accelerator associated with your instances.
    def describe_elastic_gpus(input : AEC::DescribeElasticGpusRequest) : Core::ParsedResponse(AEC::DescribeElasticGpusResult)
      Log.info { "performing 'DescribeElasticGpus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeElasticGpus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeElasticGpus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeElasticGpusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeElasticGpusResult).new(response, result)
    end

    # Describes the specified export image tasks or all of your export image tasks.
    def describe_export_image_tasks(input : AEC::DescribeExportImageTasksRequest) : Core::ParsedResponse(AEC::DescribeExportImageTasksResult)
      Log.info { "performing 'DescribeExportImageTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeExportImageTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeExportImageTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeExportImageTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeExportImageTasksResult).new(response, result)
    end

    # Describes the specified export instance tasks or all of your export instance tasks.
    def describe_export_tasks(input : AEC::DescribeExportTasksRequest) : Core::ParsedResponse(AEC::DescribeExportTasksResult)
      Log.info { "performing 'DescribeExportTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeExportTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeExportTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeExportTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeExportTasksResult).new(response, result)
    end

    # Describe details for Windows AMIs that are configured for Windows fast launch.
    def describe_fast_launch_images(input : AEC::DescribeFastLaunchImagesRequest) : Core::ParsedResponse(AEC::DescribeFastLaunchImagesResult)
      Log.info { "performing 'DescribeFastLaunchImages' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFastLaunchImages")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFastLaunchImages", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFastLaunchImagesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFastLaunchImagesResult).new(response, result)
    end

    # Describes the state of fast snapshot restores for your snapshots.
    def describe_fast_snapshot_restores(input : AEC::DescribeFastSnapshotRestoresRequest) : Core::ParsedResponse(AEC::DescribeFastSnapshotRestoresResult)
      Log.info { "performing 'DescribeFastSnapshotRestores' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFastSnapshotRestores")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFastSnapshotRestores", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFastSnapshotRestoresResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFastSnapshotRestoresResult).new(response, result)
    end

    # Describes the events for the specified EC2 Fleet during the specified time.
    #
    # EC2 Fleet events are delayed by up to 30 seconds before they can be described. This ensures that
    # you can query by the last evaluated time and not miss a recorded event. EC2 Fleet events are
    # available for 48 hours.
    #
    # For more information, see [Monitor fleet events using Amazon
    # EventBridge](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/fleet-monitor.html) in the
    # *Amazon EC2 User Guide*.
    def describe_fleet_history(input : AEC::DescribeFleetHistoryRequest) : Core::ParsedResponse(AEC::DescribeFleetHistoryResult)
      Log.info { "performing 'DescribeFleetHistory' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFleetHistory")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFleetHistory", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFleetHistoryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFleetHistoryResult).new(response, result)
    end

    # Describes the running instances for the specified EC2 Fleet.
    #
    # Currently, `DescribeFleetInstances` does not support fleets of type `instant`. Instead, use
    # `DescribeFleets`, specifying the `instant` fleet ID in the request.
    #
    # For more information, see [Describe your EC2
    # Fleet](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/manage-ec2-fleet.html#monitor-ec2-fleet)
    # in the *Amazon EC2 User Guide*.
    def describe_fleet_instances(input : AEC::DescribeFleetInstancesRequest) : Core::ParsedResponse(AEC::DescribeFleetInstancesResult)
      Log.info { "performing 'DescribeFleetInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFleetInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFleetInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFleetInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFleetInstancesResult).new(response, result)
    end

    # Describes the specified EC2 Fleet or all of your EC2 Fleets.
    #
    # If a fleet is of type `instant`, you must specify the fleet ID in the request, otherwise the
    # fleet does not appear in the response.
    #
    # For more information, see [Describe your EC2
    # Fleet](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/manage-ec2-fleet.html#monitor-ec2-fleet)
    # in the *Amazon EC2 User Guide*.
    def describe_fleets(input : AEC::DescribeFleetsRequest) : Core::ParsedResponse(AEC::DescribeFleetsResult)
      Log.info { "performing 'DescribeFleets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFleets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFleets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFleetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFleetsResult).new(response, result)
    end

    # Describes one or more flow logs.
    #
    # To view the published flow log records, you must view the log destination. For example, the
    # CloudWatch Logs log group, the Amazon S3 bucket, or the Kinesis Data Firehose delivery stream.
    def describe_flow_logs(input : AEC::DescribeFlowLogsRequest) : Core::ParsedResponse(AEC::DescribeFlowLogsResult)
      Log.info { "performing 'DescribeFlowLogs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFlowLogs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFlowLogs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFlowLogsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFlowLogsResult).new(response, result)
    end

    # Describes the specified attribute of the specified Amazon FPGA Image (AFI).
    def describe_fpga_image_attribute(input : AEC::DescribeFpgaImageAttributeRequest) : Core::ParsedResponse(AEC::DescribeFpgaImageAttributeResult)
      Log.info { "performing 'DescribeFpgaImageAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFpgaImageAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFpgaImageAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFpgaImageAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFpgaImageAttributeResult).new(response, result)
    end

    # Describes the Amazon FPGA Images (AFIs) available to you. These include public AFIs, private
    # AFIs that you own, and AFIs owned by other Amazon Web Services accounts for which you have load
    # permissions.
    def describe_fpga_images(input : AEC::DescribeFpgaImagesRequest) : Core::ParsedResponse(AEC::DescribeFpgaImagesResult)
      Log.info { "performing 'DescribeFpgaImages' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeFpgaImages")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeFpgaImages", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeFpgaImagesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeFpgaImagesResult).new(response, result)
    end

    # Describes the Dedicated Host reservations that are available to purchase.
    #
    # The results describe all of the Dedicated Host reservation offerings, including offerings that
    # might not match the instance family and Region of your Dedicated Hosts. When purchasing an
    # offering, ensure that the instance family and Region of the offering matches that of the
    # Dedicated Hosts with which it is to be associated. For more information about supported instance
    # types, see [Dedicated
    # Hosts](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/dedicated-hosts-overview.html) in the
    # *Amazon EC2 User Guide*.
    def describe_host_reservation_offerings(input : AEC::DescribeHostReservationOfferingsRequest) : Core::ParsedResponse(AEC::DescribeHostReservationOfferingsResult)
      Log.info { "performing 'DescribeHostReservationOfferings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeHostReservationOfferings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeHostReservationOfferings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeHostReservationOfferingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeHostReservationOfferingsResult).new(response, result)
    end

    # Describes reservations that are associated with Dedicated Hosts in your account.
    def describe_host_reservations(input : AEC::DescribeHostReservationsRequest) : Core::ParsedResponse(AEC::DescribeHostReservationsResult)
      Log.info { "performing 'DescribeHostReservations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeHostReservations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeHostReservations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeHostReservationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeHostReservationsResult).new(response, result)
    end

    # Describes the specified Dedicated Hosts or all your Dedicated Hosts.
    #
    # The results describe only the Dedicated Hosts in the Region you're currently using. All listed
    # instances consume capacity on your Dedicated Host. Dedicated Hosts that have recently been
    # released are listed with the state `released`.
    def describe_hosts(input : AEC::DescribeHostsRequest) : Core::ParsedResponse(AEC::DescribeHostsResult)
      Log.info { "performing 'DescribeHosts' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeHosts")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeHosts", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeHostsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeHostsResult).new(response, result)
    end

    # Describes your IAM instance profile associations.
    def describe_iam_instance_profile_associations(input : AEC::DescribeIamInstanceProfileAssociationsRequest) : Core::ParsedResponse(AEC::DescribeIamInstanceProfileAssociationsResult)
      Log.info { "performing 'DescribeIamInstanceProfileAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIamInstanceProfileAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIamInstanceProfileAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIamInstanceProfileAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIamInstanceProfileAssociationsResult).new(response, result)
    end

    # Describes the ID format settings for resources for the specified IAM user, IAM role, or root
    # user. For example, you can view the resource types that are enabled for longer IDs. This request
    # only returns information about resource types whose ID formats can be modified; it does not
    # return information about other resource types. For more information, see [Resource
    # IDs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/resource-ids.html) in the *Amazon
    # Elastic Compute Cloud User Guide*.
    #
    # The following resource types support longer IDs: `bundle` | `conversion-task` |
    # `customer-gateway` | `dhcp-options` | `elastic-ip-allocation` | `elastic-ip-association` |
    # `export-task` | `flow-log` | `image` | `import-task` | `instance` | `internet-gateway` |
    # `network-acl` | `network-acl-association` | `network-interface` | `network-interface-attachment`
    # | `prefix-list` | `reservation` | `route-table` | `route-table-association` | `security-group` |
    # `snapshot` | `subnet` | `subnet-cidr-block-association` | `volume` | `vpc` |
    # `vpc-cidr-block-association` | `vpc-endpoint` | `vpc-peering-connection` | `vpn-connection` |
    # `vpn-gateway`.
    #
    # These settings apply to the principal specified in the request. They do not apply to the
    # principal that makes the request.
    def describe_identity_id_format(input : AEC::DescribeIdentityIdFormatRequest) : Core::ParsedResponse(AEC::DescribeIdentityIdFormatResult)
      Log.info { "performing 'DescribeIdentityIdFormat' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIdentityIdFormat")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIdentityIdFormat", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIdentityIdFormatResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIdentityIdFormatResult).new(response, result)
    end

    # Describes the ID format settings for your resources on a per-Region basis, for example, to view
    # which resource types are enabled for longer IDs. This request only returns information about
    # resource types whose ID formats can be modified; it does not return information about other
    # resource types.
    #
    # The following resource types support longer IDs: `bundle` | `conversion-task` |
    # `customer-gateway` | `dhcp-options` | `elastic-ip-allocation` | `elastic-ip-association` |
    # `export-task` | `flow-log` | `image` | `import-task` | `instance` | `internet-gateway` |
    # `network-acl` | `network-acl-association` | `network-interface` | `network-interface-attachment`
    # | `prefix-list` | `reservation` | `route-table` | `route-table-association` | `security-group` |
    # `snapshot` | `subnet` | `subnet-cidr-block-association` | `volume` | `vpc` |
    # `vpc-cidr-block-association` | `vpc-endpoint` | `vpc-peering-connection` | `vpn-connection` |
    # `vpn-gateway`.
    #
    # These settings apply to the IAM user who makes the request; they do not apply to the entire
    # Amazon Web Services account. By default, an IAM user defaults to the same settings as the root
    # user, unless they explicitly override the settings by running the ModifyIdFormat command.
    # Resources created with longer IDs are visible to all IAM users, regardless of these settings and
    # provided that they have permission to use the relevant `Describe` command for the resource type.
    def describe_id_format(input : AEC::DescribeIdFormatRequest) : Core::ParsedResponse(AEC::DescribeIdFormatResult)
      Log.info { "performing 'DescribeIdFormat' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIdFormat")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIdFormat", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIdFormatResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIdFormatResult).new(response, result)
    end

    # Describes the specified attribute of the specified AMI. You can specify only one attribute at a
    # time.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_image_attribute(input : AEC::DescribeImageAttributeRequest) : Core::ParsedResponse(AEC::ImageAttribute)
      Log.info { "performing 'DescribeImageAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeImageAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeImageAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ImageAttribute.from_xml(result_node)
      Core::ParsedResponse(AEC::ImageAttribute).new(response, result)
    end

    # Describes your Amazon Web Services resources that are referencing the specified images.
    #
    # For more information, see [Identify your resources referencing specified
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-ami-references.html) in the
    # *Amazon EC2 User Guide*.
    def describe_image_references(input : AEC::DescribeImageReferencesRequest) : Core::ParsedResponse(AEC::DescribeImageReferencesResult)
      Log.info { "performing 'DescribeImageReferences' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeImageReferences")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeImageReferences", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeImageReferencesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeImageReferencesResult).new(response, result)
    end

    # Describes the specified images (AMIs, AKIs, and ARIs) available to you or all of the images
    # available to you.
    #
    # The images available to you include public images, private images that you own, and private
    # images owned by other Amazon Web Services accounts for which you have explicit launch
    # permissions.
    #
    # Recently deregistered images appear in the returned results for a short interval and then return
    # empty results. After all instances that reference a deregistered AMI are terminated, specifying
    # the ID of the image will eventually return an error indicating that the AMI ID cannot be found.
    #
    # When Allowed AMIs is set to `enabled`, only allowed images are returned in the results, with the
    # `imageAllowed` field set to `true` for each image. In `audit-mode`, the `imageAllowed` field is
    # set to `true` for images that meet the account's Allowed AMIs criteria, and `false` for images
    # that don't meet the criteria. For more information, see [Allowed
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-allowed-amis.html).
    #
    # The Amazon EC2 API follows an eventual consistency model. This means that the result of an API
    # command you run that creates or modifies resources might not be immediately available to all
    # subsequent commands you run. For guidance on how to manage eventual consistency, see [Eventual
    # consistency in the Amazon EC2
    # API](https://docs.aws.amazon.com/ec2/latest/devguide/eventual-consistency.html) in the *Amazon
    # EC2 Developer Guide*.
    #
    # We strongly recommend using only paginated requests. Unpaginated requests are susceptible to
    # throttling and timeouts.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_images(input : AEC::DescribeImagesRequest) : Core::ParsedResponse(AEC::DescribeImagesResult)
      Log.info { "performing 'DescribeImages' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeImages")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeImages", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeImagesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeImagesResult).new(response, result)
    end

    # Describes the entries in image usage reports, showing how your images are used across other
    # Amazon Web Services accounts.
    #
    # For more information, see [View your AMI
    # usage](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/your-ec2-ami-usage.html) in the
    # *Amazon EC2 User Guide*.
    def describe_image_usage_report_entries(input : AEC::DescribeImageUsageReportEntriesRequest) : Core::ParsedResponse(AEC::DescribeImageUsageReportEntriesResult)
      Log.info { "performing 'DescribeImageUsageReportEntries' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeImageUsageReportEntries")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeImageUsageReportEntries", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeImageUsageReportEntriesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeImageUsageReportEntriesResult).new(response, result)
    end

    # Describes the configuration and status of image usage reports, filtered by report IDs or image
    # IDs.
    #
    # For more information, see [View your AMI
    # usage](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/your-ec2-ami-usage.html) in the
    # *Amazon EC2 User Guide*.
    def describe_image_usage_reports(input : AEC::DescribeImageUsageReportsRequest) : Core::ParsedResponse(AEC::DescribeImageUsageReportsResult)
      Log.info { "performing 'DescribeImageUsageReports' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeImageUsageReports")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeImageUsageReports", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeImageUsageReportsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeImageUsageReportsResult).new(response, result)
    end

    # Displays details about an import virtual machine or import snapshot tasks that are already
    # created.
    def describe_import_image_tasks(input : AEC::DescribeImportImageTasksRequest) : Core::ParsedResponse(AEC::DescribeImportImageTasksResult)
      Log.info { "performing 'DescribeImportImageTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeImportImageTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeImportImageTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeImportImageTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeImportImageTasksResult).new(response, result)
    end

    # Describes your import snapshot tasks.
    def describe_import_snapshot_tasks(input : AEC::DescribeImportSnapshotTasksRequest) : Core::ParsedResponse(AEC::DescribeImportSnapshotTasksResult)
      Log.info { "performing 'DescribeImportSnapshotTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeImportSnapshotTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeImportSnapshotTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeImportSnapshotTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeImportSnapshotTasksResult).new(response, result)
    end

    # Describes the specified attribute of the specified instance. You can specify only one attribute
    # at a time. Available attributes include SQL license exemption configuration for instances
    # registered with the SQL LE service.
    def describe_instance_attribute(input : AEC::DescribeInstanceAttributeRequest) : Core::ParsedResponse(AEC::InstanceAttribute)
      Log.info { "performing 'DescribeInstanceAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::InstanceAttribute.from_xml(result_node)
      Core::ParsedResponse(AEC::InstanceAttribute).new(response, result)
    end

    # Describes the specified EC2 Instance Connect Endpoints or all EC2 Instance Connect Endpoints.
    def describe_instance_connect_endpoints(input : AEC::DescribeInstanceConnectEndpointsRequest) : Core::ParsedResponse(AEC::DescribeInstanceConnectEndpointsResult)
      Log.info { "performing 'DescribeInstanceConnectEndpoints' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceConnectEndpoints")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceConnectEndpoints", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceConnectEndpointsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceConnectEndpointsResult).new(response, result)
    end

    # Describes the credit option for CPU usage of the specified burstable performance instances. The
    # credit options are `standard` and `unlimited`.
    #
    # If you do not specify an instance ID, Amazon EC2 returns burstable performance instances with
    # the `unlimited` credit option, as well as instances that were previously configured as T2, T3,
    # and T3a with the `unlimited` credit option. For example, if you resize a T2 instance, while it
    # is configured as `unlimited`, to an M4 instance, Amazon EC2 returns the M4 instance.
    #
    # If you specify one or more instance IDs, Amazon EC2 returns the credit option (`standard` or
    # `unlimited`) of those instances. If you specify an instance ID that is not valid, such as an
    # instance that is not a burstable performance instance, an error is returned.
    #
    # Recently terminated instances might appear in the returned results. This interval is usually
    # less than one hour.
    #
    # If an Availability Zone is experiencing a service disruption and you specify instance IDs in the
    # affected zone, or do not specify any instance IDs at all, the call fails. If you specify only
    # instance IDs in an unaffected zone, the call works normally.
    #
    # For more information, see [Burstable performance
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/burstable-performance-instances.html)
    # in the *Amazon EC2 User Guide*.
    def describe_instance_credit_specifications(input : AEC::DescribeInstanceCreditSpecificationsRequest) : Core::ParsedResponse(AEC::DescribeInstanceCreditSpecificationsResult)
      Log.info { "performing 'DescribeInstanceCreditSpecifications' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceCreditSpecifications")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceCreditSpecifications", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceCreditSpecificationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceCreditSpecificationsResult).new(response, result)
    end

    # Describes the tag keys that are registered to appear in scheduled event notifications for
    # resources in the current Region.
    def describe_instance_event_notification_attributes(input : AEC::DescribeInstanceEventNotificationAttributesRequest) : Core::ParsedResponse(AEC::DescribeInstanceEventNotificationAttributesResult)
      Log.info { "performing 'DescribeInstanceEventNotificationAttributes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceEventNotificationAttributes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceEventNotificationAttributes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceEventNotificationAttributesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceEventNotificationAttributesResult).new(response, result)
    end

    # Describes the specified event windows or all event windows.
    #
    # If you specify event window IDs, the output includes information for only the specified event
    # windows. If you specify filters, the output includes information for only those event windows
    # that meet the filter criteria. If you do not specify event windows IDs or filters, the output
    # includes information for all event windows, which can affect performance. We recommend that you
    # use pagination to ensure that the operation returns quickly and successfully.
    #
    # For more information, see [Define event windows for scheduled
    # events](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/event-windows.html) in the *Amazon
    # EC2 User Guide*.
    def describe_instance_event_windows(input : AEC::DescribeInstanceEventWindowsRequest) : Core::ParsedResponse(AEC::DescribeInstanceEventWindowsResult)
      Log.info { "performing 'DescribeInstanceEventWindows' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceEventWindows")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceEventWindows", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceEventWindowsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceEventWindowsResult).new(response, result)
    end

    # Describes the AMI that was used to launch an instance, even if the AMI is deprecated,
    # deregistered, made private (no longer public or shared with your account), or not allowed.
    #
    # If you specify instance IDs, the output includes information for only the specified instances.
    # If you specify filters, the output includes information for only those instances that meet the
    # filter criteria. If you do not specify instance IDs or filters, the output includes information
    # for all instances, which can affect performance.
    #
    # If you specify an instance ID that is not valid, an instance that doesn't exist, or an instance
    # that you do not own, an error (`InvalidInstanceID.NotFound`) is returned.
    #
    # Recently terminated instances might appear in the returned results. This interval is usually
    # less than one hour.
    #
    # In the rare case where an Availability Zone is experiencing a service disruption and you specify
    # instance IDs that are in the affected Availability Zone, or do not specify any instance IDs at
    # all, the call fails. If you specify only instance IDs that are in an unaffected Availability
    # Zone, the call works normally.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_instance_image_metadata(input : AEC::DescribeInstanceImageMetadataRequest) : Core::ParsedResponse(AEC::DescribeInstanceImageMetadataResult)
      Log.info { "performing 'DescribeInstanceImageMetadata' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceImageMetadata")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceImageMetadata", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceImageMetadataResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceImageMetadataResult).new(response, result)
    end

    # Describes the specified instances or all instances.
    #
    # If you specify instance IDs, the output includes information for only the specified instances.
    # If you specify filters, the output includes information for only those instances that meet the
    # filter criteria. If you do not specify instance IDs or filters, the output includes information
    # for all instances, which can affect performance. We recommend that you use pagination to ensure
    # that the operation returns quickly and successfully.
    #
    # The response includes SQL license exemption status information for instances registered with the
    # SQL LE service, providing visibility into license exemption configuration and status.
    #
    # If you specify an instance ID that is not valid, an error is returned. If you specify an
    # instance that you do not own, it is not included in the output.
    #
    # Recently terminated instances might appear in the returned results. This interval is usually
    # less than one hour.
    #
    # If you describe instances in the rare case where an Availability Zone is experiencing a service
    # disruption and you specify instance IDs that are in the affected zone, or do not specify any
    # instance IDs at all, the call fails. If you describe instances and specify only instance IDs
    # that are in an unaffected zone, the call works normally.
    #
    # The Amazon EC2 API follows an eventual consistency model. This means that the result of an API
    # command you run that creates or modifies resources might not be immediately available to all
    # subsequent commands you run. For guidance on how to manage eventual consistency, see [Eventual
    # consistency in the Amazon EC2
    # API](https://docs.aws.amazon.com/ec2/latest/devguide/eventual-consistency.html) in the *Amazon
    # EC2 Developer Guide*.
    #
    # We strongly recommend using only paginated requests. Unpaginated requests are susceptible to
    # throttling and timeouts.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_instances(input : AEC::DescribeInstancesRequest) : Core::ParsedResponse(AEC::DescribeInstancesResult)
      Log.info { "performing 'DescribeInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstancesResult).new(response, result)
    end

    # Describes the historical SQL Server High Availability states for Amazon EC2 instances that are
    # enabled for Amazon EC2 High Availability for SQL Server monitoring.
    def describe_instance_sql_ha_history_states(input : AEC::DescribeInstanceSqlHaHistoryStatesRequest) : Core::ParsedResponse(AEC::DescribeInstanceSqlHaHistoryStatesResult)
      Log.info { "performing 'DescribeInstanceSqlHaHistoryStates' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceSqlHaHistoryStates")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceSqlHaHistoryStates", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceSqlHaHistoryStatesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceSqlHaHistoryStatesResult).new(response, result)
    end

    # Describes the SQL Server High Availability states for Amazon EC2 instances that are enabled for
    # Amazon EC2 High Availability for SQL Server monitoring.
    def describe_instance_sql_ha_states(input : AEC::DescribeInstanceSqlHaStatesRequest) : Core::ParsedResponse(AEC::DescribeInstanceSqlHaStatesResult)
      Log.info { "performing 'DescribeInstanceSqlHaStates' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceSqlHaStates")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceSqlHaStates", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceSqlHaStatesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceSqlHaStatesResult).new(response, result)
    end

    # Describes the status of the specified instances or all of your instances. By default, only
    # running instances are described, unless you specifically indicate to return the status of all
    # instances.
    #
    # Instance status includes the following components:
    #
    # - **Status checks** - Amazon EC2 performs status checks on running EC2 instances to identify
    # hardware and software issues. For more information, see [Status checks for your
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/monitoring-system-instance-status-check.html)
    # and [Troubleshoot instances with failed status
    # checks](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/TroubleshootingInstances.html) in
    # the *Amazon EC2 User Guide*.
    #
    # - **Application status checks** - Amazon EC2 reports application-level health status for
    # instances, indicating whether applications running on the instance are functioning properly.
    #
    # - **Scheduled events** - Amazon EC2 can schedule events (such as reboot, stop, or terminate) for
    # your instances related to hardware issues, software updates, or system maintenance. For more
    # information, see [Scheduled events for your
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/monitoring-instances-status-check_sched.html)
    # in the *Amazon EC2 User Guide*.
    #
    # - **Instance state** - You can manage your instances from the moment you launch them through
    # their termination. For more information, see [Instance
    # lifecycle](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-lifecycle.html) in
    # the *Amazon EC2 User Guide*.
    #
    # The Amazon EC2 API follows an eventual consistency model. This means that the result of an API
    # command you run that creates or modifies resources might not be immediately available to all
    # subsequent commands you run. For guidance on how to manage eventual consistency, see [Eventual
    # consistency in the Amazon EC2
    # API](https://docs.aws.amazon.com/ec2/latest/devguide/eventual-consistency.html) in the *Amazon
    # EC2 Developer Guide*.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_instance_status(input : AEC::DescribeInstanceStatusRequest) : Core::ParsedResponse(AEC::DescribeInstanceStatusResult)
      Log.info { "performing 'DescribeInstanceStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceStatusResult).new(response, result)
    end

    # Describes a tree-based hierarchy that represents the physical host placement of your EC2
    # instances within an Availability Zone or Local Zone. You can use this information to determine
    # the relative proximity of your EC2 instances within the Amazon Web Services network to support
    # your tightly coupled workloads.
    #
    # Instance topology is supported for specific instance types only. For more information, see
    # [Prerequisites for Amazon EC2 instance
    # topology](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-topology-prerequisites.html)
    # in the *Amazon EC2 User Guide*.
    #
    # The Amazon EC2 API follows an eventual consistency model due to the distributed nature of the
    # system supporting it. As a result, when you call the DescribeInstanceTopology API command
    # immediately after launching instances, the response might return a `null` value for
    # `capacityBlockId` because the data might not have fully propagated across all subsystems. For
    # more information, see [Eventual consistency in the Amazon EC2
    # API](https://docs.aws.amazon.com/ec2/latest/devguide/eventual-consistency.html) in the *Amazon
    # EC2 Developer Guide*.
    #
    # For more information, see [Amazon EC2
    # topology](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-topology.html) in the
    # *Amazon EC2 User Guide*.
    def describe_instance_topology(input : AEC::DescribeInstanceTopologyRequest) : Core::ParsedResponse(AEC::DescribeInstanceTopologyResult)
      Log.info { "performing 'DescribeInstanceTopology' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceTopology")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceTopology", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceTopologyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceTopologyResult).new(response, result)
    end

    # Lists the instance types that are offered for the specified location. If no location is
    # specified, the default is to list the instance types that are offered in the current Region.
    def describe_instance_type_offerings(input : AEC::DescribeInstanceTypeOfferingsRequest) : Core::ParsedResponse(AEC::DescribeInstanceTypeOfferingsResult)
      Log.info { "performing 'DescribeInstanceTypeOfferings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceTypeOfferings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceTypeOfferings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceTypeOfferingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceTypeOfferingsResult).new(response, result)
    end

    # Describes the specified instance types. By default, all instance types for the current Region
    # are described. Alternatively, you can filter the results. To include instance types that are not
    # supported in the current Region, set `IncludeUnsupportedInRegion` to `true`.
    def describe_instance_types(input : AEC::DescribeInstanceTypesRequest) : Core::ParsedResponse(AEC::DescribeInstanceTypesResult)
      Log.info { "performing 'DescribeInstanceTypes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInstanceTypes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInstanceTypes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInstanceTypesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInstanceTypesResult).new(response, result)
    end

    # Describes your internet gateways. The default is to describe all your internet gateways.
    # Alternatively, you can specify specific internet gateway IDs or filter the results to include
    # only the internet gateways that match specific criteria.
    def describe_internet_gateways(input : AEC::DescribeInternetGatewaysRequest) : Core::ParsedResponse(AEC::DescribeInternetGatewaysResult)
      Log.info { "performing 'DescribeInternetGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeInternetGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeInternetGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeInternetGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeInternetGatewaysResult).new(response, result)
    end

    # Describes your Autonomous System Numbers (ASNs), their provisioning statuses, and the BYOIP
    # CIDRs with which they are associated. For more information, see [Tutorial: Bring your ASN to
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/tutorials-byoasn.html) in the *Amazon VPC IPAM
    # guide*.
    def describe_ipam_byoasn(input : AEC::DescribeIpamByoasnRequest) : Core::ParsedResponse(AEC::DescribeIpamByoasnResult)
      Log.info { "performing 'DescribeIpamByoasn' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamByoasn")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamByoasn", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamByoasnResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamByoasnResult).new(response, result)
    end

    # Describe verification tokens.
    #
    # A verification token is an Amazon Web Services-generated random value that you can use to prove
    # ownership of an external resource. For example, you can use a verification token to validate
    # that you control a public IP address range when you bring an IP address range to Amazon Web
    # Services (BYOIP).
    def describe_ipam_external_resource_verification_tokens(input : AEC::DescribeIpamExternalResourceVerificationTokensRequest) : Core::ParsedResponse(AEC::DescribeIpamExternalResourceVerificationTokensResult)
      Log.info { "performing 'DescribeIpamExternalResourceVerificationTokens' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamExternalResourceVerificationTokens")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamExternalResourceVerificationTokens", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamExternalResourceVerificationTokensResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamExternalResourceVerificationTokensResult).new(response, result)
    end

    # Describes one or more IPAM internet registry associations. We recommend using pagination to
    # ensure that the operation returns quickly and successfully.
    def describe_ipam_internet_registry_associations(input : AEC::DescribeIpamInternetRegistryAssociationsRequest) : Core::ParsedResponse(AEC::DescribeIpamInternetRegistryAssociationsResult)
      Log.info { "performing 'DescribeIpamInternetRegistryAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamInternetRegistryAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamInternetRegistryAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamInternetRegistryAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamInternetRegistryAssociationsResult).new(response, result)
    end

    # Describes one or more IPAM policies.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    def describe_ipam_policies(input : AEC::DescribeIpamPoliciesRequest) : Core::ParsedResponse(AEC::DescribeIpamPoliciesResult)
      Log.info { "performing 'DescribeIpamPolicies' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamPolicies")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamPolicies", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamPoliciesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamPoliciesResult).new(response, result)
    end

    # Describes IPAM pool allocations. You can describe all allocations owned by you across all pools,
    # or you can describe specific allocations by ID.
    #
    # If you specify `IpamPoolAllocationIds`, the results include only the specified allocations. If
    # you do not specify `IpamPoolAllocationIds`, the results include all allocations owned by you.
    # You can use `Filters` to narrow the results.
    #
    # This action returns only allocations directly owned by you. To view all allocations in a pool
    # you own or that has been shared with you, including allocations owned by other accounts, use
    # [GetIpamPoolAllocations](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_GetIpamPoolAllocations.html).
    def describe_ipam_pool_allocations(input : AEC::DescribeIpamPoolAllocationsRequest) : Core::ParsedResponse(AEC::DescribeIpamPoolAllocationsResult)
      Log.info { "performing 'DescribeIpamPoolAllocations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamPoolAllocations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamPoolAllocations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamPoolAllocationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamPoolAllocationsResult).new(response, result)
    end

    # Get information about your IPAM pools.
    def describe_ipam_pools(input : AEC::DescribeIpamPoolsRequest) : Core::ParsedResponse(AEC::DescribeIpamPoolsResult)
      Log.info { "performing 'DescribeIpamPools' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamPools")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamPools", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamPoolsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamPoolsResult).new(response, result)
    end

    # Describes one or more IPAM prefix list resolvers. Use this operation to view the configuration,
    # status, and properties of your resolvers.
    def describe_ipam_prefix_list_resolvers(input : AEC::DescribeIpamPrefixListResolversRequest) : Core::ParsedResponse(AEC::DescribeIpamPrefixListResolversResult)
      Log.info { "performing 'DescribeIpamPrefixListResolvers' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamPrefixListResolvers")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamPrefixListResolvers", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamPrefixListResolversResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamPrefixListResolversResult).new(response, result)
    end

    # Describes one or more IPAM prefix list resolver Targets. Use this operation to view the
    # configuration and status of resolver targets.
    def describe_ipam_prefix_list_resolver_targets(input : AEC::DescribeIpamPrefixListResolverTargetsRequest) : Core::ParsedResponse(AEC::DescribeIpamPrefixListResolverTargetsResult)
      Log.info { "performing 'DescribeIpamPrefixListResolverTargets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamPrefixListResolverTargets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamPrefixListResolverTargets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamPrefixListResolverTargetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamPrefixListResolverTargetsResult).new(response, result)
    end

    # Describes IPAM resource discoveries. A resource discovery is an IPAM component that enables IPAM
    # to manage and monitor resources that belong to the owning account.
    def describe_ipam_resource_discoveries(input : AEC::DescribeIpamResourceDiscoveriesRequest) : Core::ParsedResponse(AEC::DescribeIpamResourceDiscoveriesResult)
      Log.info { "performing 'DescribeIpamResourceDiscoveries' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamResourceDiscoveries")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamResourceDiscoveries", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamResourceDiscoveriesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamResourceDiscoveriesResult).new(response, result)
    end

    # Describes resource discovery association with an Amazon VPC IPAM. An associated resource
    # discovery is a resource discovery that has been associated with an IPAM..
    def describe_ipam_resource_discovery_associations(input : AEC::DescribeIpamResourceDiscoveryAssociationsRequest) : Core::ParsedResponse(AEC::DescribeIpamResourceDiscoveryAssociationsResult)
      Log.info { "performing 'DescribeIpamResourceDiscoveryAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamResourceDiscoveryAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamResourceDiscoveryAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamResourceDiscoveryAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamResourceDiscoveryAssociationsResult).new(response, result)
    end

    # Get information about your IPAM pools.
    #
    # For more information, see [What is
    # IPAM?](https://docs.aws.amazon.com/vpc/latest/ipam/what-is-it-ipam.html) in the *Amazon VPC IPAM
    # User Guide*.
    def describe_ipams(input : AEC::DescribeIpamsRequest) : Core::ParsedResponse(AEC::DescribeIpamsResult)
      Log.info { "performing 'DescribeIpams' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpams")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpams", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamsResult).new(response, result)
    end

    # Get information about your IPAM scopes.
    def describe_ipam_scopes(input : AEC::DescribeIpamScopesRequest) : Core::ParsedResponse(AEC::DescribeIpamScopesResult)
      Log.info { "performing 'DescribeIpamScopes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpamScopes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpamScopes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpamScopesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpamScopesResult).new(response, result)
    end

    # Describes your IPv6 address pools.
    def describe_ipv_6_pools(input : AEC::DescribeIpv6PoolsRequest) : Core::ParsedResponse(AEC::DescribeIpv6PoolsResult)
      Log.info { "performing 'DescribeIpv6Pools' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeIpv6Pools")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeIpv6Pools", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeIpv6PoolsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeIpv6PoolsResult).new(response, result)
    end

    # Describes the specified key pairs or all of your key pairs.
    #
    # For more information about key pairs, see [Amazon EC2 key
    # pairs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-key-pairs.html) in the *Amazon
    # EC2 User Guide*.
    def describe_key_pairs(input : AEC::DescribeKeyPairsRequest) : Core::ParsedResponse(AEC::DescribeKeyPairsResult)
      Log.info { "performing 'DescribeKeyPairs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeKeyPairs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeKeyPairs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeKeyPairsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeKeyPairsResult).new(response, result)
    end

    # Describes one or more launch templates.
    def describe_launch_templates(input : AEC::DescribeLaunchTemplatesRequest) : Core::ParsedResponse(AEC::DescribeLaunchTemplatesResult)
      Log.info { "performing 'DescribeLaunchTemplates' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLaunchTemplates")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLaunchTemplates", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLaunchTemplatesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLaunchTemplatesResult).new(response, result)
    end

    # Describes one or more versions of a specified launch template. You can describe all versions,
    # individual versions, or a range of versions. You can also describe all the latest versions or
    # all the default versions of all the launch templates in your account.
    def describe_launch_template_versions(input : AEC::DescribeLaunchTemplateVersionsRequest) : Core::ParsedResponse(AEC::DescribeLaunchTemplateVersionsResult)
      Log.info { "performing 'DescribeLaunchTemplateVersions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLaunchTemplateVersions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLaunchTemplateVersions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLaunchTemplateVersionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLaunchTemplateVersionsResult).new(response, result)
    end

    # Describes one or more local gateway route tables. By default, all local gateway route tables are
    # described. Alternatively, you can filter the results.
    def describe_local_gateway_route_tables(input : AEC::DescribeLocalGatewayRouteTablesRequest) : Core::ParsedResponse(AEC::DescribeLocalGatewayRouteTablesResult)
      Log.info { "performing 'DescribeLocalGatewayRouteTables' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLocalGatewayRouteTables")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLocalGatewayRouteTables", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLocalGatewayRouteTablesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLocalGatewayRouteTablesResult).new(response, result)
    end

    # Describes the associations between virtual interface groups and local gateway route tables.
    def describe_local_gateway_route_table_virtual_interface_group_associations(input : AEC::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsRequest) : Core::ParsedResponse(AEC::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult)
      Log.info { "performing 'DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult).new(response, result)
    end

    # Describes the specified associations between VPCs and local gateway route tables.
    def describe_local_gateway_route_table_vpc_associations(input : AEC::DescribeLocalGatewayRouteTableVpcAssociationsRequest) : Core::ParsedResponse(AEC::DescribeLocalGatewayRouteTableVpcAssociationsResult)
      Log.info { "performing 'DescribeLocalGatewayRouteTableVpcAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLocalGatewayRouteTableVpcAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLocalGatewayRouteTableVpcAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLocalGatewayRouteTableVpcAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLocalGatewayRouteTableVpcAssociationsResult).new(response, result)
    end

    # Describes one or more local gateways. By default, all local gateways are described.
    # Alternatively, you can filter the results.
    def describe_local_gateways(input : AEC::DescribeLocalGatewaysRequest) : Core::ParsedResponse(AEC::DescribeLocalGatewaysResult)
      Log.info { "performing 'DescribeLocalGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLocalGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLocalGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLocalGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLocalGatewaysResult).new(response, result)
    end

    # Describes the specified local gateway virtual interface groups.
    def describe_local_gateway_virtual_interface_groups(input : AEC::DescribeLocalGatewayVirtualInterfaceGroupsRequest) : Core::ParsedResponse(AEC::DescribeLocalGatewayVirtualInterfaceGroupsResult)
      Log.info { "performing 'DescribeLocalGatewayVirtualInterfaceGroups' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLocalGatewayVirtualInterfaceGroups")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLocalGatewayVirtualInterfaceGroups", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLocalGatewayVirtualInterfaceGroupsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLocalGatewayVirtualInterfaceGroupsResult).new(response, result)
    end

    # Describes the specified local gateway virtual interfaces.
    def describe_local_gateway_virtual_interfaces(input : AEC::DescribeLocalGatewayVirtualInterfacesRequest) : Core::ParsedResponse(AEC::DescribeLocalGatewayVirtualInterfacesResult)
      Log.info { "performing 'DescribeLocalGatewayVirtualInterfaces' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLocalGatewayVirtualInterfaces")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLocalGatewayVirtualInterfaces", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLocalGatewayVirtualInterfacesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLocalGatewayVirtualInterfacesResult).new(response, result)
    end

    # Describes the lock status for a snapshot.
    def describe_locked_snapshots(input : AEC::DescribeLockedSnapshotsRequest) : Core::ParsedResponse(AEC::DescribeLockedSnapshotsResult)
      Log.info { "performing 'DescribeLockedSnapshots' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeLockedSnapshots")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeLockedSnapshots", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeLockedSnapshotsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeLockedSnapshotsResult).new(response, result)
    end

    # Describes the specified EC2 Mac Dedicated Host or all of your EC2 Mac Dedicated Hosts.
    def describe_mac_hosts(input : AEC::DescribeMacHostsRequest) : Core::ParsedResponse(AEC::DescribeMacHostsResult)
      Log.info { "performing 'DescribeMacHosts' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeMacHosts")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeMacHosts", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeMacHostsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeMacHostsResult).new(response, result)
    end

    # Describes a System Integrity Protection (SIP) modification task or volume ownership delegation
    # task for an Amazon EC2 Mac instance. For more information, see [Configure SIP for Amazon EC2
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/mac-sip-settings.html#mac-sip-configure)
    # in the *Amazon EC2 User Guide*.
    def describe_mac_modification_tasks(input : AEC::DescribeMacModificationTasksRequest) : Core::ParsedResponse(AEC::DescribeMacModificationTasksResult)
      Log.info { "performing 'DescribeMacModificationTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeMacModificationTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeMacModificationTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeMacModificationTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeMacModificationTasksResult).new(response, result)
    end

    # Describes your managed prefix lists and any Amazon Web Services-managed prefix lists.
    def describe_managed_prefix_lists(input : AEC::DescribeManagedPrefixListsRequest) : Core::ParsedResponse(AEC::DescribeManagedPrefixListsResult)
      Log.info { "performing 'DescribeManagedPrefixLists' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeManagedPrefixLists")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeManagedPrefixLists", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeManagedPrefixListsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeManagedPrefixListsResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Describes your Elastic IP addresses that are being moved from or being restored to the
    # EC2-Classic platform. This request does not return information about any other Elastic IP
    # addresses in your account.
    def describe_moving_addresses(input : AEC::DescribeMovingAddressesRequest) : Core::ParsedResponse(AEC::DescribeMovingAddressesResult)
      Log.info { "performing 'DescribeMovingAddresses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeMovingAddresses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeMovingAddresses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeMovingAddressesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeMovingAddressesResult).new(response, result)
    end

    # Describes your NAT gateways. The default is to describe all your NAT gateways. Alternatively,
    # you can specify specific NAT gateway IDs or filter the results to include only the NAT gateways
    # that match specific criteria.
    def describe_nat_gateways(input : AEC::DescribeNatGatewaysRequest) : Core::ParsedResponse(AEC::DescribeNatGatewaysResult)
      Log.info { "performing 'DescribeNatGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNatGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNatGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNatGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNatGatewaysResult).new(response, result)
    end

    # Describes your network ACLs. The default is to describe all your network ACLs. Alternatively,
    # you can specify specific network ACL IDs or filter the results to include only the network ACLs
    # that match specific criteria.
    #
    # For more information, see [Network
    # ACLs](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-network-acls.html) in the *Amazon VPC
    # User Guide*.
    def describe_network_acls(input : AEC::DescribeNetworkAclsRequest) : Core::ParsedResponse(AEC::DescribeNetworkAclsResult)
      Log.info { "performing 'DescribeNetworkAcls' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkAcls")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkAcls", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkAclsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkAclsResult).new(response, result)
    end

    # Describes the specified Network Access Scope analyses.
    def describe_network_insights_access_scope_analyses(input : AEC::DescribeNetworkInsightsAccessScopeAnalysesRequest) : Core::ParsedResponse(AEC::DescribeNetworkInsightsAccessScopeAnalysesResult)
      Log.info { "performing 'DescribeNetworkInsightsAccessScopeAnalyses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkInsightsAccessScopeAnalyses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkInsightsAccessScopeAnalyses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkInsightsAccessScopeAnalysesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkInsightsAccessScopeAnalysesResult).new(response, result)
    end

    # Describes the specified Network Access Scopes.
    def describe_network_insights_access_scopes(input : AEC::DescribeNetworkInsightsAccessScopesRequest) : Core::ParsedResponse(AEC::DescribeNetworkInsightsAccessScopesResult)
      Log.info { "performing 'DescribeNetworkInsightsAccessScopes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkInsightsAccessScopes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkInsightsAccessScopes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkInsightsAccessScopesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkInsightsAccessScopesResult).new(response, result)
    end

    # Describes one or more of your network insights analyses.
    def describe_network_insights_analyses(input : AEC::DescribeNetworkInsightsAnalysesRequest) : Core::ParsedResponse(AEC::DescribeNetworkInsightsAnalysesResult)
      Log.info { "performing 'DescribeNetworkInsightsAnalyses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkInsightsAnalyses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkInsightsAnalyses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkInsightsAnalysesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkInsightsAnalysesResult).new(response, result)
    end

    # Describes one or more of your paths.
    def describe_network_insights_paths(input : AEC::DescribeNetworkInsightsPathsRequest) : Core::ParsedResponse(AEC::DescribeNetworkInsightsPathsResult)
      Log.info { "performing 'DescribeNetworkInsightsPaths' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkInsightsPaths")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkInsightsPaths", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkInsightsPathsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkInsightsPathsResult).new(response, result)
    end

    # Describes a network interface attribute. You can specify only one attribute at a time.
    def describe_network_interface_attribute(input : AEC::DescribeNetworkInterfaceAttributeRequest) : Core::ParsedResponse(AEC::DescribeNetworkInterfaceAttributeResult)
      Log.info { "performing 'DescribeNetworkInterfaceAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkInterfaceAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkInterfaceAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkInterfaceAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkInterfaceAttributeResult).new(response, result)
    end

    # Describes the permissions for your network interfaces.
    def describe_network_interface_permissions(input : AEC::DescribeNetworkInterfacePermissionsRequest) : Core::ParsedResponse(AEC::DescribeNetworkInterfacePermissionsResult)
      Log.info { "performing 'DescribeNetworkInterfacePermissions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkInterfacePermissions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkInterfacePermissions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkInterfacePermissionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkInterfacePermissionsResult).new(response, result)
    end

    # Describes the specified network interfaces or all your network interfaces.
    #
    # If you have a large number of network interfaces, the operation fails unless you use pagination
    # or one of the following filters: `group-id`, `mac-address`, `private-dns-name`,
    # `private-ip-address`, `subnet-id`, or `vpc-id`.
    #
    # We strongly recommend using only paginated requests. Unpaginated requests are susceptible to
    # throttling and timeouts.
    def describe_network_interfaces(input : AEC::DescribeNetworkInterfacesRequest) : Core::ParsedResponse(AEC::DescribeNetworkInterfacesResult)
      Log.info { "performing 'DescribeNetworkInterfaces' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeNetworkInterfaces")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeNetworkInterfaces", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeNetworkInterfacesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeNetworkInterfacesResult).new(response, result)
    end

    # Describes the Outposts link aggregation groups (LAGs).
    #
    # LAGs are only available for second-generation Outposts racks at this time.
    def describe_outpost_lags(input : AEC::DescribeOutpostLagsRequest) : Core::ParsedResponse(AEC::DescribeOutpostLagsResult)
      Log.info { "performing 'DescribeOutpostLags' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeOutpostLags")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeOutpostLags", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeOutpostLagsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeOutpostLagsResult).new(response, result)
    end

    # Describes the specified placement groups or all of your placement groups.
    #
    # To describe a specific placement group that is *shared* with your account, you must specify the
    # ID of the placement group using the `GroupId` parameter. Specifying the name of a *shared*
    # placement group using the `GroupNames` parameter will result in an error.
    #
    # For more information, see [Placement
    # groups](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/placement-groups.html) in the
    # *Amazon EC2 User Guide*.
    def describe_placement_groups(input : AEC::DescribePlacementGroupsRequest) : Core::ParsedResponse(AEC::DescribePlacementGroupsResult)
      Log.info { "performing 'DescribePlacementGroups' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribePlacementGroups")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribePlacementGroups", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribePlacementGroupsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribePlacementGroupsResult).new(response, result)
    end

    # Describes available Amazon Web Services services in a prefix list format, which includes the
    # prefix list name and prefix list ID of the service and the IP address range for the service.
    def describe_prefix_lists(input : AEC::DescribePrefixListsRequest) : Core::ParsedResponse(AEC::DescribePrefixListsResult)
      Log.info { "performing 'DescribePrefixLists' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribePrefixLists")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribePrefixLists", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribePrefixListsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribePrefixListsResult).new(response, result)
    end

    # Describes the ID format settings for the root user and all IAM roles and IAM users that have
    # explicitly specified a longer ID (17-character ID) preference.
    #
    # By default, all IAM roles and IAM users default to the same ID settings as the root user, unless
    # they explicitly override the settings. This request is useful for identifying those IAM users
    # and IAM roles that have overridden the default ID settings.
    #
    # The following resource types support longer IDs: `bundle` | `conversion-task` |
    # `customer-gateway` | `dhcp-options` | `elastic-ip-allocation` | `elastic-ip-association` |
    # `export-task` | `flow-log` | `image` | `import-task` | `instance` | `internet-gateway` |
    # `network-acl` | `network-acl-association` | `network-interface` | `network-interface-attachment`
    # | `prefix-list` | `reservation` | `route-table` | `route-table-association` | `security-group` |
    # `snapshot` | `subnet` | `subnet-cidr-block-association` | `volume` | `vpc` |
    # `vpc-cidr-block-association` | `vpc-endpoint` | `vpc-peering-connection` | `vpn-connection` |
    # `vpn-gateway`.
    def describe_principal_id_format(input : AEC::DescribePrincipalIdFormatRequest) : Core::ParsedResponse(AEC::DescribePrincipalIdFormatResult)
      Log.info { "performing 'DescribePrincipalIdFormat' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribePrincipalIdFormat")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribePrincipalIdFormat", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribePrincipalIdFormatResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribePrincipalIdFormatResult).new(response, result)
    end

    # Describes the specified IPv4 address pools.
    def describe_public_ipv_4_pools(input : AEC::DescribePublicIpv4PoolsRequest) : Core::ParsedResponse(AEC::DescribePublicIpv4PoolsResult)
      Log.info { "performing 'DescribePublicIpv4Pools' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribePublicIpv4Pools")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribePublicIpv4Pools", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribePublicIpv4PoolsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribePublicIpv4PoolsResult).new(response, result)
    end

    # Describes the Regions that are enabled for your account, or all Regions.
    #
    # For a list of the Regions supported by Amazon EC2, see [Amazon EC2 service
    # endpoints](https://docs.aws.amazon.com/ec2/latest/devguide/ec2-endpoints.html).
    #
    # For information about enabling and disabling Regions for your account, see [Specify which Amazon
    # Web Services Regions your account can
    # use](https://docs.aws.amazon.com/accounts/latest/reference/manage-acct-regions.html) in the
    # *Amazon Web Services Account Management Reference Guide*.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_regions(input : AEC::DescribeRegionsRequest) : Core::ParsedResponse(AEC::DescribeRegionsResult)
      Log.info { "performing 'DescribeRegions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeRegions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeRegions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeRegionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeRegionsResult).new(response, result)
    end

    # Describes a root volume replacement task. For more information, see [Replace a root
    # volume](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/replace-root.html) in the *Amazon
    # EC2 User Guide*.
    def describe_replace_root_volume_tasks(input : AEC::DescribeReplaceRootVolumeTasksRequest) : Core::ParsedResponse(AEC::DescribeReplaceRootVolumeTasksResult)
      Log.info { "performing 'DescribeReplaceRootVolumeTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeReplaceRootVolumeTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeReplaceRootVolumeTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeReplaceRootVolumeTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeReplaceRootVolumeTasksResult).new(response, result)
    end

    # Describes one or more of the Reserved Instances that you purchased.
    #
    # For more information about Reserved Instances, see [Reserved
    # Instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/concepts-on-demand-reserved-instances.html)
    # in the *Amazon EC2 User Guide*.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_reserved_instances(input : AEC::DescribeReservedInstancesRequest) : Core::ParsedResponse(AEC::DescribeReservedInstancesResult)
      Log.info { "performing 'DescribeReservedInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeReservedInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeReservedInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeReservedInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeReservedInstancesResult).new(response, result)
    end

    # Describes your account's Reserved Instance listings in the Reserved Instance Marketplace.
    #
    # The Reserved Instance Marketplace matches sellers who want to resell Reserved Instance capacity
    # that they no longer need with buyers who want to purchase additional capacity. Reserved
    # Instances bought and sold through the Reserved Instance Marketplace work like any other Reserved
    # Instances.
    #
    # As a seller, you choose to list some or all of your Reserved Instances, and you specify the
    # upfront price to receive for them. Your Reserved Instances are then listed in the Reserved
    # Instance Marketplace and are available for purchase.
    #
    # As a buyer, you specify the configuration of the Reserved Instance to purchase, and the
    # Marketplace matches what you're searching for with what's available. The Marketplace first sells
    # the lowest priced Reserved Instances to you, and continues to sell available Reserved Instance
    # listings to you until your demand is met. You are charged based on the total price of all of the
    # listings that you purchase.
    #
    # For more information, see [Sell in the Reserved Instance
    # Marketplace](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ri-market-general.html) in the
    # *Amazon EC2 User Guide*.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_reserved_instances_listings(input : AEC::DescribeReservedInstancesListingsRequest) : Core::ParsedResponse(AEC::DescribeReservedInstancesListingsResult)
      Log.info { "performing 'DescribeReservedInstancesListings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeReservedInstancesListings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeReservedInstancesListings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeReservedInstancesListingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeReservedInstancesListingsResult).new(response, result)
    end

    # Describes the modifications made to your Reserved Instances. If no parameter is specified,
    # information about all your Reserved Instances modification requests is returned. If a
    # modification ID is specified, only information about the specific modification is returned.
    #
    # For more information, see [Modify Reserved
    # Instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ri-modifying.html) in the *Amazon
    # EC2 User Guide*.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_reserved_instances_modifications(input : AEC::DescribeReservedInstancesModificationsRequest) : Core::ParsedResponse(AEC::DescribeReservedInstancesModificationsResult)
      Log.info { "performing 'DescribeReservedInstancesModifications' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeReservedInstancesModifications")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeReservedInstancesModifications", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeReservedInstancesModificationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeReservedInstancesModificationsResult).new(response, result)
    end

    # Describes Reserved Instance offerings that are available for purchase. With Reserved Instances,
    # you purchase the right to launch instances for a period of time. During that time period, you do
    # not receive insufficient capacity errors, and you pay a lower usage rate than the rate charged
    # for On-Demand instances for the actual time used.
    #
    # If you have listed your own Reserved Instances for sale in the Reserved Instance Marketplace,
    # they will be excluded from these results. This is to ensure that you do not purchase your own
    # Reserved Instances.
    #
    # For more information, see [Sell in the Reserved Instance
    # Marketplace](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ri-market-general.html) in the
    # *Amazon EC2 User Guide*.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_reserved_instances_offerings(input : AEC::DescribeReservedInstancesOfferingsRequest) : Core::ParsedResponse(AEC::DescribeReservedInstancesOfferingsResult)
      Log.info { "performing 'DescribeReservedInstancesOfferings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeReservedInstancesOfferings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeReservedInstancesOfferings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeReservedInstancesOfferingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeReservedInstancesOfferingsResult).new(response, result)
    end

    # Describes one or more route server endpoints.
    #
    # A route server endpoint is an Amazon Web Services-managed component inside a subnet that
    # facilitates [BGP (Border Gateway
    # Protocol)](https://en.wikipedia.org/wiki/Border_Gateway_Protocol) connections between your route
    # server and your BGP peers.
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def describe_route_server_endpoints(input : AEC::DescribeRouteServerEndpointsRequest) : Core::ParsedResponse(AEC::DescribeRouteServerEndpointsResult)
      Log.info { "performing 'DescribeRouteServerEndpoints' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeRouteServerEndpoints")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeRouteServerEndpoints", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeRouteServerEndpointsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeRouteServerEndpointsResult).new(response, result)
    end

    # Describes one or more route server peers.
    #
    # A route server peer is a session between a route server endpoint and the device deployed in
    # Amazon Web Services (such as a firewall appliance or other network security function running on
    # an EC2 instance). The device must meet these requirements:
    #
    # - Have an elastic network interface in the VPC
    #
    # - Support BGP (Border Gateway Protocol)
    #
    # - Can initiate BGP sessions
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def describe_route_server_peers(input : AEC::DescribeRouteServerPeersRequest) : Core::ParsedResponse(AEC::DescribeRouteServerPeersResult)
      Log.info { "performing 'DescribeRouteServerPeers' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeRouteServerPeers")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeRouteServerPeers", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeRouteServerPeersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeRouteServerPeersResult).new(response, result)
    end

    # Describes one or more route servers.
    #
    # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed
    # within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates
    # VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing
    # fault tolerance for those workloads. This enables you to automatically reroute traffic within a
    # VPC, which increases the manageability of VPC routing and interoperability with third-party
    # workloads.
    #
    # Route server supports the follow route table types:
    #
    # - VPC route tables not associated with subnets
    #
    # - Subnet route tables
    #
    # - Internet gateway route tables
    #
    # Route server does not support route tables associated with virtual private gateways. To
    # propagate routes into a transit gateway route table, use [Transit Gateway
    # Connect](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html).
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def describe_route_servers(input : AEC::DescribeRouteServersRequest) : Core::ParsedResponse(AEC::DescribeRouteServersResult)
      Log.info { "performing 'DescribeRouteServers' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeRouteServers")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeRouteServers", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeRouteServersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeRouteServersResult).new(response, result)
    end

    # Describes your route tables. The default is to describe all your route tables. Alternatively,
    # you can specify specific route table IDs or filter the results to include only the route tables
    # that match specific criteria.
    #
    # Each subnet in your VPC must be associated with a route table. If a subnet is not explicitly
    # associated with any route table, it is implicitly associated with the main route table. This
    # command does not return the subnet ID for implicit associations.
    #
    # For more information, see [Route
    # tables](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html) in the *Amazon
    # VPC User Guide*.
    def describe_route_tables(input : AEC::DescribeRouteTablesRequest) : Core::ParsedResponse(AEC::DescribeRouteTablesResult)
      Log.info { "performing 'DescribeRouteTables' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeRouteTables")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeRouteTables", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeRouteTablesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeRouteTablesResult).new(response, result)
    end

    # Finds available schedules that meet the specified criteria.
    #
    # You can search for an available schedule no more than 3 months in advance. You must meet the
    # minimum required duration of 1,200 hours per year. For example, the minimum daily schedule is 4
    # hours, the minimum weekly schedule is 24 hours, and the minimum monthly schedule is 100 hours.
    #
    # After you find a schedule that meets your needs, call PurchaseScheduledInstances to purchase
    # Scheduled Instances with that schedule.
    def describe_scheduled_instance_availability(input : AEC::DescribeScheduledInstanceAvailabilityRequest) : Core::ParsedResponse(AEC::DescribeScheduledInstanceAvailabilityResult)
      Log.info { "performing 'DescribeScheduledInstanceAvailability' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeScheduledInstanceAvailability")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeScheduledInstanceAvailability", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeScheduledInstanceAvailabilityResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeScheduledInstanceAvailabilityResult).new(response, result)
    end

    # Describes the specified Scheduled Instances or all your Scheduled Instances.
    def describe_scheduled_instances(input : AEC::DescribeScheduledInstancesRequest) : Core::ParsedResponse(AEC::DescribeScheduledInstancesResult)
      Log.info { "performing 'DescribeScheduledInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeScheduledInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeScheduledInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeScheduledInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeScheduledInstancesResult).new(response, result)
    end

    # Describes one or more of your secondary interfaces.
    def describe_secondary_interfaces(input : AEC::DescribeSecondaryInterfacesRequest) : Core::ParsedResponse(AEC::DescribeSecondaryInterfacesResult)
      Log.info { "performing 'DescribeSecondaryInterfaces' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSecondaryInterfaces")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSecondaryInterfaces", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSecondaryInterfacesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSecondaryInterfacesResult).new(response, result)
    end

    # Describes one or more secondary networks.
    def describe_secondary_networks(input : AEC::DescribeSecondaryNetworksRequest) : Core::ParsedResponse(AEC::DescribeSecondaryNetworksResult)
      Log.info { "performing 'DescribeSecondaryNetworks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSecondaryNetworks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSecondaryNetworks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSecondaryNetworksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSecondaryNetworksResult).new(response, result)
    end

    # Describes one or more of your secondary subnets.
    def describe_secondary_subnets(input : AEC::DescribeSecondarySubnetsRequest) : Core::ParsedResponse(AEC::DescribeSecondarySubnetsResult)
      Log.info { "performing 'DescribeSecondarySubnets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSecondarySubnets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSecondarySubnets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSecondarySubnetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSecondarySubnetsResult).new(response, result)
    end

    # Describes the VPCs on the other side of a VPC peering or Transit Gateway connection that are
    # referencing the security groups you've specified in this request.
    def describe_security_group_references(input : AEC::DescribeSecurityGroupReferencesRequest) : Core::ParsedResponse(AEC::DescribeSecurityGroupReferencesResult)
      Log.info { "performing 'DescribeSecurityGroupReferences' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSecurityGroupReferences")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSecurityGroupReferences", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSecurityGroupReferencesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSecurityGroupReferencesResult).new(response, result)
    end

    # Describes one or more of your security group rules.
    def describe_security_group_rules(input : AEC::DescribeSecurityGroupRulesRequest) : Core::ParsedResponse(AEC::DescribeSecurityGroupRulesResult)
      Log.info { "performing 'DescribeSecurityGroupRules' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSecurityGroupRules")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSecurityGroupRules", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSecurityGroupRulesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSecurityGroupRulesResult).new(response, result)
    end

    # Describes the specified security groups or all of your security groups.
    def describe_security_groups(input : AEC::DescribeSecurityGroupsRequest) : Core::ParsedResponse(AEC::DescribeSecurityGroupsResult)
      Log.info { "performing 'DescribeSecurityGroups' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSecurityGroups")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSecurityGroups", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSecurityGroupsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSecurityGroupsResult).new(response, result)
    end

    # Describes security group VPC associations made with
    # [AssociateSecurityGroupVpc](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_AssociateSecurityGroupVpc.html).
    def describe_security_group_vpc_associations(input : AEC::DescribeSecurityGroupVpcAssociationsRequest) : Core::ParsedResponse(AEC::DescribeSecurityGroupVpcAssociationsResult)
      Log.info { "performing 'DescribeSecurityGroupVpcAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSecurityGroupVpcAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSecurityGroupVpcAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSecurityGroupVpcAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSecurityGroupVpcAssociationsResult).new(response, result)
    end

    # Describes the Outpost service link virtual interfaces.
    def describe_service_link_virtual_interfaces(input : AEC::DescribeServiceLinkVirtualInterfacesRequest) : Core::ParsedResponse(AEC::DescribeServiceLinkVirtualInterfacesResult)
      Log.info { "performing 'DescribeServiceLinkVirtualInterfaces' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeServiceLinkVirtualInterfaces")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeServiceLinkVirtualInterfaces", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeServiceLinkVirtualInterfacesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeServiceLinkVirtualInterfacesResult).new(response, result)
    end

    # Describes the specified attribute of the specified snapshot. You can specify only one attribute
    # at a time.
    #
    # For more information about EBS snapshots, see [Amazon EBS
    # snapshots](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-snapshots.html) in the *Amazon
    # EBS User Guide*.
    def describe_snapshot_attribute(input : AEC::DescribeSnapshotAttributeRequest) : Core::ParsedResponse(AEC::DescribeSnapshotAttributeResult)
      Log.info { "performing 'DescribeSnapshotAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSnapshotAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSnapshotAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSnapshotAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSnapshotAttributeResult).new(response, result)
    end

    # Describes the specified EBS snapshots available to you or all of the EBS snapshots available to
    # you.
    #
    # The snapshots available to you include public snapshots, private snapshots that you own, and
    # private snapshots owned by other Amazon Web Services accounts for which you have explicit create
    # volume permissions.
    #
    # The create volume permissions fall into the following categories:
    #
    # - *public*: The owner of the snapshot granted create volume permissions for the snapshot to the
    # `all` group. All Amazon Web Services accounts have create volume permissions for these
    # snapshots.
    #
    # - *explicit*: The owner of the snapshot granted create volume permissions to a specific Amazon
    # Web Services account.
    #
    # - *implicit*: An Amazon Web Services account has implicit create volume permissions for all
    # snapshots it owns.
    #
    # The list of snapshots returned can be filtered by specifying snapshot IDs, snapshot owners, or
    # Amazon Web Services accounts with create volume permissions. If no options are specified, Amazon
    # EC2 returns all snapshots for which you have create volume permissions.
    #
    # If you specify one or more snapshot IDs, only snapshots that have the specified IDs are
    # returned. If you specify an invalid snapshot ID, an error is returned. If you specify a snapshot
    # ID for which you do not have access, it is not included in the returned results.
    #
    # If you specify one or more snapshot owners using the `OwnerIds` option, only snapshots from the
    # specified owners and for which you have access are returned. The results can include the Amazon
    # Web Services account IDs of the specified owners, `amazon` for snapshots owned by Amazon, or
    # `self` for snapshots that you own.
    #
    # If you specify a list of restorable users, only snapshots with create snapshot permissions for
    # those users are returned. You can specify Amazon Web Services account IDs (if you own the
    # snapshots), `self` for snapshots for which you own or have explicit permissions, or `all` for
    # public snapshots.
    #
    # If you are describing a long list of snapshots, we recommend that you paginate the output to
    # make the list more manageable. For more information, see
    # [Pagination](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/Query-Requests.html#api-pagination).
    #
    # For more information about EBS snapshots, see [Amazon EBS
    # snapshots](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-snapshots.html) in the *Amazon
    # EBS User Guide*.
    #
    # We strongly recommend using only paginated requests. Unpaginated requests are susceptible to
    # throttling and timeouts.
    def describe_snapshots(input : AEC::DescribeSnapshotsRequest) : Core::ParsedResponse(AEC::DescribeSnapshotsResult)
      Log.info { "performing 'DescribeSnapshots' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSnapshots")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSnapshots", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSnapshotsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSnapshotsResult).new(response, result)
    end

    # Describes the storage tier status of one or more Amazon EBS snapshots.
    def describe_snapshot_tier_status(input : AEC::DescribeSnapshotTierStatusRequest) : Core::ParsedResponse(AEC::DescribeSnapshotTierStatusResult)
      Log.info { "performing 'DescribeSnapshotTierStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSnapshotTierStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSnapshotTierStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSnapshotTierStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSnapshotTierStatusResult).new(response, result)
    end

    # Describes the data feed for Spot Instances. For more information, see [Spot Instance data
    # feed](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-data-feeds.html) in the *Amazon
    # EC2 User Guide*.
    def describe_spot_datafeed_subscription(input : AEC::DescribeSpotDatafeedSubscriptionRequest) : Core::ParsedResponse(AEC::DescribeSpotDatafeedSubscriptionResult)
      Log.info { "performing 'DescribeSpotDatafeedSubscription' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSpotDatafeedSubscription")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSpotDatafeedSubscription", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSpotDatafeedSubscriptionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSpotDatafeedSubscriptionResult).new(response, result)
    end

    # Describes the running instances for the specified Spot Fleet.
    def describe_spot_fleet_instances(input : AEC::DescribeSpotFleetInstancesRequest) : Core::ParsedResponse(AEC::DescribeSpotFleetInstancesResponse)
      Log.info { "performing 'DescribeSpotFleetInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSpotFleetInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSpotFleetInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSpotFleetInstancesResponse.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSpotFleetInstancesResponse).new(response, result)
    end

    # Describes the events for the specified Spot Fleet request during the specified time.
    #
    # Spot Fleet events are delayed by up to 30 seconds before they can be described. This ensures
    # that you can query by the last evaluated time and not miss a recorded event. Spot Fleet events
    # are available for 48 hours.
    #
    # For more information, see [Monitor fleet events using Amazon
    # EventBridge](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/fleet-monitor.html) in the
    # *Amazon EC2 User Guide*.
    def describe_spot_fleet_request_history(input : AEC::DescribeSpotFleetRequestHistoryRequest) : Core::ParsedResponse(AEC::DescribeSpotFleetRequestHistoryResponse)
      Log.info { "performing 'DescribeSpotFleetRequestHistory' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSpotFleetRequestHistory")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSpotFleetRequestHistory", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSpotFleetRequestHistoryResponse.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSpotFleetRequestHistoryResponse).new(response, result)
    end

    # Describes your Spot Fleet requests.
    #
    # Spot Fleet requests are deleted 48 hours after they are canceled and their instances are
    # terminated.
    def describe_spot_fleet_requests(input : AEC::DescribeSpotFleetRequestsRequest) : Core::ParsedResponse(AEC::DescribeSpotFleetRequestsResponse)
      Log.info { "performing 'DescribeSpotFleetRequests' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSpotFleetRequests")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSpotFleetRequests", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSpotFleetRequestsResponse.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSpotFleetRequestsResponse).new(response, result)
    end

    # Describes the specified Spot Instance requests.
    #
    # You can use `DescribeSpotInstanceRequests` to find a running Spot Instance by examining the
    # response. If the status of the Spot Instance is `fulfilled`, the instance ID appears in the
    # response and contains the identifier of the instance. Alternatively, you can use
    # [DescribeInstances](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeInstances)
    # with a filter to look for instances where the instance lifecycle is `spot`.
    #
    # We recommend that you set `MaxResults` to a value between 5 and 1000 to limit the number of
    # items returned. This paginates the output, which makes the list more manageable and returns the
    # items faster. If the list of items exceeds your `MaxResults` value, then that number of items is
    # returned along with a `NextToken` value that can be passed to a subsequent
    # `DescribeSpotInstanceRequests` request to retrieve the remaining items.
    #
    # Spot Instance requests are deleted four hours after they are canceled and their instances are
    # terminated.
    def describe_spot_instance_requests(input : AEC::DescribeSpotInstanceRequestsRequest) : Core::ParsedResponse(AEC::DescribeSpotInstanceRequestsResult)
      Log.info { "performing 'DescribeSpotInstanceRequests' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSpotInstanceRequests")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSpotInstanceRequests", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSpotInstanceRequestsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSpotInstanceRequestsResult).new(response, result)
    end

    # Describes the Spot price history. For more information, see [Spot Instance pricing
    # history](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-spot-instances-history.html)
    # in the *Amazon EC2 User Guide*.
    #
    # When you specify a start and end time, the operation returns the prices of the instance types
    # within that time range. It also returns the last price change before the start time, which is
    # the effective price as of the start time.
    def describe_spot_price_history(input : AEC::DescribeSpotPriceHistoryRequest) : Core::ParsedResponse(AEC::DescribeSpotPriceHistoryResult)
      Log.info { "performing 'DescribeSpotPriceHistory' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSpotPriceHistory")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSpotPriceHistory", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSpotPriceHistoryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSpotPriceHistoryResult).new(response, result)
    end

    # Describes the stale security group rules for security groups referenced across a VPC peering
    # connection, transit gateway connection, or with a security group VPC association. Rules are
    # stale when they reference a deleted security group. Rules can also be stale if they reference a
    # security group in a peer VPC for which the VPC peering connection has been deleted, across a
    # transit gateway where the transit gateway has been deleted (or [the transit gateway security
    # group referencing
    # feature](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-vpc-attachments.html#vpc-attachment-security)
    # has been disabled), or if a security group VPC association has been disassociated.
    def describe_stale_security_groups(input : AEC::DescribeStaleSecurityGroupsRequest) : Core::ParsedResponse(AEC::DescribeStaleSecurityGroupsResult)
      Log.info { "performing 'DescribeStaleSecurityGroups' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeStaleSecurityGroups")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeStaleSecurityGroups", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeStaleSecurityGroupsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeStaleSecurityGroupsResult).new(response, result)
    end

    # Describes the progress of the AMI store tasks. You can describe the store tasks for specified
    # AMIs. If you don't specify the AMIs, you get a paginated list of store tasks from the last 31
    # days.
    #
    # For each AMI task, the response indicates if the task is `InProgress`, `Completed`, or `Failed`.
    # For tasks `InProgress`, the response shows the estimated progress as a percentage.
    #
    # Tasks are listed in reverse chronological order. Currently, only tasks from the past 31 days can
    # be viewed.
    #
    # To use this API, you must have the required permissions. For more information, see [Permissions
    # for storing and restoring AMIs using
    # S3](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/work-with-ami-store-restore.html#ami-s3-permissions)
    # in the *Amazon EC2 User Guide*.
    #
    # For more information, see [Store and restore an AMI using
    # S3](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-store-restore.html) in the *Amazon
    # EC2 User Guide*.
    def describe_store_image_tasks(input : AEC::DescribeStoreImageTasksRequest) : Core::ParsedResponse(AEC::DescribeStoreImageTasksResult)
      Log.info { "performing 'DescribeStoreImageTasks' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeStoreImageTasks")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeStoreImageTasks", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeStoreImageTasksResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeStoreImageTasksResult).new(response, result)
    end

    # Describes your subnets. The default is to describe all your subnets. Alternatively, you can
    # specify specific subnet IDs or filter the results to include only the subnets that match
    # specific criteria.
    #
    # For more information, see
    # [Subnets](https://docs.aws.amazon.com/vpc/latest/userguide/configure-subnets.html) in the
    # *Amazon VPC User Guide*.
    def describe_subnets(input : AEC::DescribeSubnetsRequest) : Core::ParsedResponse(AEC::DescribeSubnetsResult)
      Log.info { "performing 'DescribeSubnets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeSubnets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeSubnets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeSubnetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeSubnetsResult).new(response, result)
    end

    # Describes the specified tags for your EC2 resources.
    #
    # For more information about tags, see [Tag your Amazon EC2
    # resources](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html) in the *Amazon
    # Elastic Compute Cloud User Guide*.
    #
    # We strongly recommend using only paginated requests. Unpaginated requests are susceptible to
    # throttling and timeouts.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_tags(input : AEC::DescribeTagsRequest) : Core::ParsedResponse(AEC::DescribeTagsResult)
      Log.info { "performing 'DescribeTags' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTags")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTags", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTagsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTagsResult).new(response, result)
    end

    # Describe traffic mirror filters that determine the traffic that is mirrored.
    def describe_traffic_mirror_filter_rules(input : AEC::DescribeTrafficMirrorFilterRulesRequest) : Core::ParsedResponse(AEC::DescribeTrafficMirrorFilterRulesResult)
      Log.info { "performing 'DescribeTrafficMirrorFilterRules' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTrafficMirrorFilterRules")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTrafficMirrorFilterRules", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTrafficMirrorFilterRulesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTrafficMirrorFilterRulesResult).new(response, result)
    end

    # Describes one or more Traffic Mirror filters.
    def describe_traffic_mirror_filters(input : AEC::DescribeTrafficMirrorFiltersRequest) : Core::ParsedResponse(AEC::DescribeTrafficMirrorFiltersResult)
      Log.info { "performing 'DescribeTrafficMirrorFilters' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTrafficMirrorFilters")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTrafficMirrorFilters", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTrafficMirrorFiltersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTrafficMirrorFiltersResult).new(response, result)
    end

    # Describes one or more Traffic Mirror sessions. By default, all Traffic Mirror sessions are
    # described. Alternatively, you can filter the results.
    def describe_traffic_mirror_sessions(input : AEC::DescribeTrafficMirrorSessionsRequest) : Core::ParsedResponse(AEC::DescribeTrafficMirrorSessionsResult)
      Log.info { "performing 'DescribeTrafficMirrorSessions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTrafficMirrorSessions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTrafficMirrorSessions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTrafficMirrorSessionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTrafficMirrorSessionsResult).new(response, result)
    end

    # Information about one or more Traffic Mirror targets.
    def describe_traffic_mirror_targets(input : AEC::DescribeTrafficMirrorTargetsRequest) : Core::ParsedResponse(AEC::DescribeTrafficMirrorTargetsResult)
      Log.info { "performing 'DescribeTrafficMirrorTargets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTrafficMirrorTargets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTrafficMirrorTargets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTrafficMirrorTargetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTrafficMirrorTargetsResult).new(response, result)
    end

    # Describes one or more attachments between resources and transit gateways. By default, all
    # attachments are described. Alternatively, you can filter the results by attachment ID,
    # attachment state, resource ID, or resource owner.
    def describe_transit_gateway_attachments(input : AEC::DescribeTransitGatewayAttachmentsRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayAttachmentsResult)
      Log.info { "performing 'DescribeTransitGatewayAttachments' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayAttachments")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayAttachments", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayAttachmentsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayAttachmentsResult).new(response, result)
    end

    # Describes one or more Connect peers.
    def describe_transit_gateway_connect_peers(input : AEC::DescribeTransitGatewayConnectPeersRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayConnectPeersResult)
      Log.info { "performing 'DescribeTransitGatewayConnectPeers' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayConnectPeers")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayConnectPeers", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayConnectPeersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayConnectPeersResult).new(response, result)
    end

    # Describes one or more Connect attachments.
    def describe_transit_gateway_connects(input : AEC::DescribeTransitGatewayConnectsRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayConnectsResult)
      Log.info { "performing 'DescribeTransitGatewayConnects' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayConnects")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayConnects", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayConnectsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayConnectsResult).new(response, result)
    end

    # Describes one or more transit gateway metering policies.
    def describe_transit_gateway_metering_policies(input : AEC::DescribeTransitGatewayMeteringPoliciesRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayMeteringPoliciesResult)
      Log.info { "performing 'DescribeTransitGatewayMeteringPolicies' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayMeteringPolicies")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayMeteringPolicies", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayMeteringPoliciesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayMeteringPoliciesResult).new(response, result)
    end

    # Describes one or more transit gateway multicast domains.
    def describe_transit_gateway_multicast_domains(input : AEC::DescribeTransitGatewayMulticastDomainsRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayMulticastDomainsResult)
      Log.info { "performing 'DescribeTransitGatewayMulticastDomains' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayMulticastDomains")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayMulticastDomains", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayMulticastDomainsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayMulticastDomainsResult).new(response, result)
    end

    # Describes your transit gateway peering attachments.
    def describe_transit_gateway_peering_attachments(input : AEC::DescribeTransitGatewayPeeringAttachmentsRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayPeeringAttachmentsResult)
      Log.info { "performing 'DescribeTransitGatewayPeeringAttachments' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayPeeringAttachments")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayPeeringAttachments", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayPeeringAttachmentsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayPeeringAttachmentsResult).new(response, result)
    end

    # Describes one or more transit gateway route policy tables.
    def describe_transit_gateway_policy_tables(input : AEC::DescribeTransitGatewayPolicyTablesRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayPolicyTablesResult)
      Log.info { "performing 'DescribeTransitGatewayPolicyTables' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayPolicyTables")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayPolicyTables", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayPolicyTablesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayPolicyTablesResult).new(response, result)
    end

    # Describes one or more transit gateway route table advertisements.
    def describe_transit_gateway_route_table_announcements(input : AEC::DescribeTransitGatewayRouteTableAnnouncementsRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayRouteTableAnnouncementsResult)
      Log.info { "performing 'DescribeTransitGatewayRouteTableAnnouncements' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayRouteTableAnnouncements")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayRouteTableAnnouncements", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayRouteTableAnnouncementsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayRouteTableAnnouncementsResult).new(response, result)
    end

    # Describes one or more transit gateway route tables. By default, all transit gateway route tables
    # are described. Alternatively, you can filter the results.
    def describe_transit_gateway_route_tables(input : AEC::DescribeTransitGatewayRouteTablesRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayRouteTablesResult)
      Log.info { "performing 'DescribeTransitGatewayRouteTables' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayRouteTables")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayRouteTables", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayRouteTablesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayRouteTablesResult).new(response, result)
    end

    # Describes one or more transit gateways. By default, all transit gateways are described.
    # Alternatively, you can filter the results.
    def describe_transit_gateways(input : AEC::DescribeTransitGatewaysRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewaysResult)
      Log.info { "performing 'DescribeTransitGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewaysResult).new(response, result)
    end

    # Describes one or more VPC attachments. By default, all VPC attachments are described.
    # Alternatively, you can filter the results.
    def describe_transit_gateway_vpc_attachments(input : AEC::DescribeTransitGatewayVpcAttachmentsRequest) : Core::ParsedResponse(AEC::DescribeTransitGatewayVpcAttachmentsResult)
      Log.info { "performing 'DescribeTransitGatewayVpcAttachments' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTransitGatewayVpcAttachments")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTransitGatewayVpcAttachments", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTransitGatewayVpcAttachmentsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTransitGatewayVpcAttachmentsResult).new(response, result)
    end

    # Describes one or more network interface trunk associations.
    def describe_trunk_interface_associations(input : AEC::DescribeTrunkInterfaceAssociationsRequest) : Core::ParsedResponse(AEC::DescribeTrunkInterfaceAssociationsResult)
      Log.info { "performing 'DescribeTrunkInterfaceAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeTrunkInterfaceAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeTrunkInterfaceAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeTrunkInterfaceAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeTrunkInterfaceAssociationsResult).new(response, result)
    end

    # Describes the specified Amazon Web Services Verified Access endpoints.
    def describe_verified_access_endpoints(input : AEC::DescribeVerifiedAccessEndpointsRequest) : Core::ParsedResponse(AEC::DescribeVerifiedAccessEndpointsResult)
      Log.info { "performing 'DescribeVerifiedAccessEndpoints' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVerifiedAccessEndpoints")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVerifiedAccessEndpoints", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVerifiedAccessEndpointsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVerifiedAccessEndpointsResult).new(response, result)
    end

    # Describes the specified Verified Access groups.
    def describe_verified_access_groups(input : AEC::DescribeVerifiedAccessGroupsRequest) : Core::ParsedResponse(AEC::DescribeVerifiedAccessGroupsResult)
      Log.info { "performing 'DescribeVerifiedAccessGroups' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVerifiedAccessGroups")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVerifiedAccessGroups", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVerifiedAccessGroupsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVerifiedAccessGroupsResult).new(response, result)
    end

    # Describes the specified Amazon Web Services Verified Access instances.
    def describe_verified_access_instance_logging_configurations(input : AEC::DescribeVerifiedAccessInstanceLoggingConfigurationsRequest) : Core::ParsedResponse(AEC::DescribeVerifiedAccessInstanceLoggingConfigurationsResult)
      Log.info { "performing 'DescribeVerifiedAccessInstanceLoggingConfigurations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVerifiedAccessInstanceLoggingConfigurations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVerifiedAccessInstanceLoggingConfigurations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVerifiedAccessInstanceLoggingConfigurationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVerifiedAccessInstanceLoggingConfigurationsResult).new(response, result)
    end

    # Describes the specified Amazon Web Services Verified Access instances.
    def describe_verified_access_instances(input : AEC::DescribeVerifiedAccessInstancesRequest) : Core::ParsedResponse(AEC::DescribeVerifiedAccessInstancesResult)
      Log.info { "performing 'DescribeVerifiedAccessInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVerifiedAccessInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVerifiedAccessInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVerifiedAccessInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVerifiedAccessInstancesResult).new(response, result)
    end

    # Describes the specified Amazon Web Services Verified Access trust providers.
    def describe_verified_access_trust_providers(input : AEC::DescribeVerifiedAccessTrustProvidersRequest) : Core::ParsedResponse(AEC::DescribeVerifiedAccessTrustProvidersResult)
      Log.info { "performing 'DescribeVerifiedAccessTrustProviders' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVerifiedAccessTrustProviders")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVerifiedAccessTrustProviders", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVerifiedAccessTrustProvidersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVerifiedAccessTrustProvidersResult).new(response, result)
    end

    # Describes the specified attribute of the specified volume. You can specify only one attribute at
    # a time.
    #
    # For more information about EBS volumes, see [Amazon EBS
    # volumes](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-volumes.html) in the *Amazon EBS
    # User Guide*.
    def describe_volume_attribute(input : AEC::DescribeVolumeAttributeRequest) : Core::ParsedResponse(AEC::DescribeVolumeAttributeResult)
      Log.info { "performing 'DescribeVolumeAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVolumeAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVolumeAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVolumeAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVolumeAttributeResult).new(response, result)
    end

    # Describes the specified EBS volumes or all of your EBS volumes.
    #
    # If you are describing a long list of volumes, we recommend that you paginate the output to make
    # the list more manageable. For more information, see
    # [Pagination](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/Query-Requests.html#api-pagination).
    #
    # For more information about EBS volumes, see [Amazon EBS
    # volumes](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-volumes.html) in the *Amazon EBS
    # User Guide*.
    #
    # We strongly recommend using only paginated requests. Unpaginated requests are susceptible to
    # throttling and timeouts.
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_volumes(input : AEC::DescribeVolumesRequest) : Core::ParsedResponse(AEC::DescribeVolumesResult)
      Log.info { "performing 'DescribeVolumes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVolumes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVolumes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVolumesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVolumesResult).new(response, result)
    end

    # Describes the most recent volume modification request for the specified EBS volumes.
    #
    # For more information, see [ Monitor the progress of volume
    # modifications](https://docs.aws.amazon.com/ebs/latest/userguide/monitoring-volume-modifications.html)
    # in the *Amazon EBS User Guide*.
    def describe_volumes_modifications(input : AEC::DescribeVolumesModificationsRequest) : Core::ParsedResponse(AEC::DescribeVolumesModificationsResult)
      Log.info { "performing 'DescribeVolumesModifications' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVolumesModifications")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVolumesModifications", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVolumesModificationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVolumesModificationsResult).new(response, result)
    end

    # Describes the status of the specified volumes. Volume status provides the result of the checks
    # performed on your volumes to determine events that can impair the performance of your volumes.
    # The performance of a volume can be affected if an issue occurs on the volume's underlying host.
    # If the volume's underlying host experiences a power outage or system issue, after the system is
    # restored, there could be data inconsistencies on the volume. Volume events notify you if this
    # occurs. Volume actions notify you if any action needs to be taken in response to the event.
    #
    # The `DescribeVolumeStatus` operation provides the following information about the specified
    # volumes:
    #
    # *Status*: Reflects the current status of the volume. The possible values are `ok`, `impaired` ,
    # `warning`, or `insufficient-data`. If all checks pass, the overall status of the volume is `ok`.
    # If the check fails, the overall status is `impaired`. If the status is `insufficient-data`, then
    # the checks might still be taking place on your volume at the time. We recommend that you retry
    # the request. For more information about volume status, see [Monitor the status of your
    # volumes](https://docs.aws.amazon.com/ebs/latest/userguide/monitoring-volume-status.html) in the
    # *Amazon EBS User Guide*.
    #
    # *Events*: Reflect the cause of a volume status and might require you to take action. For
    # example, if your volume returns an `impaired` status, then the volume event might be
    # `potential-data-inconsistency`. This means that your volume has been affected by an issue with
    # the underlying host, has all I/O operations disabled, and might have inconsistent data.
    #
    # *Actions*: Reflect the actions you might have to take in response to an event. For example, if
    # the status of the volume is `impaired` and the volume event shows
    # `potential-data-inconsistency`, then the action shows `enable-volume-io`. This means that you
    # may want to enable the I/O operations for the volume and then check the volume for data
    # consistency. For more information, see [Work with an impaired EBS
    # volume](https://docs.aws.amazon.com/ebs/latest/userguide/work_volumes_impaired.html).
    #
    # Volume status is based on the volume status checks, and does not reflect the volume state.
    # Therefore, volume status does not indicate volumes in the `error` state (for example, when a
    # volume is incapable of accepting I/O.)
    #
    # The order of the elements in the response, including those within nested structures, might vary.
    # Applications should not assume the elements appear in a particular order.
    def describe_volume_status(input : AEC::DescribeVolumeStatusRequest) : Core::ParsedResponse(AEC::DescribeVolumeStatusResult)
      Log.info { "performing 'DescribeVolumeStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVolumeStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVolumeStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVolumeStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVolumeStatusResult).new(response, result)
    end

    # Describes the specified attribute of the specified VPC. You can specify only one attribute at a
    # time.
    def describe_vpc_attribute(input : AEC::DescribeVpcAttributeRequest) : Core::ParsedResponse(AEC::DescribeVpcAttributeResult)
      Log.info { "performing 'DescribeVpcAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcAttributeResult).new(response, result)
    end

    # Describe VPC Block Public Access (BPA) exclusions. A VPC BPA exclusion is a mode that can be
    # applied to a single VPC or subnet that exempts it from the account’s BPA mode and will allow
    # bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even
    # when BPA is not enabled on the account to ensure that there is no traffic disruption to the
    # exclusions when VPC BPA is turned on. To learn more about VPC BPA, see [Block public access to
    # VPCs and subnets](https://docs.aws.amazon.com/vpc/latest/userguide/security-vpc-bpa.html) in the
    # *Amazon VPC User Guide*.
    def describe_vpc_block_public_access_exclusions(input : AEC::DescribeVpcBlockPublicAccessExclusionsRequest) : Core::ParsedResponse(AEC::DescribeVpcBlockPublicAccessExclusionsResult)
      Log.info { "performing 'DescribeVpcBlockPublicAccessExclusions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcBlockPublicAccessExclusions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcBlockPublicAccessExclusions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcBlockPublicAccessExclusionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcBlockPublicAccessExclusionsResult).new(response, result)
    end

    # Describe VPC Block Public Access (BPA) options. VPC Block Public Access (BPA) enables you to
    # block resources in VPCs and subnets that you own in a Region from reaching or being reached from
    # the internet through internet gateways and egress-only internet gateways. To learn more about
    # VPC BPA, see [Block public access to VPCs and
    # subnets](https://docs.aws.amazon.com/vpc/latest/userguide/security-vpc-bpa.html) in the *Amazon
    # VPC User Guide*.
    def describe_vpc_block_public_access_options(input : AEC::DescribeVpcBlockPublicAccessOptionsRequest) : Core::ParsedResponse(AEC::DescribeVpcBlockPublicAccessOptionsResult)
      Log.info { "performing 'DescribeVpcBlockPublicAccessOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcBlockPublicAccessOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcBlockPublicAccessOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcBlockPublicAccessOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcBlockPublicAccessOptionsResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Describes the ClassicLink status of the specified VPCs.
    def describe_vpc_classic_link(input : AEC::DescribeVpcClassicLinkRequest) : Core::ParsedResponse(AEC::DescribeVpcClassicLinkResult)
      Log.info { "performing 'DescribeVpcClassicLink' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcClassicLink")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcClassicLink", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcClassicLinkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcClassicLinkResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Describes the ClassicLink DNS support status of one or more VPCs. If enabled, the DNS hostname
    # of a linked EC2-Classic instance resolves to its private IP address when addressed from an
    # instance in the VPC to which it's linked. Similarly, the DNS hostname of an instance in a VPC
    # resolves to its private IP address when addressed from a linked EC2-Classic instance.
    def describe_vpc_classic_link_dns_support(input : AEC::DescribeVpcClassicLinkDnsSupportRequest) : Core::ParsedResponse(AEC::DescribeVpcClassicLinkDnsSupportResult)
      Log.info { "performing 'DescribeVpcClassicLinkDnsSupport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcClassicLinkDnsSupport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcClassicLinkDnsSupport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcClassicLinkDnsSupportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcClassicLinkDnsSupportResult).new(response, result)
    end

    # Describes one or more VPC Encryption Control configurations. VPC Encryption Control enables you
    # to enforce encryption for all data in transit within and between VPCs to meet compliance
    # requirements You can filter the results to return information about specific encryption controls
    # or VPCs.
    #
    # For more information, see [Enforce VPC encryption in
    # transit](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-encryption-controls.html) in the
    # *Amazon VPC User Guide*.
    def describe_vpc_encryption_controls(input : AEC::DescribeVpcEncryptionControlsRequest) : Core::ParsedResponse(AEC::DescribeVpcEncryptionControlsResult)
      Log.info { "performing 'DescribeVpcEncryptionControls' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEncryptionControls")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEncryptionControls", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEncryptionControlsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEncryptionControlsResult).new(response, result)
    end

    # Describes the VPC resources, VPC endpoint services, Amazon Lattice services, or service networks
    # associated with the VPC endpoint.
    def describe_vpc_endpoint_associations(input : AEC::DescribeVpcEndpointAssociationsRequest) : Core::ParsedResponse(AEC::DescribeVpcEndpointAssociationsResult)
      Log.info { "performing 'DescribeVpcEndpointAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEndpointAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEndpointAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEndpointAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEndpointAssociationsResult).new(response, result)
    end

    # Describes the connection notifications for VPC endpoints and VPC endpoint services.
    def describe_vpc_endpoint_connection_notifications(input : AEC::DescribeVpcEndpointConnectionNotificationsRequest) : Core::ParsedResponse(AEC::DescribeVpcEndpointConnectionNotificationsResult)
      Log.info { "performing 'DescribeVpcEndpointConnectionNotifications' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEndpointConnectionNotifications")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEndpointConnectionNotifications", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEndpointConnectionNotificationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEndpointConnectionNotificationsResult).new(response, result)
    end

    # Describes the VPC endpoint connections to your VPC endpoint services, including any endpoints
    # that are pending your acceptance.
    def describe_vpc_endpoint_connections(input : AEC::DescribeVpcEndpointConnectionsRequest) : Core::ParsedResponse(AEC::DescribeVpcEndpointConnectionsResult)
      Log.info { "performing 'DescribeVpcEndpointConnections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEndpointConnections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEndpointConnections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEndpointConnectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEndpointConnectionsResult).new(response, result)
    end

    # Describes your VPC endpoints. The default is to describe all your VPC endpoints. Alternatively,
    # you can specify specific VPC endpoint IDs or filter the results to include only the VPC
    # endpoints that match specific criteria.
    def describe_vpc_endpoints(input : AEC::DescribeVpcEndpointsRequest) : Core::ParsedResponse(AEC::DescribeVpcEndpointsResult)
      Log.info { "performing 'DescribeVpcEndpoints' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEndpoints")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEndpoints", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEndpointsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEndpointsResult).new(response, result)
    end

    # Describes the VPC endpoint service configurations in your account (your services).
    def describe_vpc_endpoint_service_configurations(input : AEC::DescribeVpcEndpointServiceConfigurationsRequest) : Core::ParsedResponse(AEC::DescribeVpcEndpointServiceConfigurationsResult)
      Log.info { "performing 'DescribeVpcEndpointServiceConfigurations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEndpointServiceConfigurations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEndpointServiceConfigurations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEndpointServiceConfigurationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEndpointServiceConfigurationsResult).new(response, result)
    end

    # Describes the principals (service consumers) that are permitted to discover your VPC endpoint
    # service. Principal ARNs with path components aren't supported.
    def describe_vpc_endpoint_service_permissions(input : AEC::DescribeVpcEndpointServicePermissionsRequest) : Core::ParsedResponse(AEC::DescribeVpcEndpointServicePermissionsResult)
      Log.info { "performing 'DescribeVpcEndpointServicePermissions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEndpointServicePermissions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEndpointServicePermissions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEndpointServicePermissionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEndpointServicePermissionsResult).new(response, result)
    end

    # Describes available services to which you can create a VPC endpoint.
    #
    # When the service provider and the consumer have different accounts in multiple Availability
    # Zones, and the consumer views the VPC endpoint service information, the response only includes
    # the common Availability Zones. For example, when the service provider account uses `us-east-1a`
    # and `us-east-1c` and the consumer uses `us-east-1a` and `us-east-1b`, the response includes the
    # VPC endpoint services in the common Availability Zone, `us-east-1a`.
    def describe_vpc_endpoint_services(input : AEC::DescribeVpcEndpointServicesRequest) : Core::ParsedResponse(AEC::DescribeVpcEndpointServicesResult)
      Log.info { "performing 'DescribeVpcEndpointServices' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcEndpointServices")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcEndpointServices", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcEndpointServicesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcEndpointServicesResult).new(response, result)
    end

    # Describes your VPC peering connections. The default is to describe all your VPC peering
    # connections. Alternatively, you can specify specific VPC peering connection IDs or filter the
    # results to include only the VPC peering connections that match specific criteria.
    def describe_vpc_peering_connections(input : AEC::DescribeVpcPeeringConnectionsRequest) : Core::ParsedResponse(AEC::DescribeVpcPeeringConnectionsResult)
      Log.info { "performing 'DescribeVpcPeeringConnections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcPeeringConnections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcPeeringConnections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcPeeringConnectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcPeeringConnectionsResult).new(response, result)
    end

    # Describes your VPCs. The default is to describe all your VPCs. Alternatively, you can specify
    # specific VPC IDs or filter the results to include only the VPCs that match specific criteria.
    def describe_vpcs(input : AEC::DescribeVpcsRequest) : Core::ParsedResponse(AEC::DescribeVpcsResult)
      Log.info { "performing 'DescribeVpcs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpcs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpcs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpcsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpcsResult).new(response, result)
    end

    # Describes one or more of your VPN concentrators.
    def describe_vpn_concentrators(input : AEC::DescribeVpnConcentratorsRequest) : Core::ParsedResponse(AEC::DescribeVpnConcentratorsResult)
      Log.info { "performing 'DescribeVpnConcentrators' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpnConcentrators")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpnConcentrators", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpnConcentratorsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpnConcentratorsResult).new(response, result)
    end

    # Describes one or more of your VPN connections.
    #
    # For more information, see [Amazon Web Services Site-to-Site
    # VPN](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPC_VPN.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def describe_vpn_connections(input : AEC::DescribeVpnConnectionsRequest) : Core::ParsedResponse(AEC::DescribeVpnConnectionsResult)
      Log.info { "performing 'DescribeVpnConnections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpnConnections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpnConnections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpnConnectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpnConnectionsResult).new(response, result)
    end

    # Describes one or more of your virtual private gateways.
    #
    # For more information, see [Amazon Web Services Site-to-Site
    # VPN](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPC_VPN.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def describe_vpn_gateways(input : AEC::DescribeVpnGatewaysRequest) : Core::ParsedResponse(AEC::DescribeVpnGatewaysResult)
      Log.info { "performing 'DescribeVpnGateways' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DescribeVpnGateways")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DescribeVpnGateways", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DescribeVpnGatewaysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DescribeVpnGatewaysResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Unlinks (detaches) a linked EC2-Classic instance from a VPC. After the instance has been
    # unlinked, the VPC security groups are no longer associated with it. An instance is automatically
    # unlinked from a VPC when it's stopped.
    def detach_classic_link_vpc(input : AEC::DetachClassicLinkVpcRequest) : Core::ParsedResponse(AEC::DetachClassicLinkVpcResult)
      Log.info { "performing 'DetachClassicLinkVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DetachClassicLinkVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DetachClassicLinkVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DetachClassicLinkVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DetachClassicLinkVpcResult).new(response, result)
    end

    # Removes a watermark from the specified AMI. This is an idempotent operation. It succeeds even if
    # the watermark does not exist on the image.
    #
    # Removing a watermark from an image does not affect derivative images that already carry the
    # watermark.
    #
    # Only the AMI owner can detach watermarks.
    def detach_image_watermark(input : AEC::DetachImageWatermarkRequest) : Core::ParsedResponse(AEC::DetachImageWatermarkResult)
      Log.info { "performing 'DetachImageWatermark' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DetachImageWatermark")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DetachImageWatermark", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DetachImageWatermarkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DetachImageWatermarkResult).new(response, result)
    end

    # Detaches an internet gateway from a VPC, disabling connectivity between the internet and the
    # VPC. The VPC must not contain any running instances with Elastic IP addresses or public IPv4
    # addresses.
    def detach_internet_gateway(input : AEC::DetachInternetGatewayRequest) : Core::Response
      Log.info { "performing 'DetachInternetGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DetachInternetGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DetachInternetGateway", body)
      Core::Response.new(response)
    end

    # Detaches a network interface from an instance.
    def detach_network_interface(input : AEC::DetachNetworkInterfaceRequest) : Core::Response
      Log.info { "performing 'DetachNetworkInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DetachNetworkInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DetachNetworkInterface", body)
      Core::Response.new(response)
    end

    # Detaches the specified Amazon Web Services Verified Access trust provider from the specified
    # Amazon Web Services Verified Access instance.
    def detach_verified_access_trust_provider(input : AEC::DetachVerifiedAccessTrustProviderRequest) : Core::ParsedResponse(AEC::DetachVerifiedAccessTrustProviderResult)
      Log.info { "performing 'DetachVerifiedAccessTrustProvider' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DetachVerifiedAccessTrustProvider")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DetachVerifiedAccessTrustProvider", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DetachVerifiedAccessTrustProviderResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DetachVerifiedAccessTrustProviderResult).new(response, result)
    end

    # Detaches an EBS volume from an instance. Make sure to unmount any file systems on the device
    # within your operating system before detaching the volume. Failure to do so can result in the
    # volume becoming stuck in the `busy` state while detaching. If this happens, detachment can be
    # delayed indefinitely until you unmount the volume, force detachment, reboot the instance, or all
    # three. If an EBS volume is the root device of an instance, it can't be detached while the
    # instance is running. To detach the root volume, stop the instance first.
    #
    # When a volume with an Amazon Web Services Marketplace product code is detached from an instance,
    # the product code is no longer associated with the instance.
    #
    # You can't detach or force detach volumes that are attached to Amazon Web Services-managed
    # resources. Attempting to do this results in the `UnsupportedOperationException` exception.
    #
    # For more information, see [Detach an Amazon EBS
    # volume](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-detaching-volume.html) in the
    # *Amazon EBS User Guide*.
    def detach_volume(input : AEC::DetachVolumeRequest) : Core::ParsedResponse(AEC::VolumeAttachment)
      Log.info { "performing 'DetachVolume' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DetachVolume")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DetachVolume", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::VolumeAttachment.from_xml(result_node)
      Core::ParsedResponse(AEC::VolumeAttachment).new(response, result)
    end

    # Detaches a virtual private gateway from a VPC. You do this if you're planning to turn off the
    # VPC and not use it anymore. You can confirm a virtual private gateway has been completely
    # detached from a VPC by describing the virtual private gateway (any attachments to the virtual
    # private gateway are also described).
    #
    # You must wait for the attachment's state to switch to `detached` before you can delete the VPC
    # or attach a different VPC to the virtual private gateway.
    def detach_vpn_gateway(input : AEC::DetachVpnGatewayRequest) : Core::Response
      Log.info { "performing 'DetachVpnGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DetachVpnGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DetachVpnGateway", body)
      Core::Response.new(response)
    end

    # Disables Elastic IP address transfer. For more information, see [Transfer Elastic IP
    # addresses](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-eips.html#transfer-EIPs-intro)
    # in the *Amazon VPC User Guide*.
    def disable_address_transfer(input : AEC::DisableAddressTransferRequest) : Core::ParsedResponse(AEC::DisableAddressTransferResult)
      Log.info { "performing 'DisableAddressTransfer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableAddressTransfer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableAddressTransfer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableAddressTransferResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableAddressTransferResult).new(response, result)
    end

    # Disables Allowed AMIs for your account in the specified Amazon Web Services Region. When set to
    # `disabled`, the image criteria in your Allowed AMIs settings do not apply, and no restrictions
    # are placed on AMI discoverability or usage. Users in your account can launch instances using any
    # public AMI or AMI shared with your account.
    #
    # The Allowed AMIs feature does not restrict the AMIs owned by your account. Regardless of the
    # criteria you set, the AMIs created by your account will always be discoverable and usable by
    # users in your account.
    #
    # For more information, see [Control the discovery and use of AMIs in Amazon EC2 with Allowed
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-allowed-amis.html) in *Amazon EC2
    # User Guide*.
    def disable_allowed_images_settings(input : AEC::DisableAllowedImagesSettingsRequest) : Core::ParsedResponse(AEC::DisableAllowedImagesSettingsResult)
      Log.info { "performing 'DisableAllowedImagesSettings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableAllowedImagesSettings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableAllowedImagesSettings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableAllowedImagesSettingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableAllowedImagesSettingsResult).new(response, result)
    end

    # Disables suppression of application status checks for the specified instances. After suppression
    # is disabled, health check results resume affecting the instance-level application status. You
    # can specify a maximum of 100 instance IDs for each request.
    def disable_application_status_check_suppression(input : AEC::DisableApplicationStatusCheckSuppressionRequest) : Core::ParsedResponse(AEC::DisableApplicationStatusCheckSuppressionResult)
      Log.info { "performing 'DisableApplicationStatusCheckSuppression' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableApplicationStatusCheckSuppression")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableApplicationStatusCheckSuppression", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableApplicationStatusCheckSuppressionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableApplicationStatusCheckSuppressionResult).new(response, result)
    end

    # Disables Infrastructure Performance metric subscriptions.
    def disable_aws_network_performance_metric_subscription(input : AEC::DisableAwsNetworkPerformanceMetricSubscriptionRequest) : Core::ParsedResponse(AEC::DisableAwsNetworkPerformanceMetricSubscriptionResult)
      Log.info { "performing 'DisableAwsNetworkPerformanceMetricSubscription' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableAwsNetworkPerformanceMetricSubscription")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableAwsNetworkPerformanceMetricSubscription", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableAwsNetworkPerformanceMetricSubscriptionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableAwsNetworkPerformanceMetricSubscriptionResult).new(response, result)
    end

    # Disables EC2 Capacity Manager for your account. This stops data ingestion and removes access to
    # capacity analytics and optimization recommendations. Previously collected data is retained but
    # no new data will be processed.
    def disable_capacity_manager(input : AEC::DisableCapacityManagerRequest) : Core::ParsedResponse(AEC::DisableCapacityManagerResult)
      Log.info { "performing 'DisableCapacityManager' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableCapacityManager")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableCapacityManager", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableCapacityManagerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableCapacityManagerResult).new(response, result)
    end

    # Disables EBS encryption by default for your account in the current Region.
    #
    # After you disable encryption by default, you can still create encrypted volumes by enabling
    # encryption when you create each volume.
    #
    # Disabling encryption by default does not change the encryption status of your existing volumes.
    #
    # For more information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    def disable_ebs_encryption_by_default(input : AEC::DisableEbsEncryptionByDefaultRequest) : Core::ParsedResponse(AEC::DisableEbsEncryptionByDefaultResult)
      Log.info { "performing 'DisableEbsEncryptionByDefault' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableEbsEncryptionByDefault")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableEbsEncryptionByDefault", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableEbsEncryptionByDefaultResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableEbsEncryptionByDefaultResult).new(response, result)
    end

    # Discontinue Windows fast launch for a Windows AMI, and clean up existing pre-provisioned
    # snapshots. After you disable Windows fast launch, the AMI uses the standard launch process for
    # each new instance. Amazon EC2 must remove all pre-provisioned snapshots before you can enable
    # Windows fast launch again.
    #
    # You can only change these settings for Windows AMIs that you own or that have been shared with
    # you.
    def disable_fast_launch(input : AEC::DisableFastLaunchRequest) : Core::ParsedResponse(AEC::DisableFastLaunchResult)
      Log.info { "performing 'DisableFastLaunch' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableFastLaunch")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableFastLaunch", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableFastLaunchResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableFastLaunchResult).new(response, result)
    end

    # Disables fast snapshot restores for the specified snapshots in the specified Availability Zones.
    def disable_fast_snapshot_restores(input : AEC::DisableFastSnapshotRestoresRequest) : Core::ParsedResponse(AEC::DisableFastSnapshotRestoresResult)
      Log.info { "performing 'DisableFastSnapshotRestores' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableFastSnapshotRestores")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableFastSnapshotRestores", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableFastSnapshotRestoresResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableFastSnapshotRestoresResult).new(response, result)
    end

    # Sets the AMI state to `disabled` and removes all launch permissions from the AMI. A disabled AMI
    # can't be used for instance launches.
    #
    # A disabled AMI can't be shared. If an AMI was public or previously shared, it is made private.
    # If an AMI was shared with an Amazon Web Services account, organization, or Organizational Unit,
    # they lose access to the disabled AMI.
    #
    # A disabled AMI does not appear in
    # [DescribeImages](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeImages.html)
    # API calls by default.
    #
    # Only the AMI owner can disable an AMI.
    #
    # You can re-enable a disabled AMI using
    # [EnableImage](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_EnableImage.html).
    #
    # For more information, see [Disable an
    # AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/disable-an-ami.html) in the *Amazon EC2
    # User Guide*.
    def disable_image(input : AEC::DisableImageRequest) : Core::ParsedResponse(AEC::DisableImageResult)
      Log.info { "performing 'DisableImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableImageResult).new(response, result)
    end

    # Disables *block public access for AMIs* at the account level in the specified Amazon Web
    # Services Region. This removes the *block public access* restriction from your account. With the
    # restriction removed, you can publicly share your AMIs in the specified Amazon Web Services
    # Region.
    #
    # For more information, see [Block public access to your
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/block-public-access-to-amis.html) in
    # the *Amazon EC2 User Guide*.
    def disable_image_block_public_access(input : AEC::DisableImageBlockPublicAccessRequest) : Core::ParsedResponse(AEC::DisableImageBlockPublicAccessResult)
      Log.info { "performing 'DisableImageBlockPublicAccess' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableImageBlockPublicAccess")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableImageBlockPublicAccess", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableImageBlockPublicAccessResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableImageBlockPublicAccessResult).new(response, result)
    end

    # Cancels the deprecation of the specified AMI.
    #
    # For more information, see [Deprecate an Amazon EC2
    # AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-deprecate.html) in the *Amazon EC2
    # User Guide*.
    def disable_image_deprecation(input : AEC::DisableImageDeprecationRequest) : Core::ParsedResponse(AEC::DisableImageDeprecationResult)
      Log.info { "performing 'DisableImageDeprecation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableImageDeprecation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableImageDeprecation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableImageDeprecationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableImageDeprecationResult).new(response, result)
    end

    # Disables deregistration protection for an AMI. When deregistration protection is disabled, the
    # AMI can be deregistered.
    #
    # If you chose to include a 24-hour cooldown period when you enabled deregistration protection for
    # the AMI, then, when you disable deregistration protection, you won’t immediately be able to
    # deregister the AMI.
    #
    # For more information, see [Protect an Amazon EC2 AMI from
    # deregistration](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-deregistration-protection.html)
    # in the *Amazon EC2 User Guide*.
    def disable_image_deregistration_protection(input : AEC::DisableImageDeregistrationProtectionRequest) : Core::ParsedResponse(AEC::DisableImageDeregistrationProtectionResult)
      Log.info { "performing 'DisableImageDeregistrationProtection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableImageDeregistrationProtection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableImageDeregistrationProtection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableImageDeregistrationProtectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableImageDeregistrationProtectionResult).new(response, result)
    end

    # Disable Amazon EC2 instances running in an SQL Server High Availability cluster from SQL Server
    # High Availability instance standby detection monitoring. Once disabled, Amazon Web Services no
    # longer monitors the metadata for the instances to determine whether they are active or standby
    # nodes in the SQL Server High Availability cluster.
    def disable_instance_sql_ha_standby_detections(input : AEC::DisableInstanceSqlHaStandbyDetectionsRequest) : Core::ParsedResponse(AEC::DisableInstanceSqlHaStandbyDetectionsResult)
      Log.info { "performing 'DisableInstanceSqlHaStandbyDetections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableInstanceSqlHaStandbyDetections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableInstanceSqlHaStandbyDetections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableInstanceSqlHaStandbyDetectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableInstanceSqlHaStandbyDetectionsResult).new(response, result)
    end

    # Disable the IPAM account. For more information, see [Enable integration with
    # Organizations](https://docs.aws.amazon.com/vpc/latest/ipam/enable-integ-ipam.html) in the
    # *Amazon VPC IPAM User Guide*.
    def disable_ipam_organization_admin_account(input : AEC::DisableIpamOrganizationAdminAccountRequest) : Core::ParsedResponse(AEC::DisableIpamOrganizationAdminAccountResult)
      Log.info { "performing 'DisableIpamOrganizationAdminAccount' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableIpamOrganizationAdminAccount")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableIpamOrganizationAdminAccount", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableIpamOrganizationAdminAccountResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableIpamOrganizationAdminAccountResult).new(response, result)
    end

    # Disables an IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    def disable_ipam_policy(input : AEC::DisableIpamPolicyRequest) : Core::ParsedResponse(AEC::DisableIpamPolicyResult)
      Log.info { "performing 'DisableIpamPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableIpamPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableIpamPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableIpamPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableIpamPolicyResult).new(response, result)
    end

    # Disables route propagation from a route server to a specified route table.
    #
    # When enabled, route server propagation installs the routes in the FIB on the route table you've
    # specified. Route server supports IPv4 and IPv6 route propagation.
    #
    # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed
    # within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates
    # VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing
    # fault tolerance for those workloads. This enables you to automatically reroute traffic within a
    # VPC, which increases the manageability of VPC routing and interoperability with third-party
    # workloads.
    #
    # Route server supports the follow route table types:
    #
    # - VPC route tables not associated with subnets
    #
    # - Subnet route tables
    #
    # - Internet gateway route tables
    #
    # Route server does not support route tables associated with virtual private gateways. To
    # propagate routes into a transit gateway route table, use [Transit Gateway
    # Connect](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html).
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def disable_route_server_propagation(input : AEC::DisableRouteServerPropagationRequest) : Core::ParsedResponse(AEC::DisableRouteServerPropagationResult)
      Log.info { "performing 'DisableRouteServerPropagation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableRouteServerPropagation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableRouteServerPropagation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableRouteServerPropagationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableRouteServerPropagationResult).new(response, result)
    end

    # Disables access to the EC2 serial console of all instances for your account. By default, access
    # to the EC2 serial console is disabled for your account. For more information, see [Manage
    # account access to the EC2 serial
    # console](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configure-access-to-serial-console.html#serial-console-account-access)
    # in the *Amazon EC2 User Guide*.
    def disable_serial_console_access(input : AEC::DisableSerialConsoleAccessRequest) : Core::ParsedResponse(AEC::DisableSerialConsoleAccessResult)
      Log.info { "performing 'DisableSerialConsoleAccess' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableSerialConsoleAccess")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableSerialConsoleAccess", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableSerialConsoleAccessResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableSerialConsoleAccessResult).new(response, result)
    end

    # Disables the *block public access for snapshots* setting at the account level for the specified
    # Amazon Web Services Region. After you disable block public access for snapshots in a Region,
    # users can publicly share snapshots in that Region.
    #
    # Enabling block public access for snapshots in *block-all-sharing* mode does not change the
    # permissions for snapshots that are already publicly shared. Instead, it prevents these snapshots
    # from be publicly visible and publicly accessible. Therefore, the attributes for these snapshots
    # still indicate that they are publicly shared, even though they are not publicly available.
    #
    # If you disable block public access , these snapshots will become publicly available again.
    #
    # For more information, see [ Block public access for
    # snapshots](https://docs.aws.amazon.com/ebs/latest/userguide/block-public-access-snapshots.html)
    # in the *Amazon EBS User Guide* .
    def disable_snapshot_block_public_access(input : AEC::DisableSnapshotBlockPublicAccessRequest) : Core::ParsedResponse(AEC::DisableSnapshotBlockPublicAccessResult)
      Log.info { "performing 'DisableSnapshotBlockPublicAccess' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableSnapshotBlockPublicAccess")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableSnapshotBlockPublicAccess", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableSnapshotBlockPublicAccessResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableSnapshotBlockPublicAccessResult).new(response, result)
    end

    # Disables the specified resource attachment from propagating routes to the specified propagation
    # route table.
    def disable_transit_gateway_route_table_propagation(input : AEC::DisableTransitGatewayRouteTablePropagationRequest) : Core::ParsedResponse(AEC::DisableTransitGatewayRouteTablePropagationResult)
      Log.info { "performing 'DisableTransitGatewayRouteTablePropagation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableTransitGatewayRouteTablePropagation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableTransitGatewayRouteTablePropagation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableTransitGatewayRouteTablePropagationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableTransitGatewayRouteTablePropagationResult).new(response, result)
    end

    # Disables a virtual private gateway (VGW) from propagating routes to a specified route table of a
    # VPC.
    def disable_vgw_route_propagation(input : AEC::DisableVgwRoutePropagationRequest) : Core::Response
      Log.info { "performing 'DisableVgwRoutePropagation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableVgwRoutePropagation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableVgwRoutePropagation", body)
      Core::Response.new(response)
    end

    # This action is deprecated.
    #
    # Disables ClassicLink for a VPC. You cannot disable ClassicLink for a VPC that has EC2-Classic
    # instances linked to it.
    def disable_vpc_classic_link(input : AEC::DisableVpcClassicLinkRequest) : Core::ParsedResponse(AEC::DisableVpcClassicLinkResult)
      Log.info { "performing 'DisableVpcClassicLink' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableVpcClassicLink")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableVpcClassicLink", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableVpcClassicLinkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableVpcClassicLinkResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Disables ClassicLink DNS support for a VPC. If disabled, DNS hostnames resolve to public IP
    # addresses when addressed between a linked EC2-Classic instance and instances in the VPC to which
    # it's linked.
    #
    # You must specify a VPC ID in the request.
    def disable_vpc_classic_link_dns_support(input : AEC::DisableVpcClassicLinkDnsSupportRequest) : Core::ParsedResponse(AEC::DisableVpcClassicLinkDnsSupportResult)
      Log.info { "performing 'DisableVpcClassicLinkDnsSupport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisableVpcClassicLinkDnsSupport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisableVpcClassicLinkDnsSupport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisableVpcClassicLinkDnsSupportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisableVpcClassicLinkDnsSupportResult).new(response, result)
    end

    # Disassociates an Elastic IP address from the instance or network interface it's associated with.
    #
    # This is an idempotent operation. If you perform the operation more than once, Amazon EC2 doesn't
    # return an error.
    #
    # An address cannot be disassociated if the all of the following conditions are met:
    #
    # - Network interface has a `publicDualStackDnsName` publicDnsName
    #
    # - Public IPv4 address is the primary public IPv4 address
    #
    # - Network interface only has one remaining public IPv4 address
    def disassociate_address(input : AEC::DisassociateAddressRequest) : Core::Response
      Log.info { "performing 'DisassociateAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateAddress", body)
      Core::Response.new(response)
    end

    # Disassociates an application status check from instances or
    # [tags](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html). After
    # disassociation, health monitoring stops for the affected instances. The following rules apply:
    #
    # - You must specify either `TargetTagAssociations` or `InstanceIds`, but not both. Specifying
    # both results in an `InvalidParameterCombination` error.
    #
    # - The application status check must already exist and belong to your account.
    #
    # - Tag keys must not be blank.
    def disassociate_application_status_check(input : AEC::DisassociateApplicationStatusCheckRequest) : Core::ParsedResponse(AEC::DisassociateApplicationStatusCheckResult)
      Log.info { "performing 'DisassociateApplicationStatusCheck' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateApplicationStatusCheck")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateApplicationStatusCheck", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateApplicationStatusCheckResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateApplicationStatusCheckResult).new(response, result)
    end

    # Cancels a pending request to assign billing of the unused capacity of a Capacity Reservation to
    # a consumer account, or revokes a request that has already been accepted. For more information,
    # see [Billing assignment for shared Amazon EC2 Capacity
    # Reservations](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/assign-billing.html).
    def disassociate_capacity_reservation_billing_owner(input : AEC::DisassociateCapacityReservationBillingOwnerRequest) : Core::ParsedResponse(AEC::DisassociateCapacityReservationBillingOwnerResult)
      Log.info { "performing 'DisassociateCapacityReservationBillingOwner' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateCapacityReservationBillingOwner")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateCapacityReservationBillingOwner", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateCapacityReservationBillingOwnerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateCapacityReservationBillingOwnerResult).new(response, result)
    end

    # Disassociates a target network from the specified Client VPN endpoint. When you disassociate the
    # last target network from a Client VPN, the following happens:
    #
    # - The route that was automatically added for the VPC is deleted
    #
    # - All active client connections are terminated
    #
    # - New client connections are disallowed
    #
    # - The Client VPN endpoint's status changes to `pending-associate`
    def disassociate_client_vpn_target_network(input : AEC::DisassociateClientVpnTargetNetworkRequest) : Core::ParsedResponse(AEC::DisassociateClientVpnTargetNetworkResult)
      Log.info { "performing 'DisassociateClientVpnTargetNetwork' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateClientVpnTargetNetwork")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateClientVpnTargetNetwork", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateClientVpnTargetNetworkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateClientVpnTargetNetworkResult).new(response, result)
    end

    # Disassociates an IAM role from an Certificate Manager (ACM) certificate. Disassociating an IAM
    # role from an ACM certificate removes the Amazon S3 object that contains the certificate,
    # certificate chain, and encrypted private key from the Amazon S3 bucket. It also revokes the IAM
    # role's permission to use the KMS key used to encrypt the private key. This effectively revokes
    # the role's permission to use the certificate.
    def disassociate_enclave_certificate_iam_role(input : AEC::DisassociateEnclaveCertificateIamRoleRequest) : Core::ParsedResponse(AEC::DisassociateEnclaveCertificateIamRoleResult)
      Log.info { "performing 'DisassociateEnclaveCertificateIamRole' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateEnclaveCertificateIamRole")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateEnclaveCertificateIamRole", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateEnclaveCertificateIamRoleResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateEnclaveCertificateIamRoleResult).new(response, result)
    end

    # Disassociates an IAM instance profile from a running or stopped instance.
    #
    # Use DescribeIamInstanceProfileAssociations to get the association ID.
    def disassociate_iam_instance_profile(input : AEC::DisassociateIamInstanceProfileRequest) : Core::ParsedResponse(AEC::DisassociateIamInstanceProfileResult)
      Log.info { "performing 'DisassociateIamInstanceProfile' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateIamInstanceProfile")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateIamInstanceProfile", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateIamInstanceProfileResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateIamInstanceProfileResult).new(response, result)
    end

    # Disassociates one or more targets from an event window.
    #
    # For more information, see [Define event windows for scheduled
    # events](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/event-windows.html) in the *Amazon
    # EC2 User Guide*.
    def disassociate_instance_event_window(input : AEC::DisassociateInstanceEventWindowRequest) : Core::ParsedResponse(AEC::DisassociateInstanceEventWindowResult)
      Log.info { "performing 'DisassociateInstanceEventWindow' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateInstanceEventWindow")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateInstanceEventWindow", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateInstanceEventWindowResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateInstanceEventWindowResult).new(response, result)
    end

    # Remove the association between your Autonomous System Number (ASN) and your BYOIP CIDR. You may
    # want to use this action to disassociate an ASN from a CIDR or if you want to swap ASNs. For more
    # information, see [Tutorial: Bring your ASN to
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/tutorials-byoasn.html) in the *Amazon VPC IPAM
    # guide*.
    def disassociate_ipam_byoasn(input : AEC::DisassociateIpamByoasnRequest) : Core::ParsedResponse(AEC::DisassociateIpamByoasnResult)
      Log.info { "performing 'DisassociateIpamByoasn' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateIpamByoasn")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateIpamByoasn", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateIpamByoasnResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateIpamByoasnResult).new(response, result)
    end

    # Disassociates a resource discovery from an Amazon VPC IPAM. A resource discovery is an IPAM
    # component that enables IPAM to manage and monitor resources that belong to the owning account.
    def disassociate_ipam_resource_discovery(input : AEC::DisassociateIpamResourceDiscoveryRequest) : Core::ParsedResponse(AEC::DisassociateIpamResourceDiscoveryResult)
      Log.info { "performing 'DisassociateIpamResourceDiscovery' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateIpamResourceDiscovery")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateIpamResourceDiscovery", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateIpamResourceDiscoveryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateIpamResourceDiscoveryResult).new(response, result)
    end

    # Disassociates secondary Elastic IP addresses (EIPs) from a public NAT gateway. You cannot
    # disassociate your primary EIP. For more information, see [Edit secondary IP address
    # associations](https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateway-working-with.html#nat-gateway-edit-secondary)
    # in the *Amazon VPC User Guide*.
    #
    # While disassociating is in progress, you cannot associate/disassociate additional EIPs while the
    # connections are being drained. You are, however, allowed to delete the NAT gateway.
    #
    # An EIP is released only at the end of MaxDrainDurationSeconds. It stays associated and supports
    # the existing connections but does not support any new connections (new connections are
    # distributed across the remaining associated EIPs). As the existing connections drain out, the
    # EIPs (and the corresponding private IP addresses mapped to them) are released.
    def disassociate_nat_gateway_address(input : AEC::DisassociateNatGatewayAddressRequest) : Core::ParsedResponse(AEC::DisassociateNatGatewayAddressResult)
      Log.info { "performing 'DisassociateNatGatewayAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateNatGatewayAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateNatGatewayAddress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateNatGatewayAddressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateNatGatewayAddressResult).new(response, result)
    end

    # Disassociates a route server from a VPC.
    #
    # A route server association is the connection established between a route server and a VPC.
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def disassociate_route_server(input : AEC::DisassociateRouteServerRequest) : Core::ParsedResponse(AEC::DisassociateRouteServerResult)
      Log.info { "performing 'DisassociateRouteServer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateRouteServer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateRouteServer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateRouteServerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateRouteServerResult).new(response, result)
    end

    # Disassociates a subnet or gateway from a route table.
    #
    # After you perform this action, the subnet no longer uses the routes in the route table. Instead,
    # it uses the routes in the VPC's main route table. For more information about route tables, see
    # [Route tables](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html) in the
    # *Amazon VPC User Guide*.
    def disassociate_route_table(input : AEC::DisassociateRouteTableRequest) : Core::Response
      Log.info { "performing 'DisassociateRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateRouteTable", body)
      Core::Response.new(response)
    end

    # Disassociates a security group from a VPC. You cannot disassociate the security group if any
    # Elastic network interfaces in the associated VPC are still associated with the security group.
    # Note that the disassociation is asynchronous and you can check the status of the request with
    # [DescribeSecurityGroupVpcAssociations](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeSecurityGroupVpcAssociations.html).
    def disassociate_security_group_vpc(input : AEC::DisassociateSecurityGroupVpcRequest) : Core::ParsedResponse(AEC::DisassociateSecurityGroupVpcResult)
      Log.info { "performing 'DisassociateSecurityGroupVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateSecurityGroupVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateSecurityGroupVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateSecurityGroupVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateSecurityGroupVpcResult).new(response, result)
    end

    # Disassociates a CIDR block from a subnet. Currently, you can disassociate an IPv6 CIDR block
    # only. You must detach or delete all gateways and resources that are associated with the CIDR
    # block before you can disassociate it.
    def disassociate_subnet_cidr_block(input : AEC::DisassociateSubnetCidrBlockRequest) : Core::ParsedResponse(AEC::DisassociateSubnetCidrBlockResult)
      Log.info { "performing 'DisassociateSubnetCidrBlock' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateSubnetCidrBlock")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateSubnetCidrBlock", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateSubnetCidrBlockResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateSubnetCidrBlockResult).new(response, result)
    end

    # Disassociates the specified subnets from the transit gateway multicast domain.
    def disassociate_transit_gateway_multicast_domain(input : AEC::DisassociateTransitGatewayMulticastDomainRequest) : Core::ParsedResponse(AEC::DisassociateTransitGatewayMulticastDomainResult)
      Log.info { "performing 'DisassociateTransitGatewayMulticastDomain' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateTransitGatewayMulticastDomain")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateTransitGatewayMulticastDomain", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateTransitGatewayMulticastDomainResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateTransitGatewayMulticastDomainResult).new(response, result)
    end

    # Removes the association between an an attachment and a policy table.
    def disassociate_transit_gateway_policy_table(input : AEC::DisassociateTransitGatewayPolicyTableRequest) : Core::ParsedResponse(AEC::DisassociateTransitGatewayPolicyTableResult)
      Log.info { "performing 'DisassociateTransitGatewayPolicyTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateTransitGatewayPolicyTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateTransitGatewayPolicyTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateTransitGatewayPolicyTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateTransitGatewayPolicyTableResult).new(response, result)
    end

    # Disassociates a resource attachment from a transit gateway route table.
    def disassociate_transit_gateway_route_table(input : AEC::DisassociateTransitGatewayRouteTableRequest) : Core::ParsedResponse(AEC::DisassociateTransitGatewayRouteTableResult)
      Log.info { "performing 'DisassociateTransitGatewayRouteTable' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateTransitGatewayRouteTable")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateTransitGatewayRouteTable", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateTransitGatewayRouteTableResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateTransitGatewayRouteTableResult).new(response, result)
    end

    # Removes an association between a branch network interface with a trunk network interface.
    def disassociate_trunk_interface(input : AEC::DisassociateTrunkInterfaceRequest) : Core::ParsedResponse(AEC::DisassociateTrunkInterfaceResult)
      Log.info { "performing 'DisassociateTrunkInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateTrunkInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateTrunkInterface", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateTrunkInterfaceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateTrunkInterfaceResult).new(response, result)
    end

    # Disassociates a CIDR block from a VPC. To disassociate the CIDR block, you must specify its
    # association ID. You can get the association ID by using DescribeVpcs. You must detach or delete
    # all gateways and resources that are associated with the CIDR block before you can disassociate
    # it.
    #
    # You cannot disassociate the CIDR block with which you originally created the VPC (the primary
    # CIDR block).
    def disassociate_vpc_cidr_block(input : AEC::DisassociateVpcCidrBlockRequest) : Core::ParsedResponse(AEC::DisassociateVpcCidrBlockResult)
      Log.info { "performing 'DisassociateVpcCidrBlock' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "DisassociateVpcCidrBlock")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("DisassociateVpcCidrBlock", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::DisassociateVpcCidrBlockResult.from_xml(result_node)
      Core::ParsedResponse(AEC::DisassociateVpcCidrBlockResult).new(response, result)
    end

    # Enables Elastic IP address transfer. For more information, see [Transfer Elastic IP
    # addresses](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-eips.html#transfer-EIPs-intro)
    # in the *Amazon VPC User Guide*.
    def enable_address_transfer(input : AEC::EnableAddressTransferRequest) : Core::ParsedResponse(AEC::EnableAddressTransferResult)
      Log.info { "performing 'EnableAddressTransfer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableAddressTransfer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableAddressTransfer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableAddressTransferResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableAddressTransferResult).new(response, result)
    end

    # Enables Allowed AMIs for your account in the specified Amazon Web Services Region. Two values
    # are accepted:
    #
    # - `enabled`: The image criteria in your Allowed AMIs settings are applied. As a result, only
    # AMIs matching these criteria are discoverable and can be used by your account to launch
    # instances.
    #
    # - `audit-mode`: The image criteria in your Allowed AMIs settings are not applied. No
    # restrictions are placed on AMI discoverability or usage. Users in your account can launch
    # instances using any public AMI or AMI shared with your account.
    #
    # The purpose of `audit-mode` is to indicate which AMIs will be affected when Allowed AMIs is
    # `enabled`. In `audit-mode`, each AMI displays either `"ImageAllowed": true` or `"ImageAllowed":
    # false` to indicate whether the AMI will be discoverable and available to users in the account
    # when Allowed AMIs is enabled.
    #
    # The Allowed AMIs feature does not restrict the AMIs owned by your account. Regardless of the
    # criteria you set, the AMIs created by your account will always be discoverable and usable by
    # users in your account.
    #
    # For more information, see [Control the discovery and use of AMIs in Amazon EC2 with Allowed
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-allowed-amis.html) in *Amazon EC2
    # User Guide*.
    def enable_allowed_images_settings(input : AEC::EnableAllowedImagesSettingsRequest) : Core::ParsedResponse(AEC::EnableAllowedImagesSettingsResult)
      Log.info { "performing 'EnableAllowedImagesSettings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableAllowedImagesSettings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableAllowedImagesSettings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableAllowedImagesSettingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableAllowedImagesSettingsResult).new(response, result)
    end

    # Suppresses application status checks for the specified instances. While suppressed, health
    # checks continue to run but do not affect the instance-level application status. The following
    # rules apply:
    #
    # - You can specify a maximum of 100 instance IDs for each request.
    #
    # - Use `DisableApplicationStatusCheckSuppression` to resume normal health check reporting.
    #
    # - If you do not specify `DurationSeconds`, suppression continues indefinitely until you call
    # `DisableApplicationStatusCheckSuppression`.
    def enable_application_status_check_suppression(input : AEC::EnableApplicationStatusCheckSuppressionRequest) : Core::ParsedResponse(AEC::EnableApplicationStatusCheckSuppressionResult)
      Log.info { "performing 'EnableApplicationStatusCheckSuppression' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableApplicationStatusCheckSuppression")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableApplicationStatusCheckSuppression", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableApplicationStatusCheckSuppressionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableApplicationStatusCheckSuppressionResult).new(response, result)
    end

    # Enables Infrastructure Performance subscriptions.
    def enable_aws_network_performance_metric_subscription(input : AEC::EnableAwsNetworkPerformanceMetricSubscriptionRequest) : Core::ParsedResponse(AEC::EnableAwsNetworkPerformanceMetricSubscriptionResult)
      Log.info { "performing 'EnableAwsNetworkPerformanceMetricSubscription' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableAwsNetworkPerformanceMetricSubscription")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableAwsNetworkPerformanceMetricSubscription", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableAwsNetworkPerformanceMetricSubscriptionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableAwsNetworkPerformanceMetricSubscriptionResult).new(response, result)
    end

    # Enables EC2 Capacity Manager for your account. This starts data ingestion for your EC2 capacity
    # usage across On-Demand, Spot, and Capacity Reservations. Initial data processing may take
    # several hours to complete.
    def enable_capacity_manager(input : AEC::EnableCapacityManagerRequest) : Core::ParsedResponse(AEC::EnableCapacityManagerResult)
      Log.info { "performing 'EnableCapacityManager' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableCapacityManager")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableCapacityManager", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableCapacityManagerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableCapacityManagerResult).new(response, result)
    end

    # Enables EBS encryption by default for your account in the current Region.
    #
    # After you enable encryption by default, the EBS volumes that you create are always encrypted,
    # either using the default KMS key or the KMS key that you specified when you created each volume.
    # For more information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    #
    # Enabling encryption by default has no effect on the encryption status of your existing volumes.
    #
    # After you enable encryption by default, you can no longer launch instances using instance types
    # that do not support encryption. For more information, see [Supported instance
    # types](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption-requirements.html#ebs-encryption_supported_instances).
    def enable_ebs_encryption_by_default(input : AEC::EnableEbsEncryptionByDefaultRequest) : Core::ParsedResponse(AEC::EnableEbsEncryptionByDefaultResult)
      Log.info { "performing 'EnableEbsEncryptionByDefault' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableEbsEncryptionByDefault")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableEbsEncryptionByDefault", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableEbsEncryptionByDefaultResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableEbsEncryptionByDefaultResult).new(response, result)
    end

    # When you enable Windows fast launch for a Windows AMI, images are pre-provisioned, using
    # snapshots to launch instances up to 65% faster. To create the optimized Windows image, Amazon
    # EC2 launches an instance and runs through Sysprep steps, rebooting as required. Then it creates
    # a set of reserved snapshots that are used for subsequent launches. The reserved snapshots are
    # automatically replenished as they are used, depending on your settings for launch frequency.
    #
    # You can only change these settings for Windows AMIs that you own or that have been shared with
    # you.
    def enable_fast_launch(input : AEC::EnableFastLaunchRequest) : Core::ParsedResponse(AEC::EnableFastLaunchResult)
      Log.info { "performing 'EnableFastLaunch' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableFastLaunch")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableFastLaunch", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableFastLaunchResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableFastLaunchResult).new(response, result)
    end

    # Enables fast snapshot restores for the specified snapshots in the specified Availability Zones.
    #
    # You get the full benefit of fast snapshot restores after they enter the `enabled` state.
    #
    # For more information, see [Amazon EBS fast snapshot
    # restore](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-fast-snapshot-restore.html) in the
    # *Amazon EBS User Guide*.
    def enable_fast_snapshot_restores(input : AEC::EnableFastSnapshotRestoresRequest) : Core::ParsedResponse(AEC::EnableFastSnapshotRestoresResult)
      Log.info { "performing 'EnableFastSnapshotRestores' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableFastSnapshotRestores")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableFastSnapshotRestores", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableFastSnapshotRestoresResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableFastSnapshotRestoresResult).new(response, result)
    end

    # Re-enables a disabled AMI. The re-enabled AMI is marked as `available` and can be used for
    # instance launches, appears in describe operations, and can be shared. Amazon Web Services
    # accounts, organizations, and Organizational Units that lost access to the AMI when it was
    # disabled do not regain access automatically. Once the AMI is available, it can be shared with
    # them again.
    #
    # Only the AMI owner can re-enable a disabled AMI.
    #
    # For more information, see [Disable an Amazon EC2
    # AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/disable-an-ami.html) in the *Amazon EC2
    # User Guide*.
    def enable_image(input : AEC::EnableImageRequest) : Core::ParsedResponse(AEC::EnableImageResult)
      Log.info { "performing 'EnableImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableImageResult).new(response, result)
    end

    # Enables *block public access for AMIs* at the account level in the specified Amazon Web Services
    # Region. This prevents the public sharing of your AMIs. However, if you already have public AMIs,
    # they will remain publicly available.
    #
    # The API can take up to 10 minutes to configure this setting. During this time, if you run
    # [GetImageBlockPublicAccessState](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_GetImageBlockPublicAccessState.html),
    # the response will be `unblocked`. When the API has completed the configuration, the response
    # will be `block-new-sharing`.
    #
    # For more information, see [Block public access to your
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/block-public-access-to-amis.html) in
    # the *Amazon EC2 User Guide*.
    def enable_image_block_public_access(input : AEC::EnableImageBlockPublicAccessRequest) : Core::ParsedResponse(AEC::EnableImageBlockPublicAccessResult)
      Log.info { "performing 'EnableImageBlockPublicAccess' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableImageBlockPublicAccess")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableImageBlockPublicAccess", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableImageBlockPublicAccessResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableImageBlockPublicAccessResult).new(response, result)
    end

    # Enables deprecation of the specified AMI at the specified date and time.
    #
    # For more information, see [Deprecate an
    # AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-deprecate.html) in the *Amazon EC2
    # User Guide*.
    def enable_image_deprecation(input : AEC::EnableImageDeprecationRequest) : Core::ParsedResponse(AEC::EnableImageDeprecationResult)
      Log.info { "performing 'EnableImageDeprecation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableImageDeprecation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableImageDeprecation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableImageDeprecationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableImageDeprecationResult).new(response, result)
    end

    # Enables deregistration protection for an AMI. When deregistration protection is enabled, the AMI
    # can't be deregistered.
    #
    # To allow the AMI to be deregistered, you must first disable deregistration protection.
    #
    # For more information, see [Protect an Amazon EC2 AMI from
    # deregistration](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-deregistration-protection.html)
    # in the *Amazon EC2 User Guide*.
    def enable_image_deregistration_protection(input : AEC::EnableImageDeregistrationProtectionRequest) : Core::ParsedResponse(AEC::EnableImageDeregistrationProtectionResult)
      Log.info { "performing 'EnableImageDeregistrationProtection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableImageDeregistrationProtection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableImageDeregistrationProtection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableImageDeregistrationProtectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableImageDeregistrationProtectionResult).new(response, result)
    end

    # Enable Amazon EC2 instances running in an SQL Server High Availability cluster for SQL Server
    # High Availability instance standby detection monitoring. Once enabled, Amazon Web Services
    # monitors the metadata for the instances to determine whether they are active or standby nodes in
    # the SQL Server High Availability cluster. If the instances are determined to be standby failover
    # nodes, Amazon Web Services automatically applies SQL Server licensing fee waiver for those
    # instances.
    #
    # To register an instance, it must be running a Windows SQL Server license-included AMI and have
    # the Amazon Web Services Systems Manager agent installed and running. Only Windows Server 2019
    # and later and SQL Server (Standard and Enterprise editions) 2017 and later are supported. For
    # more information, see [ Prerequisites for using SQL Server High Availability instance standby
    # detection](https://docs.aws.amazon.com/sql-server-ec2/latest/userguide/prerequisites-and-requirements.html).
    def enable_instance_sql_ha_standby_detections(input : AEC::EnableInstanceSqlHaStandbyDetectionsRequest) : Core::ParsedResponse(AEC::EnableInstanceSqlHaStandbyDetectionsResult)
      Log.info { "performing 'EnableInstanceSqlHaStandbyDetections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableInstanceSqlHaStandbyDetections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableInstanceSqlHaStandbyDetections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableInstanceSqlHaStandbyDetectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableInstanceSqlHaStandbyDetectionsResult).new(response, result)
    end

    # Enables Resource Public Key Infrastructure (RPKI) on an existing IPAM internet registry
    # association by providing BGP Public Key Infrastructure (BPKI) certificate details. After
    # enabling, you can create Route Origin Authorizations (ROAs) for prefixes registered with the
    # internet registry.
    def enable_ipam_internet_registry_association(input : AEC::EnableIpamInternetRegistryAssociationRequest) : Core::ParsedResponse(AEC::EnableIpamInternetRegistryAssociationResult)
      Log.info { "performing 'EnableIpamInternetRegistryAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableIpamInternetRegistryAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableIpamInternetRegistryAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableIpamInternetRegistryAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableIpamInternetRegistryAssociationResult).new(response, result)
    end

    # Enable an Organizations member account as the IPAM admin account. You cannot select the
    # Organizations management account as the IPAM admin account. For more information, see [Enable
    # integration with
    # Organizations](https://docs.aws.amazon.com/vpc/latest/ipam/enable-integ-ipam.html) in the
    # *Amazon VPC IPAM User Guide*.
    def enable_ipam_organization_admin_account(input : AEC::EnableIpamOrganizationAdminAccountRequest) : Core::ParsedResponse(AEC::EnableIpamOrganizationAdminAccountResult)
      Log.info { "performing 'EnableIpamOrganizationAdminAccount' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableIpamOrganizationAdminAccount")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableIpamOrganizationAdminAccount", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableIpamOrganizationAdminAccountResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableIpamOrganizationAdminAccountResult).new(response, result)
    end

    # Enables an IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    #
    # For more information, see [Define public IPv4 allocation strategy with IPAM
    # policies](https://docs.aws.amazon.com/vpc/latest/ipam/define-public-ipv4-allocation-strategy-with-ipam-policies.html)
    # in the *Amazon VPC IPAM User Guide*.
    def enable_ipam_policy(input : AEC::EnableIpamPolicyRequest) : Core::ParsedResponse(AEC::EnableIpamPolicyResult)
      Log.info { "performing 'EnableIpamPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableIpamPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableIpamPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableIpamPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableIpamPolicyResult).new(response, result)
    end

    # Establishes a trust relationship between Reachability Analyzer and Organizations. This operation
    # must be performed by the management account for the organization.
    #
    # After you establish a trust relationship, a user in the management account or a delegated
    # administrator account can run a cross-account analysis using resources from the member accounts.
    def enable_reachability_analyzer_organization_sharing(input : AEC::EnableReachabilityAnalyzerOrganizationSharingRequest) : Core::ParsedResponse(AEC::EnableReachabilityAnalyzerOrganizationSharingResult)
      Log.info { "performing 'EnableReachabilityAnalyzerOrganizationSharing' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableReachabilityAnalyzerOrganizationSharing")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableReachabilityAnalyzerOrganizationSharing", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableReachabilityAnalyzerOrganizationSharingResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableReachabilityAnalyzerOrganizationSharingResult).new(response, result)
    end

    # Defines which route tables the route server can update with routes.
    #
    # When enabled, route server propagation installs the routes in the FIB on the route table you've
    # specified. Route server supports IPv4 and IPv6 route propagation.
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def enable_route_server_propagation(input : AEC::EnableRouteServerPropagationRequest) : Core::ParsedResponse(AEC::EnableRouteServerPropagationResult)
      Log.info { "performing 'EnableRouteServerPropagation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableRouteServerPropagation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableRouteServerPropagation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableRouteServerPropagationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableRouteServerPropagationResult).new(response, result)
    end

    # Enables access to the EC2 serial console of all instances for your account. By default, access
    # to the EC2 serial console is disabled for your account. For more information, see [Manage
    # account access to the EC2 serial
    # console](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configure-access-to-serial-console.html#serial-console-account-access)
    # in the *Amazon EC2 User Guide*.
    def enable_serial_console_access(input : AEC::EnableSerialConsoleAccessRequest) : Core::ParsedResponse(AEC::EnableSerialConsoleAccessResult)
      Log.info { "performing 'EnableSerialConsoleAccess' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableSerialConsoleAccess")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableSerialConsoleAccess", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableSerialConsoleAccessResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableSerialConsoleAccessResult).new(response, result)
    end

    # Enables or modifies the *block public access for snapshots* setting at the account level for the
    # specified Amazon Web Services Region. After you enable block public access for snapshots in a
    # Region, users can no longer request public sharing for snapshots in that Region. Snapshots that
    # are already publicly shared are either treated as private or they remain publicly shared,
    # depending on the **State** that you specify.
    #
    # Enabling block public access for snapshots in *block all sharing* mode does not change the
    # permissions for snapshots that are already publicly shared. Instead, it prevents these snapshots
    # from be publicly visible and publicly accessible. Therefore, the attributes for these snapshots
    # still indicate that they are publicly shared, even though they are not publicly available.
    #
    # If you later disable block public access or change the mode to *block new sharing*, these
    # snapshots will become publicly available again.
    #
    # For more information, see [ Block public access for
    # snapshots](https://docs.aws.amazon.com/ebs/latest/userguide/block-public-access-snapshots.html)
    # in the *Amazon EBS User Guide*.
    def enable_snapshot_block_public_access(input : AEC::EnableSnapshotBlockPublicAccessRequest) : Core::ParsedResponse(AEC::EnableSnapshotBlockPublicAccessResult)
      Log.info { "performing 'EnableSnapshotBlockPublicAccess' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableSnapshotBlockPublicAccess")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableSnapshotBlockPublicAccess", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableSnapshotBlockPublicAccessResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableSnapshotBlockPublicAccessResult).new(response, result)
    end

    # Enables the specified attachment to propagate routes to the specified propagation route table.
    def enable_transit_gateway_route_table_propagation(input : AEC::EnableTransitGatewayRouteTablePropagationRequest) : Core::ParsedResponse(AEC::EnableTransitGatewayRouteTablePropagationResult)
      Log.info { "performing 'EnableTransitGatewayRouteTablePropagation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableTransitGatewayRouteTablePropagation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableTransitGatewayRouteTablePropagation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableTransitGatewayRouteTablePropagationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableTransitGatewayRouteTablePropagationResult).new(response, result)
    end

    # Enables a virtual private gateway (VGW) to propagate routes to the specified route table of a
    # VPC.
    def enable_vgw_route_propagation(input : AEC::EnableVgwRoutePropagationRequest) : Core::Response
      Log.info { "performing 'EnableVgwRoutePropagation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableVgwRoutePropagation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableVgwRoutePropagation", body)
      Core::Response.new(response)
    end

    # Enables I/O operations for a volume that had I/O operations disabled because the data on the
    # volume was potentially inconsistent.
    def enable_volume_io(input : AEC::EnableVolumeIORequest) : Core::Response
      Log.info { "performing 'EnableVolumeIO' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableVolumeIO")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableVolumeIO", body)
      Core::Response.new(response)
    end

    # This action is deprecated.
    #
    # Enables a VPC for ClassicLink. You can then link EC2-Classic instances to your
    # ClassicLink-enabled VPC to allow communication over private IP addresses. You cannot enable your
    # VPC for ClassicLink if any of your VPC route tables have existing routes for address ranges
    # within the `10.0.0.0/8` IP address range, excluding local routes for VPCs in the `10.0.0.0/16`
    # and `10.1.0.0/16` IP address ranges.
    def enable_vpc_classic_link(input : AEC::EnableVpcClassicLinkRequest) : Core::ParsedResponse(AEC::EnableVpcClassicLinkResult)
      Log.info { "performing 'EnableVpcClassicLink' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableVpcClassicLink")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableVpcClassicLink", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableVpcClassicLinkResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableVpcClassicLinkResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Enables a VPC to support DNS hostname resolution for ClassicLink. If enabled, the DNS hostname
    # of a linked EC2-Classic instance resolves to its private IP address when addressed from an
    # instance in the VPC to which it's linked. Similarly, the DNS hostname of an instance in a VPC
    # resolves to its private IP address when addressed from a linked EC2-Classic instance.
    #
    # You must specify a VPC ID in the request.
    def enable_vpc_classic_link_dns_support(input : AEC::EnableVpcClassicLinkDnsSupportRequest) : Core::ParsedResponse(AEC::EnableVpcClassicLinkDnsSupportResult)
      Log.info { "performing 'EnableVpcClassicLinkDnsSupport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "EnableVpcClassicLinkDnsSupport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("EnableVpcClassicLinkDnsSupport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::EnableVpcClassicLinkDnsSupportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::EnableVpcClassicLinkDnsSupportResult).new(response, result)
    end

    # Downloads the client certificate revocation list for the specified Client VPN endpoint.
    def export_client_vpn_client_certificate_revocation_list(input : AEC::ExportClientVpnClientCertificateRevocationListRequest) : Core::ParsedResponse(AEC::ExportClientVpnClientCertificateRevocationListResult)
      Log.info { "performing 'ExportClientVpnClientCertificateRevocationList' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ExportClientVpnClientCertificateRevocationList")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ExportClientVpnClientCertificateRevocationList", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ExportClientVpnClientCertificateRevocationListResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ExportClientVpnClientCertificateRevocationListResult).new(response, result)
    end

    # Downloads the contents of the Client VPN endpoint configuration file for the specified Client
    # VPN endpoint. The Client VPN endpoint configuration file includes the Client VPN endpoint and
    # certificate information clients need to establish a connection with the Client VPN endpoint.
    def export_client_vpn_client_configuration(input : AEC::ExportClientVpnClientConfigurationRequest) : Core::ParsedResponse(AEC::ExportClientVpnClientConfigurationResult)
      Log.info { "performing 'ExportClientVpnClientConfiguration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ExportClientVpnClientConfiguration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ExportClientVpnClientConfiguration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ExportClientVpnClientConfigurationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ExportClientVpnClientConfigurationResult).new(response, result)
    end

    # Exports an Amazon Machine Image (AMI) to a VM file. For more information, see [Exporting a VM
    # directly from an Amazon Machine Image
    # (AMI)](https://docs.aws.amazon.com/vm-import/latest/userguide/vmexport_image.html) in the *VM
    # Import/Export User Guide*.
    def export_image(input : AEC::ExportImageRequest) : Core::ParsedResponse(AEC::ExportImageResult)
      Log.info { "performing 'ExportImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ExportImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ExportImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ExportImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ExportImageResult).new(response, result)
    end

    # Exports routes from the specified transit gateway route table to the specified S3 bucket. By
    # default, all routes are exported. Alternatively, you can filter by CIDR range.
    #
    # The routes are saved to the specified bucket in a JSON file. For more information, see [Export
    # route tables to Amazon
    # S3](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-route-tables.html#tgw-export-route-tables) in
    # the *Amazon Web Services Transit Gateways Guide*.
    def export_transit_gateway_routes(input : AEC::ExportTransitGatewayRoutesRequest) : Core::ParsedResponse(AEC::ExportTransitGatewayRoutesResult)
      Log.info { "performing 'ExportTransitGatewayRoutes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ExportTransitGatewayRoutes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ExportTransitGatewayRoutes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ExportTransitGatewayRoutesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ExportTransitGatewayRoutesResult).new(response, result)
    end

    # Exports the client configuration for a Verified Access instance.
    def export_verified_access_instance_client_configuration(input : AEC::ExportVerifiedAccessInstanceClientConfigurationRequest) : Core::ParsedResponse(AEC::ExportVerifiedAccessInstanceClientConfigurationResult)
      Log.info { "performing 'ExportVerifiedAccessInstanceClientConfiguration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ExportVerifiedAccessInstanceClientConfiguration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ExportVerifiedAccessInstanceClientConfiguration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ExportVerifiedAccessInstanceClientConfigurationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ExportVerifiedAccessInstanceClientConfigurationResult).new(response, result)
    end

    # Returns the currently negotiated security parameters for an active VPN tunnel, including IKE
    # version, DH groups, encryption algorithms, and integrity algorithms.
    def get_active_vpn_tunnel_status(input : AEC::GetActiveVpnTunnelStatusRequest) : Core::ParsedResponse(AEC::GetActiveVpnTunnelStatusResult)
      Log.info { "performing 'GetActiveVpnTunnelStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetActiveVpnTunnelStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetActiveVpnTunnelStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetActiveVpnTunnelStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetActiveVpnTunnelStatusResult).new(response, result)
    end

    # Gets the current state of the Allowed AMIs setting and the list of Allowed AMIs criteria at the
    # account level in the specified Region.
    #
    # The Allowed AMIs feature does not restrict the AMIs owned by your account. Regardless of the
    # criteria you set, the AMIs created by your account will always be discoverable and usable by
    # users in your account.
    #
    # For more information, see [Control the discovery and use of AMIs in Amazon EC2 with Allowed
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-allowed-amis.html) in *Amazon EC2
    # User Guide*.
    def get_allowed_images_settings(input : AEC::GetAllowedImagesSettingsRequest) : Core::ParsedResponse(AEC::GetAllowedImagesSettingsResult)
      Log.info { "performing 'GetAllowedImagesSettings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetAllowedImagesSettings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetAllowedImagesSettings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetAllowedImagesSettingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetAllowedImagesSettingsResult).new(response, result)
    end

    # Returns the IAM roles that are associated with the specified ACM (ACM) certificate. It also
    # returns the name of the Amazon S3 bucket and the Amazon S3 object key where the certificate,
    # certificate chain, and encrypted private key bundle are stored, and the ARN of the KMS key
    # that's used to encrypt the private key.
    def get_associated_enclave_certificate_iam_roles(input : AEC::GetAssociatedEnclaveCertificateIamRolesRequest) : Core::ParsedResponse(AEC::GetAssociatedEnclaveCertificateIamRolesResult)
      Log.info { "performing 'GetAssociatedEnclaveCertificateIamRoles' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetAssociatedEnclaveCertificateIamRoles")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetAssociatedEnclaveCertificateIamRoles", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetAssociatedEnclaveCertificateIamRolesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetAssociatedEnclaveCertificateIamRolesResult).new(response, result)
    end

    # Gets information about the IPv6 CIDR block associations for a specified IPv6 address pool.
    def get_associated_ipv_6_pool_cidrs(input : AEC::GetAssociatedIpv6PoolCidrsRequest) : Core::ParsedResponse(AEC::GetAssociatedIpv6PoolCidrsResult)
      Log.info { "performing 'GetAssociatedIpv6PoolCidrs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetAssociatedIpv6PoolCidrs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetAssociatedIpv6PoolCidrs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetAssociatedIpv6PoolCidrsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetAssociatedIpv6PoolCidrsResult).new(response, result)
    end

    # Gets network performance data.
    def get_aws_network_performance_data(input : AEC::GetAwsNetworkPerformanceDataRequest) : Core::ParsedResponse(AEC::GetAwsNetworkPerformanceDataResult)
      Log.info { "performing 'GetAwsNetworkPerformanceData' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetAwsNetworkPerformanceData")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetAwsNetworkPerformanceData", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetAwsNetworkPerformanceDataResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetAwsNetworkPerformanceDataResult).new(response, result)
    end

    # Retrieves the current configuration and status of EC2 Capacity Manager for your account,
    # including enablement status, Organizations access settings, and data ingestion status.
    def get_capacity_manager_attributes(input : AEC::GetCapacityManagerAttributesRequest) : Core::ParsedResponse(AEC::GetCapacityManagerAttributesResult)
      Log.info { "performing 'GetCapacityManagerAttributes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetCapacityManagerAttributes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetCapacityManagerAttributes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetCapacityManagerAttributesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetCapacityManagerAttributesResult).new(response, result)
    end

    # Retrieves capacity usage metrics for your EC2 resources. Returns time-series data for metrics
    # like unused capacity, utilization rates, and costs across On-Demand, Spot, and Capacity
    # Reservations. Data can be grouped and filtered by various dimensions such as region, account,
    # and instance family.
    def get_capacity_manager_metric_data(input : AEC::GetCapacityManagerMetricDataRequest) : Core::ParsedResponse(AEC::GetCapacityManagerMetricDataResult)
      Log.info { "performing 'GetCapacityManagerMetricData' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetCapacityManagerMetricData")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetCapacityManagerMetricData", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetCapacityManagerMetricDataResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetCapacityManagerMetricDataResult).new(response, result)
    end

    # Retrieves the available dimension values for capacity metrics within a specified time range.
    # This is useful for discovering what accounts, regions, instance families, and other dimensions
    # have data available for filtering and grouping.
    def get_capacity_manager_metric_dimensions(input : AEC::GetCapacityManagerMetricDimensionsRequest) : Core::ParsedResponse(AEC::GetCapacityManagerMetricDimensionsResult)
      Log.info { "performing 'GetCapacityManagerMetricDimensions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetCapacityManagerMetricDimensions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetCapacityManagerMetricDimensions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetCapacityManagerMetricDimensionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetCapacityManagerMetricDimensionsResult).new(response, result)
    end

    # Retrieves the tag keys that are currently being monitored by EC2 Capacity Manager. Monitored tag
    # keys are included as dimensions in capacity metric data, enabling you to group and filter
    # metrics by tag values.
    def get_capacity_manager_monitored_tag_keys(input : AEC::GetCapacityManagerMonitoredTagKeysRequest) : Core::ParsedResponse(AEC::GetCapacityManagerMonitoredTagKeysResult)
      Log.info { "performing 'GetCapacityManagerMonitoredTagKeys' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetCapacityManagerMonitoredTagKeys")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetCapacityManagerMonitoredTagKeys", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetCapacityManagerMonitoredTagKeysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetCapacityManagerMonitoredTagKeysResult).new(response, result)
    end

    # Gets usage information about a Capacity Reservation. If the Capacity Reservation is shared, it
    # shows usage information for the Capacity Reservation owner and each Amazon Web Services account
    # that is currently using the shared capacity. If the Capacity Reservation is not shared, it shows
    # only the Capacity Reservation owner's usage.
    def get_capacity_reservation_usage(input : AEC::GetCapacityReservationUsageRequest) : Core::ParsedResponse(AEC::GetCapacityReservationUsageResult)
      Log.info { "performing 'GetCapacityReservationUsage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetCapacityReservationUsage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetCapacityReservationUsage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetCapacityReservationUsageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetCapacityReservationUsageResult).new(response, result)
    end

    # Describes the allocations from the specified customer-owned address pool.
    def get_coip_pool_usage(input : AEC::GetCoipPoolUsageRequest) : Core::ParsedResponse(AEC::GetCoipPoolUsageResult)
      Log.info { "performing 'GetCoipPoolUsage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetCoipPoolUsage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetCoipPoolUsage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetCoipPoolUsageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetCoipPoolUsageResult).new(response, result)
    end

    # Gets the console output for the specified instance. For Linux instances, the instance console
    # output displays the exact console output that would normally be displayed on a physical monitor
    # attached to a computer. For Windows instances, the instance console output includes the last
    # three system event log errors.
    #
    # For more information, see [Instance console
    # output](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-console.html#instance-console-console-output)
    # in the *Amazon EC2 User Guide*.
    def get_console_output(input : AEC::GetConsoleOutputRequest) : Core::ParsedResponse(AEC::GetConsoleOutputResult)
      Log.info { "performing 'GetConsoleOutput' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetConsoleOutput")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetConsoleOutput", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetConsoleOutputResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetConsoleOutputResult).new(response, result)
    end

    # Retrieve a JPG-format screenshot of a running instance to help with troubleshooting.
    #
    # The returned content is Base64-encoded.
    #
    # For more information, see [Instance console
    # output](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/troubleshoot-unreachable-instance.html#instance-console-console-output)
    # in the *Amazon EC2 User Guide*.
    def get_console_screenshot(input : AEC::GetConsoleScreenshotRequest) : Core::ParsedResponse(AEC::GetConsoleScreenshotResult)
      Log.info { "performing 'GetConsoleScreenshot' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetConsoleScreenshot")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetConsoleScreenshot", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetConsoleScreenshotResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetConsoleScreenshotResult).new(response, result)
    end

    # Retrieves a summary of the account status report.
    #
    # To view the full report, download it from the Amazon S3 bucket where it was saved. Reports are
    # accessible only when they have the `complete` status. Reports with other statuses (`running`,
    # `cancelled`, or `error`) are not available in the S3 bucket. For more information about
    # downloading objects from an S3 bucket, see [Downloading
    # objects](https://docs.aws.amazon.com/AmazonS3/latest/userguide/download-objects.html) in the
    # *Amazon Simple Storage Service User Guide*.
    #
    # For more information, see [Generating the account status report for declarative
    # policies](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_declarative_status-report.html)
    # in the *Amazon Web Services Organizations User Guide*.
    def get_declarative_policies_report_summary(input : AEC::GetDeclarativePoliciesReportSummaryRequest) : Core::ParsedResponse(AEC::GetDeclarativePoliciesReportSummaryResult)
      Log.info { "performing 'GetDeclarativePoliciesReportSummary' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetDeclarativePoliciesReportSummary")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetDeclarativePoliciesReportSummary", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetDeclarativePoliciesReportSummaryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetDeclarativePoliciesReportSummaryResult).new(response, result)
    end

    # Describes the default credit option for CPU usage of a burstable performance instance family.
    #
    # For more information, see [Burstable performance
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/burstable-performance-instances.html)
    # in the *Amazon EC2 User Guide*.
    def get_default_credit_specification(input : AEC::GetDefaultCreditSpecificationRequest) : Core::ParsedResponse(AEC::GetDefaultCreditSpecificationResult)
      Log.info { "performing 'GetDefaultCreditSpecification' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetDefaultCreditSpecification")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetDefaultCreditSpecification", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetDefaultCreditSpecificationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetDefaultCreditSpecificationResult).new(response, result)
    end

    # Describes the default KMS key for EBS encryption by default for your account in this Region.
    #
    # For more information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    def get_ebs_default_kms_key_id(input : AEC::GetEbsDefaultKmsKeyIdRequest) : Core::ParsedResponse(AEC::GetEbsDefaultKmsKeyIdResult)
      Log.info { "performing 'GetEbsDefaultKmsKeyId' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetEbsDefaultKmsKeyId")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetEbsDefaultKmsKeyId", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetEbsDefaultKmsKeyIdResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetEbsDefaultKmsKeyIdResult).new(response, result)
    end

    # Describes whether EBS encryption by default is enabled for your account in the current Region.
    #
    # For more information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    def get_ebs_encryption_by_default(input : AEC::GetEbsEncryptionByDefaultRequest) : Core::ParsedResponse(AEC::GetEbsEncryptionByDefaultResult)
      Log.info { "performing 'GetEbsEncryptionByDefault' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetEbsEncryptionByDefault")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetEbsEncryptionByDefault", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetEbsEncryptionByDefaultResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetEbsEncryptionByDefaultResult).new(response, result)
    end

    # Gets the enabled IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    def get_enabled_ipam_policy(input : AEC::GetEnabledIpamPolicyRequest) : Core::ParsedResponse(AEC::GetEnabledIpamPolicyResult)
      Log.info { "performing 'GetEnabledIpamPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetEnabledIpamPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetEnabledIpamPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetEnabledIpamPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetEnabledIpamPolicyResult).new(response, result)
    end

    # Generates a CloudFormation template that streamlines and automates the integration of VPC flow
    # logs with Amazon Athena. This make it easier for you to query and gain insights from VPC flow
    # logs data. Based on the information that you provide, we configure resources in the template to
    # do the following:
    #
    # - Create a table in Athena that maps fields to a custom log format
    #
    # - Create a Lambda function that updates the table with new partitions on a daily, weekly, or
    # monthly basis
    #
    # - Create a table partitioned between two timestamps in the past
    #
    # - Create a set of named queries in Athena that you can use to get started quickly
    #
    # `GetFlowLogsIntegrationTemplate` does not support integration between Amazon Web Services
    # Transit Gateway Flow Logs and Amazon Athena.
    def get_flow_logs_integration_template(input : AEC::GetFlowLogsIntegrationTemplateRequest) : Core::ParsedResponse(AEC::GetFlowLogsIntegrationTemplateResult)
      Log.info { "performing 'GetFlowLogsIntegrationTemplate' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetFlowLogsIntegrationTemplate")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetFlowLogsIntegrationTemplate", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetFlowLogsIntegrationTemplateResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetFlowLogsIntegrationTemplateResult).new(response, result)
    end

    # Lists the resource groups to which a Capacity Reservation has been added.
    def get_groups_for_capacity_reservation(input : AEC::GetGroupsForCapacityReservationRequest) : Core::ParsedResponse(AEC::GetGroupsForCapacityReservationResult)
      Log.info { "performing 'GetGroupsForCapacityReservation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetGroupsForCapacityReservation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetGroupsForCapacityReservation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetGroupsForCapacityReservationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetGroupsForCapacityReservationResult).new(response, result)
    end

    # Preview a reservation purchase with configurations that match those of your Dedicated Host. You
    # must have active Dedicated Hosts in your account before you purchase a reservation.
    #
    # This is a preview of the PurchaseHostReservation action and does not result in the offering
    # being purchased.
    def get_host_reservation_purchase_preview(input : AEC::GetHostReservationPurchasePreviewRequest) : Core::ParsedResponse(AEC::GetHostReservationPurchasePreviewResult)
      Log.info { "performing 'GetHostReservationPurchasePreview' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetHostReservationPurchasePreview")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetHostReservationPurchasePreview", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetHostReservationPurchasePreviewResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetHostReservationPurchasePreviewResult).new(response, result)
    end

    # Retrieves the ancestry chain of the specified AMI, tracing its lineage back to the root AMI. For
    # more information, see [AMI
    # ancestry](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-ancestry.html) in *Amazon EC2
    # User Guide*.
    def get_image_ancestry(input : AEC::GetImageAncestryRequest) : Core::ParsedResponse(AEC::GetImageAncestryResult)
      Log.info { "performing 'GetImageAncestry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetImageAncestry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetImageAncestry", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetImageAncestryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetImageAncestryResult).new(response, result)
    end

    # Gets the current state of *block public access for AMIs* at the account level in the specified
    # Amazon Web Services Region.
    #
    # For more information, see [Block public access to your
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/block-public-access-to-amis.html) in
    # the *Amazon EC2 User Guide*.
    def get_image_block_public_access_state(input : AEC::GetImageBlockPublicAccessStateRequest) : Core::ParsedResponse(AEC::GetImageBlockPublicAccessStateResult)
      Log.info { "performing 'GetImageBlockPublicAccessState' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetImageBlockPublicAccessState")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetImageBlockPublicAccessState", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetImageBlockPublicAccessStateResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetImageBlockPublicAccessStateResult).new(response, result)
    end

    # Gets the default instance metadata service (IMDS) settings that are set at the account level in
    # the specified Amazon Web Services Region.
    #
    # For more information, see [Order of precedence for instance metadata
    # options](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configuring-instance-metadata-options.html#instance-metadata-options-order-of-precedence)
    # in the *Amazon EC2 User Guide*.
    def get_instance_metadata_defaults(input : AEC::GetInstanceMetadataDefaultsRequest) : Core::ParsedResponse(AEC::GetInstanceMetadataDefaultsResult)
      Log.info { "performing 'GetInstanceMetadataDefaults' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetInstanceMetadataDefaults")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetInstanceMetadataDefaults", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetInstanceMetadataDefaultsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetInstanceMetadataDefaultsResult).new(response, result)
    end

    # Gets the public endorsement key associated with the Nitro Trusted Platform Module (NitroTPM) for
    # the specified instance.
    def get_instance_tpm_ek_pub(input : AEC::GetInstanceTpmEkPubRequest) : Core::ParsedResponse(AEC::GetInstanceTpmEkPubResult)
      Log.info { "performing 'GetInstanceTpmEkPub' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetInstanceTpmEkPub")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetInstanceTpmEkPub", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetInstanceTpmEkPubResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetInstanceTpmEkPubResult).new(response, result)
    end

    # Returns a list of instance types with the specified instance attributes. You can use the
    # response to preview the instance types without launching instances. Note that the response does
    # not consider capacity.
    #
    # When you specify multiple parameters, you get instance types that satisfy all of the specified
    # parameters. If you specify multiple values for a parameter, you get instance types that satisfy
    # any of the specified values.
    #
    # For more information, see [Preview instance types with specified
    # attributes](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-fleet-attribute-based-instance-type-selection.html#ec2fleet-get-instance-types-from-instance-requirements),
    # [Specify attributes for instance type selection for EC2 Fleet or Spot
    # Fleet](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-fleet-attribute-based-instance-type-selection.html),
    # and [Spot placement
    # score](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-placement-score.html) in the
    # *Amazon EC2 User Guide*, and [Creating mixed instance groups using attribute-based instance type
    # selection](https://docs.aws.amazon.com/autoscaling/ec2/userguide/create-asg-instance-type-requirements.html)
    # in the *Amazon EC2 Auto Scaling User Guide*.
    def get_instance_types_from_instance_requirements(input : AEC::GetInstanceTypesFromInstanceRequirementsRequest) : Core::ParsedResponse(AEC::GetInstanceTypesFromInstanceRequirementsResult)
      Log.info { "performing 'GetInstanceTypesFromInstanceRequirements' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetInstanceTypesFromInstanceRequirements")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetInstanceTypesFromInstanceRequirements", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetInstanceTypesFromInstanceRequirementsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetInstanceTypesFromInstanceRequirementsResult).new(response, result)
    end

    # A binary representation of the UEFI variable store. Only non-volatile variables are stored. This
    # is a base64 encoded and zlib compressed binary value that must be properly encoded.
    #
    # When you use
    # [register-image](https://docs.aws.amazon.com/cli/latest/reference/ec2/register-image.html) to
    # create an AMI, you can create an exact copy of your variable store by passing the UEFI data in
    # the `UefiData` parameter. You can modify the UEFI data by using the [python-uefivars
    # tool](https://github.com/awslabs/python-uefivars) on GitHub. You can use the tool to convert the
    # UEFI data into a human-readable format (JSON), which you can inspect and modify, and then
    # convert back into the binary format to use with register-image.
    #
    # For more information, see [UEFI Secure
    # Boot](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/uefi-secure-boot.html) in the *Amazon
    # EC2 User Guide*.
    def get_instance_uefi_data(input : AEC::GetInstanceUefiDataRequest) : Core::ParsedResponse(AEC::GetInstanceUefiDataResult)
      Log.info { "performing 'GetInstanceUefiData' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetInstanceUefiData")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetInstanceUefiData", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetInstanceUefiDataResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetInstanceUefiDataResult).new(response, result)
    end

    # Retrieve historical information about a CIDR within an IPAM scope. For more information, see
    # [View the history of IP
    # addresses](https://docs.aws.amazon.com/vpc/latest/ipam/view-history-cidr-ipam.html) in the
    # *Amazon VPC IPAM User Guide*.
    def get_ipam_address_history(input : AEC::GetIpamAddressHistoryRequest) : Core::ParsedResponse(AEC::GetIpamAddressHistoryResult)
      Log.info { "performing 'GetIpamAddressHistory' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamAddressHistory")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamAddressHistory", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamAddressHistoryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamAddressHistoryResult).new(response, result)
    end

    # Gets IPAM discovered accounts. A discovered account is an Amazon Web Services account that is
    # monitored under a resource discovery. If you have integrated IPAM with Amazon Web Services
    # Organizations, all accounts in the organization are discovered accounts. Only the IPAM account
    # can get all discovered accounts in the organization.
    def get_ipam_discovered_accounts(input : AEC::GetIpamDiscoveredAccountsRequest) : Core::ParsedResponse(AEC::GetIpamDiscoveredAccountsResult)
      Log.info { "performing 'GetIpamDiscoveredAccounts' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamDiscoveredAccounts")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamDiscoveredAccounts", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamDiscoveredAccountsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamDiscoveredAccountsResult).new(response, result)
    end

    # Gets the public IP addresses that have been discovered by IPAM.
    def get_ipam_discovered_public_addresses(input : AEC::GetIpamDiscoveredPublicAddressesRequest) : Core::ParsedResponse(AEC::GetIpamDiscoveredPublicAddressesResult)
      Log.info { "performing 'GetIpamDiscoveredPublicAddresses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamDiscoveredPublicAddresses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamDiscoveredPublicAddresses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamDiscoveredPublicAddressesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamDiscoveredPublicAddressesResult).new(response, result)
    end

    # Returns the resource CIDRs that are monitored as part of a resource discovery. A discovered
    # resource is a resource CIDR monitored under a resource discovery. The following resources can be
    # discovered: VPCs, Public IPv4 pools, VPC subnets, and Elastic IP addresses.
    def get_ipam_discovered_resource_cidrs(input : AEC::GetIpamDiscoveredResourceCidrsRequest) : Core::ParsedResponse(AEC::GetIpamDiscoveredResourceCidrsResult)
      Log.info { "performing 'GetIpamDiscoveredResourceCidrs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamDiscoveredResourceCidrs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamDiscoveredResourceCidrs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamDiscoveredResourceCidrsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamDiscoveredResourceCidrsResult).new(response, result)
    end

    # Retrieves Border Gateway Protocol (BGP) routes discovered by IPAM resource discovery for a
    # specified Region. Use this operation to view the Bring Your Own IP (BYOIP) address ranges that
    # are currently advertised through BGP. We recommend using pagination to ensure that the operation
    # returns quickly and successfully.
    def get_ipam_discovered_routes(input : AEC::GetIpamDiscoveredRoutesRequest) : Core::ParsedResponse(AEC::GetIpamDiscoveredRoutesResult)
      Log.info { "performing 'GetIpamDiscoveredRoutes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamDiscoveredRoutes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamDiscoveredRoutes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamDiscoveredRoutesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamDiscoveredRoutesResult).new(response, result)
    end

    # Retrieves Autonomous System Numbers (ASNs) registered with an internet registry for an IPAM
    # internet registry association. We recommend using pagination to ensure that the operation
    # returns quickly and successfully.
    def get_ipam_internet_registry_association_asns(input : AEC::GetIpamInternetRegistryAssociationAsnsRequest) : Core::ParsedResponse(AEC::GetIpamInternetRegistryAssociationAsnsResult)
      Log.info { "performing 'GetIpamInternetRegistryAssociationAsns' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamInternetRegistryAssociationAsns")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamInternetRegistryAssociationAsns", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamInternetRegistryAssociationAsnsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamInternetRegistryAssociationAsnsResult).new(response, result)
    end

    # Retrieves IP address CIDRs registered with an internet registry for an IPAM internet registry
    # association. We recommend using pagination to ensure that the operation returns quickly and
    # successfully.
    def get_ipam_internet_registry_association_cidrs(input : AEC::GetIpamInternetRegistryAssociationCidrsRequest) : Core::ParsedResponse(AEC::GetIpamInternetRegistryAssociationCidrsResult)
      Log.info { "performing 'GetIpamInternetRegistryAssociationCidrs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamInternetRegistryAssociationCidrs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamInternetRegistryAssociationCidrs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamInternetRegistryAssociationCidrsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamInternetRegistryAssociationCidrsResult).new(response, result)
    end

    # Gets the allocation rules for an IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    #
    # Allocation rules are optional configurations within an IPAM policy that map Amazon Web Services
    # resource types to specific IPAM pools. If no rules are defined, the resource types default to
    # using Amazon-provided IP addresses.
    def get_ipam_policy_allocation_rules(input : AEC::GetIpamPolicyAllocationRulesRequest) : Core::ParsedResponse(AEC::GetIpamPolicyAllocationRulesResult)
      Log.info { "performing 'GetIpamPolicyAllocationRules' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamPolicyAllocationRules")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamPolicyAllocationRules", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamPolicyAllocationRulesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamPolicyAllocationRulesResult).new(response, result)
    end

    # Gets the Amazon Web Services Organizations targets for an IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    #
    # A target can be an individual Amazon Web Services account or an entity within an Amazon Web
    # Services Organization to which an IPAM policy can be applied.
    def get_ipam_policy_organization_targets(input : AEC::GetIpamPolicyOrganizationTargetsRequest) : Core::ParsedResponse(AEC::GetIpamPolicyOrganizationTargetsResult)
      Log.info { "performing 'GetIpamPolicyOrganizationTargets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamPolicyOrganizationTargets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamPolicyOrganizationTargets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamPolicyOrganizationTargetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamPolicyOrganizationTargetsResult).new(response, result)
    end

    # Get a list of all the CIDR allocations in an IPAM pool. The Region you use should be the IPAM
    # pool locale. The locale is the Amazon Web Services Region where this IPAM pool is available for
    # allocations.
    #
    # If you use this action after
    # [AllocateIpamPoolCidr](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_AllocateIpamPoolCidr.html)
    # or
    # [ReleaseIpamPoolAllocation](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_ReleaseIpamPoolAllocation.html),
    # note that all EC2 API actions follow an [eventual
    # consistency](https://docs.aws.amazon.com/ec2/latest/devguide/eventual-consistency.html) model.
    def get_ipam_pool_allocations(input : AEC::GetIpamPoolAllocationsRequest) : Core::ParsedResponse(AEC::GetIpamPoolAllocationsResult)
      Log.info { "performing 'GetIpamPoolAllocations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamPoolAllocations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamPoolAllocations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamPoolAllocationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamPoolAllocationsResult).new(response, result)
    end

    # Get the CIDRs provisioned to an IPAM pool.
    def get_ipam_pool_cidrs(input : AEC::GetIpamPoolCidrsRequest) : Core::ParsedResponse(AEC::GetIpamPoolCidrsResult)
      Log.info { "performing 'GetIpamPoolCidrs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamPoolCidrs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamPoolCidrs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamPoolCidrsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamPoolCidrsResult).new(response, result)
    end

    # Retrieves the CIDR selection rules for an IPAM prefix list resolver. Use this operation to view
    # the business logic that determines which CIDRs are selected for synchronization with prefix
    # lists.
    def get_ipam_prefix_list_resolver_rules(input : AEC::GetIpamPrefixListResolverRulesRequest) : Core::ParsedResponse(AEC::GetIpamPrefixListResolverRulesResult)
      Log.info { "performing 'GetIpamPrefixListResolverRules' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamPrefixListResolverRules")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamPrefixListResolverRules", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamPrefixListResolverRulesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamPrefixListResolverRulesResult).new(response, result)
    end

    # Retrieves the CIDR entries for a specific version of an IPAM prefix list resolver. This shows
    # the actual CIDRs that were selected and synchronized at a particular point in time.
    def get_ipam_prefix_list_resolver_version_entries(input : AEC::GetIpamPrefixListResolverVersionEntriesRequest) : Core::ParsedResponse(AEC::GetIpamPrefixListResolverVersionEntriesResult)
      Log.info { "performing 'GetIpamPrefixListResolverVersionEntries' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamPrefixListResolverVersionEntries")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamPrefixListResolverVersionEntries", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamPrefixListResolverVersionEntriesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamPrefixListResolverVersionEntriesResult).new(response, result)
    end

    # Retrieves version information for an IPAM prefix list resolver.
    #
    # Each version is a snapshot of what CIDRs matched your rules at that moment in time. The version
    # number increments every time the CIDR list changes due to infrastructure changes.
    #
    # **Version example:**
    #
    # **Initial State (Version 1)**
    #
    # Production environment:
    #
    # - vpc-prod-web (10.1.0.0/16) - tagged env=prod
    #
    # - vpc-prod-db (10.2.0.0/16) - tagged env=prod
    #
    # Resolver rule: Include all VPCs tagged env=prod
    #
    # **Version 1 CIDRs:** 10.1.0.0/16, 10.2.0.0/16
    #
    # **Infrastructure Change (Version 2)**
    #
    # New VPC added:
    #
    # - vpc-prod-api (10.3.0.0/16) - tagged env=prod
    #
    # IPAM automatically detects the change and creates a new version.
    #
    # **Version 2 CIDRs:** 10.1.0.0/16, 10.2.0.0/16, 10.3.0.0/16
    def get_ipam_prefix_list_resolver_versions(input : AEC::GetIpamPrefixListResolverVersionsRequest) : Core::ParsedResponse(AEC::GetIpamPrefixListResolverVersionsResult)
      Log.info { "performing 'GetIpamPrefixListResolverVersions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamPrefixListResolverVersions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamPrefixListResolverVersions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamPrefixListResolverVersionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamPrefixListResolverVersionsResult).new(response, result)
    end

    # Returns resource CIDRs managed by IPAM in a given scope. If an IPAM is associated with more than
    # one resource discovery, the resource CIDRs across all of the resource discoveries is returned. A
    # resource discovery is an IPAM component that enables IPAM to manage and monitor resources that
    # belong to the owning account.
    def get_ipam_resource_cidrs(input : AEC::GetIpamResourceCidrsRequest) : Core::ParsedResponse(AEC::GetIpamResourceCidrsResult)
      Log.info { "performing 'GetIpamResourceCidrs' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamResourceCidrs")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamResourceCidrs", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamResourceCidrsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamResourceCidrsResult).new(response, result)
    end

    # Retrieves the current Route Origin Authorizations (ROAs) published to the RPKI for an IPAM
    # internet registry association. We recommend using pagination to ensure that the operation
    # returns quickly and successfully.
    def get_ipam_route_origin_authorizations(input : AEC::GetIpamRouteOriginAuthorizationsRequest) : Core::ParsedResponse(AEC::GetIpamRouteOriginAuthorizationsResult)
      Log.info { "performing 'GetIpamRouteOriginAuthorizations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamRouteOriginAuthorizations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamRouteOriginAuthorizations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamRouteOriginAuthorizationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamRouteOriginAuthorizationsResult).new(response, result)
    end

    # Retrieves route protection findings for an IPAM. Route protection findings show the Resource
    # Public Key Infrastructure (RPKI) validation status of your Bring Your Own IP (BYOIP) routes.
    # Findings identify routes that have valid, invalid, or unknown validation states. We recommend
    # using pagination to ensure that the operation returns quickly and successfully.
    def get_ipam_route_protection_findings(input : AEC::GetIpamRouteProtectionFindingsRequest) : Core::ParsedResponse(AEC::GetIpamRouteProtectionFindingsResult)
      Log.info { "performing 'GetIpamRouteProtectionFindings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamRouteProtectionFindings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamRouteProtectionFindings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamRouteProtectionFindingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamRouteProtectionFindingsResult).new(response, result)
    end

    # Retrieves the history of routing policy registration changes for an IPAM internet registry
    # association. We recommend using pagination to ensure that the operation returns quickly and
    # successfully.
    def get_ipam_routing_policy_registration_deltas(input : AEC::GetIpamRoutingPolicyRegistrationDeltasRequest) : Core::ParsedResponse(AEC::GetIpamRoutingPolicyRegistrationDeltasResult)
      Log.info { "performing 'GetIpamRoutingPolicyRegistrationDeltas' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamRoutingPolicyRegistrationDeltas")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamRoutingPolicyRegistrationDeltas", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamRoutingPolicyRegistrationDeltasResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamRoutingPolicyRegistrationDeltasResult).new(response, result)
    end

    # Retrieves routing policy registrations for an IPAM internet registry association. Each
    # registration represents a Route Origin Authorization (ROA) that has been created or is pending
    # publication to the RPKI. We recommend using pagination to ensure that the operation returns
    # quickly and successfully.
    def get_ipam_routing_policy_registrations(input : AEC::GetIpamRoutingPolicyRegistrationsRequest) : Core::ParsedResponse(AEC::GetIpamRoutingPolicyRegistrationsResult)
      Log.info { "performing 'GetIpamRoutingPolicyRegistrations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetIpamRoutingPolicyRegistrations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetIpamRoutingPolicyRegistrations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetIpamRoutingPolicyRegistrationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetIpamRoutingPolicyRegistrationsResult).new(response, result)
    end

    # Retrieves the configuration data of the specified instance. You can use this data to create a
    # launch template.
    #
    # This action calls on other describe actions to get instance information. Depending on your
    # instance configuration, you may need to allow the following actions in your IAM policy:
    # `DescribeSpotInstanceRequests`, `DescribeInstanceCreditSpecifications`, `DescribeVolumes`, and
    # `DescribeInstanceAttribute`. Or, you can allow `describe*` depending on your instance
    # requirements.
    def get_launch_template_data(input : AEC::GetLaunchTemplateDataRequest) : Core::ParsedResponse(AEC::GetLaunchTemplateDataResult)
      Log.info { "performing 'GetLaunchTemplateData' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetLaunchTemplateData")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetLaunchTemplateData", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetLaunchTemplateDataResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetLaunchTemplateDataResult).new(response, result)
    end

    # Gets information about the resources that are associated with the specified managed prefix list.
    def get_managed_prefix_list_associations(input : AEC::GetManagedPrefixListAssociationsRequest) : Core::ParsedResponse(AEC::GetManagedPrefixListAssociationsResult)
      Log.info { "performing 'GetManagedPrefixListAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetManagedPrefixListAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetManagedPrefixListAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetManagedPrefixListAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetManagedPrefixListAssociationsResult).new(response, result)
    end

    # Gets information about the entries for a specified managed prefix list.
    def get_managed_prefix_list_entries(input : AEC::GetManagedPrefixListEntriesRequest) : Core::ParsedResponse(AEC::GetManagedPrefixListEntriesResult)
      Log.info { "performing 'GetManagedPrefixListEntries' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetManagedPrefixListEntries")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetManagedPrefixListEntries", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetManagedPrefixListEntriesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetManagedPrefixListEntriesResult).new(response, result)
    end

    # Retrieves the managed resource visibility configuration for the account. The response indicates
    # whether managed resources are hidden or visible by default.
    def get_managed_resource_visibility(input : AEC::GetManagedResourceVisibilityRequest) : Core::ParsedResponse(AEC::GetManagedResourceVisibilityResult)
      Log.info { "performing 'GetManagedResourceVisibility' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetManagedResourceVisibility")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetManagedResourceVisibility", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetManagedResourceVisibilityResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetManagedResourceVisibilityResult).new(response, result)
    end

    # Gets the findings for the specified Network Access Scope analysis.
    def get_network_insights_access_scope_analysis_findings(input : AEC::GetNetworkInsightsAccessScopeAnalysisFindingsRequest) : Core::ParsedResponse(AEC::GetNetworkInsightsAccessScopeAnalysisFindingsResult)
      Log.info { "performing 'GetNetworkInsightsAccessScopeAnalysisFindings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetNetworkInsightsAccessScopeAnalysisFindings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetNetworkInsightsAccessScopeAnalysisFindings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetNetworkInsightsAccessScopeAnalysisFindingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetNetworkInsightsAccessScopeAnalysisFindingsResult).new(response, result)
    end

    # Gets the content for the specified Network Access Scope.
    def get_network_insights_access_scope_content(input : AEC::GetNetworkInsightsAccessScopeContentRequest) : Core::ParsedResponse(AEC::GetNetworkInsightsAccessScopeContentResult)
      Log.info { "performing 'GetNetworkInsightsAccessScopeContent' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetNetworkInsightsAccessScopeContent")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetNetworkInsightsAccessScopeContent", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetNetworkInsightsAccessScopeContentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetNetworkInsightsAccessScopeContentResult).new(response, result)
    end

    # Retrieves the encrypted administrator password for a running Windows instance.
    #
    # The Windows password is generated at boot by the `EC2Config` service or `EC2Launch` scripts
    # (Windows Server 2016 and later). This usually only happens the first time an instance is
    # launched. For more information, see
    # [EC2Config](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/UsingConfig_WinAMI.html) and
    # [EC2Launch](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2launch.html) in the *Amazon
    # EC2 User Guide*.
    #
    # For the `EC2Config` service, the password is not generated for rebundled AMIs unless
    # `Ec2SetPassword` is enabled before bundling.
    #
    # The password is encrypted using the key pair that you specified when you launched the instance.
    # You must provide the corresponding key pair file.
    #
    # When you launch an instance, password generation and encryption may take a few minutes. If you
    # try to retrieve the password before it's available, the output returns an empty string. We
    # recommend that you wait up to 15 minutes after launching an instance before trying to retrieve
    # the generated password.
    def get_password_data(input : AEC::GetPasswordDataRequest) : Core::ParsedResponse(AEC::GetPasswordDataResult)
      Log.info { "performing 'GetPasswordData' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetPasswordData")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetPasswordData", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetPasswordDataResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetPasswordDataResult).new(response, result)
    end

    # Returns a quote and exchange information for exchanging one or more specified Convertible
    # Reserved Instances for a new Convertible Reserved Instance. If the exchange cannot be performed,
    # the reason is returned in the response. Use AcceptReservedInstancesExchangeQuote to perform the
    # exchange.
    def get_reserved_instances_exchange_quote(input : AEC::GetReservedInstancesExchangeQuoteRequest) : Core::ParsedResponse(AEC::GetReservedInstancesExchangeQuoteResult)
      Log.info { "performing 'GetReservedInstancesExchangeQuote' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetReservedInstancesExchangeQuote")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetReservedInstancesExchangeQuote", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetReservedInstancesExchangeQuoteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetReservedInstancesExchangeQuoteResult).new(response, result)
    end

    # Gets information about the associations for the specified route server.
    #
    # A route server association is the connection established between a route server and a VPC.
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def get_route_server_associations(input : AEC::GetRouteServerAssociationsRequest) : Core::ParsedResponse(AEC::GetRouteServerAssociationsResult)
      Log.info { "performing 'GetRouteServerAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetRouteServerAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetRouteServerAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetRouteServerAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetRouteServerAssociationsResult).new(response, result)
    end

    # Gets information about the route propagations for the specified route server.
    #
    # When enabled, route server propagation installs the routes in the FIB on the route table you've
    # specified. Route server supports IPv4 and IPv6 route propagation.
    #
    # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed
    # within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates
    # VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing
    # fault tolerance for those workloads. This enables you to automatically reroute traffic within a
    # VPC, which increases the manageability of VPC routing and interoperability with third-party
    # workloads.
    #
    # Route server supports the follow route table types:
    #
    # - VPC route tables not associated with subnets
    #
    # - Subnet route tables
    #
    # - Internet gateway route tables
    #
    # Route server does not support route tables associated with virtual private gateways. To
    # propagate routes into a transit gateway route table, use [Transit Gateway
    # Connect](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html).
    def get_route_server_propagations(input : AEC::GetRouteServerPropagationsRequest) : Core::ParsedResponse(AEC::GetRouteServerPropagationsResult)
      Log.info { "performing 'GetRouteServerPropagations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetRouteServerPropagations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetRouteServerPropagations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetRouteServerPropagationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetRouteServerPropagationsResult).new(response, result)
    end

    # Gets the routing database for the specified route server. The [Routing Information Base
    # (RIB)](https://en.wikipedia.org/wiki/Routing_table) serves as a database that stores all the
    # routing information and network topology data collected by a router or routing system, such as
    # routes learned from BGP peers. The RIB is constantly updated as new routing information is
    # received or existing routes change. This ensures that the route server always has the most
    # current view of the network topology and can make optimal routing decisions.
    #
    # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed
    # within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates
    # VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing
    # fault tolerance for those workloads. This enables you to automatically reroute traffic within a
    # VPC, which increases the manageability of VPC routing and interoperability with third-party
    # workloads.
    #
    # Route server supports the follow route table types:
    #
    # - VPC route tables not associated with subnets
    #
    # - Subnet route tables
    #
    # - Internet gateway route tables
    #
    # Route server does not support route tables associated with virtual private gateways. To
    # propagate routes into a transit gateway route table, use [Transit Gateway
    # Connect](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html).
    def get_route_server_routing_database(input : AEC::GetRouteServerRoutingDatabaseRequest) : Core::ParsedResponse(AEC::GetRouteServerRoutingDatabaseResult)
      Log.info { "performing 'GetRouteServerRoutingDatabase' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetRouteServerRoutingDatabase")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetRouteServerRoutingDatabase", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetRouteServerRoutingDatabaseResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetRouteServerRoutingDatabaseResult).new(response, result)
    end

    # Gets security groups that can be associated by the Amazon Web Services account making the
    # request with network interfaces in the specified VPC.
    def get_security_groups_for_vpc(input : AEC::GetSecurityGroupsForVpcRequest) : Core::ParsedResponse(AEC::GetSecurityGroupsForVpcResult)
      Log.info { "performing 'GetSecurityGroupsForVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetSecurityGroupsForVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetSecurityGroupsForVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetSecurityGroupsForVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetSecurityGroupsForVpcResult).new(response, result)
    end

    # Retrieves the access status of your account to the EC2 serial console of all instances. By
    # default, access to the EC2 serial console is disabled for your account. For more information,
    # see [Manage account access to the EC2 serial
    # console](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configure-access-to-serial-console.html#serial-console-account-access)
    # in the *Amazon EC2 User Guide*.
    def get_serial_console_access_status(input : AEC::GetSerialConsoleAccessStatusRequest) : Core::ParsedResponse(AEC::GetSerialConsoleAccessStatusResult)
      Log.info { "performing 'GetSerialConsoleAccessStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetSerialConsoleAccessStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetSerialConsoleAccessStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetSerialConsoleAccessStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetSerialConsoleAccessStatusResult).new(response, result)
    end

    # Gets the current state of *block public access for snapshots* setting for the account and
    # Region.
    #
    # For more information, see [ Block public access for
    # snapshots](https://docs.aws.amazon.com/ebs/latest/userguide/block-public-access-snapshots.html)
    # in the *Amazon EBS User Guide*.
    def get_snapshot_block_public_access_state(input : AEC::GetSnapshotBlockPublicAccessStateRequest) : Core::ParsedResponse(AEC::GetSnapshotBlockPublicAccessStateResult)
      Log.info { "performing 'GetSnapshotBlockPublicAccessState' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetSnapshotBlockPublicAccessState")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetSnapshotBlockPublicAccessState", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetSnapshotBlockPublicAccessStateResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetSnapshotBlockPublicAccessStateResult).new(response, result)
    end

    # Calculates the Spot placement score for a Region or Availability Zone based on the specified
    # target capacity and compute requirements.
    #
    # You can specify your compute requirements either by using `InstanceRequirementsWithMetadata` and
    # letting Amazon EC2 choose the optimal instance types to fulfill your Spot request, or you can
    # specify the instance types by using `InstanceTypes`.
    #
    # For more information, see [Spot placement
    # score](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-placement-score.html) in the
    # *Amazon EC2 User Guide*.
    def get_spot_placement_scores(input : AEC::GetSpotPlacementScoresRequest) : Core::ParsedResponse(AEC::GetSpotPlacementScoresResult)
      Log.info { "performing 'GetSpotPlacementScores' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetSpotPlacementScores")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetSpotPlacementScores", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetSpotPlacementScoresResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetSpotPlacementScoresResult).new(response, result)
    end

    # Gets information about the subnet CIDR reservations.
    def get_subnet_cidr_reservations(input : AEC::GetSubnetCidrReservationsRequest) : Core::ParsedResponse(AEC::GetSubnetCidrReservationsResult)
      Log.info { "performing 'GetSubnetCidrReservations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetSubnetCidrReservations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetSubnetCidrReservations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetSubnetCidrReservationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetSubnetCidrReservationsResult).new(response, result)
    end

    # Lists the route tables to which the specified resource attachment propagates routes.
    def get_transit_gateway_attachment_propagations(input : AEC::GetTransitGatewayAttachmentPropagationsRequest) : Core::ParsedResponse(AEC::GetTransitGatewayAttachmentPropagationsResult)
      Log.info { "performing 'GetTransitGatewayAttachmentPropagations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayAttachmentPropagations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayAttachmentPropagations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayAttachmentPropagationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayAttachmentPropagationsResult).new(response, result)
    end

    # Retrieves the entries for a transit gateway metering policy.
    def get_transit_gateway_metering_policy_entries(input : AEC::GetTransitGatewayMeteringPolicyEntriesRequest) : Core::ParsedResponse(AEC::GetTransitGatewayMeteringPolicyEntriesResult)
      Log.info { "performing 'GetTransitGatewayMeteringPolicyEntries' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayMeteringPolicyEntries")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayMeteringPolicyEntries", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayMeteringPolicyEntriesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayMeteringPolicyEntriesResult).new(response, result)
    end

    # Gets information about the associations for the transit gateway multicast domain.
    def get_transit_gateway_multicast_domain_associations(input : AEC::GetTransitGatewayMulticastDomainAssociationsRequest) : Core::ParsedResponse(AEC::GetTransitGatewayMulticastDomainAssociationsResult)
      Log.info { "performing 'GetTransitGatewayMulticastDomainAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayMulticastDomainAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayMulticastDomainAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayMulticastDomainAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayMulticastDomainAssociationsResult).new(response, result)
    end

    # Gets a list of the transit gateway policy table associations.
    def get_transit_gateway_policy_table_associations(input : AEC::GetTransitGatewayPolicyTableAssociationsRequest) : Core::ParsedResponse(AEC::GetTransitGatewayPolicyTableAssociationsResult)
      Log.info { "performing 'GetTransitGatewayPolicyTableAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayPolicyTableAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayPolicyTableAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayPolicyTableAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayPolicyTableAssociationsResult).new(response, result)
    end

    # Returns a list of transit gateway policy table entries.
    def get_transit_gateway_policy_table_entries(input : AEC::GetTransitGatewayPolicyTableEntriesRequest) : Core::ParsedResponse(AEC::GetTransitGatewayPolicyTableEntriesResult)
      Log.info { "performing 'GetTransitGatewayPolicyTableEntries' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayPolicyTableEntries")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayPolicyTableEntries", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayPolicyTableEntriesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayPolicyTableEntriesResult).new(response, result)
    end

    # Gets information about the prefix list references in a specified transit gateway route table.
    def get_transit_gateway_prefix_list_references(input : AEC::GetTransitGatewayPrefixListReferencesRequest) : Core::ParsedResponse(AEC::GetTransitGatewayPrefixListReferencesResult)
      Log.info { "performing 'GetTransitGatewayPrefixListReferences' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayPrefixListReferences")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayPrefixListReferences", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayPrefixListReferencesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayPrefixListReferencesResult).new(response, result)
    end

    # Gets information about the associations for the specified transit gateway route table.
    def get_transit_gateway_route_table_associations(input : AEC::GetTransitGatewayRouteTableAssociationsRequest) : Core::ParsedResponse(AEC::GetTransitGatewayRouteTableAssociationsResult)
      Log.info { "performing 'GetTransitGatewayRouteTableAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayRouteTableAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayRouteTableAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayRouteTableAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayRouteTableAssociationsResult).new(response, result)
    end

    # Gets information about the route table propagations for the specified transit gateway route
    # table.
    def get_transit_gateway_route_table_propagations(input : AEC::GetTransitGatewayRouteTablePropagationsRequest) : Core::ParsedResponse(AEC::GetTransitGatewayRouteTablePropagationsResult)
      Log.info { "performing 'GetTransitGatewayRouteTablePropagations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetTransitGatewayRouteTablePropagations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetTransitGatewayRouteTablePropagations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetTransitGatewayRouteTablePropagationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetTransitGatewayRouteTablePropagationsResult).new(response, result)
    end

    # Get the Verified Access policy associated with the endpoint.
    def get_verified_access_endpoint_policy(input : AEC::GetVerifiedAccessEndpointPolicyRequest) : Core::ParsedResponse(AEC::GetVerifiedAccessEndpointPolicyResult)
      Log.info { "performing 'GetVerifiedAccessEndpointPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetVerifiedAccessEndpointPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetVerifiedAccessEndpointPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetVerifiedAccessEndpointPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetVerifiedAccessEndpointPolicyResult).new(response, result)
    end

    # Gets the targets for the specified network CIDR endpoint for Verified Access.
    def get_verified_access_endpoint_targets(input : AEC::GetVerifiedAccessEndpointTargetsRequest) : Core::ParsedResponse(AEC::GetVerifiedAccessEndpointTargetsResult)
      Log.info { "performing 'GetVerifiedAccessEndpointTargets' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetVerifiedAccessEndpointTargets")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetVerifiedAccessEndpointTargets", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetVerifiedAccessEndpointTargetsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetVerifiedAccessEndpointTargetsResult).new(response, result)
    end

    # Shows the contents of the Verified Access policy associated with the group.
    def get_verified_access_group_policy(input : AEC::GetVerifiedAccessGroupPolicyRequest) : Core::ParsedResponse(AEC::GetVerifiedAccessGroupPolicyResult)
      Log.info { "performing 'GetVerifiedAccessGroupPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetVerifiedAccessGroupPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetVerifiedAccessGroupPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetVerifiedAccessGroupPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetVerifiedAccessGroupPolicyResult).new(response, result)
    end

    # Gets information about resources in a VPC that are blocking encryption enforcement.
    #
    # For more information, see [Enforce VPC encryption in
    # transit](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-encryption-controls.html) in the
    # *Amazon VPC User Guide*.
    def get_vpc_resources_blocking_encryption_enforcement(input : AEC::GetVpcResourcesBlockingEncryptionEnforcementRequest) : Core::ParsedResponse(AEC::GetVpcResourcesBlockingEncryptionEnforcementResult)
      Log.info { "performing 'GetVpcResourcesBlockingEncryptionEnforcement' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetVpcResourcesBlockingEncryptionEnforcement")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetVpcResourcesBlockingEncryptionEnforcement", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetVpcResourcesBlockingEncryptionEnforcementResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetVpcResourcesBlockingEncryptionEnforcementResult).new(response, result)
    end

    # Download an Amazon Web Services-provided sample configuration file to be used with the customer
    # gateway device specified for your Site-to-Site VPN connection.
    def get_vpn_connection_device_sample_configuration(input : AEC::GetVpnConnectionDeviceSampleConfigurationRequest) : Core::ParsedResponse(AEC::GetVpnConnectionDeviceSampleConfigurationResult)
      Log.info { "performing 'GetVpnConnectionDeviceSampleConfiguration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetVpnConnectionDeviceSampleConfiguration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetVpnConnectionDeviceSampleConfiguration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetVpnConnectionDeviceSampleConfigurationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetVpnConnectionDeviceSampleConfigurationResult).new(response, result)
    end

    # Obtain a list of customer gateway devices for which sample configuration files can be provided.
    # The request has no additional parameters. You can also see the list of device types with sample
    # configuration files available under [Your customer gateway
    # device](https://docs.aws.amazon.com/vpn/latest/s2svpn/your-cgw.html) in the *Amazon Web Services
    # Site-to-Site VPN User Guide*.
    def get_vpn_connection_device_types(input : AEC::GetVpnConnectionDeviceTypesRequest) : Core::ParsedResponse(AEC::GetVpnConnectionDeviceTypesResult)
      Log.info { "performing 'GetVpnConnectionDeviceTypes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetVpnConnectionDeviceTypes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetVpnConnectionDeviceTypes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetVpnConnectionDeviceTypesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetVpnConnectionDeviceTypesResult).new(response, result)
    end

    # Get details of available tunnel endpoint maintenance.
    def get_vpn_tunnel_replacement_status(input : AEC::GetVpnTunnelReplacementStatusRequest) : Core::ParsedResponse(AEC::GetVpnTunnelReplacementStatusResult)
      Log.info { "performing 'GetVpnTunnelReplacementStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "GetVpnTunnelReplacementStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("GetVpnTunnelReplacementStatus", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::GetVpnTunnelReplacementStatusResult.from_xml(result_node)
      Core::ParsedResponse(AEC::GetVpnTunnelReplacementStatusResult).new(response, result)
    end

    # Uploads a client certificate revocation list to the specified Client VPN endpoint. Uploading a
    # client certificate revocation list overwrites the existing client certificate revocation list.
    #
    # Uploading a client certificate revocation list resets existing client connections.
    def import_client_vpn_client_certificate_revocation_list(input : AEC::ImportClientVpnClientCertificateRevocationListRequest) : Core::ParsedResponse(AEC::ImportClientVpnClientCertificateRevocationListResult)
      Log.info { "performing 'ImportClientVpnClientCertificateRevocationList' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ImportClientVpnClientCertificateRevocationList")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ImportClientVpnClientCertificateRevocationList", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ImportClientVpnClientCertificateRevocationListResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ImportClientVpnClientCertificateRevocationListResult).new(response, result)
    end

    # To import your virtual machines (VMs) with a console-based experience, you can use the *Import
    # virtual machine images to Amazon Web Services* template in the [Migration Hub Orchestrator
    # console](https://console.aws.amazon.com/migrationhub/orchestrator). For more information, see
    # the [ *Migration Hub Orchestrator User Guide*
    # ](https://docs.aws.amazon.com/migrationhub-orchestrator/latest/userguide/import-vm-images.html).
    #
    # Import single or multi-volume disk images or EBS snapshots into an Amazon Machine Image (AMI).
    #
    # Amazon Web Services VM Import/Export strongly recommends specifying a value for either the
    # `--license-type` or `--usage-operation` parameter when you create a new VM Import task. This
    # ensures your operating system is licensed appropriately and your billing is optimized.
    #
    # For more information, see [Importing a VM as an image using VM
    # Import/Export](https://docs.aws.amazon.com/vm-import/latest/userguide/vmimport-image-import.html)
    # in the *VM Import/Export User Guide*.
    def import_image(input : AEC::ImportImageRequest) : Core::ParsedResponse(AEC::ImportImageResult)
      Log.info { "performing 'ImportImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ImportImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ImportImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ImportImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ImportImageResult).new(response, result)
    end

    # We recommend that you use the [ `ImportImage`
    # ](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_ImportImage.html) API instead. For
    # more information, see [Importing a VM as an image using VM
    # Import/Export](https://docs.aws.amazon.com/vm-import/latest/userguide/vmimport-image-import.html)
    # in the *VM Import/Export User Guide*.
    #
    # Creates an import instance task using metadata from the specified disk image.
    #
    # This API action supports only single-volume VMs. To import multi-volume VMs, use ImportImage
    # instead.
    #
    # For information about the import manifest referenced by this API action, see [VM Import
    # Manifest](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/manifest.html).
    #
    # This API action is not supported by the Command Line Interface (CLI).
    def import_instance(input : AEC::ImportInstanceRequest) : Core::ParsedResponse(AEC::ImportInstanceResult)
      Log.info { "performing 'ImportInstance' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ImportInstance")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ImportInstance", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ImportInstanceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ImportInstanceResult).new(response, result)
    end

    # Imports the public key from an RSA or ED25519 key pair that you created using a third-party
    # tool. You give Amazon Web Services only the public key. The private key is never transferred
    # between you and Amazon Web Services.
    #
    # For more information about the requirements for importing a key pair, see [Create a key pair and
    # import the public key to Amazon
    # EC2](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/create-key-pairs.html#how-to-generate-your-own-key-and-import-it-to-aws)
    # in the *Amazon EC2 User Guide*.
    def import_key_pair(input : AEC::ImportKeyPairRequest) : Core::ParsedResponse(AEC::ImportKeyPairResult)
      Log.info { "performing 'ImportKeyPair' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ImportKeyPair")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ImportKeyPair", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ImportKeyPairResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ImportKeyPairResult).new(response, result)
    end

    # Imports a disk into an EBS snapshot.
    #
    # For more information, see [Importing a disk as a snapshot using VM
    # Import/Export](https://docs.aws.amazon.com/vm-import/latest/userguide/vmimport-import-snapshot.html)
    # in the *VM Import/Export User Guide*.
    def import_snapshot(input : AEC::ImportSnapshotRequest) : Core::ParsedResponse(AEC::ImportSnapshotResult)
      Log.info { "performing 'ImportSnapshot' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ImportSnapshot")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ImportSnapshot", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ImportSnapshotResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ImportSnapshotResult).new(response, result)
    end

    # This API action supports only single-volume VMs. To import multi-volume VMs, use ImportImage
    # instead. To import a disk to a snapshot, use ImportSnapshot instead.
    #
    # Creates an import volume task using metadata from the specified disk image.
    #
    # For information about the import manifest referenced by this API action, see [VM Import
    # Manifest](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/manifest.html).
    #
    # This API action is not supported by the Command Line Interface (CLI).
    def import_volume(input : AEC::ImportVolumeRequest) : Core::ParsedResponse(AEC::ImportVolumeResult)
      Log.info { "performing 'ImportVolume' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ImportVolume")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ImportVolume", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ImportVolumeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ImportVolumeResult).new(response, result)
    end

    # Lists one or more AMIs that are currently in the Recycle Bin. For more information, see [Recycle
    # Bin](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/recycle-bin.html) in the *Amazon EC2
    # User Guide*.
    def list_images_in_recycle_bin(input : AEC::ListImagesInRecycleBinRequest) : Core::ParsedResponse(AEC::ListImagesInRecycleBinResult)
      Log.info { "performing 'ListImagesInRecycleBin' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ListImagesInRecycleBin")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ListImagesInRecycleBin", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ListImagesInRecycleBinResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ListImagesInRecycleBinResult).new(response, result)
    end

    # Lists one or more snapshots that are currently in the Recycle Bin.
    def list_snapshots_in_recycle_bin(input : AEC::ListSnapshotsInRecycleBinRequest) : Core::ParsedResponse(AEC::ListSnapshotsInRecycleBinResult)
      Log.info { "performing 'ListSnapshotsInRecycleBin' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ListSnapshotsInRecycleBin")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ListSnapshotsInRecycleBin", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ListSnapshotsInRecycleBinResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ListSnapshotsInRecycleBinResult).new(response, result)
    end

    # Lists one or more volumes that are currently in the Recycle Bin.
    def list_volumes_in_recycle_bin(input : AEC::ListVolumesInRecycleBinRequest) : Core::ParsedResponse(AEC::ListVolumesInRecycleBinResult)
      Log.info { "performing 'ListVolumesInRecycleBin' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ListVolumesInRecycleBin")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ListVolumesInRecycleBin", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ListVolumesInRecycleBinResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ListVolumesInRecycleBinResult).new(response, result)
    end

    # Locks an Amazon EBS snapshot in either *governance* or *compliance* mode to protect it against
    # accidental or malicious deletions for a specific duration. A locked snapshot can't be deleted.
    #
    # You can also use this action to modify the lock settings for a snapshot that is already locked.
    # The allowed modifications depend on the lock mode and lock state:
    #
    # - If the snapshot is locked in governance mode, you can modify the lock mode and the lock
    # duration or lock expiration date.
    #
    # - If the snapshot is locked in compliance mode and it is in the cooling-off period, you can
    # modify the lock mode and the lock duration or lock expiration date.
    #
    # - If the snapshot is locked in compliance mode and the cooling-off period has lapsed, you can
    # only increase the lock duration or extend the lock expiration date.
    def lock_snapshot(input : AEC::LockSnapshotRequest) : Core::ParsedResponse(AEC::LockSnapshotResult)
      Log.info { "performing 'LockSnapshot' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "LockSnapshot")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("LockSnapshot", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::LockSnapshotResult.from_xml(result_node)
      Core::ParsedResponse(AEC::LockSnapshotResult).new(response, result)
    end

    # Modifies the account-level VPC Encryption Control configuration. This sets the encryption
    # control mode and resource exclusions that apply to the VPCs in your account. VPC Encryption
    # Control enables you to enforce encryption for all data in transit within and between VPCs to
    # meet compliance requirements.
    #
    # For more information, see [Enforce VPC encryption in
    # transit](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-encryption-controls.html) in the
    # *Amazon VPC User Guide*.
    def modify_account_vpc_encryption_control(input : AEC::ModifyAccountVpcEncryptionControlRequest) : Core::ParsedResponse(AEC::ModifyAccountVpcEncryptionControlResult)
      Log.info { "performing 'ModifyAccountVpcEncryptionControl' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyAccountVpcEncryptionControl")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyAccountVpcEncryptionControl", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyAccountVpcEncryptionControlResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyAccountVpcEncryptionControlResult).new(response, result)
    end

    # Modifies an attribute of the specified Elastic IP address. For requirements, see [Using reverse
    # DNS for email
    # applications](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html#Using_Elastic_Addressing_Reverse_DNS).
    def modify_address_attribute(input : AEC::ModifyAddressAttributeRequest) : Core::ParsedResponse(AEC::ModifyAddressAttributeResult)
      Log.info { "performing 'ModifyAddressAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyAddressAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyAddressAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyAddressAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyAddressAttributeResult).new(response, result)
    end

    # Modifies an existing application status check. You can update the protocol, port, path,
    # thresholds, and other configuration settings. The following rules apply:
    #
    # - The application status check must exist and belong to your account.
    #
    # - Changes take effect on the next health check interval.
    def modify_application_status_check(input : AEC::ModifyApplicationStatusCheckRequest) : Core::ParsedResponse(AEC::ModifyApplicationStatusCheckResult)
      Log.info { "performing 'ModifyApplicationStatusCheck' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyApplicationStatusCheck")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyApplicationStatusCheck", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyApplicationStatusCheckResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyApplicationStatusCheckResult).new(response, result)
    end

    # Changes the opt-in status of the specified zone group for your account.
    def modify_availability_zone_group(input : AEC::ModifyAvailabilityZoneGroupRequest) : Core::ParsedResponse(AEC::ModifyAvailabilityZoneGroupResult)
      Log.info { "performing 'ModifyAvailabilityZoneGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyAvailabilityZoneGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyAvailabilityZoneGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyAvailabilityZoneGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyAvailabilityZoneGroupResult).new(response, result)
    end

    # Modifies a Capacity Reservation's capacity, instance eligibility, and the conditions under which
    # it is to be released. You can't modify a Capacity Reservation's instance type, EBS optimization,
    # platform, instance store settings, Availability Zone, or tenancy. If you need to modify any of
    # these attributes, we recommend that you cancel the Capacity Reservation, and then create a new
    # one with the required attributes. For more information, see [ Modify an active Capacity
    # Reservation](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/capacity-reservations-modify.html).
    #
    # The allowed modifications depend on the state of the Capacity Reservation:
    #
    # - `assessing` or `scheduled` state - You can modify the tags only.
    #
    # - `pending` state - You can't modify the Capacity Reservation in any way.
    #
    # - `active` state but still within the commitment duration - You can't decrease the instance
    # count or set an end date that is within the commitment duration. All other modifications are
    # allowed.
    #
    # - `active` state with no commitment duration or elapsed commitment duration - All modifications
    # are allowed.
    #
    # - `expired`, `cancelled`, `unsupported`, or `failed` state - You can't modify the Capacity
    # Reservation in any way.
    def modify_capacity_reservation(input : AEC::ModifyCapacityReservationRequest) : Core::ParsedResponse(AEC::ModifyCapacityReservationResult)
      Log.info { "performing 'ModifyCapacityReservation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyCapacityReservation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyCapacityReservation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyCapacityReservationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyCapacityReservationResult).new(response, result)
    end

    # Modifies a Capacity Reservation Fleet.
    #
    # When you modify the total target capacity of a Capacity Reservation Fleet, the Fleet
    # automatically creates new Capacity Reservations, or modifies or cancels existing Capacity
    # Reservations in the Fleet to meet the new total target capacity. When you modify the end date
    # for the Fleet, the end dates for all of the individual Capacity Reservations in the Fleet are
    # updated accordingly.
    def modify_capacity_reservation_fleet(input : AEC::ModifyCapacityReservationFleetRequest) : Core::ParsedResponse(AEC::ModifyCapacityReservationFleetResult)
      Log.info { "performing 'ModifyCapacityReservationFleet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyCapacityReservationFleet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyCapacityReservationFleet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyCapacityReservationFleetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyCapacityReservationFleetResult).new(response, result)
    end

    # Modifies the specified Client VPN endpoint. Modifying the DNS server resets existing client
    # connections.
    def modify_client_vpn_endpoint(input : AEC::ModifyClientVpnEndpointRequest) : Core::ParsedResponse(AEC::ModifyClientVpnEndpointResult)
      Log.info { "performing 'ModifyClientVpnEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyClientVpnEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyClientVpnEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyClientVpnEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyClientVpnEndpointResult).new(response, result)
    end

    # Modifies the default credit option for CPU usage of burstable performance instances. The default
    # credit option is set at the account level per Amazon Web Services Region, and is specified per
    # instance family. All new burstable performance instances in the account launch using the default
    # credit option.
    #
    # `ModifyDefaultCreditSpecification` is an asynchronous operation, which works at an Amazon Web
    # Services Region level and modifies the credit option for each Availability Zone. All zones in a
    # Region are updated within five minutes. But if instances are launched during this operation,
    # they might not get the new credit option until the zone is updated. To verify whether the update
    # has occurred, you can call `GetDefaultCreditSpecification` and check
    # `DefaultCreditSpecification` for updates.
    #
    # For more information, see [Burstable performance
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/burstable-performance-instances.html)
    # in the *Amazon EC2 User Guide*.
    def modify_default_credit_specification(input : AEC::ModifyDefaultCreditSpecificationRequest) : Core::ParsedResponse(AEC::ModifyDefaultCreditSpecificationResult)
      Log.info { "performing 'ModifyDefaultCreditSpecification' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyDefaultCreditSpecification")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyDefaultCreditSpecification", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyDefaultCreditSpecificationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyDefaultCreditSpecificationResult).new(response, result)
    end

    # Changes the default KMS key for EBS encryption by default for your account in this Region.
    #
    # Amazon Web Services creates a unique Amazon Web Services managed KMS key in each Region for use
    # with encryption by default. If you change the default KMS key to a symmetric customer managed
    # KMS key, it is used instead of the Amazon Web Services managed KMS key. Amazon EBS does not
    # support asymmetric KMS keys.
    #
    # If you delete or disable the customer managed KMS key that you specified for use with encryption
    # by default, your instances will fail to launch.
    #
    # For more information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    def modify_ebs_default_kms_key_id(input : AEC::ModifyEbsDefaultKmsKeyIdRequest) : Core::ParsedResponse(AEC::ModifyEbsDefaultKmsKeyIdResult)
      Log.info { "performing 'ModifyEbsDefaultKmsKeyId' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyEbsDefaultKmsKeyId")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyEbsDefaultKmsKeyId", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyEbsDefaultKmsKeyIdResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyEbsDefaultKmsKeyIdResult).new(response, result)
    end

    # Modifies the specified EC2 Fleet.
    #
    # You can only modify an EC2 Fleet request of type `maintain`.
    #
    # While the EC2 Fleet is being modified, it is in the `modifying` state.
    #
    # To scale up your EC2 Fleet, increase its target capacity. The EC2 Fleet launches the additional
    # Spot Instances according to the allocation strategy for the EC2 Fleet request. If the allocation
    # strategy is `lowest-price`, the EC2 Fleet launches instances using the Spot Instance pool with
    # the lowest price. If the allocation strategy is `diversified`, the EC2 Fleet distributes the
    # instances across the Spot Instance pools. If the allocation strategy is `capacity-optimized`,
    # EC2 Fleet launches instances from Spot Instance pools with optimal capacity for the number of
    # instances that are launching.
    #
    # To scale down your EC2 Fleet, decrease its target capacity. First, the EC2 Fleet cancels any
    # open requests that exceed the new target capacity. You can request that the EC2 Fleet terminate
    # Spot Instances until the size of the fleet no longer exceeds the new target capacity. If the
    # allocation strategy is `lowest-price`, the EC2 Fleet terminates the instances with the highest
    # price per unit. If the allocation strategy is `capacity-optimized`, the EC2 Fleet terminates the
    # instances in the Spot Instance pools that have the least available Spot Instance capacity. If
    # the allocation strategy is `diversified`, the EC2 Fleet terminates instances across the Spot
    # Instance pools. Alternatively, you can request that the EC2 Fleet keep the fleet at its current
    # size, but not replace any Spot Instances that are interrupted or that you terminate manually.
    #
    # If you are finished with your EC2 Fleet for now, but will use it again later, you can set the
    # target capacity to 0.
    def modify_fleet(input : AEC::ModifyFleetRequest) : Core::ParsedResponse(AEC::ModifyFleetResult)
      Log.info { "performing 'ModifyFleet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyFleet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyFleet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyFleetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyFleetResult).new(response, result)
    end

    # Modifies the specified attribute of the specified Amazon FPGA Image (AFI).
    def modify_fpga_image_attribute(input : AEC::ModifyFpgaImageAttributeRequest) : Core::ParsedResponse(AEC::ModifyFpgaImageAttributeResult)
      Log.info { "performing 'ModifyFpgaImageAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyFpgaImageAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyFpgaImageAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyFpgaImageAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyFpgaImageAttributeResult).new(response, result)
    end

    # Modify the auto-placement setting of a Dedicated Host. When auto-placement is enabled, any
    # instances that you launch with a tenancy of `host` but without a specific host ID are placed
    # onto any available Dedicated Host in your account that has auto-placement enabled. When
    # auto-placement is disabled, you need to provide a host ID to have the instance launch onto a
    # specific host. If no host ID is provided, the instance is launched onto a suitable host with
    # auto-placement enabled.
    #
    # You can also use this API action to modify a Dedicated Host to support either multiple instance
    # types in an instance family, or to support a specific instance type only.
    def modify_hosts(input : AEC::ModifyHostsRequest) : Core::ParsedResponse(AEC::ModifyHostsResult)
      Log.info { "performing 'ModifyHosts' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyHosts")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyHosts", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyHostsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyHostsResult).new(response, result)
    end

    # Modifies the ID format of a resource for a specified IAM user, IAM role, or the root user for an
    # account; or all IAM users, IAM roles, and the root user for an account. You can specify that
    # resources should receive longer IDs (17-character IDs) when they are created.
    #
    # This request can only be used to modify longer ID settings for resource types that are within
    # the opt-in period. Resources currently in their opt-in period include: `bundle` |
    # `conversion-task` | `customer-gateway` | `dhcp-options` | `elastic-ip-allocation` |
    # `elastic-ip-association` | `export-task` | `flow-log` | `image` | `import-task` |
    # `internet-gateway` | `network-acl` | `network-acl-association` | `network-interface` |
    # `network-interface-attachment` | `prefix-list` | `route-table` | `route-table-association` |
    # `security-group` | `subnet` | `subnet-cidr-block-association` | `vpc` |
    # `vpc-cidr-block-association` | `vpc-endpoint` | `vpc-peering-connection` | `vpn-connection` |
    # `vpn-gateway`.
    #
    # For more information, see [Resource
    # IDs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/resource-ids.html) in the *Amazon
    # Elastic Compute Cloud User Guide*.
    #
    # This setting applies to the principal specified in the request; it does not apply to the
    # principal that makes the request.
    #
    # Resources created with longer IDs are visible to all IAM roles and users, regardless of these
    # settings and provided that they have permission to use the relevant `Describe` command for the
    # resource type.
    def modify_identity_id_format(input : AEC::ModifyIdentityIdFormatRequest) : Core::Response
      Log.info { "performing 'ModifyIdentityIdFormat' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIdentityIdFormat")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIdentityIdFormat", body)
      Core::Response.new(response)
    end

    # Modifies the ID format for the specified resource on a per-Region basis. You can specify that
    # resources should receive longer IDs (17-character IDs) when they are created.
    #
    # This request can only be used to modify longer ID settings for resource types that are within
    # the opt-in period. Resources currently in their opt-in period include: `bundle` |
    # `conversion-task` | `customer-gateway` | `dhcp-options` | `elastic-ip-allocation` |
    # `elastic-ip-association` | `export-task` | `flow-log` | `image` | `import-task` |
    # `internet-gateway` | `network-acl` | `network-acl-association` | `network-interface` |
    # `network-interface-attachment` | `prefix-list` | `route-table` | `route-table-association` |
    # `security-group` | `subnet` | `subnet-cidr-block-association` | `vpc` |
    # `vpc-cidr-block-association` | `vpc-endpoint` | `vpc-peering-connection` | `vpn-connection` |
    # `vpn-gateway`.
    #
    # This setting applies to the IAM user who makes the request; it does not apply to the entire
    # Amazon Web Services account. By default, an IAM user defaults to the same settings as the root
    # user. If you're using this action as the root user, then these settings apply to the entire
    # account, unless an IAM user explicitly overrides these settings for themselves. For more
    # information, see [Resource
    # IDs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/resource-ids.html) in the *Amazon
    # Elastic Compute Cloud User Guide*.
    #
    # Resources created with longer IDs are visible to all IAM roles and users, regardless of these
    # settings and provided that they have permission to use the relevant `Describe` command for the
    # resource type.
    def modify_id_format(input : AEC::ModifyIdFormatRequest) : Core::Response
      Log.info { "performing 'ModifyIdFormat' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIdFormat")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIdFormat", body)
      Core::Response.new(response)
    end

    # Modifies the specified attribute of the specified AMI. You can specify only one attribute at a
    # time.
    #
    # To specify the attribute, you can use the `Attribute` parameter, or one of the following
    # parameters: `Description`, `ImdsSupport`, or `LaunchPermission`.
    #
    # Images with an Amazon Web Services Marketplace product code cannot be made public.
    #
    # To enable the SriovNetSupport enhanced networking attribute of an image, enable SriovNetSupport
    # on an instance and create an AMI from the instance.
    def modify_image_attribute(input : AEC::ModifyImageAttributeRequest) : Core::Response
      Log.info { "performing 'ModifyImageAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyImageAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyImageAttribute", body)
      Core::Response.new(response)
    end

    # Modifies the specified attribute of the specified instance. You can specify only one attribute
    # at a time.
    #
    # **Note: **Using this action to change the security groups associated with an elastic network
    # interface (ENI) attached to an instance can result in an error if the instance has more than one
    # ENI. To change the security groups associated with an ENI attached to an instance that has
    # multiple ENIs, we recommend that you use the ModifyNetworkInterfaceAttribute action.
    #
    # To modify some attributes, the instance must be stopped. For more information, see [Modify a
    # stopped
    # instance](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_ChangingAttributesWhileInstanceStopped.html)
    # in the *Amazon EC2 User Guide*.
    def modify_instance_attribute(input : AEC::ModifyInstanceAttributeRequest) : Core::Response
      Log.info { "performing 'ModifyInstanceAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceAttribute", body)
      Core::Response.new(response)
    end

    # Modifies the Capacity Reservation settings for a stopped instance. Use this action to configure
    # an instance to target a specific Capacity Reservation, run in any `open` Capacity Reservation
    # with matching attributes, run in On-Demand Instance capacity, or only run in a Capacity
    # Reservation.
    def modify_instance_capacity_reservation_attributes(input : AEC::ModifyInstanceCapacityReservationAttributesRequest) : Core::ParsedResponse(AEC::ModifyInstanceCapacityReservationAttributesResult)
      Log.info { "performing 'ModifyInstanceCapacityReservationAttributes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceCapacityReservationAttributes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceCapacityReservationAttributes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceCapacityReservationAttributesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceCapacityReservationAttributesResult).new(response, result)
    end

    # Modifies the specified EC2 Instance Connect Endpoint.
    #
    # For more information, see [Modify an EC2 Instance Connect
    # Endpoint](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/modify-ec2-instance-connect-endpoint.html)
    # in the *Amazon EC2 User Guide*.
    def modify_instance_connect_endpoint(input : AEC::ModifyInstanceConnectEndpointRequest) : Core::ParsedResponse(AEC::ModifyInstanceConnectEndpointResult)
      Log.info { "performing 'ModifyInstanceConnectEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceConnectEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceConnectEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceConnectEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceConnectEndpointResult).new(response, result)
    end

    # By default, all vCPUs for the instance type are active when you launch an instance. When you
    # configure the number of active vCPUs for the instance, it can help you save on licensing costs
    # and optimize performance. The base cost of the instance remains unchanged.
    #
    # The number of active vCPUs equals the number of threads per CPU core multiplied by the number of
    # cores. The instance must be in a `Stopped` state before you make changes.
    #
    # Some instance type options do not support this capability. For more information, see [Supported
    # CPU
    # options](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/cpu-options-supported-instances-values.html)
    # in the *Amazon EC2 User Guide*.
    def modify_instance_cpu_options(input : AEC::ModifyInstanceCpuOptionsRequest) : Core::ParsedResponse(AEC::ModifyInstanceCpuOptionsResult)
      Log.info { "performing 'ModifyInstanceCpuOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceCpuOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceCpuOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceCpuOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceCpuOptionsResult).new(response, result)
    end

    # Modifies the credit option for CPU usage on a running or stopped burstable performance instance.
    # The credit options are `standard` and `unlimited`.
    #
    # For more information, see [Burstable performance
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/burstable-performance-instances.html)
    # in the *Amazon EC2 User Guide*.
    def modify_instance_credit_specification(input : AEC::ModifyInstanceCreditSpecificationRequest) : Core::ParsedResponse(AEC::ModifyInstanceCreditSpecificationResult)
      Log.info { "performing 'ModifyInstanceCreditSpecification' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceCreditSpecification")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceCreditSpecification", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceCreditSpecificationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceCreditSpecificationResult).new(response, result)
    end

    # Modifies the start time for a scheduled Amazon EC2 instance event.
    def modify_instance_event_start_time(input : AEC::ModifyInstanceEventStartTimeRequest) : Core::ParsedResponse(AEC::ModifyInstanceEventStartTimeResult)
      Log.info { "performing 'ModifyInstanceEventStartTime' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceEventStartTime")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceEventStartTime", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceEventStartTimeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceEventStartTimeResult).new(response, result)
    end

    # Modifies the specified event window.
    #
    # You can define either a set of time ranges or a cron expression when modifying the event window,
    # but not both.
    #
    # To modify the targets associated with the event window, use the AssociateInstanceEventWindow and
    # DisassociateInstanceEventWindow API.
    #
    # If Amazon Web Services has already scheduled an event, modifying an event window won't change
    # the time of the scheduled event.
    #
    # For more information, see [Define event windows for scheduled
    # events](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/event-windows.html) in the *Amazon
    # EC2 User Guide*.
    def modify_instance_event_window(input : AEC::ModifyInstanceEventWindowRequest) : Core::ParsedResponse(AEC::ModifyInstanceEventWindowResult)
      Log.info { "performing 'ModifyInstanceEventWindow' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceEventWindow")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceEventWindow", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceEventWindowResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceEventWindowResult).new(response, result)
    end

    # Modifies the recovery behavior of your instance to disable simplified automatic recovery or set
    # the recovery behavior to default. The default configuration will not enable simplified automatic
    # recovery for an unsupported instance type. For more information, see [Simplified automatic
    # recovery](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-recover.html#instance-configuration-recovery).
    #
    # Modifies the reboot migration behavior during a user-initiated reboot of an instance that has a
    # pending `system-reboot` event. For more information, see [Enable or disable reboot
    # migration](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/schedevents_actions_reboot.html#reboot-migration).
    def modify_instance_maintenance_options(input : AEC::ModifyInstanceMaintenanceOptionsRequest) : Core::ParsedResponse(AEC::ModifyInstanceMaintenanceOptionsResult)
      Log.info { "performing 'ModifyInstanceMaintenanceOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceMaintenanceOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceMaintenanceOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceMaintenanceOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceMaintenanceOptionsResult).new(response, result)
    end

    # Modifies the default instance metadata service (IMDS) settings at the account level in the
    # specified Amazon Web Services Region.
    #
    # To remove a parameter's account-level default setting, specify `no-preference`. If an
    # account-level setting is cleared with `no-preference`, then the instance launch considers the
    # other instance metadata settings. For more information, see [Order of precedence for instance
    # metadata
    # options](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configuring-instance-metadata-options.html#instance-metadata-options-order-of-precedence)
    # in the *Amazon EC2 User Guide*.
    def modify_instance_metadata_defaults(input : AEC::ModifyInstanceMetadataDefaultsRequest) : Core::ParsedResponse(AEC::ModifyInstanceMetadataDefaultsResult)
      Log.info { "performing 'ModifyInstanceMetadataDefaults' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceMetadataDefaults")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceMetadataDefaults", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceMetadataDefaultsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceMetadataDefaultsResult).new(response, result)
    end

    # Modify the instance metadata parameters on a running or stopped instance. When you modify the
    # parameters on a stopped instance, they are applied when the instance is started. When you modify
    # the parameters on a running instance, the API responds with a state of “pending”. After the
    # parameter modifications are successfully applied to the instance, the state of the modifications
    # changes from “pending” to “applied” in subsequent describe-instances API calls. For more
    # information, see [Instance metadata and user
    # data](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-metadata.html) in the
    # *Amazon EC2 User Guide*.
    def modify_instance_metadata_options(input : AEC::ModifyInstanceMetadataOptionsRequest) : Core::ParsedResponse(AEC::ModifyInstanceMetadataOptionsResult)
      Log.info { "performing 'ModifyInstanceMetadataOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceMetadataOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceMetadataOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceMetadataOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceMetadataOptionsResult).new(response, result)
    end

    # Change the configuration of the network performance options for an existing instance.
    def modify_instance_network_performance_options(input : AEC::ModifyInstanceNetworkPerformanceRequest) : Core::ParsedResponse(AEC::ModifyInstanceNetworkPerformanceResult)
      Log.info { "performing 'ModifyInstanceNetworkPerformanceOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstanceNetworkPerformanceOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstanceNetworkPerformanceOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstanceNetworkPerformanceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstanceNetworkPerformanceResult).new(response, result)
    end

    # Modifies the placement attributes for a specified instance. You can do the following:
    #
    # - Modify the affinity between an instance and a [Dedicated
    # Host](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/dedicated-hosts-overview.html). When
    # affinity is set to `host` and the instance is not associated with a specific Dedicated Host, the
    # next time the instance is started, it is automatically associated with the host on which it
    # lands. If the instance is restarted or rebooted, this relationship persists.
    #
    # - Change the Dedicated Host with which an instance is associated.
    #
    # - Change the instance tenancy of an instance.
    #
    # - Move an instance to or from a [placement
    # group](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/placement-groups.html).
    #
    # At least one attribute for affinity, host ID, tenancy, or placement group name must be specified
    # in the request. Affinity and tenancy can be modified in the same request.
    #
    # To modify the host ID, tenancy, placement group, or partition for an instance, the instance must
    # be in the `stopped` state.
    def modify_instance_placement(input : AEC::ModifyInstancePlacementRequest) : Core::ParsedResponse(AEC::ModifyInstancePlacementResult)
      Log.info { "performing 'ModifyInstancePlacement' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyInstancePlacement")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyInstancePlacement", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyInstancePlacementResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyInstancePlacementResult).new(response, result)
    end

    # Modify the configurations of an IPAM.
    def modify_ipam(input : AEC::ModifyIpamRequest) : Core::ParsedResponse(AEC::ModifyIpamResult)
      Log.info { "performing 'ModifyIpam' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpam")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpam", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamResult).new(response, result)
    end

    # Modifies the allocation rules in an IPAM policy.
    #
    # An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are
    # allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to
    # IPAM pools that the service will use to get IP addresses. A single policy can have multiple
    # rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of
    # addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to
    # an individual Amazon Web Services account or an entity within Amazon Web Services Organizations.
    #
    # Allocation rules are optional configurations within an IPAM policy that map Amazon Web Services
    # resource types to specific IPAM pools. If no rules are defined, the resource types default to
    # using Amazon-provided IP addresses.
    def modify_ipam_policy_allocation_rules(input : AEC::ModifyIpamPolicyAllocationRulesRequest) : Core::ParsedResponse(AEC::ModifyIpamPolicyAllocationRulesResult)
      Log.info { "performing 'ModifyIpamPolicyAllocationRules' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamPolicyAllocationRules")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamPolicyAllocationRules", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamPolicyAllocationRulesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamPolicyAllocationRulesResult).new(response, result)
    end

    # Modify the configurations of an IPAM pool.
    #
    # For more information, see [Modify a
    # pool](https://docs.aws.amazon.com/vpc/latest/ipam/mod-pool-ipam.html) in the *Amazon VPC IPAM
    # User Guide*.
    def modify_ipam_pool(input : AEC::ModifyIpamPoolRequest) : Core::ParsedResponse(AEC::ModifyIpamPoolResult)
      Log.info { "performing 'ModifyIpamPool' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamPool")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamPool", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamPoolResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamPoolResult).new(response, result)
    end

    # Modifies the description of an IPAM pool allocation. For more information, see [Modify an IPAM
    # pool allocation](https://docs.aws.amazon.com/vpc/latest/ipam/modify-alloc-ipam.html) in the
    # *Amazon VPC IPAM User Guide*.
    def modify_ipam_pool_allocation(input : AEC::ModifyIpamPoolAllocationRequest) : Core::ParsedResponse(AEC::ModifyIpamPoolAllocationResult)
      Log.info { "performing 'ModifyIpamPoolAllocation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamPoolAllocation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamPoolAllocation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamPoolAllocationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamPoolAllocationResult).new(response, result)
    end

    # Modifies an IPAM prefix list resolver. You can update the description and CIDR selection rules.
    # Changes to rules will trigger re-evaluation and potential updates to associated prefix lists.
    def modify_ipam_prefix_list_resolver(input : AEC::ModifyIpamPrefixListResolverRequest) : Core::ParsedResponse(AEC::ModifyIpamPrefixListResolverResult)
      Log.info { "performing 'ModifyIpamPrefixListResolver' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamPrefixListResolver")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamPrefixListResolver", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamPrefixListResolverResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamPrefixListResolverResult).new(response, result)
    end

    # Modifies an IPAM prefix list resolver target. You can update version tracking settings and the
    # desired version of the target prefix list.
    def modify_ipam_prefix_list_resolver_target(input : AEC::ModifyIpamPrefixListResolverTargetRequest) : Core::ParsedResponse(AEC::ModifyIpamPrefixListResolverTargetResult)
      Log.info { "performing 'ModifyIpamPrefixListResolverTarget' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamPrefixListResolverTarget")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamPrefixListResolverTarget", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamPrefixListResolverTargetResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamPrefixListResolverTargetResult).new(response, result)
    end

    # Modify a resource CIDR. You can use this action to transfer resource CIDRs between scopes and
    # ignore resource CIDRs that you do not want to manage. If set to false, the resource will not be
    # tracked for overlap, it cannot be auto-imported into a pool, and it will be removed from any
    # pool it has an allocation in.
    #
    # For more information, see [Move resource CIDRs between
    # scopes](https://docs.aws.amazon.com/vpc/latest/ipam/move-resource-ipam.html) and [Change the
    # monitoring state of resource
    # CIDRs](https://docs.aws.amazon.com/vpc/latest/ipam/change-monitoring-state-ipam.html) in the
    # *Amazon VPC IPAM User Guide*.
    def modify_ipam_resource_cidr(input : AEC::ModifyIpamResourceCidrRequest) : Core::ParsedResponse(AEC::ModifyIpamResourceCidrResult)
      Log.info { "performing 'ModifyIpamResourceCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamResourceCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamResourceCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamResourceCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamResourceCidrResult).new(response, result)
    end

    # Modifies a resource discovery. A resource discovery is an IPAM component that enables IPAM to
    # manage and monitor resources that belong to the owning account.
    def modify_ipam_resource_discovery(input : AEC::ModifyIpamResourceDiscoveryRequest) : Core::ParsedResponse(AEC::ModifyIpamResourceDiscoveryResult)
      Log.info { "performing 'ModifyIpamResourceDiscovery' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamResourceDiscovery")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamResourceDiscovery", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamResourceDiscoveryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamResourceDiscoveryResult).new(response, result)
    end

    # Modifies an existing routing policy registration. You can update the authorized ASNs, maximum
    # prefix length, and other properties of a Route Origin Authorization (ROA).
    def modify_ipam_routing_policy_registration(input : AEC::ModifyIpamRoutingPolicyRegistrationRequest) : Core::ParsedResponse(AEC::ModifyIpamRoutingPolicyRegistrationResult)
      Log.info { "performing 'ModifyIpamRoutingPolicyRegistration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamRoutingPolicyRegistration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamRoutingPolicyRegistration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamRoutingPolicyRegistrationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamRoutingPolicyRegistrationResult).new(response, result)
    end

    # Modify an IPAM scope.
    def modify_ipam_scope(input : AEC::ModifyIpamScopeRequest) : Core::ParsedResponse(AEC::ModifyIpamScopeResult)
      Log.info { "performing 'ModifyIpamScope' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyIpamScope")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyIpamScope", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyIpamScopeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyIpamScopeResult).new(response, result)
    end

    # Modifies a launch template. You can specify which version of the launch template to set as the
    # default version. When launching an instance, the default version applies when a launch template
    # version is not specified.
    def modify_launch_template(input : AEC::ModifyLaunchTemplateRequest) : Core::ParsedResponse(AEC::ModifyLaunchTemplateResult)
      Log.info { "performing 'ModifyLaunchTemplate' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyLaunchTemplate")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyLaunchTemplate", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyLaunchTemplateResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyLaunchTemplateResult).new(response, result)
    end

    # Modifies the specified local gateway route.
    def modify_local_gateway_route(input : AEC::ModifyLocalGatewayRouteRequest) : Core::ParsedResponse(AEC::ModifyLocalGatewayRouteResult)
      Log.info { "performing 'ModifyLocalGatewayRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyLocalGatewayRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyLocalGatewayRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyLocalGatewayRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyLocalGatewayRouteResult).new(response, result)
    end

    # Modifies the specified managed prefix list.
    #
    # Adding or removing entries in a prefix list creates a new version of the prefix list. Changing
    # the name of the prefix list does not affect the version.
    #
    # If you specify a current version number that does not match the true current version number, the
    # request fails.
    def modify_managed_prefix_list(input : AEC::ModifyManagedPrefixListRequest) : Core::ParsedResponse(AEC::ModifyManagedPrefixListResult)
      Log.info { "performing 'ModifyManagedPrefixList' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyManagedPrefixList")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyManagedPrefixList", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyManagedPrefixListResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyManagedPrefixListResult).new(response, result)
    end

    # Modifies the managed resource visibility configuration for the account. Use this operation to
    # control whether managed resources are hidden or visible by default. Visibility settings are
    # account-wide and affect all IAM principals uniformly. Hidden resources remain fully operational
    # and billable.
    def modify_managed_resource_visibility(input : AEC::ModifyManagedResourceVisibilityRequest) : Core::ParsedResponse(AEC::ModifyManagedResourceVisibilityResult)
      Log.info { "performing 'ModifyManagedResourceVisibility' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyManagedResourceVisibility")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyManagedResourceVisibility", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyManagedResourceVisibilityResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyManagedResourceVisibilityResult).new(response, result)
    end

    # Modifies the specified network interface attribute. You can specify only one attribute at a
    # time. You can use this action to attach and detach security groups from an existing EC2
    # instance.
    def modify_network_interface_attribute(input : AEC::ModifyNetworkInterfaceAttributeRequest) : Core::Response
      Log.info { "performing 'ModifyNetworkInterfaceAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyNetworkInterfaceAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyNetworkInterfaceAttribute", body)
      Core::Response.new(response)
    end

    # Modifies the options for instance hostnames for the specified instance.
    def modify_private_dns_name_options(input : AEC::ModifyPrivateDnsNameOptionsRequest) : Core::ParsedResponse(AEC::ModifyPrivateDnsNameOptionsResult)
      Log.info { "performing 'ModifyPrivateDnsNameOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyPrivateDnsNameOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyPrivateDnsNameOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyPrivateDnsNameOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyPrivateDnsNameOptionsResult).new(response, result)
    end

    # Modify public hostname options for a network interface. For more information, see [EC2 instance
    # hostnames, DNS names, and
    # domains](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-naming.html) in the
    # *Amazon EC2 User Guide*.
    def modify_public_ip_dns_name_options(input : AEC::ModifyPublicIpDnsNameOptionsRequest) : Core::ParsedResponse(AEC::ModifyPublicIpDnsNameOptionsResult)
      Log.info { "performing 'ModifyPublicIpDnsNameOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyPublicIpDnsNameOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyPublicIpDnsNameOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyPublicIpDnsNameOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyPublicIpDnsNameOptionsResult).new(response, result)
    end

    # Modifies the configuration of your Reserved Instances, such as the Availability Zone, instance
    # count, or instance type. The Reserved Instances to be modified must be identical, except for
    # Availability Zone, network platform, and instance type.
    #
    # For more information, see [Modify Reserved
    # Instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ri-modifying.html) in the *Amazon
    # EC2 User Guide*.
    def modify_reserved_instances(input : AEC::ModifyReservedInstancesRequest) : Core::ParsedResponse(AEC::ModifyReservedInstancesResult)
      Log.info { "performing 'ModifyReservedInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyReservedInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyReservedInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyReservedInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyReservedInstancesResult).new(response, result)
    end

    # Modifies the configuration of an existing route server.
    #
    # Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed
    # within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates
    # VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing
    # fault tolerance for those workloads. This enables you to automatically reroute traffic within a
    # VPC, which increases the manageability of VPC routing and interoperability with third-party
    # workloads.
    #
    # Route server supports the follow route table types:
    #
    # - VPC route tables not associated with subnets
    #
    # - Subnet route tables
    #
    # - Internet gateway route tables
    #
    # Route server does not support route tables associated with virtual private gateways. To
    # propagate routes into a transit gateway route table, use [Transit Gateway
    # Connect](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-connect.html).
    #
    # For more information see [Dynamic routing in your VPC with VPC Route
    # Server](https://docs.aws.amazon.com/vpc/latest/userguide/dynamic-routing-route-server.html) in
    # the *Amazon VPC User Guide*.
    def modify_route_server(input : AEC::ModifyRouteServerRequest) : Core::ParsedResponse(AEC::ModifyRouteServerResult)
      Log.info { "performing 'ModifyRouteServer' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyRouteServer")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyRouteServer", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyRouteServerResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyRouteServerResult).new(response, result)
    end

    # Modifies the rules of a security group.
    def modify_security_group_rules(input : AEC::ModifySecurityGroupRulesRequest) : Core::ParsedResponse(AEC::ModifySecurityGroupRulesResult)
      Log.info { "performing 'ModifySecurityGroupRules' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifySecurityGroupRules")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifySecurityGroupRules", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifySecurityGroupRulesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifySecurityGroupRulesResult).new(response, result)
    end

    # Adds or removes permission settings for the specified snapshot. You may add or remove specified
    # Amazon Web Services account IDs from a snapshot's list of create volume permissions, but you
    # cannot do both in a single operation. If you need to both add and remove account IDs for a
    # snapshot, you must use multiple operations. You can make up to 500 modifications to a snapshot
    # in a single operation.
    #
    # Encrypted snapshots and snapshots with Amazon Web Services Marketplace product codes cannot be
    # made public. Snapshots encrypted with your default KMS key cannot be shared with other accounts.
    #
    # For more information about modifying snapshot permissions, see [Share a
    # snapshot](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-modifying-snapshot-permissions.html)
    # in the *Amazon EBS User Guide*.
    def modify_snapshot_attribute(input : AEC::ModifySnapshotAttributeRequest) : Core::Response
      Log.info { "performing 'ModifySnapshotAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifySnapshotAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifySnapshotAttribute", body)
      Core::Response.new(response)
    end

    # Archives an Amazon EBS snapshot. When you archive a snapshot, it is converted to a full snapshot
    # that includes all of the blocks of data that were written to the volume at the time the snapshot
    # was created, and moved from the standard tier to the archive tier. For more information, see
    # [Archive Amazon EBS
    # snapshots](https://docs.aws.amazon.com/ebs/latest/userguide/snapshot-archive.html) in the
    # *Amazon EBS User Guide*.
    def modify_snapshot_tier(input : AEC::ModifySnapshotTierRequest) : Core::ParsedResponse(AEC::ModifySnapshotTierResult)
      Log.info { "performing 'ModifySnapshotTier' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifySnapshotTier")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifySnapshotTier", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifySnapshotTierResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifySnapshotTierResult).new(response, result)
    end

    # Modifies the specified Spot Fleet request.
    #
    # You can only modify a Spot Fleet request of type `maintain`.
    #
    # While the Spot Fleet request is being modified, it is in the `modifying` state.
    #
    # To scale up your Spot Fleet, increase its target capacity. The Spot Fleet launches the
    # additional Spot Instances according to the allocation strategy for the Spot Fleet request. If
    # the allocation strategy is `lowestPrice`, the Spot Fleet launches instances using the Spot
    # Instance pool with the lowest price. If the allocation strategy is `diversified`, the Spot Fleet
    # distributes the instances across the Spot Instance pools. If the allocation strategy is
    # `capacityOptimized`, Spot Fleet launches instances from Spot Instance pools with optimal
    # capacity for the number of instances that are launching.
    #
    # To scale down your Spot Fleet, decrease its target capacity. First, the Spot Fleet cancels any
    # open requests that exceed the new target capacity. You can request that the Spot Fleet terminate
    # Spot Instances until the size of the fleet no longer exceeds the new target capacity. If the
    # allocation strategy is `lowestPrice`, the Spot Fleet terminates the instances with the highest
    # price per unit. If the allocation strategy is `capacityOptimized`, the Spot Fleet terminates the
    # instances in the Spot Instance pools that have the least available Spot Instance capacity. If
    # the allocation strategy is `diversified`, the Spot Fleet terminates instances across the Spot
    # Instance pools. Alternatively, you can request that the Spot Fleet keep the fleet at its current
    # size, but not replace any Spot Instances that are interrupted or that you terminate manually.
    #
    # If you are finished with your Spot Fleet for now, but will use it again later, you can set the
    # target capacity to 0.
    def modify_spot_fleet_request(input : AEC::ModifySpotFleetRequestRequest) : Core::ParsedResponse(AEC::ModifySpotFleetRequestResponse)
      Log.info { "performing 'ModifySpotFleetRequest' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifySpotFleetRequest")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifySpotFleetRequest", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifySpotFleetRequestResponse.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifySpotFleetRequestResponse).new(response, result)
    end

    # Modifies a subnet attribute. You can only modify one attribute at a time.
    #
    # Use this action to modify subnets on Amazon Web Services Outposts.
    #
    # - To modify a subnet on an Outpost rack, set both `MapCustomerOwnedIpOnLaunch` and
    # `CustomerOwnedIpv4Pool`. These two parameters act as a single attribute.
    #
    # - To modify a subnet on an Outpost server, set either `EnableLniAtDeviceIndex` or
    # `DisableLniAtDeviceIndex`.
    #
    # For more information about Amazon Web Services Outposts, see the following:
    #
    # - [Outpost servers](https://docs.aws.amazon.com/outposts/latest/userguide/how-servers-work.html)
    #
    # - [Outpost racks](https://docs.aws.amazon.com/outposts/latest/userguide/how-racks-work.html)
    def modify_subnet_attribute(input : AEC::ModifySubnetAttributeRequest) : Core::Response
      Log.info { "performing 'ModifySubnetAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifySubnetAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifySubnetAttribute", body)
      Core::Response.new(response)
    end

    # Allows or restricts mirroring network services.
    #
    # By default, Amazon DNS network services are not eligible for Traffic Mirror. Use
    # `AddNetworkServices` to add network services to a Traffic Mirror filter. When a network service
    # is added to the Traffic Mirror filter, all traffic related to that network service will be
    # mirrored. When you no longer want to mirror network services, use `RemoveNetworkServices` to
    # remove the network services from the Traffic Mirror filter.
    def modify_traffic_mirror_filter_network_services(input : AEC::ModifyTrafficMirrorFilterNetworkServicesRequest) : Core::ParsedResponse(AEC::ModifyTrafficMirrorFilterNetworkServicesResult)
      Log.info { "performing 'ModifyTrafficMirrorFilterNetworkServices' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTrafficMirrorFilterNetworkServices")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTrafficMirrorFilterNetworkServices", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTrafficMirrorFilterNetworkServicesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTrafficMirrorFilterNetworkServicesResult).new(response, result)
    end

    # Modifies the specified Traffic Mirror rule.
    #
    # `DestinationCidrBlock` and `SourceCidrBlock` must both be an IPv4 range or an IPv6 range.
    def modify_traffic_mirror_filter_rule(input : AEC::ModifyTrafficMirrorFilterRuleRequest) : Core::ParsedResponse(AEC::ModifyTrafficMirrorFilterRuleResult)
      Log.info { "performing 'ModifyTrafficMirrorFilterRule' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTrafficMirrorFilterRule")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTrafficMirrorFilterRule", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTrafficMirrorFilterRuleResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTrafficMirrorFilterRuleResult).new(response, result)
    end

    # Modifies a Traffic Mirror session.
    def modify_traffic_mirror_session(input : AEC::ModifyTrafficMirrorSessionRequest) : Core::ParsedResponse(AEC::ModifyTrafficMirrorSessionResult)
      Log.info { "performing 'ModifyTrafficMirrorSession' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTrafficMirrorSession")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTrafficMirrorSession", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTrafficMirrorSessionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTrafficMirrorSessionResult).new(response, result)
    end

    # Modifies the specified transit gateway. When you modify a transit gateway, the modified options
    # are applied to new transit gateway attachments only. Your existing transit gateway attachments
    # are not modified.
    def modify_transit_gateway(input : AEC::ModifyTransitGatewayRequest) : Core::ParsedResponse(AEC::ModifyTransitGatewayResult)
      Log.info { "performing 'ModifyTransitGateway' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTransitGateway")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTransitGateway", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTransitGatewayResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTransitGatewayResult).new(response, result)
    end

    # Modifies a transit gateway metering policy.
    def modify_transit_gateway_metering_policy(input : AEC::ModifyTransitGatewayMeteringPolicyRequest) : Core::ParsedResponse(AEC::ModifyTransitGatewayMeteringPolicyResult)
      Log.info { "performing 'ModifyTransitGatewayMeteringPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTransitGatewayMeteringPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTransitGatewayMeteringPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTransitGatewayMeteringPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTransitGatewayMeteringPolicyResult).new(response, result)
    end

    # Modifies the specified transit gateway policy table entry.
    def modify_transit_gateway_policy_table_entry(input : AEC::ModifyTransitGatewayPolicyTableEntryRequest) : Core::ParsedResponse(AEC::ModifyTransitGatewayPolicyTableEntryResult)
      Log.info { "performing 'ModifyTransitGatewayPolicyTableEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTransitGatewayPolicyTableEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTransitGatewayPolicyTableEntry", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTransitGatewayPolicyTableEntryResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTransitGatewayPolicyTableEntryResult).new(response, result)
    end

    # Modifies a reference (route) to a prefix list in a specified transit gateway route table.
    def modify_transit_gateway_prefix_list_reference(input : AEC::ModifyTransitGatewayPrefixListReferenceRequest) : Core::ParsedResponse(AEC::ModifyTransitGatewayPrefixListReferenceResult)
      Log.info { "performing 'ModifyTransitGatewayPrefixListReference' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTransitGatewayPrefixListReference")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTransitGatewayPrefixListReference", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTransitGatewayPrefixListReferenceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTransitGatewayPrefixListReferenceResult).new(response, result)
    end

    # Modifies the specified VPC attachment.
    def modify_transit_gateway_vpc_attachment(input : AEC::ModifyTransitGatewayVpcAttachmentRequest) : Core::ParsedResponse(AEC::ModifyTransitGatewayVpcAttachmentResult)
      Log.info { "performing 'ModifyTransitGatewayVpcAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyTransitGatewayVpcAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyTransitGatewayVpcAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyTransitGatewayVpcAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyTransitGatewayVpcAttachmentResult).new(response, result)
    end

    # Modifies the configuration of the specified Amazon Web Services Verified Access endpoint.
    def modify_verified_access_endpoint(input : AEC::ModifyVerifiedAccessEndpointRequest) : Core::ParsedResponse(AEC::ModifyVerifiedAccessEndpointResult)
      Log.info { "performing 'ModifyVerifiedAccessEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVerifiedAccessEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVerifiedAccessEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVerifiedAccessEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVerifiedAccessEndpointResult).new(response, result)
    end

    # Modifies the specified Amazon Web Services Verified Access endpoint policy.
    def modify_verified_access_endpoint_policy(input : AEC::ModifyVerifiedAccessEndpointPolicyRequest) : Core::ParsedResponse(AEC::ModifyVerifiedAccessEndpointPolicyResult)
      Log.info { "performing 'ModifyVerifiedAccessEndpointPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVerifiedAccessEndpointPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVerifiedAccessEndpointPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVerifiedAccessEndpointPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVerifiedAccessEndpointPolicyResult).new(response, result)
    end

    # Modifies the specified Amazon Web Services Verified Access group configuration.
    def modify_verified_access_group(input : AEC::ModifyVerifiedAccessGroupRequest) : Core::ParsedResponse(AEC::ModifyVerifiedAccessGroupResult)
      Log.info { "performing 'ModifyVerifiedAccessGroup' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVerifiedAccessGroup")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVerifiedAccessGroup", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVerifiedAccessGroupResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVerifiedAccessGroupResult).new(response, result)
    end

    # Modifies the specified Amazon Web Services Verified Access group policy.
    def modify_verified_access_group_policy(input : AEC::ModifyVerifiedAccessGroupPolicyRequest) : Core::ParsedResponse(AEC::ModifyVerifiedAccessGroupPolicyResult)
      Log.info { "performing 'ModifyVerifiedAccessGroupPolicy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVerifiedAccessGroupPolicy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVerifiedAccessGroupPolicy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVerifiedAccessGroupPolicyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVerifiedAccessGroupPolicyResult).new(response, result)
    end

    # Modifies the configuration of the specified Amazon Web Services Verified Access instance.
    def modify_verified_access_instance(input : AEC::ModifyVerifiedAccessInstanceRequest) : Core::ParsedResponse(AEC::ModifyVerifiedAccessInstanceResult)
      Log.info { "performing 'ModifyVerifiedAccessInstance' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVerifiedAccessInstance")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVerifiedAccessInstance", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVerifiedAccessInstanceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVerifiedAccessInstanceResult).new(response, result)
    end

    # Modifies the logging configuration for the specified Amazon Web Services Verified Access
    # instance.
    def modify_verified_access_instance_logging_configuration(input : AEC::ModifyVerifiedAccessInstanceLoggingConfigurationRequest) : Core::ParsedResponse(AEC::ModifyVerifiedAccessInstanceLoggingConfigurationResult)
      Log.info { "performing 'ModifyVerifiedAccessInstanceLoggingConfiguration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVerifiedAccessInstanceLoggingConfiguration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVerifiedAccessInstanceLoggingConfiguration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVerifiedAccessInstanceLoggingConfigurationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVerifiedAccessInstanceLoggingConfigurationResult).new(response, result)
    end

    # Modifies the configuration of the specified Amazon Web Services Verified Access trust provider.
    def modify_verified_access_trust_provider(input : AEC::ModifyVerifiedAccessTrustProviderRequest) : Core::ParsedResponse(AEC::ModifyVerifiedAccessTrustProviderResult)
      Log.info { "performing 'ModifyVerifiedAccessTrustProvider' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVerifiedAccessTrustProvider")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVerifiedAccessTrustProvider", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVerifiedAccessTrustProviderResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVerifiedAccessTrustProviderResult).new(response, result)
    end

    # You can modify several parameters of an existing EBS volume, including volume size, volume type,
    # and IOPS capacity. If your EBS volume is attached to a current-generation EC2 instance type, you
    # might be able to apply these changes without stopping the instance or detaching the volume from
    # it. For more information about modifying EBS volumes, see [Amazon EBS Elastic
    # Volumes](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-modify-volume.html) in the *Amazon
    # EBS User Guide*.
    #
    # When you complete a resize operation on your volume, you need to extend the volume's file-system
    # size to take advantage of the new storage capacity. For more information, see [Extend the file
    # system](https://docs.aws.amazon.com/ebs/latest/userguide/recognize-expanded-volume-linux.html).
    #
    # For more information, see [Monitor the progress of volume
    # modifications](https://docs.aws.amazon.com/ebs/latest/userguide/monitoring-volume-modifications.html)
    # in the *Amazon EBS User Guide*.
    #
    # With previous-generation instance types, resizing an EBS volume might require detaching and
    # reattaching the volume or stopping and restarting the instance.
    #
    # After you initiate a volume modification, you must wait for that modification to reach the
    # `completed` state before you can initiate another modification for the same volume. You can
    # modify a volume up to four times within a rolling 24-hour period, as long as the volume is in
    # the `in-use` or `available` state, and all previous modifications for that volume are
    # `completed`. If you exceed this limit, you get an error message that indicates when you can
    # perform your next modification.
    def modify_volume(input : AEC::ModifyVolumeRequest) : Core::ParsedResponse(AEC::ModifyVolumeResult)
      Log.info { "performing 'ModifyVolume' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVolume")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVolume", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVolumeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVolumeResult).new(response, result)
    end

    # Modifies a volume attribute.
    #
    # By default, all I/O operations for the volume are suspended when the data on the volume is
    # determined to be potentially inconsistent, to prevent undetectable, latent data corruption. The
    # I/O access to the volume can be resumed by first enabling I/O access and then checking the data
    # consistency on your volume.
    #
    # You can change the default behavior to resume I/O operations. We recommend that you change this
    # only for boot volumes or for volumes that are stateless or disposable.
    def modify_volume_attribute(input : AEC::ModifyVolumeAttributeRequest) : Core::Response
      Log.info { "performing 'ModifyVolumeAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVolumeAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVolumeAttribute", body)
      Core::Response.new(response)
    end

    # Modifies the specified attribute of the specified VPC.
    def modify_vpc_attribute(input : AEC::ModifyVpcAttributeRequest) : Core::Response
      Log.info { "performing 'ModifyVpcAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcAttribute", body)
      Core::Response.new(response)
    end

    # Modify VPC Block Public Access (BPA) exclusions. A VPC BPA exclusion is a mode that can be
    # applied to a single VPC or subnet that exempts it from the account’s BPA mode and will allow
    # bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even
    # when BPA is not enabled on the account to ensure that there is no traffic disruption to the
    # exclusions when VPC BPA is turned on.
    def modify_vpc_block_public_access_exclusion(input : AEC::ModifyVpcBlockPublicAccessExclusionRequest) : Core::ParsedResponse(AEC::ModifyVpcBlockPublicAccessExclusionResult)
      Log.info { "performing 'ModifyVpcBlockPublicAccessExclusion' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcBlockPublicAccessExclusion")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcBlockPublicAccessExclusion", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcBlockPublicAccessExclusionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcBlockPublicAccessExclusionResult).new(response, result)
    end

    # Modify VPC Block Public Access (BPA) options. VPC Block Public Access (BPA) enables you to block
    # resources in VPCs and subnets that you own in a Region from reaching or being reached from the
    # internet through internet gateways and egress-only internet gateways. To learn more about VPC
    # BPA, see [Block public access to VPCs and
    # subnets](https://docs.aws.amazon.com/vpc/latest/userguide/security-vpc-bpa.html) in the *Amazon
    # VPC User Guide*.
    def modify_vpc_block_public_access_options(input : AEC::ModifyVpcBlockPublicAccessOptionsRequest) : Core::ParsedResponse(AEC::ModifyVpcBlockPublicAccessOptionsResult)
      Log.info { "performing 'ModifyVpcBlockPublicAccessOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcBlockPublicAccessOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcBlockPublicAccessOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcBlockPublicAccessOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcBlockPublicAccessOptionsResult).new(response, result)
    end

    # Modifies the encryption control configuration for a VPC. You can update the encryption mode and
    # exclusion settings for various gateway types and peering connections.
    #
    # For more information, see [Enforce VPC encryption in
    # transit](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-encryption-controls.html) in the
    # *Amazon VPC User Guide*.
    def modify_vpc_encryption_control(input : AEC::ModifyVpcEncryptionControlRequest) : Core::ParsedResponse(AEC::ModifyVpcEncryptionControlResult)
      Log.info { "performing 'ModifyVpcEncryptionControl' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcEncryptionControl")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcEncryptionControl", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcEncryptionControlResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcEncryptionControlResult).new(response, result)
    end

    # Modifies attributes of a specified VPC endpoint. The attributes that you can modify depend on
    # the type of VPC endpoint (interface, gateway, or Gateway Load Balancer). For more information,
    # see the [Amazon Web Services PrivateLink
    # Guide](https://docs.aws.amazon.com/vpc/latest/privatelink/).
    def modify_vpc_endpoint(input : AEC::ModifyVpcEndpointRequest) : Core::ParsedResponse(AEC::ModifyVpcEndpointResult)
      Log.info { "performing 'ModifyVpcEndpoint' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcEndpoint")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcEndpoint", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcEndpointResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcEndpointResult).new(response, result)
    end

    # Modifies a connection notification for VPC endpoint or VPC endpoint service. You can change the
    # SNS topic for the notification, or the events for which to be notified.
    def modify_vpc_endpoint_connection_notification(input : AEC::ModifyVpcEndpointConnectionNotificationRequest) : Core::ParsedResponse(AEC::ModifyVpcEndpointConnectionNotificationResult)
      Log.info { "performing 'ModifyVpcEndpointConnectionNotification' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcEndpointConnectionNotification")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcEndpointConnectionNotification", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcEndpointConnectionNotificationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcEndpointConnectionNotificationResult).new(response, result)
    end

    # Modifies the billing account for VPC endpoint usage/charges.
    def modify_vpc_endpoint_payer_responsibility(input : AEC::ModifyVpcEndpointPayerResponsibilityRequest) : Core::ParsedResponse(AEC::ModifyVpcEndpointPayerResponsibilityResult)
      Log.info { "performing 'ModifyVpcEndpointPayerResponsibility' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcEndpointPayerResponsibility")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcEndpointPayerResponsibility", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcEndpointPayerResponsibilityResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcEndpointPayerResponsibilityResult).new(response, result)
    end

    # Modifies the attributes of the specified VPC endpoint service configuration.
    #
    # If you set or modify the private DNS name, you must prove that you own the private DNS domain
    # name.
    def modify_vpc_endpoint_service_configuration(input : AEC::ModifyVpcEndpointServiceConfigurationRequest) : Core::ParsedResponse(AEC::ModifyVpcEndpointServiceConfigurationResult)
      Log.info { "performing 'ModifyVpcEndpointServiceConfiguration' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcEndpointServiceConfiguration")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcEndpointServiceConfiguration", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcEndpointServiceConfigurationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcEndpointServiceConfigurationResult).new(response, result)
    end

    # Modifies the payer responsibility for your VPC endpoint service.
    def modify_vpc_endpoint_service_payer_responsibility(input : AEC::ModifyVpcEndpointServicePayerResponsibilityRequest) : Core::ParsedResponse(AEC::ModifyVpcEndpointServicePayerResponsibilityResult)
      Log.info { "performing 'ModifyVpcEndpointServicePayerResponsibility' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcEndpointServicePayerResponsibility")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcEndpointServicePayerResponsibility", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcEndpointServicePayerResponsibilityResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcEndpointServicePayerResponsibilityResult).new(response, result)
    end

    # Modifies the permissions for your VPC endpoint service. You can add or remove permissions for
    # service consumers (Amazon Web Services accounts, users, and IAM roles) to connect to your
    # endpoint service. Principal ARNs with path components aren't supported.
    #
    # If you grant permissions to all principals, the service is public. Any users who know the name
    # of a public service can send a request to attach an endpoint. If the service does not require
    # manual approval, attachments are automatically approved.
    def modify_vpc_endpoint_service_permissions(input : AEC::ModifyVpcEndpointServicePermissionsRequest) : Core::ParsedResponse(AEC::ModifyVpcEndpointServicePermissionsResult)
      Log.info { "performing 'ModifyVpcEndpointServicePermissions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcEndpointServicePermissions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcEndpointServicePermissions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcEndpointServicePermissionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcEndpointServicePermissionsResult).new(response, result)
    end

    # Modifies the VPC peering connection options on one side of a VPC peering connection.
    #
    # If the peered VPCs are in the same Amazon Web Services account, you can enable DNS resolution
    # for queries from the local VPC. This ensures that queries from the local VPC resolve to private
    # IP addresses in the peer VPC. This option is not available if the peered VPCs are in different
    # Amazon Web Services accounts or different Regions. For peered VPCs in different Amazon Web
    # Services accounts, each Amazon Web Services account owner must initiate a separate request to
    # modify the peering connection options. For inter-region peering connections, you must use the
    # Region for the requester VPC to modify the requester VPC peering options and the Region for the
    # accepter VPC to modify the accepter VPC peering options. To verify which VPCs are the accepter
    # and the requester for a VPC peering connection, use the DescribeVpcPeeringConnections command.
    def modify_vpc_peering_connection_options(input : AEC::ModifyVpcPeeringConnectionOptionsRequest) : Core::ParsedResponse(AEC::ModifyVpcPeeringConnectionOptionsResult)
      Log.info { "performing 'ModifyVpcPeeringConnectionOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcPeeringConnectionOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcPeeringConnectionOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcPeeringConnectionOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcPeeringConnectionOptionsResult).new(response, result)
    end

    # Modifies the instance tenancy attribute of the specified VPC. You can change the instance
    # tenancy attribute of a VPC to `default` only. You cannot change the instance tenancy attribute
    # to `dedicated`.
    #
    # After you modify the tenancy of the VPC, any new instances that you launch into the VPC have a
    # tenancy of `default`, unless you specify otherwise during launch. The tenancy of any existing
    # instances in the VPC is not affected.
    #
    # For more information, see [Dedicated
    # Instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/dedicated-instance.html) in the
    # *Amazon EC2 User Guide*.
    def modify_vpc_tenancy(input : AEC::ModifyVpcTenancyRequest) : Core::ParsedResponse(AEC::ModifyVpcTenancyResult)
      Log.info { "performing 'ModifyVpcTenancy' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpcTenancy")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpcTenancy", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpcTenancyResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpcTenancyResult).new(response, result)
    end

    # Modifies the customer gateway or the target gateway of an Amazon Web Services Site-to-Site VPN
    # connection. To modify the target gateway, the following migration options are available:
    #
    # - An existing virtual private gateway to a new virtual private gateway
    #
    # - An existing virtual private gateway to a transit gateway
    #
    # - An existing transit gateway to a new transit gateway
    #
    # - An existing transit gateway to a virtual private gateway
    #
    # Before you perform the migration to the new gateway, you must configure the new gateway. Use
    # CreateVpnGateway to create a virtual private gateway, or CreateTransitGateway to create a
    # transit gateway.
    #
    # This step is required when you migrate from a virtual private gateway with static routes to a
    # transit gateway.
    #
    # You must delete the static routes before you migrate to the new gateway.
    #
    # Keep a copy of the static route before you delete it. You will need to add back these routes to
    # the transit gateway after the VPN connection migration is complete.
    #
    # After you migrate to the new gateway, you might need to modify your VPC route table. Use
    # CreateRoute and DeleteRoute to make the changes described in [Update VPC route
    # tables](https://docs.aws.amazon.com/vpn/latest/s2svpn/modify-vpn-target.html#step-update-routing)
    # in the *Amazon Web Services Site-to-Site VPN User Guide*.
    #
    # When the new gateway is a transit gateway, modify the transit gateway route table to allow
    # traffic between the VPC and the Amazon Web Services Site-to-Site VPN connection. Use
    # CreateTransitGatewayRoute to add the routes.
    #
    # If you deleted VPN static routes, you must add the static routes to the transit gateway route
    # table.
    #
    # After you perform this operation, the VPN endpoint's IP addresses on the Amazon Web Services
    # side and the tunnel options remain intact. Your Amazon Web Services Site-to-Site VPN connection
    # will be temporarily unavailable for a brief period while we provision the new endpoints.
    def modify_vpn_connection(input : AEC::ModifyVpnConnectionRequest) : Core::ParsedResponse(AEC::ModifyVpnConnectionResult)
      Log.info { "performing 'ModifyVpnConnection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpnConnection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpnConnection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpnConnectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpnConnectionResult).new(response, result)
    end

    # Modifies the connection options for your Site-to-Site VPN connection.
    #
    # When you modify the VPN connection options, the VPN endpoint IP addresses on the Amazon Web
    # Services side do not change, and the tunnel options do not change. Your VPN connection will be
    # temporarily unavailable for a brief period while the VPN connection is updated.
    def modify_vpn_connection_options(input : AEC::ModifyVpnConnectionOptionsRequest) : Core::ParsedResponse(AEC::ModifyVpnConnectionOptionsResult)
      Log.info { "performing 'ModifyVpnConnectionOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpnConnectionOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpnConnectionOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpnConnectionOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpnConnectionOptionsResult).new(response, result)
    end

    # Modifies the VPN tunnel endpoint certificate.
    def modify_vpn_tunnel_certificate(input : AEC::ModifyVpnTunnelCertificateRequest) : Core::ParsedResponse(AEC::ModifyVpnTunnelCertificateResult)
      Log.info { "performing 'ModifyVpnTunnelCertificate' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpnTunnelCertificate")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpnTunnelCertificate", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpnTunnelCertificateResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpnTunnelCertificateResult).new(response, result)
    end

    # Modifies the options for a VPN tunnel in an Amazon Web Services Site-to-Site VPN connection. You
    # can modify multiple options for a tunnel in a single request, but you can only modify one tunnel
    # at a time. For more information, see [Site-to-Site VPN tunnel options for your Site-to-Site VPN
    # connection](https://docs.aws.amazon.com/vpn/latest/s2svpn/VPNTunnels.html) in the *Amazon Web
    # Services Site-to-Site VPN User Guide*.
    def modify_vpn_tunnel_options(input : AEC::ModifyVpnTunnelOptionsRequest) : Core::ParsedResponse(AEC::ModifyVpnTunnelOptionsResult)
      Log.info { "performing 'ModifyVpnTunnelOptions' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ModifyVpnTunnelOptions")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ModifyVpnTunnelOptions", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ModifyVpnTunnelOptionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ModifyVpnTunnelOptionsResult).new(response, result)
    end

    # Enables detailed monitoring for a running instance. Otherwise, basic monitoring is enabled. For
    # more information, see [Monitor your instances using
    # CloudWatch](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-cloudwatch.html) in the
    # *Amazon EC2 User Guide*.
    #
    # To disable detailed monitoring, see
    # [UnmonitorInstances](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_UnmonitorInstances.html).
    def monitor_instances(input : AEC::MonitorInstancesRequest) : Core::ParsedResponse(AEC::MonitorInstancesResult)
      Log.info { "performing 'MonitorInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "MonitorInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("MonitorInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::MonitorInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::MonitorInstancesResult).new(response, result)
    end

    # This action is deprecated.
    #
    # Moves an Elastic IP address from the EC2-Classic platform to the EC2-VPC platform. The Elastic
    # IP address must be allocated to your account for more than 24 hours, and it must not be
    # associated with an instance. After the Elastic IP address is moved, it is no longer available
    # for use in the EC2-Classic platform. You cannot move an Elastic IP address that was originally
    # allocated for use in the EC2-VPC platform to the EC2-Classic platform.
    def move_address_to_vpc(input : AEC::MoveAddressToVpcRequest) : Core::ParsedResponse(AEC::MoveAddressToVpcResult)
      Log.info { "performing 'MoveAddressToVpc' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "MoveAddressToVpc")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("MoveAddressToVpc", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::MoveAddressToVpcResult.from_xml(result_node)
      Core::ParsedResponse(AEC::MoveAddressToVpcResult).new(response, result)
    end

    # Move a BYOIPv4 CIDR to IPAM from a public IPv4 pool.
    #
    # If you already have a BYOIPv4 CIDR with Amazon Web Services, you can move the CIDR to IPAM from
    # a public IPv4 pool. You cannot move an IPv6 CIDR to IPAM. If you are bringing a new IP address
    # to Amazon Web Services for the first time, complete the steps in [Tutorial: BYOIP address CIDRs
    # to IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/tutorials-byoip-ipam.html).
    def move_byoip_cidr_to_ipam(input : AEC::MoveByoipCidrToIpamRequest) : Core::ParsedResponse(AEC::MoveByoipCidrToIpamResult)
      Log.info { "performing 'MoveByoipCidrToIpam' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "MoveByoipCidrToIpam")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("MoveByoipCidrToIpam", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::MoveByoipCidrToIpamResult.from_xml(result_node)
      Core::ParsedResponse(AEC::MoveByoipCidrToIpamResult).new(response, result)
    end

    # Move available capacity from a source Capacity Reservation to a destination Capacity
    # Reservation. The source Capacity Reservation and the destination Capacity Reservation must be
    # `active`, owned by your Amazon Web Services account, and share the following:
    #
    # - Instance type
    #
    # - Platform
    #
    # - Availability Zone
    #
    # - Tenancy
    #
    # - Placement group
    #
    # - Capacity Reservation end time - `At specific time` or `Manually`.
    def move_capacity_reservation_instances(input : AEC::MoveCapacityReservationInstancesRequest) : Core::ParsedResponse(AEC::MoveCapacityReservationInstancesResult)
      Log.info { "performing 'MoveCapacityReservationInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "MoveCapacityReservationInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("MoveCapacityReservationInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::MoveCapacityReservationInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::MoveCapacityReservationInstancesResult).new(response, result)
    end

    # Provisions an IPv4 or IPv6 address range for use with your Amazon Web Services resources through
    # bring your own IP addresses (BYOIP) and creates a corresponding address pool. After the address
    # range is provisioned, it is ready to be advertised.
    #
    # Amazon Web Services verifies that you own the address range and are authorized to advertise it.
    # You must ensure that the address range is registered to you and that you created an RPKI ROA to
    # authorize Amazon ASNs 16509 and 14618 to advertise the address range. For the Amazon Web
    # Services GovCloud (US) Regions, authorize only ASN 8987. For the Amazon Web Services European
    # Sovereign Cloud, authorize ASNs 16509 and 214101. For more information, see [Bring your own IP
    # addresses (BYOIP)](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-byoip.html) in the
    # *Amazon EC2 User Guide*.
    #
    # Provisioning an address range is an asynchronous operation, so the call returns immediately, but
    # the address range is not ready to use until its status changes from `pending-provision` to
    # `provisioned`. For more information, see [Onboard your address
    # range](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/byoip-onboard.html).
    def provision_byoip_cidr(input : AEC::ProvisionByoipCidrRequest) : Core::ParsedResponse(AEC::ProvisionByoipCidrResult)
      Log.info { "performing 'ProvisionByoipCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ProvisionByoipCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ProvisionByoipCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ProvisionByoipCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ProvisionByoipCidrResult).new(response, result)
    end

    # Provisions your Autonomous System Number (ASN) for use in your Amazon Web Services account. This
    # action requires authorization context for Amazon to bring the ASN to an Amazon Web Services
    # account. For more information, see [Tutorial: Bring your ASN to
    # IPAM](https://docs.aws.amazon.com/vpc/latest/ipam/tutorials-byoasn.html) in the *Amazon VPC IPAM
    # guide*.
    def provision_ipam_byoasn(input : AEC::ProvisionIpamByoasnRequest) : Core::ParsedResponse(AEC::ProvisionIpamByoasnResult)
      Log.info { "performing 'ProvisionIpamByoasn' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ProvisionIpamByoasn")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ProvisionIpamByoasn", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ProvisionIpamByoasnResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ProvisionIpamByoasnResult).new(response, result)
    end

    # Provision a CIDR to an IPAM pool. You can use this action to provision new CIDRs to a top-level
    # pool or to transfer a CIDR from a top-level pool to a pool within it.
    #
    # For more information, see [Provision CIDRs to
    # pools](https://docs.aws.amazon.com/vpc/latest/ipam/prov-cidr-ipam.html) in the *Amazon VPC IPAM
    # User Guide*.
    def provision_ipam_pool_cidr(input : AEC::ProvisionIpamPoolCidrRequest) : Core::ParsedResponse(AEC::ProvisionIpamPoolCidrResult)
      Log.info { "performing 'ProvisionIpamPoolCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ProvisionIpamPoolCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ProvisionIpamPoolCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ProvisionIpamPoolCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ProvisionIpamPoolCidrResult).new(response, result)
    end

    # Provision a CIDR to a public IPv4 pool.
    #
    # For more information about IPAM, see [What is
    # IPAM?](https://docs.aws.amazon.com/vpc/latest/ipam/what-is-it-ipam.html) in the *Amazon VPC IPAM
    # User Guide*.
    def provision_public_ipv_4_pool_cidr(input : AEC::ProvisionPublicIpv4PoolCidrRequest) : Core::ParsedResponse(AEC::ProvisionPublicIpv4PoolCidrResult)
      Log.info { "performing 'ProvisionPublicIpv4PoolCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ProvisionPublicIpv4PoolCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ProvisionPublicIpv4PoolCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ProvisionPublicIpv4PoolCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ProvisionPublicIpv4PoolCidrResult).new(response, result)
    end

    # Purchase the Capacity Block for use with your account. With Capacity Blocks you ensure GPU
    # capacity is available for machine learning (ML) workloads. You must specify the ID of the
    # Capacity Block offering you are purchasing.
    def purchase_capacity_block(input : AEC::PurchaseCapacityBlockRequest) : Core::ParsedResponse(AEC::PurchaseCapacityBlockResult)
      Log.info { "performing 'PurchaseCapacityBlock' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "PurchaseCapacityBlock")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("PurchaseCapacityBlock", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::PurchaseCapacityBlockResult.from_xml(result_node)
      Core::ParsedResponse(AEC::PurchaseCapacityBlockResult).new(response, result)
    end

    # Purchase the Capacity Block extension for use with your account. You must specify the ID of the
    # Capacity Block extension offering you are purchasing.
    def purchase_capacity_block_extension(input : AEC::PurchaseCapacityBlockExtensionRequest) : Core::ParsedResponse(AEC::PurchaseCapacityBlockExtensionResult)
      Log.info { "performing 'PurchaseCapacityBlockExtension' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "PurchaseCapacityBlockExtension")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("PurchaseCapacityBlockExtension", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::PurchaseCapacityBlockExtensionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::PurchaseCapacityBlockExtensionResult).new(response, result)
    end

    # Purchase a reservation with configurations that match those of your Dedicated Host. You must
    # have active Dedicated Hosts in your account before you purchase a reservation. This action
    # results in the specified reservation being purchased and charged to your account.
    def purchase_host_reservation(input : AEC::PurchaseHostReservationRequest) : Core::ParsedResponse(AEC::PurchaseHostReservationResult)
      Log.info { "performing 'PurchaseHostReservation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "PurchaseHostReservation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("PurchaseHostReservation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::PurchaseHostReservationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::PurchaseHostReservationResult).new(response, result)
    end

    # Purchases a Reserved Instance for use with your account. With Reserved Instances, you pay a
    # lower hourly rate compared to On-Demand instance pricing.
    #
    # Use DescribeReservedInstancesOfferings to get a list of Reserved Instance offerings that match
    # your specifications. After you've purchased a Reserved Instance, you can check for your new
    # Reserved Instance with DescribeReservedInstances.
    #
    # To queue a purchase for a future date and time, specify a purchase time. If you do not specify a
    # purchase time, the default is the current time.
    #
    # For more information, see [Reserved
    # Instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/concepts-on-demand-reserved-instances.html)
    # and [Sell in the Reserved Instance
    # Marketplace](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ri-market-general.html) in the
    # *Amazon EC2 User Guide*.
    def purchase_reserved_instances_offering(input : AEC::PurchaseReservedInstancesOfferingRequest) : Core::ParsedResponse(AEC::PurchaseReservedInstancesOfferingResult)
      Log.info { "performing 'PurchaseReservedInstancesOffering' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "PurchaseReservedInstancesOffering")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("PurchaseReservedInstancesOffering", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::PurchaseReservedInstancesOfferingResult.from_xml(result_node)
      Core::ParsedResponse(AEC::PurchaseReservedInstancesOfferingResult).new(response, result)
    end

    # You can no longer purchase Scheduled Instances.
    #
    # Purchases the Scheduled Instances with the specified schedule.
    #
    # Scheduled Instances enable you to purchase Amazon EC2 compute capacity by the hour for a
    # one-year term. Before you can purchase a Scheduled Instance, you must call
    # DescribeScheduledInstanceAvailability to check for available schedules and obtain a purchase
    # token. After you purchase a Scheduled Instance, you must call RunScheduledInstances during each
    # scheduled time period.
    #
    # After you purchase a Scheduled Instance, you can't cancel, modify, or resell your purchase.
    def purchase_scheduled_instances(input : AEC::PurchaseScheduledInstancesRequest) : Core::ParsedResponse(AEC::PurchaseScheduledInstancesResult)
      Log.info { "performing 'PurchaseScheduledInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "PurchaseScheduledInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("PurchaseScheduledInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::PurchaseScheduledInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::PurchaseScheduledInstancesResult).new(response, result)
    end

    # Requests a reboot of the specified instances. This operation is asynchronous; it only queues a
    # request to reboot the specified instances. The operation succeeds if the instances are valid and
    # belong to you. Requests to reboot terminated instances are ignored.
    #
    # If an instance does not cleanly shut down within a few minutes, Amazon EC2 performs a hard
    # reboot.
    #
    # For more information about troubleshooting, see [Troubleshoot an unreachable
    # instance](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-console.html) in the
    # *Amazon EC2 User Guide*.
    def reboot_instances(input : AEC::RebootInstancesRequest) : Core::Response
      Log.info { "performing 'RebootInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RebootInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RebootInstances", body)
      Core::Response.new(response)
    end

    # Registers an AMI. When you're creating an instance-store backed AMI, registering the AMI is the
    # final step in the creation process. For more information about creating AMIs, see [Create an AMI
    # from a
    # snapshot](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/creating-an-ami-ebs.html#creating-launching-ami-from-snapshot)
    # and [Create an instance-store backed
    # AMI](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/creating-an-ami-instance-store.html) in
    # the *Amazon EC2 User Guide*.
    #
    # If needed, you can deregister an AMI at any time. Any modifications you make to an AMI backed by
    # an instance store volume invalidates its registration. If you make changes to an image,
    # deregister the previous image and register the new image.
    #
    # **Register a snapshot of a root device volume**
    #
    # You can use `RegisterImage` to create an Amazon EBS-backed Linux AMI from a snapshot of a root
    # device volume. You specify the snapshot using a block device mapping. You can't set the
    # encryption state of the volume using the block device mapping. If the snapshot is encrypted, or
    # encryption by default is enabled, the root volume of an instance launched from the AMI is
    # encrypted.
    #
    # For more information, see [Create an AMI from a
    # snapshot](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/creating-an-ami-ebs.html#creating-launching-ami-from-snapshot)
    # and [Use encryption with EBS-backed
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/AMIEncryption.html) in the *Amazon EC2
    # User Guide*.
    #
    # **Amazon Web Services Marketplace product codes**
    #
    # If any snapshots have Amazon Web Services Marketplace product codes, they are copied to the new
    # AMI.
    #
    # In most cases, AMIs for Windows, RedHat, SUSE, and SQL Server require correct licensing
    # information to be present on the AMI. For more information, see [Understand AMI billing
    # information](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-billing-info.html) in the
    # *Amazon EC2 User Guide*. When creating an AMI from a snapshot, the `RegisterImage` operation
    # derives the correct billing information from the snapshot's metadata, but this requires the
    # appropriate metadata to be present. To verify if the correct billing information was applied,
    # check the `PlatformDetails` field on the new AMI. If the field is empty or doesn't match the
    # expected operating system code (for example, Windows, RedHat, SUSE, or SQL), the AMI creation
    # was unsuccessful, and you should discard the AMI and instead create the AMI from an instance.
    # For more information, see [Create an AMI from an instance
    # ](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/creating-an-ami-ebs.html#how-to-create-ebs-ami)
    # in the *Amazon EC2 User Guide*.
    #
    # If you purchase a Reserved Instance to apply to an On-Demand Instance that was launched from an
    # AMI with a billing product code, make sure that the Reserved Instance has the matching billing
    # product code. If you purchase a Reserved Instance without the matching billing product code, the
    # Reserved Instance is not applied to the On-Demand Instance. For information about how to obtain
    # the platform details and billing information of an AMI, see [Understand AMI billing
    # information](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ami-billing-info.html) in the
    # *Amazon EC2 User Guide*.
    def register_image(input : AEC::RegisterImageRequest) : Core::ParsedResponse(AEC::RegisterImageResult)
      Log.info { "performing 'RegisterImage' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RegisterImage")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RegisterImage", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RegisterImageResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RegisterImageResult).new(response, result)
    end

    # Registers a set of tag keys to include in scheduled event notifications for your resources.
    #
    # To remove tags, use
    # [DeregisterInstanceEventNotificationAttributes](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DeregisterInstanceEventNotificationAttributes.html).
    def register_instance_event_notification_attributes(input : AEC::RegisterInstanceEventNotificationAttributesRequest) : Core::ParsedResponse(AEC::RegisterInstanceEventNotificationAttributesResult)
      Log.info { "performing 'RegisterInstanceEventNotificationAttributes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RegisterInstanceEventNotificationAttributes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RegisterInstanceEventNotificationAttributes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RegisterInstanceEventNotificationAttributesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RegisterInstanceEventNotificationAttributesResult).new(response, result)
    end

    # Registers members (network interfaces) with the transit gateway multicast group. A member is a
    # network interface associated with a supported EC2 instance that receives multicast traffic. For
    # more information, see [Multicast on transit
    # gateways](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-multicast-overview.html) in the *Amazon
    # Web Services Transit Gateways Guide*.
    #
    # After you add the members, use
    # [SearchTransitGatewayMulticastGroups](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_SearchTransitGatewayMulticastGroups.html)
    # to verify that the members were added to the transit gateway multicast group.
    def register_transit_gateway_multicast_group_members(input : AEC::RegisterTransitGatewayMulticastGroupMembersRequest) : Core::ParsedResponse(AEC::RegisterTransitGatewayMulticastGroupMembersResult)
      Log.info { "performing 'RegisterTransitGatewayMulticastGroupMembers' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RegisterTransitGatewayMulticastGroupMembers")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RegisterTransitGatewayMulticastGroupMembers", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RegisterTransitGatewayMulticastGroupMembersResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RegisterTransitGatewayMulticastGroupMembersResult).new(response, result)
    end

    # Registers sources (network interfaces) with the specified transit gateway multicast group.
    #
    # A multicast source is a network interface attached to a supported instance that sends multicast
    # traffic. For more information about supported instances, see [Multicast on transit
    # gateways](https://docs.aws.amazon.com/vpc/latest/tgw/tgw-multicast-overview.html) in the *Amazon
    # Web Services Transit Gateways Guide*.
    #
    # After you add the source, use
    # [SearchTransitGatewayMulticastGroups](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_SearchTransitGatewayMulticastGroups.html)
    # to verify that the source was added to the multicast group.
    def register_transit_gateway_multicast_group_sources(input : AEC::RegisterTransitGatewayMulticastGroupSourcesRequest) : Core::ParsedResponse(AEC::RegisterTransitGatewayMulticastGroupSourcesResult)
      Log.info { "performing 'RegisterTransitGatewayMulticastGroupSources' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RegisterTransitGatewayMulticastGroupSources")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RegisterTransitGatewayMulticastGroupSources", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RegisterTransitGatewayMulticastGroupSourcesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RegisterTransitGatewayMulticastGroupSourcesResult).new(response, result)
    end

    # Rejects a request to assign billing of the available capacity of a shared Capacity Reservation
    # to your account. For more information, see [ Billing assignment for shared Amazon EC2 Capacity
    # Reservations](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/assign-billing.html).
    def reject_capacity_reservation_billing_ownership(input : AEC::RejectCapacityReservationBillingOwnershipRequest) : Core::ParsedResponse(AEC::RejectCapacityReservationBillingOwnershipResult)
      Log.info { "performing 'RejectCapacityReservationBillingOwnership' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RejectCapacityReservationBillingOwnership")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RejectCapacityReservationBillingOwnership", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RejectCapacityReservationBillingOwnershipResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RejectCapacityReservationBillingOwnershipResult).new(response, result)
    end

    # Rejects a Transit Gateway attachment request for a Client VPN endpoint. The Transit Gateway
    # owner can reject the attachment request to prevent the Client VPN endpoint from routing traffic
    # through the Transit Gateway.
    def reject_transit_gateway_client_vpn_attachment(input : AEC::RejectTransitGatewayClientVpnAttachmentRequest) : Core::ParsedResponse(AEC::RejectTransitGatewayClientVpnAttachmentResult)
      Log.info { "performing 'RejectTransitGatewayClientVpnAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RejectTransitGatewayClientVpnAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RejectTransitGatewayClientVpnAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RejectTransitGatewayClientVpnAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RejectTransitGatewayClientVpnAttachmentResult).new(response, result)
    end

    # Rejects a request to associate cross-account subnets with a transit gateway multicast domain.
    def reject_transit_gateway_multicast_domain_associations(input : AEC::RejectTransitGatewayMulticastDomainAssociationsRequest) : Core::ParsedResponse(AEC::RejectTransitGatewayMulticastDomainAssociationsResult)
      Log.info { "performing 'RejectTransitGatewayMulticastDomainAssociations' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RejectTransitGatewayMulticastDomainAssociations")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RejectTransitGatewayMulticastDomainAssociations", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RejectTransitGatewayMulticastDomainAssociationsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RejectTransitGatewayMulticastDomainAssociationsResult).new(response, result)
    end

    # Rejects a transit gateway peering attachment request.
    def reject_transit_gateway_peering_attachment(input : AEC::RejectTransitGatewayPeeringAttachmentRequest) : Core::ParsedResponse(AEC::RejectTransitGatewayPeeringAttachmentResult)
      Log.info { "performing 'RejectTransitGatewayPeeringAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RejectTransitGatewayPeeringAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RejectTransitGatewayPeeringAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RejectTransitGatewayPeeringAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RejectTransitGatewayPeeringAttachmentResult).new(response, result)
    end

    # Rejects a request to attach a VPC to a transit gateway.
    #
    # The VPC attachment must be in the `pendingAcceptance` state. Use
    # DescribeTransitGatewayVpcAttachments to view your pending VPC attachment requests. Use
    # AcceptTransitGatewayVpcAttachment to accept a VPC attachment request.
    def reject_transit_gateway_vpc_attachment(input : AEC::RejectTransitGatewayVpcAttachmentRequest) : Core::ParsedResponse(AEC::RejectTransitGatewayVpcAttachmentResult)
      Log.info { "performing 'RejectTransitGatewayVpcAttachment' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RejectTransitGatewayVpcAttachment")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RejectTransitGatewayVpcAttachment", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RejectTransitGatewayVpcAttachmentResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RejectTransitGatewayVpcAttachmentResult).new(response, result)
    end

    # Rejects VPC endpoint connection requests to your VPC endpoint service.
    def reject_vpc_endpoint_connections(input : AEC::RejectVpcEndpointConnectionsRequest) : Core::ParsedResponse(AEC::RejectVpcEndpointConnectionsResult)
      Log.info { "performing 'RejectVpcEndpointConnections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RejectVpcEndpointConnections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RejectVpcEndpointConnections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RejectVpcEndpointConnectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RejectVpcEndpointConnectionsResult).new(response, result)
    end

    # Rejects a VPC peering connection request. The VPC peering connection must be in the
    # `pending-acceptance` state. Use the DescribeVpcPeeringConnections request to view your
    # outstanding VPC peering connection requests. To delete an active VPC peering connection, or to
    # delete a VPC peering connection request that you initiated, use DeleteVpcPeeringConnection.
    def reject_vpc_peering_connection(input : AEC::RejectVpcPeeringConnectionRequest) : Core::ParsedResponse(AEC::RejectVpcPeeringConnectionResult)
      Log.info { "performing 'RejectVpcPeeringConnection' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RejectVpcPeeringConnection")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RejectVpcPeeringConnection", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RejectVpcPeeringConnectionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RejectVpcPeeringConnectionResult).new(response, result)
    end

    # Releases the specified Elastic IP address.
    #
    # [Default VPC] Releasing an Elastic IP address automatically disassociates it from any instance
    # that it's associated with. Alternatively, you can disassociate an Elastic IP address without
    # releasing it.
    #
    # [Nondefault VPC] You must disassociate the Elastic IP address before you can release it.
    # Otherwise, Amazon EC2 returns an error (`InvalidIPAddress.InUse`).
    #
    # After releasing an Elastic IP address, it is released to the IP address pool. Be sure to update
    # your DNS records and any servers or devices that communicate with the address. If you attempt to
    # release an Elastic IP address that you already released, you'll get an `AuthFailure` error if
    # the address is already allocated to another Amazon Web Services account.
    #
    # After you release an Elastic IP address, you might be able to recover it. For more information,
    # see [Release an Elastic IP
    # address](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-instance-addressing-eips-releasing.html).
    def release_address(input : AEC::ReleaseAddressRequest) : Core::Response
      Log.info { "performing 'ReleaseAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReleaseAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReleaseAddress", body)
      Core::Response.new(response)
    end

    # When you no longer want to use an On-Demand Dedicated Host it can be released. On-Demand billing
    # is stopped and the host goes into `released` state. The host ID of Dedicated Hosts that have
    # been released can no longer be specified in another request, for example, to modify the host.
    # You must stop or terminate all instances on a host before it can be released.
    #
    # When Dedicated Hosts are released, it may take some time for them to stop counting toward your
    # limit and you may receive capacity errors when trying to allocate new Dedicated Hosts. Wait a
    # few minutes and then try again.
    #
    # Released hosts still appear in a DescribeHosts response.
    def release_hosts(input : AEC::ReleaseHostsRequest) : Core::ParsedResponse(AEC::ReleaseHostsResult)
      Log.info { "performing 'ReleaseHosts' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReleaseHosts")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReleaseHosts", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReleaseHostsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReleaseHostsResult).new(response, result)
    end

    # Release an allocation within an IPAM pool. The Region you use should be the IPAM pool locale.
    # The locale is the Amazon Web Services Region where this IPAM pool is available for allocations.
    # You can only use this action to release manual allocations. To remove an allocation for a
    # resource without deleting the resource, set its monitored state to false using
    # [ModifyIpamResourceCidr](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_ModifyIpamResourceCidr.html).
    # For more information, see [Release an
    # allocation](https://docs.aws.amazon.com/vpc/latest/ipam/release-alloc-ipam.html) in the *Amazon
    # VPC IPAM User Guide*.
    #
    # All EC2 API actions follow an [eventual
    # consistency](https://docs.aws.amazon.com/ec2/latest/devguide/eventual-consistency.html) model.
    def release_ipam_pool_allocation(input : AEC::ReleaseIpamPoolAllocationRequest) : Core::ParsedResponse(AEC::ReleaseIpamPoolAllocationResult)
      Log.info { "performing 'ReleaseIpamPoolAllocation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReleaseIpamPoolAllocation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReleaseIpamPoolAllocation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReleaseIpamPoolAllocationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReleaseIpamPoolAllocationResult).new(response, result)
    end

    # Replaces an IAM instance profile for the specified running instance. You can use this action to
    # change the IAM instance profile that's associated with an instance without having to
    # disassociate the existing IAM instance profile first.
    #
    # Use DescribeIamInstanceProfileAssociations to get the association ID.
    def replace_iam_instance_profile_association(input : AEC::ReplaceIamInstanceProfileAssociationRequest) : Core::ParsedResponse(AEC::ReplaceIamInstanceProfileAssociationResult)
      Log.info { "performing 'ReplaceIamInstanceProfileAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceIamInstanceProfileAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceIamInstanceProfileAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReplaceIamInstanceProfileAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReplaceIamInstanceProfileAssociationResult).new(response, result)
    end

    # Sets or replaces the criteria for Allowed AMIs.
    #
    # The `ImageCriteria` can include up to:
    #
    # - 10 `ImageCriterion`
    #
    # The Allowed AMIs feature does not restrict the AMIs owned by your account. Regardless of the
    # criteria you set, the AMIs created by your account will always be discoverable and usable by
    # users in your account.
    #
    # For more information, see [Control the discovery and use of AMIs in Amazon EC2 with Allowed
    # AMIs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-allowed-amis.html) in *Amazon EC2
    # User Guide*.
    def replace_image_criteria_in_allowed_images_settings(input : AEC::ReplaceImageCriteriaInAllowedImagesSettingsRequest) : Core::ParsedResponse(AEC::ReplaceImageCriteriaInAllowedImagesSettingsResult)
      Log.info { "performing 'ReplaceImageCriteriaInAllowedImagesSettings' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceImageCriteriaInAllowedImagesSettings")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceImageCriteriaInAllowedImagesSettings", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReplaceImageCriteriaInAllowedImagesSettingsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReplaceImageCriteriaInAllowedImagesSettingsResult).new(response, result)
    end

    # Replaces or removes the instance type specification for an AMI. The instance type specification
    # defines which instance types are compatible with the AMI.
    #
    # When you launch an instance using
    # [RunInstances](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_RunInstances.html),
    # Amazon EC2 validates the requested instance type against the AMI's instance type specification.
    # If the instance type is not compatible, the request fails with an `InvalidParameterCombination`
    # error.
    #
    # You can specify supported instance types, unsupported instance types, or both. The evaluation
    # logic is as follows:
    #
    # - No specification set – all instance types are allowed.
    #
    # - Only `UnsupportedInstanceTypes` set – All instance types are allowed except those that match
    # the unsupported list.
    #
    # - `SupportedInstanceTypes` set – The instance type must match the supported list and must not
    # match the unsupported list.
    #
    # Instance type entries support wildcard patterns using `*` (for example, `t3.*` matches all t3
    # sizes).
    #
    # To remove an existing instance type specification, omit the `InstanceTypeSpecification`
    # parameter or set it to `null`.
    #
    # To set the instance type specification, you must be the AMI owner. You cannot set an instance
    # type specification on an AMI that is listed in Amazon Web Services Marketplace, and you cannot
    # list an AMI in Amazon Web Services Marketplace if it has an instance type specification set.
    def replace_image_instance_type_specification(input : AEC::ReplaceImageInstanceTypeSpecificationRequest) : Core::ParsedResponse(AEC::ReplaceImageInstanceTypeSpecificationResult)
      Log.info { "performing 'ReplaceImageInstanceTypeSpecification' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceImageInstanceTypeSpecification")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceImageInstanceTypeSpecification", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReplaceImageInstanceTypeSpecificationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReplaceImageInstanceTypeSpecificationResult).new(response, result)
    end

    # Changes which network ACL a subnet is associated with. By default when you create a subnet, it's
    # automatically associated with the default network ACL. For more information, see [Network
    # ACLs](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-network-acls.html) in the *Amazon VPC
    # User Guide*.
    #
    # This is an idempotent operation.
    def replace_network_acl_association(input : AEC::ReplaceNetworkAclAssociationRequest) : Core::ParsedResponse(AEC::ReplaceNetworkAclAssociationResult)
      Log.info { "performing 'ReplaceNetworkAclAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceNetworkAclAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceNetworkAclAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReplaceNetworkAclAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReplaceNetworkAclAssociationResult).new(response, result)
    end

    # Replaces an entry (rule) in a network ACL. For more information, see [Network
    # ACLs](https://docs.aws.amazon.com/vpc/latest/userguide/vpc-network-acls.html) in the *Amazon VPC
    # User Guide*.
    def replace_network_acl_entry(input : AEC::ReplaceNetworkAclEntryRequest) : Core::Response
      Log.info { "performing 'ReplaceNetworkAclEntry' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceNetworkAclEntry")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceNetworkAclEntry", body)
      Core::Response.new(response)
    end

    # Replaces an existing route within a route table in a VPC.
    #
    # You must specify either a destination CIDR block or a prefix list ID. You must also specify
    # exactly one of the resources from the parameter list, or reset the local route to its default
    # target.
    #
    # For more information, see [Route
    # tables](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html) in the *Amazon
    # VPC User Guide*.
    def replace_route(input : AEC::ReplaceRouteRequest) : Core::Response
      Log.info { "performing 'ReplaceRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceRoute", body)
      Core::Response.new(response)
    end

    # Changes the route table associated with a given subnet, internet gateway, or virtual private
    # gateway in a VPC. After the operation completes, the subnet or gateway uses the routes in the
    # new route table. For more information about route tables, see [Route
    # tables](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Route_Tables.html) in the *Amazon
    # VPC User Guide*.
    #
    # You can also use this operation to change which table is the main route table in the VPC.
    # Specify the main route table's association ID and the route table ID of the new main route
    # table.
    def replace_route_table_association(input : AEC::ReplaceRouteTableAssociationRequest) : Core::ParsedResponse(AEC::ReplaceRouteTableAssociationResult)
      Log.info { "performing 'ReplaceRouteTableAssociation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceRouteTableAssociation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceRouteTableAssociation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReplaceRouteTableAssociationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReplaceRouteTableAssociationResult).new(response, result)
    end

    # Replaces the specified route in the specified transit gateway route table.
    def replace_transit_gateway_route(input : AEC::ReplaceTransitGatewayRouteRequest) : Core::ParsedResponse(AEC::ReplaceTransitGatewayRouteResult)
      Log.info { "performing 'ReplaceTransitGatewayRoute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceTransitGatewayRoute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceTransitGatewayRoute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReplaceTransitGatewayRouteResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReplaceTransitGatewayRouteResult).new(response, result)
    end

    # Trigger replacement of specified VPN tunnel.
    def replace_vpn_tunnel(input : AEC::ReplaceVpnTunnelRequest) : Core::ParsedResponse(AEC::ReplaceVpnTunnelResult)
      Log.info { "performing 'ReplaceVpnTunnel' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReplaceVpnTunnel")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReplaceVpnTunnel", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ReplaceVpnTunnelResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ReplaceVpnTunnelResult).new(response, result)
    end

    # Submits feedback about the status of an instance. The instance must be in the `running` state.
    # If your experience with the instance differs from the instance status returned by
    # DescribeInstanceStatus, use ReportInstanceStatus to report your experience with the instance.
    # Amazon EC2 collects this information to improve the accuracy of status checks.
    #
    # Use of this action does not change the value returned by DescribeInstanceStatus.
    def report_instance_status(input : AEC::ReportInstanceStatusRequest) : Core::Response
      Log.info { "performing 'ReportInstanceStatus' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ReportInstanceStatus")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ReportInstanceStatus", body)
      Core::Response.new(response)
    end

    # Creates a Spot Fleet request.
    #
    # The Spot Fleet request specifies the total target capacity and the On-Demand target capacity.
    # Amazon EC2 calculates the difference between the total capacity and On-Demand capacity, and
    # launches the difference as Spot capacity.
    #
    # You can submit a single request that includes multiple launch specifications that vary by
    # instance type, AMI, Availability Zone, or subnet.
    #
    # By default, the Spot Fleet requests Spot Instances in the Spot Instance pool where the price per
    # unit is the lowest. Each launch specification can include its own instance weighting that
    # reflects the value of the instance type to your application workload.
    #
    # Alternatively, you can specify that the Spot Fleet distribute the target capacity across the
    # Spot pools included in its launch specifications. By ensuring that the Spot Instances in your
    # Spot Fleet are in different Spot pools, you can improve the availability of your fleet.
    #
    # You can specify tags for the Spot Fleet request and instances launched by the fleet. You cannot
    # tag other resource types in a Spot Fleet request because only the `spot-fleet-request` and
    # `instance` resource types are supported.
    #
    # For more information, see [Spot Fleet
    # requests](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-fleet-requests.html) in the
    # *Amazon EC2 User Guide*.
    #
    # We strongly discourage using the RequestSpotFleet API because it is a legacy API with no planned
    # investment. For options for requesting Spot Instances, see [Which is the best Spot request
    # method to
    # use?](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-best-practices.html#which-spot-request-method-to-use)
    # in the *Amazon EC2 User Guide*.
    def request_spot_fleet(input : AEC::RequestSpotFleetRequest) : Core::ParsedResponse(AEC::RequestSpotFleetResponse)
      Log.info { "performing 'RequestSpotFleet' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RequestSpotFleet")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RequestSpotFleet", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RequestSpotFleetResponse.from_xml(result_node)
      Core::ParsedResponse(AEC::RequestSpotFleetResponse).new(response, result)
    end

    # Creates a Spot Instance request.
    #
    # For more information, see [Work with Spot
    # Instance](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-requests.html) in the *Amazon
    # EC2 User Guide*.
    #
    # We strongly discourage using the RequestSpotInstances API because it is a legacy API with no
    # planned investment. For options for requesting Spot Instances, see [Which is the best Spot
    # request method to
    # use?](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-best-practices.html#which-spot-request-method-to-use)
    # in the *Amazon EC2 User Guide*.
    def request_spot_instances(input : AEC::RequestSpotInstancesRequest) : Core::ParsedResponse(AEC::RequestSpotInstancesResult)
      Log.info { "performing 'RequestSpotInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RequestSpotInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RequestSpotInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RequestSpotInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RequestSpotInstancesResult).new(response, result)
    end

    # Resets the attribute of the specified IP address. For requirements, see [Using reverse DNS for
    # email
    # applications](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/elastic-ip-addresses-eip.html#Using_Elastic_Addressing_Reverse_DNS).
    def reset_address_attribute(input : AEC::ResetAddressAttributeRequest) : Core::ParsedResponse(AEC::ResetAddressAttributeResult)
      Log.info { "performing 'ResetAddressAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ResetAddressAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ResetAddressAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ResetAddressAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ResetAddressAttributeResult).new(response, result)
    end

    # Resets the default KMS key for EBS encryption for your account in this Region to the Amazon Web
    # Services managed KMS key for EBS.
    #
    # After resetting the default KMS key to the Amazon Web Services managed KMS key, you can continue
    # to encrypt by a customer managed KMS key by specifying it when you create the volume. For more
    # information, see [Amazon EBS
    # encryption](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-encryption.html) in the *Amazon
    # EBS User Guide*.
    def reset_ebs_default_kms_key_id(input : AEC::ResetEbsDefaultKmsKeyIdRequest) : Core::ParsedResponse(AEC::ResetEbsDefaultKmsKeyIdResult)
      Log.info { "performing 'ResetEbsDefaultKmsKeyId' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ResetEbsDefaultKmsKeyId")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ResetEbsDefaultKmsKeyId", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ResetEbsDefaultKmsKeyIdResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ResetEbsDefaultKmsKeyIdResult).new(response, result)
    end

    # Resets the specified attribute of the specified Amazon FPGA Image (AFI) to its default value.
    # You can only reset the load permission attribute.
    def reset_fpga_image_attribute(input : AEC::ResetFpgaImageAttributeRequest) : Core::ParsedResponse(AEC::ResetFpgaImageAttributeResult)
      Log.info { "performing 'ResetFpgaImageAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ResetFpgaImageAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ResetFpgaImageAttribute", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ResetFpgaImageAttributeResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ResetFpgaImageAttributeResult).new(response, result)
    end

    # Resets an attribute of an AMI to its default value.
    def reset_image_attribute(input : AEC::ResetImageAttributeRequest) : Core::Response
      Log.info { "performing 'ResetImageAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ResetImageAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ResetImageAttribute", body)
      Core::Response.new(response)
    end

    # Resets an attribute of an instance to its default value. To reset the `kernel` or `ramdisk`, the
    # instance must be in a stopped state. To reset the `sourceDestCheck`, the instance can be either
    # running or stopped.
    #
    # The `sourceDestCheck` attribute controls whether source/destination checking is enabled. The
    # default value is `true`, which means checking is enabled. This value must be `false` for a NAT
    # instance to perform NAT. For more information, see [NAT
    # instances](https://docs.aws.amazon.com/AmazonVPC/latest/UserGuide/VPC_NAT_Instance.html) in the
    # *Amazon VPC User Guide*.
    def reset_instance_attribute(input : AEC::ResetInstanceAttributeRequest) : Core::Response
      Log.info { "performing 'ResetInstanceAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ResetInstanceAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ResetInstanceAttribute", body)
      Core::Response.new(response)
    end

    # Resets a network interface attribute. You can specify only one attribute at a time.
    def reset_network_interface_attribute(input : AEC::ResetNetworkInterfaceAttributeRequest) : Core::Response
      Log.info { "performing 'ResetNetworkInterfaceAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ResetNetworkInterfaceAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ResetNetworkInterfaceAttribute", body)
      Core::Response.new(response)
    end

    # Resets permission settings for the specified snapshot.
    #
    # For more information about modifying snapshot permissions, see [Share a
    # snapshot](https://docs.aws.amazon.com/ebs/latest/userguide/ebs-modifying-snapshot-permissions.html)
    # in the *Amazon EBS User Guide*.
    def reset_snapshot_attribute(input : AEC::ResetSnapshotAttributeRequest) : Core::Response
      Log.info { "performing 'ResetSnapshotAttribute' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ResetSnapshotAttribute")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ResetSnapshotAttribute", body)
      Core::Response.new(response)
    end

    # This action is deprecated.
    #
    # Restores an Elastic IP address that was previously moved to the EC2-VPC platform back to the
    # EC2-Classic platform. You cannot move an Elastic IP address that was originally allocated for
    # use in EC2-VPC. The Elastic IP address must not be associated with an instance or network
    # interface.
    def restore_address_to_classic(input : AEC::RestoreAddressToClassicRequest) : Core::ParsedResponse(AEC::RestoreAddressToClassicResult)
      Log.info { "performing 'RestoreAddressToClassic' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RestoreAddressToClassic")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RestoreAddressToClassic", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RestoreAddressToClassicResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RestoreAddressToClassicResult).new(response, result)
    end

    # Restores an AMI from the Recycle Bin. For more information, see [Recover deleted Amazon EBS
    # snapshots and EBS-back AMIs with Recycle
    # Bin](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/recycle-bin.html) in the *Amazon EC2
    # User Guide*.
    def restore_image_from_recycle_bin(input : AEC::RestoreImageFromRecycleBinRequest) : Core::ParsedResponse(AEC::RestoreImageFromRecycleBinResult)
      Log.info { "performing 'RestoreImageFromRecycleBin' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RestoreImageFromRecycleBin")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RestoreImageFromRecycleBin", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RestoreImageFromRecycleBinResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RestoreImageFromRecycleBinResult).new(response, result)
    end

    # Restores the entries from a previous version of a managed prefix list to a new version of the
    # prefix list.
    def restore_managed_prefix_list_version(input : AEC::RestoreManagedPrefixListVersionRequest) : Core::ParsedResponse(AEC::RestoreManagedPrefixListVersionResult)
      Log.info { "performing 'RestoreManagedPrefixListVersion' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RestoreManagedPrefixListVersion")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RestoreManagedPrefixListVersion", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RestoreManagedPrefixListVersionResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RestoreManagedPrefixListVersionResult).new(response, result)
    end

    # Restores a snapshot from the Recycle Bin. For more information, see [Restore snapshots from the
    # Recycle
    # Bin](https://docs.aws.amazon.com/ebs/latest/userguide/recycle-bin-working-with-snaps.html#recycle-bin-restore-snaps)
    # in the *Amazon EBS User Guide*.
    def restore_snapshot_from_recycle_bin(input : AEC::RestoreSnapshotFromRecycleBinRequest) : Core::ParsedResponse(AEC::RestoreSnapshotFromRecycleBinResult)
      Log.info { "performing 'RestoreSnapshotFromRecycleBin' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RestoreSnapshotFromRecycleBin")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RestoreSnapshotFromRecycleBin", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RestoreSnapshotFromRecycleBinResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RestoreSnapshotFromRecycleBinResult).new(response, result)
    end

    # Restores an archived Amazon EBS snapshot for use temporarily or permanently, or modifies the
    # restore period or restore type for a snapshot that was previously temporarily restored.
    #
    # For more information see [ Restore an archived
    # snapshot](https://docs.aws.amazon.com/ebs/latest/userguide/working-with-snapshot-archiving.html#restore-archived-snapshot)
    # and [ modify the restore period or restore type for a temporarily restored
    # snapshot](https://docs.aws.amazon.com/ebs/latest/userguide/working-with-snapshot-archiving.html#modify-temp-restore-period)
    # in the *Amazon EBS User Guide*.
    def restore_snapshot_tier(input : AEC::RestoreSnapshotTierRequest) : Core::ParsedResponse(AEC::RestoreSnapshotTierResult)
      Log.info { "performing 'RestoreSnapshotTier' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RestoreSnapshotTier")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RestoreSnapshotTier", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RestoreSnapshotTierResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RestoreSnapshotTierResult).new(response, result)
    end

    # Restores a volume from the Recycle Bin. For more information, see [Restore volumes from the
    # Recycle
    # Bin](https://docs.aws.amazon.com/ebs/latest/userguide/recycle-bin-working-with-volumes.html#recycle-bin-restore-volumes)
    # in the *Amazon EBS User Guide*.
    def restore_volume_from_recycle_bin(input : AEC::RestoreVolumeFromRecycleBinRequest) : Core::ParsedResponse(AEC::RestoreVolumeFromRecycleBinResult)
      Log.info { "performing 'RestoreVolumeFromRecycleBin' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RestoreVolumeFromRecycleBin")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RestoreVolumeFromRecycleBin", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RestoreVolumeFromRecycleBinResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RestoreVolumeFromRecycleBinResult).new(response, result)
    end

    # Removes an ingress authorization rule from a Client VPN endpoint.
    def revoke_client_vpn_ingress(input : AEC::RevokeClientVpnIngressRequest) : Core::ParsedResponse(AEC::RevokeClientVpnIngressResult)
      Log.info { "performing 'RevokeClientVpnIngress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RevokeClientVpnIngress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RevokeClientVpnIngress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RevokeClientVpnIngressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RevokeClientVpnIngressResult).new(response, result)
    end

    # Removes the specified outbound (egress) rules from the specified security group.
    #
    # You can specify rules using either rule IDs or security group rule properties. If you use rule
    # properties, the values that you specify (for example, ports) must match the existing rule's
    # values exactly. Each rule has a protocol, from and to ports, and destination (CIDR range,
    # security group, or prefix list). For the TCP and UDP protocols, you must also specify the
    # destination port or range of ports. For the ICMP protocol, you must also specify the ICMP type
    # and code. If the security group rule has a description, you do not need to specify the
    # description to revoke the rule.
    #
    # For a default VPC, if the values you specify do not match the existing rule's values, no error
    # is returned, and the output describes the security group rules that were not revoked.
    #
    # Amazon Web Services recommends that you describe the security group to verify that the rules
    # were removed.
    #
    # Rule changes are propagated to instances within the security group as quickly as possible.
    # However, a small delay might occur.
    def revoke_security_group_egress(input : AEC::RevokeSecurityGroupEgressRequest) : Core::ParsedResponse(AEC::RevokeSecurityGroupEgressResult)
      Log.info { "performing 'RevokeSecurityGroupEgress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RevokeSecurityGroupEgress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RevokeSecurityGroupEgress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RevokeSecurityGroupEgressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RevokeSecurityGroupEgressResult).new(response, result)
    end

    # Removes the specified inbound (ingress) rules from a security group.
    #
    # You can specify rules using either rule IDs or security group rule properties. If you use rule
    # properties, the values that you specify (for example, ports) must match the existing rule's
    # values exactly. Each rule has a protocol, from and to ports, and source (CIDR range, security
    # group, or prefix list). For the TCP and UDP protocols, you must also specify the destination
    # port or range of ports. For the ICMP protocol, you must also specify the ICMP type and code. If
    # the security group rule has a description, you do not need to specify the description to revoke
    # the rule.
    #
    # For a default VPC, if the values you specify do not match the existing rule's values, no error
    # is returned, and the output describes the security group rules that were not revoked.
    #
    # For a non-default VPC, if the values you specify do not match the existing rule's values, an
    # `InvalidPermission.NotFound` client error is returned, and no rules are revoked.
    #
    # Amazon Web Services recommends that you describe the security group to verify that the rules
    # were removed.
    #
    # Rule changes are propagated to instances within the security group as quickly as possible.
    # However, a small delay might occur.
    def revoke_security_group_ingress(input : AEC::RevokeSecurityGroupIngressRequest) : Core::ParsedResponse(AEC::RevokeSecurityGroupIngressResult)
      Log.info { "performing 'RevokeSecurityGroupIngress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RevokeSecurityGroupIngress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RevokeSecurityGroupIngress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RevokeSecurityGroupIngressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RevokeSecurityGroupIngressResult).new(response, result)
    end

    # Launches the specified number of instances using an AMI for which you have permissions.
    #
    # You can specify a number of options, or leave the default options. The following rules apply:
    #
    # - If you don't specify a subnet ID, we choose a default subnet from your default VPC for you. If
    # you don't have a default VPC, you must specify a subnet ID in the request.
    #
    # - All instances have a network interface with a primary private IPv4 address. If you don't
    # specify this address, we choose one from the IPv4 range of your subnet.
    #
    # - Not all instance types support IPv6 addresses. For more information, see [Instance
    # types](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-types.html).
    #
    # - If you don't specify a security group ID, we use the default security group for the VPC. For
    # more information, see [Security
    # groups](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-network-security.html).
    #
    # - If any of the AMIs have a product code attached for which the user has not subscribed, the
    # request fails.
    #
    # You can create a [launch
    # template](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-launch-templates.html), which
    # is a resource that contains the parameters to launch an instance. When you launch an instance
    # using RunInstances, you can specify the launch template instead of specifying the launch
    # parameters.
    #
    # To ensure faster instance launches, break up large requests into smaller batches. For example,
    # create five separate launch requests for 100 instances each instead of one launch request for
    # 500 instances.
    #
    # `RunInstances` is subject to both request rate limiting and resource rate limiting. For more
    # information, see [Request
    # throttling](https://docs.aws.amazon.com/ec2/latest/devguide/ec2-api-throttling.html).
    #
    # An instance is ready for you to use when it's in the `running` state. You can check the state of
    # your instance using DescribeInstances. You can tag instances and EBS volumes during launch,
    # after launch, or both. For more information, see CreateTags and [Tagging your Amazon EC2
    # resources](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html).
    #
    # Linux instances have access to the public key of the key pair at boot. You can use this key to
    # provide secure access to the instance. Amazon EC2 public images use this feature to provide
    # secure access without passwords. For more information, see [Key
    # pairs](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-key-pairs.html).
    #
    # For troubleshooting, see [What to do if an instance immediately
    # terminates](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_InstanceStraightToTerminated.html),
    # and [Troubleshooting connecting to your
    # instance](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/TroubleshootingInstancesConnecting.html).
    def run_instances(input : AEC::RunInstancesRequest) : Core::ParsedResponse(AEC::Reservation)
      Log.info { "performing 'RunInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RunInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RunInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::Reservation.from_xml(result_node)
      Core::ParsedResponse(AEC::Reservation).new(response, result)
    end

    # Launches the specified Scheduled Instances.
    #
    # Before you can launch a Scheduled Instance, you must purchase it and obtain an identifier using
    # PurchaseScheduledInstances.
    #
    # You must launch a Scheduled Instance during its scheduled time period. You can't stop or reboot
    # a Scheduled Instance, but you can terminate it as needed. If you terminate a Scheduled Instance
    # before the current scheduled time period ends, you can launch it again after a few minutes.
    def run_scheduled_instances(input : AEC::RunScheduledInstancesRequest) : Core::ParsedResponse(AEC::RunScheduledInstancesResult)
      Log.info { "performing 'RunScheduledInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "RunScheduledInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("RunScheduledInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::RunScheduledInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::RunScheduledInstancesResult).new(response, result)
    end

    # Searches for routes in the specified local gateway route table.
    def search_local_gateway_routes(input : AEC::SearchLocalGatewayRoutesRequest) : Core::ParsedResponse(AEC::SearchLocalGatewayRoutesResult)
      Log.info { "performing 'SearchLocalGatewayRoutes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "SearchLocalGatewayRoutes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("SearchLocalGatewayRoutes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::SearchLocalGatewayRoutesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::SearchLocalGatewayRoutesResult).new(response, result)
    end

    # Searches one or more transit gateway multicast groups and returns the group membership
    # information.
    def search_transit_gateway_multicast_groups(input : AEC::SearchTransitGatewayMulticastGroupsRequest) : Core::ParsedResponse(AEC::SearchTransitGatewayMulticastGroupsResult)
      Log.info { "performing 'SearchTransitGatewayMulticastGroups' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "SearchTransitGatewayMulticastGroups")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("SearchTransitGatewayMulticastGroups", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::SearchTransitGatewayMulticastGroupsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::SearchTransitGatewayMulticastGroupsResult).new(response, result)
    end

    # Searches for routes in the specified transit gateway route table.
    def search_transit_gateway_routes(input : AEC::SearchTransitGatewayRoutesRequest) : Core::ParsedResponse(AEC::SearchTransitGatewayRoutesResult)
      Log.info { "performing 'SearchTransitGatewayRoutes' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "SearchTransitGatewayRoutes")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("SearchTransitGatewayRoutes", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::SearchTransitGatewayRoutesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::SearchTransitGatewayRoutesResult).new(response, result)
    end

    # Sends a diagnostic interrupt to the specified Amazon EC2 instance to trigger a *kernel panic*
    # (on Linux instances), or a *blue screen*/*stop error* (on Windows instances). For instances
    # based on Intel and AMD processors, the interrupt is received as a *non-maskable interrupt*
    # (NMI).
    #
    # In general, the operating system crashes and reboots when a kernel panic or stop error is
    # triggered. The operating system can also be configured to perform diagnostic tasks, such as
    # generating a memory dump file, loading a secondary kernel, or obtaining a call trace.
    #
    # Before sending a diagnostic interrupt to your instance, ensure that its operating system is
    # configured to perform the required diagnostic tasks.
    #
    # For more information about configuring your operating system to generate a crash dump when a
    # kernel panic or stop error occurs, see [Send a diagnostic interrupt (for advanced
    # users)](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/diagnostic-interrupt.html) in the
    # *Amazon EC2 User Guide*.
    def send_diagnostic_interrupt(input : AEC::SendDiagnosticInterruptRequest) : Core::Response
      Log.info { "performing 'SendDiagnosticInterrupt' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "SendDiagnosticInterrupt")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("SendDiagnosticInterrupt", body)
      Core::Response.new(response)
    end

    # Generates an account status report. The report is generated asynchronously, and can take several
    # hours to complete.
    #
    # The report provides the current status of all attributes supported by declarative policies for
    # the accounts within the specified scope. The scope is determined by the specified `TargetId`,
    # which can represent an individual account, or all the accounts that fall under the specified
    # organizational unit (OU) or root (the entire Amazon Web Services Organization).
    #
    # The report is saved to your specified S3 bucket, using the following path structure (with the
    # capitalized placeholders representing your specific values):
    #
    # `s3://AMZN-S3-DEMO-BUCKET/YOUR-OPTIONAL-S3-PREFIX/ec2_TARGETID_REPORTID_YYYYMMDDTHHMMZ.csv`
    #
    # **Prerequisites for generating a report**
    #
    # - The `StartDeclarativePoliciesReport` API can only be called by the management account or
    # delegated administrators for the organization.
    #
    # - An S3 bucket must be available before generating the report (you can create a new one or use
    # an existing one), it must be in the same Region where the report generation request is made, and
    # it must have an appropriate bucket policy. For a sample S3 policy, see *Sample Amazon S3 policy*
    # under
    # [Examples](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_StartDeclarativePoliciesReport.html#API_StartDeclarativePoliciesReport_Examples).
    #
    # - Trusted access must be enabled for the service for which the declarative policy will enforce a
    # baseline configuration. If you use the Amazon Web Services Organizations console, this is done
    # automatically when you enable declarative policies. The API uses the following service principal
    # to identify the EC2 service: `ec2.amazonaws.com`. For more information on how to enable trusted
    # access with the Amazon Web Services CLI and Amazon Web Services SDKs, see [Using Organizations
    # with other Amazon Web Services
    # services](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_integrate_services.html)
    # in the *Amazon Web Services Organizations User Guide*.
    #
    # - Only one report per organization can be generated at a time. Attempting to generate a report
    # while another is in progress will result in an error.
    #
    # For more information, including the required IAM permissions to run this API, see [Generating
    # the account status report for declarative
    # policies](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_declarative_status-report.html)
    # in the *Amazon Web Services Organizations User Guide*.
    def start_declarative_policies_report(input : AEC::StartDeclarativePoliciesReportRequest) : Core::ParsedResponse(AEC::StartDeclarativePoliciesReportResult)
      Log.info { "performing 'StartDeclarativePoliciesReport' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "StartDeclarativePoliciesReport")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("StartDeclarativePoliciesReport", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::StartDeclarativePoliciesReportResult.from_xml(result_node)
      Core::ParsedResponse(AEC::StartDeclarativePoliciesReportResult).new(response, result)
    end

    # Starts an Amazon EBS-backed instance that you've previously stopped.
    #
    # Instances that use Amazon EBS volumes as their root devices can be quickly stopped and started.
    # When an instance is stopped, the compute resources are released and you are not billed for
    # instance usage. However, your root partition Amazon EBS volume remains and continues to persist
    # your data, and you are charged for Amazon EBS volume usage. You can restart your instance at any
    # time. Every time you start your instance, Amazon EC2 charges a one-minute minimum for instance
    # usage, and thereafter charges per second for instance usage.
    #
    # Before stopping an instance, make sure it is in a state from which it can be restarted. Stopping
    # an instance does not preserve data stored in RAM.
    #
    # Performing this operation on an instance that uses an instance store as its root device returns
    # an error.
    #
    # If you attempt to start a T3 instance with `host` tenancy and the `unlimited` CPU credit option,
    # the request fails. The `unlimited` CPU credit option is not supported on Dedicated Hosts. Before
    # you start the instance, either change its CPU credit option to `standard`, or change its tenancy
    # to `default` or `dedicated`.
    #
    # For more information, see [Stop and start Amazon EC2
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Stop_Start.html) in the *Amazon
    # EC2 User Guide*.
    def start_instances(input : AEC::StartInstancesRequest) : Core::ParsedResponse(AEC::StartInstancesResult)
      Log.info { "performing 'StartInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "StartInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("StartInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::StartInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::StartInstancesResult).new(response, result)
    end

    # Starts analyzing the specified Network Access Scope.
    def start_network_insights_access_scope_analysis(input : AEC::StartNetworkInsightsAccessScopeAnalysisRequest) : Core::ParsedResponse(AEC::StartNetworkInsightsAccessScopeAnalysisResult)
      Log.info { "performing 'StartNetworkInsightsAccessScopeAnalysis' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "StartNetworkInsightsAccessScopeAnalysis")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("StartNetworkInsightsAccessScopeAnalysis", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::StartNetworkInsightsAccessScopeAnalysisResult.from_xml(result_node)
      Core::ParsedResponse(AEC::StartNetworkInsightsAccessScopeAnalysisResult).new(response, result)
    end

    # Starts analyzing the specified path. If the path is reachable, the operation returns the
    # shortest feasible path.
    def start_network_insights_analysis(input : AEC::StartNetworkInsightsAnalysisRequest) : Core::ParsedResponse(AEC::StartNetworkInsightsAnalysisResult)
      Log.info { "performing 'StartNetworkInsightsAnalysis' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "StartNetworkInsightsAnalysis")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("StartNetworkInsightsAnalysis", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::StartNetworkInsightsAnalysisResult.from_xml(result_node)
      Core::ParsedResponse(AEC::StartNetworkInsightsAnalysisResult).new(response, result)
    end

    # Initiates the verification process to prove that the service provider owns the private DNS name
    # domain for the endpoint service.
    #
    # The service provider must successfully perform the verification before the consumer can use the
    # name to access the service.
    #
    # Before the service provider runs this command, they must add a record to the DNS server.
    def start_vpc_endpoint_service_private_dns_verification(input : AEC::StartVpcEndpointServicePrivateDnsVerificationRequest) : Core::ParsedResponse(AEC::StartVpcEndpointServicePrivateDnsVerificationResult)
      Log.info { "performing 'StartVpcEndpointServicePrivateDnsVerification' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "StartVpcEndpointServicePrivateDnsVerification")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("StartVpcEndpointServicePrivateDnsVerification", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::StartVpcEndpointServicePrivateDnsVerificationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::StartVpcEndpointServicePrivateDnsVerificationResult).new(response, result)
    end

    # Stops an Amazon EBS-backed instance. You can restart your instance at any time using the
    # [StartInstances](https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_StartInstances.html)
    # API. For more information, see [Stop and start Amazon EC2
    # instances](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Stop_Start.html) in the *Amazon
    # EC2 User Guide*.
    #
    # When you stop or hibernate an instance, we shut it down. By default, this includes a graceful
    # operating system (OS) shutdown. To bypass the graceful shutdown, use the `skipOsShutdown`
    # parameter; however, this might risk data integrity.
    #
    # You can use the StopInstances operation together with the `Hibernate` parameter to hibernate an
    # instance if the instance is [enabled for
    # hibernation](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/enabling-hibernation.html) and
    # meets the [hibernation
    # prerequisites](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/hibernating-prerequisites.html).
    # Stopping an instance doesn't preserve data stored in RAM, while hibernation does. If hibernation
    # fails, a normal shutdown occurs. For more information, see [Hibernate your Amazon EC2
    # instance](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Hibernate.html) in the *Amazon EC2
    # User Guide*.
    #
    # If your instance appears stuck in the `stopping` state, there might be an issue with the
    # underlying host computer. You can use the StopInstances operation together with the Force
    # parameter to force stop your instance. For more information, see [Troubleshoot Amazon EC2
    # instance stop
    # issues](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/TroubleshootingInstancesStopping.html)
    # in the *Amazon EC2 User Guide*.
    #
    # Stopping and hibernating an instance differs from rebooting or terminating it. For example, a
    # stopped or hibernated instance retains its root volume and any data volumes, unlike terminated
    # instances where these volumes are automatically deleted. For more information about the
    # differences between stopping, hibernating, rebooting, and terminating instances, see [Amazon EC2
    # instance state
    # changes](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-lifecycle.html) in the
    # *Amazon EC2 User Guide*.
    #
    # We don't charge for instance usage or data transfer fees when an instance is stopped. However,
    # the root volume and any data volumes remain and continue to persist your data, and you're
    # charged for volume usage. Every time you start your instance, Amazon EC2 charges a one-minute
    # minimum for instance usage, followed by per-second billing.
    #
    # You can't stop or hibernate instance store-backed instances.
    def stop_instances(input : AEC::StopInstancesRequest) : Core::ParsedResponse(AEC::StopInstancesResult)
      Log.info { "performing 'StopInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "StopInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("StopInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::StopInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::StopInstancesResult).new(response, result)
    end

    # Terminates active Client VPN endpoint connections. This action can be used to terminate a
    # specific client connection, or up to five connections established by a specific user.
    def terminate_client_vpn_connections(input : AEC::TerminateClientVpnConnectionsRequest) : Core::ParsedResponse(AEC::TerminateClientVpnConnectionsResult)
      Log.info { "performing 'TerminateClientVpnConnections' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "TerminateClientVpnConnections")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("TerminateClientVpnConnections", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::TerminateClientVpnConnectionsResult.from_xml(result_node)
      Core::ParsedResponse(AEC::TerminateClientVpnConnectionsResult).new(response, result)
    end

    # Terminates (deletes) the specified instances. This operation is
    # [idempotent](https://docs.aws.amazon.com/ec2/latest/devguide/ec2-api-idempotency.html); if you
    # terminate an instance more than once, each call succeeds.
    #
    # **Terminating an instance is permanent and irreversible.**
    #
    # After you terminate an instance, you can no longer connect to it, and it can't be recovered. All
    # attached Amazon EBS volumes that are configured to be deleted on termination are also
    # permanently deleted and can't be recovered. All data stored on instance store volumes is
    # permanently lost. For more information, see [ How instance termination
    # works](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/how-ec2-instance-termination-works.html).
    #
    # Before you terminate an instance, ensure that you have backed up all data that you need to
    # retain after the termination to persistent storage.
    #
    # If you specify multiple instances and the request fails (for example, because of a single
    # incorrect instance ID), none of the instances are terminated.
    #
    # If you terminate multiple instances across multiple Availability Zones, and one or more of the
    # specified instances are enabled for termination protection, the request fails with the following
    # results:
    #
    # - The specified instances that are in the same Availability Zone as the protected instance are
    # not terminated.
    #
    # - The specified instances that are in different Availability Zones, where no other specified
    # instances are protected, are successfully terminated.
    #
    # For example, say you have the following instances:
    #
    # - Instance A: `us-east-1a`; Not protected
    #
    # - Instance B: `us-east-1a`; Not protected
    #
    # - Instance C: `us-east-1b`; Protected
    #
    # - Instance D: `us-east-1b`; not protected
    #
    # If you attempt to terminate all of these instances in the same request, the request reports
    # failure with the following results:
    #
    # - Instance A and Instance B are successfully terminated because none of the specified instances
    # in `us-east-1a` are enabled for termination protection.
    #
    # - Instance C and Instance D fail to terminate because at least one of the specified instances in
    # `us-east-1b` (Instance C) is enabled for termination protection.
    #
    # Terminated instances remain visible after termination (for approximately one hour).
    #
    # By default, Amazon EC2 deletes all EBS volumes that were attached when the instance launched.
    # Volumes attached after instance launch continue running.
    #
    # By default, the TerminateInstances operation includes a graceful operating system (OS) shutdown.
    # To bypass the graceful shutdown, use the `skipOsShutdown` parameter; however, this might risk
    # data integrity.
    #
    # You can stop, start, and terminate EBS-backed instances. You can only terminate instance
    # store-backed instances. What happens to an instance differs if you stop or terminate it. For
    # example, when you stop an instance, the root device and any other devices attached to the
    # instance persist. When you terminate an instance, any attached EBS volumes with the
    # `DeleteOnTermination` block device mapping parameter set to `true` are automatically deleted.
    # For more information about the differences between stopping and terminating instances, see
    # [Amazon EC2 instance state
    # changes](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-lifecycle.html) in the
    # *Amazon EC2 User Guide*.
    #
    # When you terminate an instance, we attempt to terminate it forcibly after a short while. If your
    # instance appears stuck in the shutting-down state after a period of time, there might be an
    # issue with the underlying host computer. For more information about terminating and
    # troubleshooting terminating your instances, see [Terminate Amazon EC2
    # instances](https://docs.aws.amazon.com/) and [Troubleshooting terminating your
    # instance](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/TroubleshootingInstancesShuttingDown.html)
    # in the *Amazon EC2 User Guide*.
    def terminate_instances(input : AEC::TerminateInstancesRequest) : Core::ParsedResponse(AEC::TerminateInstancesResult)
      Log.info { "performing 'TerminateInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "TerminateInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("TerminateInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::TerminateInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::TerminateInstancesResult).new(response, result)
    end

    # Unassigns the specified IPv6 addresses or Prefix Delegation prefixes from a network interface.
    def unassign_ipv_6_addresses(input : AEC::UnassignIpv6AddressesRequest) : Core::ParsedResponse(AEC::UnassignIpv6AddressesResult)
      Log.info { "performing 'UnassignIpv6Addresses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UnassignIpv6Addresses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UnassignIpv6Addresses", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UnassignIpv6AddressesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UnassignIpv6AddressesResult).new(response, result)
    end

    # Unassigns the specified secondary private IP addresses or IPv4 Prefix Delegation prefixes from a
    # network interface.
    def unassign_private_ip_addresses(input : AEC::UnassignPrivateIpAddressesRequest) : Core::Response
      Log.info { "performing 'UnassignPrivateIpAddresses' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UnassignPrivateIpAddresses")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UnassignPrivateIpAddresses", body)
      Core::Response.new(response)
    end

    # Unassigns secondary private IPv4 addresses from a private NAT gateway. You cannot unassign your
    # primary private IP. For more information, see [Edit secondary IP address
    # associations](https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateway-working-with.html#nat-gateway-edit-secondary)
    # in the *Amazon VPC User Guide*.
    #
    # While unassigning is in progress, you cannot assign/unassign additional IP addresses while the
    # connections are being drained. You are, however, allowed to delete the NAT gateway.
    #
    # A private IP address will only be released at the end of MaxDrainDurationSeconds. The private IP
    # addresses stay associated and support the existing connections, but do not support any new
    # connections (new connections are distributed across the remaining assigned private IP address).
    # After the existing connections drain out, the private IP addresses are released.
    def unassign_private_nat_gateway_address(input : AEC::UnassignPrivateNatGatewayAddressRequest) : Core::ParsedResponse(AEC::UnassignPrivateNatGatewayAddressResult)
      Log.info { "performing 'UnassignPrivateNatGatewayAddress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UnassignPrivateNatGatewayAddress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UnassignPrivateNatGatewayAddress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UnassignPrivateNatGatewayAddressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UnassignPrivateNatGatewayAddressResult).new(response, result)
    end

    # Unlocks a snapshot that is locked in governance mode or that is locked in compliance mode but
    # still in the cooling-off period. You can't unlock a snapshot that is locked in compliance mode
    # after the cooling-off period has expired.
    def unlock_snapshot(input : AEC::UnlockSnapshotRequest) : Core::ParsedResponse(AEC::UnlockSnapshotResult)
      Log.info { "performing 'UnlockSnapshot' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UnlockSnapshot")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UnlockSnapshot", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UnlockSnapshotResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UnlockSnapshotResult).new(response, result)
    end

    # Disables detailed monitoring for a running instance. For more information, see [Monitoring your
    # instances and
    # volumes](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-cloudwatch.html) in the
    # *Amazon EC2 User Guide*.
    def unmonitor_instances(input : AEC::UnmonitorInstancesRequest) : Core::ParsedResponse(AEC::UnmonitorInstancesResult)
      Log.info { "performing 'UnmonitorInstances' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UnmonitorInstances")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UnmonitorInstances", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UnmonitorInstancesResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UnmonitorInstancesResult).new(response, result)
    end

    # Activates or deactivates tag keys for monitoring by EC2 Capacity Manager. Activated tag keys are
    # included as dimensions in capacity metric data, enabling you to group and filter metrics by tag
    # values.
    def update_capacity_manager_monitored_tag_keys(input : AEC::UpdateCapacityManagerMonitoredTagKeysRequest) : Core::ParsedResponse(AEC::UpdateCapacityManagerMonitoredTagKeysResult)
      Log.info { "performing 'UpdateCapacityManagerMonitoredTagKeys' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UpdateCapacityManagerMonitoredTagKeys")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UpdateCapacityManagerMonitoredTagKeys", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UpdateCapacityManagerMonitoredTagKeysResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UpdateCapacityManagerMonitoredTagKeysResult).new(response, result)
    end

    # Updates the Organizations access setting for EC2 Capacity Manager. This controls whether
    # Capacity Manager can aggregate data from all accounts in your Amazon Web Services Organization
    # or only from the current account.
    def update_capacity_manager_organizations_access(input : AEC::UpdateCapacityManagerOrganizationsAccessRequest) : Core::ParsedResponse(AEC::UpdateCapacityManagerOrganizationsAccessResult)
      Log.info { "performing 'UpdateCapacityManagerOrganizationsAccess' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UpdateCapacityManagerOrganizationsAccess")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UpdateCapacityManagerOrganizationsAccess", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UpdateCapacityManagerOrganizationsAccessResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UpdateCapacityManagerOrganizationsAccessResult).new(response, result)
    end

    # Modifies the number of instances allocated to an interruptible reservation, allowing you to add
    # more capacity or reclaim capacity to your source Capacity Reservation.
    def update_interruptible_capacity_reservation_allocation(input : AEC::UpdateInterruptibleCapacityReservationAllocationRequest) : Core::ParsedResponse(AEC::UpdateInterruptibleCapacityReservationAllocationResult)
      Log.info { "performing 'UpdateInterruptibleCapacityReservationAllocation' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UpdateInterruptibleCapacityReservationAllocation")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UpdateInterruptibleCapacityReservationAllocation", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UpdateInterruptibleCapacityReservationAllocationResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UpdateInterruptibleCapacityReservationAllocationResult).new(response, result)
    end

    # Updates the description of an egress (outbound) security group rule. You can replace an existing
    # description, or add a description to a rule that did not have one previously. You can remove a
    # description for a security group rule by omitting the description parameter in the request.
    def update_security_group_rule_descriptions_egress(input : AEC::UpdateSecurityGroupRuleDescriptionsEgressRequest) : Core::ParsedResponse(AEC::UpdateSecurityGroupRuleDescriptionsEgressResult)
      Log.info { "performing 'UpdateSecurityGroupRuleDescriptionsEgress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UpdateSecurityGroupRuleDescriptionsEgress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UpdateSecurityGroupRuleDescriptionsEgress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UpdateSecurityGroupRuleDescriptionsEgressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UpdateSecurityGroupRuleDescriptionsEgressResult).new(response, result)
    end

    # Updates the description of an ingress (inbound) security group rule. You can replace an existing
    # description, or add a description to a rule that did not have one previously. You can remove a
    # description for a security group rule by omitting the description parameter in the request.
    def update_security_group_rule_descriptions_ingress(input : AEC::UpdateSecurityGroupRuleDescriptionsIngressRequest) : Core::ParsedResponse(AEC::UpdateSecurityGroupRuleDescriptionsIngressResult)
      Log.info { "performing 'UpdateSecurityGroupRuleDescriptionsIngress' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "UpdateSecurityGroupRuleDescriptionsIngress")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("UpdateSecurityGroupRuleDescriptionsIngress", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::UpdateSecurityGroupRuleDescriptionsIngressResult.from_xml(result_node)
      Core::ParsedResponse(AEC::UpdateSecurityGroupRuleDescriptionsIngressResult).new(response, result)
    end

    # Validates whether the specified security groups can be associated with a single network
    # interface. The operation checks Amazon Virtual Private Cloud (Amazon VPC) quotas for inbound or
    # outbound rules per security group and security groups per network interface. Only authorized AWS
    # services can call this operation.
    #
    # For more information about security group quotas, see [Amazon VPC
    # quotas](https://docs.aws.amazon.com/vpc/latest/userguide/amazon-vpc-limits.html#vpc-limits-security-groups)
    # in the *Amazon VPC User Guide*.
    def validate_security_group_quotas_for_interface(input : AEC::ValidateSecurityGroupQuotasForInterfaceRequest) : Core::ParsedResponse(AEC::ValidateSecurityGroupQuotasForInterfaceResult)
      Log.info { "performing 'ValidateSecurityGroupQuotasForInterface' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "ValidateSecurityGroupQuotasForInterface")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("ValidateSecurityGroupQuotasForInterface", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::ValidateSecurityGroupQuotasForInterfaceResult.from_xml(result_node)
      Core::ParsedResponse(AEC::ValidateSecurityGroupQuotasForInterfaceResult).new(response, result)
    end

    # Stops advertising an address range that is provisioned as an address pool.
    #
    # You can perform this operation at most once every 10 seconds, even if you specify different
    # address ranges each time.
    #
    # It can take a few minutes before traffic to the specified addresses stops routing to Amazon Web
    # Services because of BGP propagation delays.
    def withdraw_byoip_cidr(input : AEC::WithdrawByoipCidrRequest) : Core::ParsedResponse(AEC::WithdrawByoipCidrResult)
      Log.info { "performing 'WithdrawByoipCidr' operation" }
      input.validate! if config.validate_input?
      params = input.to_query_params("")
      body = URI::Params.build do |form|
        form.add("Action", "WithdrawByoipCidr")
        form.add("Version", "2016-11-15")
        params.each { |key, value| form.add(key, value) }
      end
      response = query_request("WithdrawByoipCidr", body)
      result_node = XML.parse(response.body).root.not_nil!
      result = AEC::WithdrawByoipCidrResult.from_xml(result_node)
      Core::ParsedResponse(AEC::WithdrawByoipCidrResult).new(response, result)
    end
  end
end
